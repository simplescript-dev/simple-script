# findingA_scalar_join.options.md — runtime `[scalar].join(sep)` 段错根因方案对比

> Bug: `let a=[1,2,3]; println(a.join("-"))` → Segmentation fault (exit 139)。int/double/bool 数组全崩,string/nested-ref 正常;comptime 同式正确 `1-2-3`(**oracle 反转**)。
> 来源:D171 §下一步 §收口验收 finding A(行 161,"runtime codegen bug, D171 范围外, 待立项")。

## 根因(双层)

- **R1(结构错位 — 段错主因)**:`array.join` 错置于 `genStringMethod`(`gen_builtins.ss:65`,签名只有 `objVal` 无类型信息)。dispatch 顺序(`gen_methods.ss:546`)**先**试 `genStringMethod` 即返回非空 → array.join 被 string-method 路径**截胡**,永远到不了持 `objType` 的 `genArrayMethod`(132,其 method 列表无 join)。故无条件 emit `call @_ss_join(ptr arr, ptr delim)`,而 `_ss_join`(prelude.ss:254)对元素做 `result + arr[i]`(`ss_string_concat`)——把 scalar 元素**位值**(int `1`/`2`/`3`)当 `String*` 解引用读 offset 16 buffer ptr → 非法地址段错。string 数组元素本是 ptr 故幸存。
- **R2(`genExprAsString` bool 分支缺失 — 显示偏离, 非段错, 先于 join 存在)**:runtime bool→string 落到 `ss_int_to_string`→`1`,偏离 comptime `interpToStr`→`true`(interp_op.ss:35) + JS `String(true)="true"`。模板插值 `${true}` 实测同样输出 `1`(`42|1.5|1|0`)。**非阻挡 R1 段错核心** → 独立 issue,不混入。

本表针对 **R1 段错主因**列候选。

## 方案对比表

| 候选 | 层次 | 做法 | 假设破裂入口 | 在哪层消除/绕过 | 长久/演化(底层依赖链 + 业界对标 + N年返工度) |
|---|---|---|---|---|---|
| A | **数据层 patch** | `genStringMethod` join 分支改签名传 `objType` + 内联展开循环 IR,逐元素按类型调 `ss_int_to_string`/`ss_double_to_string` 再 `ss_string_concat` | **假设破裂入口** = `genStringMethod` 在 string-method 路径无 objId/元素类型上下文(`currentFunc` 无关,是 dispatch 错位致类型信息不可达);手写循环 IR(alloca i / phi / branch 块)在调用点 | 仅在调用点**绕过**(打补丁补类型),join 仍滞留错置的 string-method 路径,治标 | 依赖链:无新依赖但手写循环 IR **重复** `_ss_join` 已有循环逻辑;业界对标:codegen 内联循环是 ad-hoc;N年返工度**高**(join 仍错置,下次加元素类型又要重补) |
| B | **接口层 trap** | array.join 归位 dispatch **type-dependent 区**(`gen_methods.ss:534` 旁,与 `length`/`indexOf` 同列,持 objId),按 `inferArrayElemType(objId)` 分派 typed prelude 变体:string→`_ss_join`(现有正确)/ int→`_ss_joinInt` / double→`_ss_joinDouble` / bool→`_ss_joinBool`;变体内用模板插值 `${arr[i]}` **复用 `genExprAsString` 单一真相源**;删 `genStringMethod` join 错置分支 | **假设破裂入口** = "join 在 string-method 路径无类型信息无条件当 string 拼接";B 把 join 归位到**类型可达**的 type-dependent 分派区(objId→`inferArrayElemType`),**从源头消除**该破裂入口(不再有"无类型信息"状态) | 在 **dispatch 接口层 trap** 消除:join 与同类内置方法(length/indexOf)同构,类型分派在 codegen 正确归位 | 依赖链:typed 变体 + 模板插值 + `inferArrayElemType` **全现成**,无未落地依赖;业界对标:Rust/C++/Go 泛型均**编译期单态化**,typed 变体正对齐此方向;N年返工度**低**(单态化是终态,未来真泛型也是扩展 B 而非推翻) |
| C | **架构层 refactor** | runtime 泛型 `List<T>` + 通用 toString dispatch(元素 boxing + 类型 tag + vtable),`_ss_join` 单一泛型版内部运行时多态 toString | **假设破裂入口** = SS 数组**无 runtime 类型 tag**(编译期单态,统一 i64 slot,`ss_arrayGet` 返 i64);C 要先建 boxing/tag/vtable 机制才能运行时多态 | 在**架构层**重建(runtime 多态),理论最通用 | 依赖链:**依赖未落地的 boxing/vtable**(更基础候选必先做);业界对标:主流编译型语言(Rust/C++/Go)**避开**运行时多态走单态化;N年返工度:对 join 而言**过度工程**——元素类型编译期已知(`getVarType="Array<int>"`),runtime 多态是反方向,低收益高成本 |

## 决策行

**选 B(接口层 trap)因** R1 段错根因 = join 拿不到元素类型(错置 `genStringMethod` 被 dispatch 截胡),B 把 join **归位** dispatch type-dependent 区(与 `length`/`indexOf` 同构,持 objId 可 `inferArrayElemType`),从源头消除"无类型信息无条件当 string 拼接"的**假设破裂入口**,且变体内复用模板插值=`genExprAsString` 单一真相源,零手写循环 IR、零新 runtime IR define。

**为何不选 A(更浅数据层)**:A 补在错置位置(string-method 路径),join 仍滞留错位、治标不治本,且手写循环 IR 重复 `_ss_join` 已有逻辑——非根因。

**为何不选 C(更深架构层)**:C 依赖未落地的 runtime boxing/tag/vtable(底层依赖链未满足,需先做更基础候选),且元素类型编译期已知 → runtime 多态是过度工程,业界(Rust/C++/Go 单态化)亦反此向,N年返工度低收益。B 的 typed 变体正是单态化方向,C 即便未来落地也是扩展 B 而非推翻 B。

**bool 子决策**:R1 修复后 bool 不再段错(核心达成);bool 显示 `1` vs `true`(R2)是 `genExprAsString` 既有缺陷(模板插值同样),**非阻挡** → 立独立 issue 升根,本轮 `_ss_joinBool` 暂随 runtime scalar→string 现状(与模板插值内部一致)。

## §实证(MNK §字段 12)

### 根因定位 grep 证据(命令 + 输出)

```
$ grep -n "join" bootstrap/gen/gen_builtins.ss
65:    if (method == "join") {        # join 在 genStringMethod(行 6-82),非 genArrayMethod
$ sed -n '546,550p' bootstrap/gen/methods/gen_methods.ss
546:    let result = genStringMethod(method, objVal, argList)   # 先试 string-method
547:    if (result != "") { return result }                     # join 返回非空 → 截胡
550:    result = genArrayMethod(method, objVal, objType, argList) # 永不到达(持 objType)
$ grep -n "setVarType(name, .Array" bootstrap/gen/gen_decls.ss
577:            setVarType(name, `Array<${aeType}>`)   # let a=[1,2,3] → varType="Array<int>"
$ grep -n "function inferArrayElemType" bootstrap/gen/gen_types.ss
126:function inferArrayElemType(arrId: int)   # IDENT 走 getVarType 提取 <T> → "int"
```

### RED 逐 kind 最小变量隔离(已跑)

```
int  [1,2,3].join("-")     → Segmentation fault  exit=139   ❌
double [1.5,2.5].join("-") → Segmentation fault  exit=139   ❌
bool [true,false].join("-")→ Segmentation fault  exit=139   ❌
string ["x","y"].join("-") → x-y               exit=0      ✅ (元素本是 ptr,幸存)
comptime [1,2,3].join("-") → 1-2-3             exit=0      ✅ (oracle:interpToStr 先转 string)
```

### prelude SSoT 不变量(已核对)

`prelude_embed.ss` 头部:`AUTO-GENERATED by tools/gen_prelude_embed.ss — DO NOT EDIT`;`main.ss:6` 实际 import 嵌入版 → 改 `prelude.ss` 后**必跑** `bin/ss run tools/gen_prelude_embed.ss` regen,否则不生效。

### 最危险假设 + 最小 spike(选定候选 B)

- **最危险假设**:`inferArrayElemType(objId)` 在 join 调用点对 `let a=[1,2,3]` 返回 `"int"`(typed 分派依赖)。事实链已核对成立:`gen_decls.ss:577` 存 `Array<int>` → `getVarType("a")="Array<int>"` → `inferArrayElemType` 提取 `int`。
- **最小 spike 实测(两次迭代,字段 12 "spike 崩→回方案层修正"精神)**:
  - **迭代 1**(dispatch join + prelude 3 变体 + 删 genStringMethod join,regen + bootstrap):`let b: Array<int>`(显式标注)→ `4-5-6` ✅,但 `let a=[1,2,3]`(推断)**仍段错**。spike 暴露最危险假设破裂的**真因下沉**:`gen_decls.ss:577` 存 `Array<int>` 依赖 `inferArrayElemType(initId)`,而 initId 是 **ARRAY_LIT**,`inferArrayElemType` 不处理 ARRAY_LIT → `a` varType 退化 `ptr` 丢元素类型。方案 B 不变,补 `inferArrayElemType` ARRAY_LIT 分支(首元素 inferType)。
  - **迭代 2**(加 ARRAY_LIT 分支,bootstrap):int `1-2-3` ✅ 但 **string/double 回归**(`x-y-z`→地址 `4457632-`、`1.5-2.5`→`0-0`)。IR 实测:string/double 数组误调 `_ss_joinInt` → elemType 全推断为 `int`。根因:`nGetList` 返回**逗号分隔字符串**非数组,误用 `elems[0]`(string 索引)/`elems.length()` → `inferType(垃圾值≤0)="int"`。改用 `listGet(elems,0)` 取首元素节点 id。
  - **迭代 3 GREEN**(listGet 修正,bootstrap 固定点):int `1-2-3` / double `1.5-2.5` / string `x-y-z` / bool `1-0`(不段错) / 单元素 `7` / 标注路径全 ✅;回归测试 `tests/phase5/findingA_scalar_join.ss` all pass;全测 343 passed/3 pre-existing fail(baseline 342/3 + 本测试,0 新 fail);reflection_health GATE PASS(gen_methods/gen_types AUTO-DRIFT < budget_max);sunset GATE OK。
