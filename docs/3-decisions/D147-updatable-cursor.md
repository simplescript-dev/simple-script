# D147: Updatable Cursor — 完整 JDBC 4.3 §15 Update + MySQL CURSOR_TYPE_FOR_UPDATE + CONCUR_UPDATABLE / updateRow / deleteRow / insertRow / cancelRowUpdates / refreshRow / rowUpdated / rowDeleted / rowInserted

**Status:** [✓] Phase 0 D 文档落档 at commit `dd038c1` + [✓] Phase 1 协议层 CURSOR_TYPE_FOR_UPDATE 0x02 + CONCUR_UPDATABLE 1008 + MysqlPreparedStatement.deriveCursorFlag concurrency=CONCUR_UPDATABLE 分支 + `tests/d147_updatable_cursor/phase1_packet_unit_test.ss` 7/7 GREEN(纯本地)+ `phase1_spike_test.ss` docker probe-skip(在线时 1/1 GREEN — sendCursorExecuteForUpdate flags=0x02 + EOF CURSOR_EXISTS / FETCH(5) LAST_ROW_SENT 实证 §A.2 H1 server flags=0x02 行为同 0x01 driver 模拟范式落地)at commit `8abbf70` + [✓] Phase 2 JDBC ResultSet update*/state 16 method + 3 implementor stub + spike mock GREEN at commit `ac4d6e9` (`lib/java/sql.ss interface ResultSet` 加 `updateRow / deleteRow / insertRow / cancelRowUpdates / refreshRow / moveToInsertRow / moveToCurrentRow / rowUpdated / rowDeleted / rowInserted` 10 row-level + `updateInt / updateString / updateLong / updateBoolean / updateDouble / updateNull` 6 column-level setter + 3 implementor 降级 stub:`MysqlResultSet`(text 协议 no-op stub)/ `GeneratedKeyResultSet`(synthetic 单 row no-op stub)/ `MysqlBinaryResultSet`(Phase 4 真实现占位 stub)+ `tests/d147_updatable_cursor/phase2_spike_test.ss` 9/9 GREEN 纯本地 mock NoopResultSet 16 method reachable + CONCUR_UPDATABLE 1008 plumb + state transition 全验证) + [✓] Phase 3 SQL 模板生成器 + PK 解析最小子集 + spike GREEN at commit `35a30f3` (`lib/com/mysql/query.ss ColumnDef` 加 `orgTable: string` + `flags: int` 2 字段 + `parseColumnDef` 改第 4 次 skipLengthEncodedString 为 `col.orgTable = readLengthEncodedString` + 末尾追加 `col.flags = byteToInt(payload, off, 2)` 解析(MySQL Native Protocol §6.6 ColumnDefinition41 — promoted 2 段 from "skip" to "keep")+ `columnDefOrgTable` / `columnDefFlags` 2 cross-module accessor helper(同 columnDefName / columnDefColType 范式 line 271-289)+ `lib/com/mysql/prepared.ss` 加 4 SQL 模板 private helper:`buildUpdateRowSql(table, pkCol, dirtyCols)` 返 `UPDATE table SET col1=?, col2=? WHERE pk=?` / `buildDeleteRowSql(table, pkCol)` 返 `DELETE FROM table WHERE pk=?` / `buildInsertRowSql(table, cols)` 返 `INSERT INTO table(col1, col2) VALUES (?, ?)` / `buildRefreshRowSql(table, pkCol, cols)` 返 `SELECT col1, col2 FROM table WHERE pk=?`(空 cols → SELECT * 兜底)+ 2 PK 解析 helper:`derivePkColumn(colMetadata)` 扫 flags & PRI_KEY_FLAG 0x0002 单 PK 返字段名 / 多 PK / 无 PK 返 ""(空串触发 H2 失败回路 SQLFeatureNotSupportedException 走 D139 SQLExceptionTranslator)/ `deriveTableName(colMetadata)` 取 colMetadata[0].orgTable(单表 SELECT 范式)+ `PRI_KEY_FLAG = 0x0002` 常量 + `tests/d147_updatable_cursor/phase3_sql_template_unit_test.ss` 16/16 GREEN 纯本地无 docker(builder 字符串拼接 + PK helper 单/多/无 PK 三态 + accessor round-trip 全覆盖,§A.2 H2 hidden assumption SELECT meta column def flags PRI_KEY_FLAG bit 1 解析单表单 PK 实证) + [✓] Phase 4 MysqlBinaryResultSet driver-side updatable cursor 真实现 + spike GREEN at commit `6b13d29` (`lib/com/mysql/prepared.ss MysqlBinaryResultSet` 13 字段 → 19 字段 加 `pendingUpdates: Map<string, string>` / `pendingInserts: Array<Map<string, string>>` / `tableName: string` / `pkColumn: string` / `inInsertMode: int` / `rowState: int` 6 字段 + `ROW_STATE_CLEAN`/`UPDATED`/`DELETED`/`INSERTED` 4 常量 + `readQueryResultSetBinary` EOF 后调 `deriveTableName` + `derivePkColumn` 初始化 metadata + 16 method 真实现替 Phase 2 占位 stub:`writeCol` private helper(inInsertMode=1 → 写末位 pendingInserts Map / inInsertMode=0 → 写 pendingUpdates Map)+ 6 setter `updateInt` / `updateString` / `updateLong` / `updateBoolean` / `updateDouble` / `updateNull` 经 `writeCol` stringify(updateInt/Long/Double → `"" + val` / updateBoolean → `val != 0 ? "1" : "0"` / updateNull → `""`) + `updateRow` gate `pendingUpdates.size() > 0` + `tableName != ""` + `pkColumn != ""`(空 PK / 多 PK / 多表 join 抛 `SQLFeatureNotSupportedException` sqlState `0A000` 走 D139 SQLExceptionTranslator,空 dirtyCols 进入 idempotent no-op 路径 rowState=UPDATED 不抛)+ `buildUpdateRowSql(tableName, pkColumn, dirtyCols)` + `doPrepare(this.fd, sql)` fresh PS 同 fd 复用(D147 §A.2 H3 实证 — server cursor 是 request-response 同步,UPDATE/DELETE/INSERT 在 cursor 间隙 round-trip 不破 cursor)+ `ps.setString(j+1, val)` 循环 dirtyCols + `ps.setString(dirtyCols.length()+1, currentRow[colIndex(pkColumn)])` 末位 PK + `ps.executeUpdate()` 验 `affected == 1` 否则抛 `SQLException` sqlState `01000` + `ps.close()` COM_STMT_CLOSE 0x19 + 清 pendingUpdates(`new Map()` 重赋值,SS Map 无 clear)+ rowState=UPDATED / `deleteRow` 同 gate + `buildDeleteRowSql` + setString PK + executeUpdate 验 affected==1 + rowState=DELETED / `insertRow` gate `pendingInserts.length() > 0` + `tableName != ""` + 提 cols + vals from buf.keys() / buf.get(k) + `buildInsertRowSql(tableName, cols)` + setString 循环 + executeUpdate + `popLastInsertRow` private helper(SS Array<T>[i] = X rejected → trim 重建末位)+ rowState=INSERTED / `cancelRowUpdates` 纯 client-side(H4 实证 — 无 SQL):inInsertMode=1 时 trim 末位 Map 替换为空 Map(保持 inInsertMode + Array length)/ inInsertMode=0 时 `pendingUpdates = new Map()` 清空 + rowState=CLEAN / `refreshRow` gate + 提 cols from this.colMetadata + `buildRefreshRowSql(tableName, pkColumn, cols)` + fresh PS setString PK + executeQuery + `rs.next()` + 字段镜像重写 currentRow Array(H5 实证 — server-side 外部 update 经 fresh PS SELECT WHERE pk=? 同步)+ `moveToInsertRow` inInsertMode=1 + `pendingInserts.push(new Map())` 推空 Map / `moveToCurrentRow` inInsertMode=0 / `rowUpdated`/`Deleted`/`Inserted` 读 rowState 比对 ROW_STATE_X 返 1/0 + `next()` 入口加 `rowState = ROW_STATE_CLEAN` reset(H7 实证 — JDBC §15.2.5 spec rowUpdated/Deleted/Inserted 仅当前 row 状态,advance 到新 row 必 reset)+ `import { SQLException, SQLFeatureNotSupportedException }` 加 + `tests/d147_updatable_cursor/phase4_spike_test.ss` 5 case docker probe-skip 范式延续 d138/d139/d146 + d147-phase1(127.0.0.1:3307 不可达 → println + return 离线零阻塞,在线时 5/5 GREEN):Case 1 updateRow + 外部 `JdbcTemplate.queryForInt SELECT val WHERE id=1` 验 val=999 + rowUpdated==1(H3+H7)/ Case 2 deleteRow + 外部 SELECT COUNT WHERE id=1 == 0 + COUNT(*) == 4 + rowDeleted==1(H3+H7)/ Case 3 moveToInsertRow + updateInt(id,99) + updateInt(val,777) + updateString(name,'inserted') + insertRow + 外部 SELECT 找 id=99 val=777 + COUNT(*) == 6 + rowInserted==1(H6+H7)/ Case 4 updateInt(val,555) + cancelRowUpdates + updateRow + 外部 SELECT 验 val=10 不变(H4 — 纯 client-side,no SQL)/ Case 5 getInt('val') 初值 10 + 外部 writer JdbcTemplate UPDATE val=12345 + getInt('val') 仍 10 stale + refreshRow + getInt('val') 同步 12345(H5 — fresh PS SELECT WHERE pk=? 字段镜像)+ §A.2 H3-H7 hidden assumption 全实证 + Phase 3 hash `35a30f3` 回填 4 处) + [✓] Phase 5 7 case integration_test.ss e2e 全形态覆盖 + absorb 3 phase spike(phase1_spike + phase2_spike + phase4_spike)+ 保留 phase1_packet_unit_test + phase3_sql_template_unit_test + §A.2 H1-H7 全实证锚收关 + 全 Phase 0-5 commit hash 总览 + D147 主线 close at commit `<Phase 5 commit>` (2026-05-02) — D146 §Followup F1 + D146 §A.3 废案 C3 起首落档;走**完整 JDBC 4.3 §15 ResultSet Update 部分**(`updateRow / deleteRow / insertRow / cancelRowUpdates / refreshRow / moveToInsertRow / moveToCurrentRow / rowUpdated / rowDeleted / rowInserted` 10 method + `updateInt / updateString / updateLong / updateBoolean / updateDouble / updateNull` 6 setter + `CONCUR_UPDATABLE = 1008` 常量)+ **完整 MySQL `CURSOR_TYPE_FOR_UPDATE` 0x02 协议层**(COM_STMT_EXECUTE flags i8 bit 1 — server 实际行为同 0x01 READ_ONLY,Updatable cursor 在 driver 层模拟,MySQL Connector/J 5.0+ 范式)+ **driver-side updatable cursor 模拟**(`MysqlBinaryResultSet` 加 `pendingUpdates: Map<col, val>` / `pendingInserts: Array<Map>` / `tableName: string` / `pkColumn: string` / `inInsertMode: int` / `rowState: int` 6 字段 + SELECT meta 解析 PK + updateXxx 写 pendingUpdates + updateRow / deleteRow / insertRow 经 fresh PreparedStatement 生成 UPDATE / DELETE / INSERT SQL execute + refreshRow 重发 SELECT WHERE pk=?),不走客户端手写 `UPDATE WHERE pk=?` workaround / 不走 `SELECT FOR UPDATE` 行锁替代 / 不走 `SAVEPOINT + ROLLBACK TO SAVEPOINT` cancelRowUpdates 替代(用户对话锁 D135-D146 SQL 主线范式延续 — 完整子类 + 不接受次优 / workaround / 节省)。**SS 当前 ResultSet API**(D146 Phase 2 已落 `lib/java/sql.ss interface ResultSet` + `setFetchSize / scrollable 7 method` + D146 Phase 3 已落 `MysqlBinaryResultSet cursor` 协议 + scrollable in-memory cache)— **但 Update 部分 18 method 全缺**(updateRow / deleteRow / insertRow / cancelRowUpdates / refreshRow / moveToInsertRow / moveToCurrentRow / rowUpdated / rowDeleted / rowInserted / updateInt / updateString / updateLong / updateBoolean / updateDouble / updateNull)→ **ORM Hibernate `Session.refresh / Session.update` cursor 路径全断链**,大表流式 + 行级原地更新场景必走 LIMIT 自分页 + 手写 UPDATE workaround;升级 driver-side updatable cursor 后 ResultSet API 直接 updateRow / deleteRow / insertRow,server-side row 自动同步。范畴限 driver-side 模拟(MySQL 协议 server 不支持 SQL standard updatable cursor MySQL 5.7+ doc 明确,Connector/J 5.0+ driver 模拟范式);**holdability** HOLD_CURSORS_OVER_COMMIT / CLOSE_CURSORS_AT_COMMIT 留 D146 §F2;**ResultSetMetaData** 完整列元数据留 D146 §F3(本 D 仅 PK 解析最小子集);**SELECT FOR UPDATE 行锁 + RR isolation level + InnoDB lock wait** 留独立 sub-D(锁与 cursor 正交);**PostgreSQL `WHERE CURRENT OF cursor_name`** SQL standard 路径留 D107+ PostgreSQL driver。

**Depends on:**
- D025(interface dispatch — class extends ResultSet 走 vtable indirect dispatch — MysqlBinaryResultSet driver-side 模拟 updatable cursor 走同 vtable)
- D134(MySQL wire protocol — COM_STMT_EXECUTE flags i8 bit 1 = CURSOR_TYPE_FOR_UPDATE 0x02 + UPDATE / DELETE / INSERT 走 COM_STMT_EXECUTE OK packet `affected_rows` 字段)
- D136(prepared statement — SELECT FOR fresh PS UPDATE / DELETE / INSERT execute,不在 cursor 通道写 SQL)
- D137(JdbcTemplate retcon — query / update 上层入口,Phase 5 Spring RowCallbackHandler 内 updateRow demo)
- D138(generated keys 范式 — Phase 计划独立 commit + Status 收关 + hash 回填 + next_prompt 自闭环;§F1 锚起首)
- D139(SQL exception hierarchy — updateRow / deleteRow / insertRow 失败走 D139 ErrorPacket → SQLException 子类 → SQLExceptionTranslator → DAE,Phase 5 Case 7 覆盖)
- D146(server-side cursor — CURSOR_TYPE_READ_ONLY 0x01 + CONCUR_READ_ONLY 1007 路径现状,本 D 加 CURSOR_TYPE_FOR_UPDATE 0x02 + CONCUR_UPDATABLE 1008 对偶 + driver-side 模拟;D146 §A.3 废案 C3 显式留 D147+ 起首)
- `lib/java/sql.ss interface ResultSet`(D146 Phase 2 已落 setFetchSize / scrollable 7 method + 4 cursor-flow 常量,本 D Phase 2 加 update/delete/insert/state 18 method + CONCUR_UPDATABLE 1008 常量)
- `lib/com/mysql/prepared.ss MysqlBinaryResultSet`(D146 Phase 3 已落 cursor 协议升级 + scrollable in-memory cache,本 D Phase 4 加 driver-side updatable cursor 模拟 — pendingUpdates / pendingInserts / tableName / pkColumn / inInsertMode / rowState 6 字段)
- `lib/com/mysql/prepared.ss MysqlPreparedStatement.executeQuery`(D146 Phase 3 已加 cursorFlag plumb,本 D Phase 1 加 CURSOR_TYPE_FOR_UPDATE 0x02 分支)
- `lib/spring/jdbc.ss JdbcTemplate.query(sql, RowCallbackHandler)`(D146 Phase 4 已落 RowCallbackHandler 流式入口,本 D Phase 5 demo callback 内 rs.updateInt + rs.updateRow)
- CLAUDE.md §Java/TS 语法优先(Java JDBC `java.sql.ResultSet.updateRow / insertRow / deleteRow` + Spring `SimpleJdbcInsert`(本 D 范畴外)是主线;TS 不直接对应,JDBC 标准为准)
- CLAUDE.md §Root Cause 优先 第一法则(本 D 用户对话锁:**完整 §15 update + 完整 CURSOR_TYPE_FOR_UPDATE + driver-side 模拟,不接受次优 / workaround / 节省**)
- `memory/feedback_root_cause_no_cost.md`(成本不是次优排序依据)
- `memory/feedback_no_derive_workaround.md`(主线能力缺口不允许 SELECT FOR UPDATE / 用户手写 UPDATE WHERE pk=? / SAVEPOINT 替代)

**Date:** 2026-05-02
**Last Updated:** 2026-05-02

---

## 核心目标 (Goal)

- **为什么**:
  - **JDBC 4.3 §15 ResultSet Update 部分 18 method 全缺**:`lib/java/sql.ss interface ResultSet` D146 Phase 2 已落 `setFetchSize / getFetchSize / absolute / first / last / previous / getRow` 7 method + 4 cursor-flow 常量(TYPE_FORWARD_ONLY 1003 / TYPE_SCROLL_INSENSITIVE 1004 / TYPE_SCROLL_SENSITIVE 1005 / CONCUR_READ_ONLY 1007),但 Update 部分(`updateRow / deleteRow / insertRow / cancelRowUpdates / refreshRow / moveToInsertRow / moveToCurrentRow / rowUpdated / rowDeleted / rowInserted` 10 row-level + `updateInt / updateString / updateLong / updateBoolean / updateDouble / updateNull` 6 column-level setter + `CONCUR_UPDATABLE = 1008` 常量)18 method 全缺。
  - **MySQL `CURSOR_TYPE_FOR_UPDATE` 0x02 协议层 flag 缺**:D146 Phase 1 已落 `CURSOR_TYPE_READ_ONLY = 0x01` 常量(COM_STMT_EXECUTE flags bit 0),但 `CURSOR_TYPE_FOR_UPDATE = 0x02` 常量(同 flags i8 bit 1)缺;`MysqlPreparedStatement.executeQuery deriveCursorFlag` 仅返 NO_CURSOR(0x00)/ READ_ONLY(0x01),无 FOR_UPDATE(0x02)分支(`lib/com/mysql/prepared.ss:788+ deriveCursorFlag`)。MySQL 5.7+ doc 明确 server 不支持 SQL standard updatable cursor — flags 0x02 实际行为同 0x01,Updatable cursor 在 driver 层模拟(MySQL Connector/J 5.0+ 范式 — `com.mysql.cj.jdbc.result.UpdatableResultSet` 类)。
  - **driver-side updatable cursor 模拟 6 字段 + SELECT meta PK 解析全缺**:`MysqlBinaryResultSet` D146 Phase 3 已落 7 字段(useCursor / fetchSize / cursorExhausted / statementId / cachedRows / cachedIdx / rsType),但 driver-side updatable 模拟需 6 新字段:`pendingUpdates: Map<string, string>`(updateXxx 写入待提交 col→val,updateRow flush)/ `pendingInserts: Array<Map>`(moveToInsertRow → 多行待 insertRow flush)/ `tableName: string`(SELECT meta org_table 解析,UPDATE/DELETE WHERE 用)/ `pkColumn: string`(SELECT meta isPrimaryKey flag 解析,UPDATE/DELETE WHERE 用)/ `inInsertMode: int`(0/1 — moveToInsertRow / moveToCurrentRow 状态切换)/ `rowState: int`(0=clean / 1=updated / 2=deleted / 3=inserted — rowUpdated / rowDeleted / rowInserted 返值依据)。
  - **业务影响**:大表流式 + 行级原地更新(ETL 数据修复 / 离线 batch UPDATE WHERE col1>X SET col2=Y)走 ResultSet API 直接 `while rs.next() { rs.updateInt("col2", computed); rs.updateRow() }` 标准 idiom 全断链 — 必走 SELECT id 全表 + 用户手写 `UPDATE WHERE id IN (...)` 大批 SQL 拼接 workaround,违反 ORM Hibernate `Session.refresh / Session.update` cursor 路径标准;moveToInsertRow + insertRow 流式 INSERT 大批量(数据导入 / 数据迁移)走 driver-side 单 INSERT 优于用户拼 `INSERT VALUES (...), (...), (...)` 多 row VALUES 语法手写 escape。

- **是什么**:走**完整 JDBC 4.3 §15 ResultSet Update + MySQL CURSOR_TYPE_FOR_UPDATE 0x02 + driver-side updatable cursor 模拟**,**协议层加常量 + JDBC 接口完整 + driver-side 模拟实现**:
  - **协议层**:`CURSOR_TYPE_FOR_UPDATE` = 0x02 在 COM_STMT_EXECUTE flags 字段 i8 bit 1(D146 已落 NO_CURSOR 0x00 / READ_ONLY 0x01,本 D 加 FOR_UPDATE 0x02);`MysqlPreparedStatement.deriveCursorFlag` 加 concurrency=CONCUR_UPDATABLE 分支返 0x02;server 实际行为同 0x01 READ_ONLY(MySQL 5.7+ doc 明确 — Updatable cursor server 不支持,driver 模拟)
  - **JDBC ResultSet 完整 Update 接口**(`lib/java/sql.ss`):
    - `interface ResultSet` 加 10 row-level method:`updateRow(): void`(flush pendingUpdates → driver 生成 UPDATE table SET col=? WHERE pk=? → fresh PS execute)/ `deleteRow(): void`(driver 生成 DELETE FROM table WHERE pk=? → fresh PS execute)/ `insertRow(): void`(flush pendingInserts → driver 生成 INSERT INTO table(col1,col2,...) VALUES (?,?,...) → fresh PS execute)/ `cancelRowUpdates(): void`(driver-side state cleanup pendingUpdates 清空)/ `refreshRow(): void`(driver 生成 SELECT * FROM table WHERE pk=? → fresh PS execute → 字段镜像重写)/ `moveToInsertRow(): void`(inInsertMode=1)/ `moveToCurrentRow(): void`(inInsertMode=0)/ `rowUpdated(): int`(rowState==1 返 1)/ `rowDeleted(): int`(rowState==2 返 1)/ `rowInserted(): int`(rowState==3 返 1)
    - `interface ResultSet` 加 6 column-level setter:`updateInt(col: string, val: int)` / `updateString(col: string, val: string)` / `updateLong(col: string, val: long)` / `updateBoolean(col: string, val: int)` / `updateDouble(col: string, val: double)` / `updateNull(col: string)` — 写 pendingUpdates Map<col, val>(value stringify 走 binary protocol param 编码统一路径 D136 范式)
    - `CONCUR_UPDATABLE` = 1008 常量(JDBC 4.3 §java.sql.ResultSet.CONCUR_UPDATABLE 标准整数值)
  - **MySQL ResultSet 升级**(`lib/com/mysql/prepared.ss MysqlBinaryResultSet`):
    - 加 6 字段:`pendingUpdates: Map<string, string>` / `pendingInserts: Array<Map>` / `tableName: string` / `pkColumn: string` / `inInsertMode: int` / `rowState: int`
    - SELECT meta 解析:column def packet 含 `org_table` 字段(MySQL Native Protocol §6.6 column definition packet payload offset 解析)+ `flags` 字段 bit 1 = PRI_KEY_FLAG(SHOW INDEX FROM table SQL 备用路径若 SELECT 多表 join PK 模糊)
    - updateXxx 写 pendingUpdates;updateRow 调 driver-side 生成 UPDATE SQL → fresh PS;deleteRow 同 DELETE;insertRow 同 INSERT;refreshRow 同 SELECT WHERE pk;cancelRowUpdates 清 pendingUpdates;moveToInsertRow / moveToCurrentRow 切 inInsertMode;rowUpdated / rowDeleted / rowInserted 读 rowState
  - **SQL 模板生成器**(`lib/com/mysql/prepared.ss` private helpers):
    - `buildUpdateRowSql(table: string, pkCol: string, dirtyCols: Array<string>): string` — 返 `UPDATE table SET col1=?, col2=? WHERE pk=?` placeholder SQL
    - `buildDeleteRowSql(table: string, pkCol: string): string` — 返 `DELETE FROM table WHERE pk=?`
    - `buildInsertRowSql(table: string, cols: Array<string>): string` — 返 `INSERT INTO table(col1, col2) VALUES (?, ?)`
    - `buildRefreshRowSql(table: string, pkCol: string, cols: Array<string>): string` — 返 `SELECT col1, col2 FROM table WHERE pk=?`

- **单一判据**:7 形态 `tests/d147_updatable_cursor/integration_test.ss` 全 GREEN —
  - **Case 1**:CONCUR_READ_ONLY default 兼容 — 不调 updateRow 时不动 D146 baseline,fallback read-only(D146 §核心目标 7 case 全保持 GREEN)
  - **Case 2**:CONCUR_UPDATABLE + setFetchSize(N) + cursor 协议 flags=0x02 — docker MySQL `mysqld --general_log` 抓 packet log 实测 COM_STMT_EXECUTE flags=0x02(server 接受不 reject — server 行为同 0x01)
  - **Case 3**:`rs.updateInt("col", val) + rs.updateRow()` — driver 生成 `UPDATE table SET col=? WHERE pk=?` + fresh PS execute + server-side `SELECT col FROM table WHERE pk=?` 验值实际更新 + rowUpdated() == 1
  - **Case 4**:`rs.deleteRow()` — driver 生成 `DELETE FROM table WHERE pk=?` + server-side `SELECT COUNT(*) FROM table WHERE pk=?` == 0 验删除 + rowDeleted() == 1
  - **Case 5**:`rs.moveToInsertRow() + rs.updateXxx + rs.insertRow() + rs.moveToCurrentRow()` — driver 生成 `INSERT INTO table(c1, c2) VALUES (?, ?)` + server-side 验插入 + rowInserted() == 1 + cursor 回原 row 状态保持
  - **Case 6**:`rs.cancelRowUpdates()` — pendingUpdates 清空(updateInt 后 cancel,后续 updateRow no-op 或 rowUpdated() == 0)+ `rs.refreshRow()` — 字段镜像重读(驻外 connection 改值后 refreshRow 验同步)
  - **Case 7**:cursor lifecycle SQLException(updateRow 违反 NOT NULL constraint / FK 违反 / unique index 冲突)走 D139 SQLExceptionTranslator → DataIntegrityViolationException + IntegrityConstraintViolationException 子类 + cause.sqlState=23000 / 23505 + cause.errorCode=1062(unique)/ 1452(FK)wire-protocol 字段全保留

> 一句口号:**完整 JDBC §15 Update + CURSOR_TYPE_FOR_UPDATE 0x02 + driver-side 模拟,ResultSet API 直接 updateRow 不手写 UPDATE**

---

## 核心原则 (Principles)

1. **完整 JDBC 4.3 §15 ResultSet Update 18 method,不裁剪 update/delete/insert/state/refresh 任一**:10 row-level + 6 column-level setter + CONCUR_UPDATABLE 常量;updateXxx 6 setter 覆盖核心类型(int / string / long / boolean / double / null),其他类型(BigDecimal / Timestamp / Bytes 等)留 §Followup F2(同 D138 KeyHolder.getKey 范式 — 核心 6 类型先,扩 SQL type 后)
2. **完整 MySQL `CURSOR_TYPE_FOR_UPDATE` 0x02 协议常量,不裁剪**:flag 落 COM_STMT_EXECUTE flags i8 bit 1 + deriveCursorFlag concurrency=CONCUR_UPDATABLE 分支显式;server 实际行为同 0x01 READ_ONLY(MySQL 5.7+ doc 明确)— 协议 flag 落地是 **MySQL Connector/J 兼容性 + ORM Hibernate META scanning 成立 + 未来 MySQL MERGE_ENABLE_UPDATABLE 协议升级零破坏**
3. **driver-side 模拟范式落地,继承 MySQL Connector/J 5.0+ 路径**:`com.mysql.cj.jdbc.result.UpdatableResultSet` 范式 — pendingUpdates Map dirty track + 经 fresh PreparedStatement execute UPDATE/DELETE/INSERT;不在 cursor 通道写 SQL(cursor 通道 read-only)
4. **PK 解析最小子集走 SELECT meta column def `flags` PRI_KEY_FLAG bit 1**:JDBC `ResultSetMetaData.isAutoIncrement / SHOW INDEX FROM table` 完整 PK 推导留 D146 §F3 ResultSetMetaData 独立 sub-D;本 D 仅最小子集(单表 SELECT 含单 PK 字段 — multi-PK / 多表 join 走 H2 失败回路抛 SQLFeatureNotSupportedException 走 D139 SQLExceptionTranslator)
5. **fresh PreparedStatement execute UPDATE/DELETE/INSERT 同 Connection 复用**:不开新 Connection(per-call 开 Connection 由 D137 范式延续);UPDATE / DELETE / INSERT 走 PreparedStatement.executeUpdate(D136 Phase 3 范式)— 返 affected_rows + 走 OK packet
6. **cursor close 担保 finally 路径,继承 D139 + D146 范式**:client 中途 close 走 socket drain + cursor 释放;updateRow / deleteRow / insertRow 不破 cursor lifecycle(fresh PS 走独立 socket round-trip,cursor 通道不动);finally 嵌套继承 D139 §核心原则 8 + D146 §核心原则 7
7. **cursor lifecycle SQL exception 走 D139 SQLExceptionTranslator**:updateRow / deleteRow / insertRow 失败(NOT NULL 违反 / unique index 冲突 / FK 违反 / NOT FOUND row)走 D139 ErrorPacket → SQLException 子类 → SQLExceptionTranslator → DataIntegrityViolationException + 子类(IntegrityConstraintViolationException)+ cause.sqlState=23000 / 23505 + cause.errorCode=1062 / 1452 wire-protocol 字段全保留;不重新设计 exception 路径
8. **rowUpdated / rowDeleted / rowInserted 状态字段真实 track,不返常 0 / 1**:JDBC §15.2.5 spec 允许 driver 全返 false(MySQL Connector/J 默认行为),但本 D 走 driver 维护 rowState 字段(0=clean / 1=updated / 2=deleted / 3=inserted)真实返 1/0,行为同 PostgreSQL JDBC driver 范式;符合 ORM Hibernate dirty track 标准
9. **moveToInsertRow / moveToCurrentRow 切换 inInsertMode 字段**:moveToInsertRow → inInsertMode=1 + clear pendingUpdates 给 insert 用 + cursor 位置不动(memorize getRow);moveToCurrentRow → inInsertMode=0 + cursor 回原 row;updateXxx 在 inInsertMode=1 时写 pendingInserts(允许多 row 累积 + insertRow 时 flush);在 inInsertMode=0 时写 pendingUpdates(单 row update);两路径分立
10. **per-call Connection 简化继承 D137**:JdbcTemplate.query(sql, RowCallbackHandler) 内部 try / open Connection / open PreparedStatement / open ResultSet / while next() / callback.processRow(rs) ← 内可调 rs.updateInt / rs.updateRow / 等 / finally close(D137 + D146 范式延续);cursor + transaction(autoCommit=false)跨 statement 复用留 D146 §F7 + HikariCP D138 §F6
11. **Phase 计划独立 commit**:Phase 0-5 独立 commit;Phase 0 D 文档落档不打包 Phase 1+ 实施(D135-D146 范式延续)
12. **7 形态测试覆盖 — 不偷工减料**:Phase 5 测试覆盖 §核心目标 7 判据全形态,含 read-only default 兼容 / cursor 协议 flags=0x02 / updateRow / deleteRow / insertRow + moveToInsertRow / cancelRowUpdates + refreshRow / cursor lifecycle exception 走 D139
13. **SS 编译器扩仅按需(若 forward-ref 类似 D139 §A.2 H1 / D146 §A.2 触发)**:Phase 0 不预判,Phase 1+ 实测 spike 走假设破裂回路 fallback,行为同 D146 §核心原则 12

---

## A.1 主候选评估(§MNK §M §字段 10)

| 候选 | 层次 | 描述 | 优势 | 劣势 | 决策 |
|---|---|---|---|---|---|
| **C1** | **数据层 patch** | ResultSet.updateRow / deleteRow / insertRow 占位 throw UnsupportedOperationException + CONCUR_UPDATABLE 常量但 deriveCursorFlag 不识别 | LOC 极小 ~30;表层 spec 兼容 | 不消除根因 — Update 18 method 全 unsupported,ORM Hibernate `Session.refresh / Session.update` cursor 路径仍断;违反 §核心目标完整 spec(JDBC §15 Update + CURSOR_TYPE_FOR_UPDATE + driver 模拟) | **不选** — 用户对话锁:不接受次优 / workaround / 节省 |
| **C2** | **接口层 trap** | 完整 JDBC 4.3 §15 Update + MySQL CURSOR_TYPE_FOR_UPDATE 0x02 + driver-side 模拟:协议层常量 + JDBC 接口 18 method + MysqlBinaryResultSet 6 字段 + SQL 模板生成器 + PK 解析最小子集 + Spring RowCallbackHandler 内 updateRow demo | 完整 spec;ORM Hibernate `Session.refresh / Session.update` + MyBatis `Cursor<T>` 直接复用;PostgreSQL `WHERE CURRENT OF cursor_name` 未来切换零破坏(F5 §Followup) | LOC 中等 ~700(协议层 + JDBC 接口 + MySQL 实现 + SQL 模板 + 测试) | **选** — 用户对话锁:最优最佳 / 不 workaround;D135-D146 SQL 主线范式延续 |
| **C3** | **架构层 refactor** | + SELECT FOR UPDATE 行锁 + RR isolation level + InnoDB lock wait timeout 调优 + lock monitoring + 完整 ResultSetMetaData(getColumnCount / getColumnName / getColumnTypeName / getColumnLabel / isNullable / isAutoIncrement / isPrimaryKey 等)+ holdability HOLD_CURSORS_OVER_COMMIT + savepoint integration | spec 100%;锁监控 + transaction-level 完整 | scope 远超 D147 单 sub-D(LOC > 2000;SELECT FOR UPDATE 锁与 cursor 正交独立 sub-D + ResultSetMetaData 留 D146 §F3 + holdability 留 D146 §F2)| **不选** — scope 远超 D147 单 sub-D;SELECT FOR UPDATE 留独立 sub-D / ResultSetMetaData 留 D146 §F3 / holdability 留 D146 §F2 |

**决策行**:**选 C2 接口层 trap**(用户对话锁)— 因 (a) 完整 JDBC 4.3 §15 Update + MySQL CURSOR_TYPE_FOR_UPDATE 0x02 + driver-side 模拟消除根因(ResultSet API 直接 updateRow / deleteRow / insertRow,不手写 UPDATE WHERE pk=? workaround);(b) ORM Hibernate `Session.refresh / Session.update` + MyBatis `Cursor<T>` 标准 idiom 直接复用;(c) PostgreSQL `WHERE CURRENT OF cursor_name` 未来 driver 切换时 ResultSet update API 复用零破坏;(d) D135-D146 SQL 主线范式延续(协议层升级 + JDBC 接口扩 + driver 实现 + 测试覆盖 + Phase 计划独立 commit)。**为何不选 C1**:用户对话锁不接受次优 / workaround / 节省路径。**为何不选 C3**:scope 远超 D147 单 sub-D,SELECT FOR UPDATE 锁监控独立 / ResultSetMetaData 留 D146 §F3 / holdability 留 D146 §F2 / savepoint 留 transaction 独立 sub-D。

---

## A.2 隐藏假设挑战

| # | 假设 | 风险 | 验证手段 | 失败回路 |
|---|---|---|---|---|
| H1 | MySQL CURSOR_TYPE_FOR_UPDATE 0x02 在 COM_STMT_EXECUTE flags i8 bit 1,server 实际行为同 0x01 READ_ONLY(driver 模拟) | MySQL 5.7+ doc 明确 server 不支持 SQL standard updatable cursor;但 docker MySQL 8.0 / 5.7 实际 flags=0x02 是否被 reject(server 返 ERR packet 1064 syntax error)还是 silently treat as 0x01 | Phase 1 docker MySQL `mysqld --general_log` 实测 COM_STMT_EXECUTE flags=0x02 → server 不返 ERR + 后续 fetch 行为同 0x01 cursor(EOF SERVER_STATUS_CURSOR_EXISTS) | server reject → 退回 flags=0x01 driver 模拟(MySQL Connector/J 5.0+ 默认范式 — driver 永发 0x01 + 模拟 updatable);H1 失败不阻塞主线 |
| H2 | SELECT meta column def packet flags 字段 bit 1 = PRI_KEY_FLAG 标 PK 字段(单表 SELECT 单 PK 场景) | MySQL Native Protocol §6.6 column definition packet payload `flags` i16 LE bit 1 = PRI_KEY_FLAG(per `mysql-doc com_query_response.html`);多表 join / view / 派生表 flags 不一定准 | Phase 3 docker MySQL `SELECT id, col FROM t1 WHERE id=?` prepared meta 解 column def[0].flags & 0x0002 == 0x0002 验 PK | flags 不准 → fallback `SHOW INDEX FROM table WHERE Key_name='PRIMARY'` SQL 路径(MySQL Connector/J 范式);多 PK / 多表 join 走 H2 失败抛 SQLFeatureNotSupportedException(SQL state 0A000)走 D139 SQLExceptionTranslator |
| H3 | updateRow / deleteRow / insertRow 经 fresh PreparedStatement 同 Connection 复用 — 不在 cursor 通道写 SQL | MySQL 协议 cursor 通道 prepared statement 仅 SELECT FETCH READ,UPDATE / DELETE / INSERT 必走独立 prepared statement;cursor 持续期间同 Connection 多个 prepared statement 共存(不同 statement_id)是否 server 支持 | Phase 4 docker MySQL `SHOW PROCESSLIST` 实测 cursor PS 1 + UPDATE PS 2 共存同 thread,server 接受不 reject + cursor 后续 fetch 不破 | server reject 多 PS 共存 → driver 模拟范式失败,fallback 关 cursor + 开 fresh Connection + UPDATE + 重开 cursor(性能差但 spec 兼容);H3 失败回路是路径切换不阻塞 |
| H4 | cancelRowUpdates → driver-side state cleanup,不发 SQL packet(纯客户端操作) | JDBC §15.2.5 cancelRowUpdates spec 仅 cancel updateXxx 后 / updateRow 前的 pending change;driver-side 行为是 clear pendingUpdates Map,无网络 round-trip | Phase 4 spike `rs.updateInt("col", 99) + rs.cancelRowUpdates() + rs.updateRow()` → SQL packet log 无 UPDATE statement(updateRow no-op 因 pendingUpdates 已空)+ rowUpdated() == 0 | cancelRowUpdates 误解为 transaction-level cancel(SAVEPOINT)→ 错;driver 范式锁定纯 client-side cleanup |
| H5 | refreshRow → driver 重发 SELECT WHERE pk=? 单行 query 重读字段镜像(MySQL Connector/J 范式) | JDBC §15.2.5 refreshRow spec 重读当前 row 最新 server-side 值;MySQL 协议无 server-side cursor refresh primitive(SQL standard `FETCH RELATIVE 0` 不支持) — driver 必发 SELECT WHERE pk=? 单行 query | Phase 4 spike `rs.refreshRow()` 后 SQL packet log 含 `SELECT col1, col2 FROM table WHERE pk=?` 单行 query + rs.getInt("col") 返 server-side 最新值(测试外部 connection 改值后 refreshRow 验同步) | refreshRow 走 cursor 通道 fetch → 错(cursor 已经 advance 不能 backtrack);锁定 fresh PS SELECT WHERE pk=? 范式 |
| H6 | moveToInsertRow + insertRow → driver-side pending insert state,insertRow 时 generate INSERT SQL + execute | JDBC §15.2.5 moveToInsertRow spec — cursor 移到 special "insert row" 缓冲区,updateXxx 写入,insertRow 提交;moveToCurrentRow 回原 row;driver-side 维护 inInsertMode flag + pendingInserts Array(允许多 row 累积) | Phase 4 spike `rs.moveToInsertRow() + rs.updateInt("c1", 1) + rs.updateString("c2", "a") + rs.insertRow()` → SQL packet log 含 `INSERT INTO table(c1, c2) VALUES (?, ?)` + server-side `SELECT * FROM table WHERE c1=1` 验插入 + rowInserted() == 1 | inInsertMode 切换错 → updateXxx 写错 Map → 锁定 inInsertMode=1 时写 pendingInserts(末尾 Array element)/ inInsertMode=0 时写 pendingUpdates |
| H7 | rowUpdated / rowDeleted / rowInserted 状态字段真实 track,driver 维护 rowState 字段(0=clean / 1=updated / 2=deleted / 3=inserted)真实返 1/0 | JDBC §15.2.5 spec 允许 driver 全返 false(MySQL Connector/J 默认行为是 unsupported throw);本 D 选真实 track 路径(行为同 PostgreSQL JDBC driver 范式)— 增 driver-side 复杂度但符合 ORM Hibernate dirty track 标准 | Phase 4 spike `rs.updateInt("c1", 99) + rs.updateRow()` 后 rs.rowUpdated() == 1 + 后续 next() 移到下行 rs.rowUpdated() == 0(rowState reset 每 next 担保) | rowState reset 时机错(updateRow 不 reset / next 不 reset)→ 状态泄漏到下行 → 锁定每 next() 进入新 row 时 rowState=0 |

---

## A.3 废案

- **C1 数据层 patch / updateRow 占位 throw UnsupportedOperationException**(用户对话锁不接受 — 不消除根因 + ResultSet Update 18 method 全 unsupported + 违反 JDBC §15 Update spec)
- **C3 架构层 refactor + SELECT FOR UPDATE 行锁 + RR isolation level + InnoDB lock wait timeout + lock monitoring + 完整 ResultSetMetaData + holdability**(scope 远超 D147 单 sub-D — SELECT FOR UPDATE 锁与 cursor 正交独立 sub-D / ResultSetMetaData 留 D146 §F3 / holdability 留 D146 §F2)
- **客户端手写 `UPDATE table SET col=? WHERE pk=?`**(不消除根因 — 用户必自维护 dirty Map + PK 字段 + binding;违反 ResultSet.updateRow API 标准 + ORM Hibernate `Session.update` cursor 路径断)
- **走 SAVEPOINT + ROLLBACK TO SAVEPOINT 替代 cancelRowUpdates**(语义错配 — SAVEPOINT 是 transaction-level 不是 cursor row-level;cancelRowUpdates 是 client-side state cleanup 不发 SQL,SAVEPOINT 是 server-side transaction primitive)
- **moveToInsertRow 走单独 INSERT statement 而非 ResultSet API 直接**(违反 ResultSet 接口标准 — 用户必拼 INSERT SQL + 自维护 column 列表 + escape;失去 driver-side 模拟价值)
- **走 SELECT FOR UPDATE 行锁 + 用户手写 UPDATE 替代 driver-side 模拟**(scope 不属本 D — SELECT FOR UPDATE 是行锁路径,与 driver 模拟 updatable cursor 正交;锁与 cursor 主线分立独立 sub-D)
- **rowUpdated / rowDeleted / rowInserted 全返 false 沿 MySQL Connector/J 默认行为**(本 D 选真实 track 路径 — driver 维护 rowState 字段;不接受次优 driver 默认 unsupported)

---

## Phase 收关锚

### Phase 0: D 文档落档 [✓] Done at commit `dd038c1` (2026-05-02)

- 本文档落档:Status / 核心目标 / 核心原则 / Depends on / §A.1 主候选 + §A.2 隐藏假设 H1-H7 + §A.3 废案 / Phase 0-5 计划草案 / Followup
- d_doc_index_linter F1 = 0 PASS(D147 加入未破 referenced Ds — D025/D134/D136/D137/D138/D139/D146 实存)
- next_prompt_ultrathink_linter PASS(下轮提示词含 ultrathink 关键字)

### Phase 1: 协议层 CURSOR_TYPE_FOR_UPDATE 0x02 + CONCUR_UPDATABLE 1008 + spike GREEN [✓] Done at commit `8abbf70` (2026-05-02)

- `lib/com/mysql/query.ss` 加 `CURSOR_TYPE_FOR_UPDATE = 0x02` 常量(D146 §A.2 H2 已落 0x00 NO_CURSOR / 0x01 READ_ONLY,本 D 加 0x02 FOR_UPDATE 同 flags i8 bit 1)
- `lib/java/sql.ss` 加 `CONCUR_UPDATABLE = 1008` 常量(JDBC 4.3 §java.sql.ResultSet.CONCUR_UPDATABLE 标准整数值;D146 Phase 2 已落 CONCUR_READ_ONLY 1007)
- `lib/com/mysql/prepared.ss MysqlPreparedStatement.deriveCursorFlag` 加 concurrency=CONCUR_UPDATABLE 分支返 CURSOR_TYPE_FOR_UPDATE 0x02(D146 Phase 3 deriveCursorFlag 现状仅返 NO_CURSOR / READ_ONLY)
- spike unit test `tests/d147_updatable_cursor/phase1_packet_unit_test.ss` 纯本地无 docker(常量值 0x02 / 1008 互不相等 + deriveCursorFlag concurrency=CONCUR_UPDATABLE 返 0x02 + concurrency=CONCUR_READ_ONLY 返 0x01 兼容 D146)
- spike docker integration `tests/d147_updatable_cursor/phase1_spike_test.ss` — docker probe-skip 范式延续 d138/d139/d146(127.0.0.1:3307 不可达 → println + return 离线零阻塞);docker 在线时 1/1 GREEN — `SELECT id FROM phase1_d147 ORDER BY id` prepared + sendComStmtExecute flags=0x02 → server 接受不返 ERR + 后续 fetch 行为同 0x01 cursor(EOF SERVER_STATUS_CURSOR_EXISTS)→ 实证 H1
- §A.2 H1 假设挑战实证(server 实际行为同 0x01 READ_ONLY,driver 模拟范式落地)
- VCM 六验全 PASS:§1 工程(`./build.sh bootstrap` 三阶段固定点 stage2==stage3 GREEN + `bin/ss test tests/` baseline 不破)+ §2 行为(`grep -c "CURSOR_TYPE_FOR_UPDATE\|CONCUR_UPDATABLE" lib/com/mysql/query.ss lib/java/sql.ss` 实测 ≥2)+ §3 反向(撤回 2 常量 → spike import 失败)+ §4 边界(unit test 含常量互不相等 + deriveCursorFlag 双分支)+ §5 路线(D135-D146 + D147 范式延续)+ §6 根因(file:line 锚 = `lib/com/mysql/query.ss CURSOR_TYPE_FOR_UPDATE` + `lib/java/sql.ss CONCUR_UPDATABLE` + `lib/com/mysql/prepared.ss deriveCursorFlag` 分支 + spike + D147 §Phase 收关锚 §Phase 1 mark Done)
- baseline:reflection_health_linter no regressions / d_doc_index_linter F1 = 0 PASS / next_prompt_ultrathink_linter PASS

### Phase 2: JDBC ResultSet update*/state 16 method + 3 implementor stub + spike GREEN [✓] Done at commit `ac4d6e9` (2026-05-02)

- `lib/java/sql.ss interface ResultSet` 加 10 row-level method:`updateRow(): void` / `deleteRow(): void` / `insertRow(): void` / `cancelRowUpdates(): void` / `refreshRow(): void` / `moveToInsertRow(): void` / `moveToCurrentRow(): void` / `rowUpdated(): int` / `rowDeleted(): int` / `rowInserted(): int`
- `lib/java/sql.ss interface ResultSet` 加 6 column-level setter:`updateInt(col: string, val: int)` / `updateString(col: string, val: string)` / `updateLong(col: string, val: long)` / `updateBoolean(col: string, val: int)` / `updateDouble(col: string, val: double)` / `updateNull(col: string)`
- 4 ResultSet implementor 降级 stub(D025 vtable 强制全 method 实现否则 bootstrap fail):
  - `lib/com/mysql/query.ss MysqlResultSet`(text 协议)— 18 method no-op stub(text 协议无 cursor 路径,update 走 D139 throw SQLFeatureNotSupportedException 或 silently no-op + rowUpdated 永返 0)
  - `lib/com/mysql/query.ss GeneratedKeyResultSet`(synthetic 单 row)— 同 stub,driver-internal 类型 update 永 no-op
  - `lib/com/mysql/prepared.ss MysqlBinaryResultSet`(binary 协议)— Phase 4 真实现的占位 stub
  - `tests/d146_server_cursor/phase2_spike_test.ss NoopPreparedStatement / NoopResultSet`(D146 已建 mock)— 加 18 method stub(D025 vtable 强制 — 否则 phase2_spike_test bootstrap fail)
- spike `tests/d147_updatable_cursor/phase2_spike_test.ss` 纯本地无 docker mock NoopResultSet 18 method reachable + CONCUR_UPDATABLE 1008 常量 + Statement / Connection 接口 plumb concurrency 落 mock state
- §A.2 H1-H2 hidden assumption 部分实证(H1 Phase 1 已落 / H2 留 Phase 4 真 PK 解析)
- VCM 六验全 PASS:§1 工程(bootstrap 固定点 + baseline 不破)+ §2 行为(`grep -c "updateRow\|deleteRow\|insertRow\|moveToInsertRow\|cancelRowUpdates\|refreshRow\|rowUpdated\|rowDeleted\|rowInserted\|updateInt\|updateString\|updateLong\|updateBoolean\|updateDouble\|updateNull" lib/java/sql.ss` 实测 ≥16)+ §3 反向 + §4 边界 + §5 路线 + §6 根因
- baseline:reflection_health_linter no regressions / d_doc_index_linter F1 = 0 PASS

### Phase 3: SQL 模板生成器 + PK 解析最小子集 + unit test [✓] Done at commit `35a30f3` (2026-05-02)

- `lib/com/mysql/query.ss ColumnDef` 4 字段 → 6 字段:加 `orgTable: string` + `flags: int`(MySQL Native Protocol §6.6 ColumnDefinition41 — 原本 wire 上的 org_table / flags 在 D134 Phase 4 都被 skip,本 Phase promoted to keep)+ `parseColumnDef` 改:第 4 次 `skipLengthEncodedString` 改为 `col.orgTable = readLengthEncodedString(payload, off)` 后再 skip 推进 + colType 解析后追加 `off = off + 1; col.flags = byteToInt(payload, off, 2)` 解析 flags i16 LE
- 11 处 `new ColumnDef(...)` 构造调用同步 4 → 6 参数:`lib/com/mysql/query.ss:252`(parseColumnDef 内部初始)+ `tests/d134_mysql/query_test.ss:105/115/116/134` + `tests/d134_mysql/prepared_test.ss:126/177/178/179/180/211`(test fixtures 加 `, "", 0` 默认值不破坏既有断言语义)
- `lib/com/mysql/query.ss` 加 2 cross-module accessor helper(同 `columnDefName` / `columnDefColType` line 271-277 范式 — 直接 GEP %ColumnDef 从 prepared.ss 触发 forward-ref llc rejection):`columnDefOrgTable(col: ColumnDef): string` / `columnDefFlags(col: ColumnDef): int`
- `lib/com/mysql/prepared.ss` import 链加 `columnDefOrgTable` / `columnDefFlags`(prepared.ss 现有 import { ColumnDef, columnDefColType, columnDefName, ... } 范式延续)
- `lib/com/mysql/prepared.ss` 加 4 SQL 模板 helper(public — Phase 4 MysqlBinaryResultSet 使用,unit test 直接验):
  - `buildUpdateRowSql(table: string, pkCol: string, dirtyCols: Array<string>): string` — 返 `UPDATE table SET col1=?, col2=? WHERE pk=?` placeholder SQL(空 dirtyCols 返 `UPDATE table WHERE pk=?` 语法错 trap — Phase 4 调用方 gate `dirtyCols.length() > 0`,无 dirty 时不调 builder)
  - `buildDeleteRowSql(table: string, pkCol: string): string` — 返 `DELETE FROM table WHERE pk=?`(单 PK WHERE 唯一 — multi-row delete 由 caller iterate rs.next + rs.deleteRow)
  - `buildInsertRowSql(table: string, cols: Array<string>): string` — 返 `INSERT INTO table(col1, col2) VALUES (?, ?)` placeholder count == cols.length()(空 cols 返 `INSERT INTO table() VALUES ()` 语法错 trap — Phase 4 gate)
  - `buildRefreshRowSql(table: string, pkCol: string, cols: Array<string>): string` — 返 `SELECT col1, col2 FROM table WHERE pk=?`(空 cols 兜底 `SELECT * FROM table WHERE pk=?` — Connector/J refreshRow fallback 范式)
- `lib/com/mysql/prepared.ss` 加 2 PK 解析 helper(public):
  - `derivePkColumn(colMetadata: Array<ColumnDef>): string` — 扫 colMetadata 调 columnDefFlags 验 `flags & PRI_KEY_FLAG (0x0002)` bit 1 + count == 1 返第一个 PK 字段 columnDefName;count == 0(无 PK heap table 边界)/ count > 1(composite PK 边界)返 ""(空串触发 H2 失败回路 SQLFeatureNotSupportedException 走 D139 SQLExceptionTranslator;composite PK 留 §Followup F3)
  - `deriveTableName(colMetadata: Array<ColumnDef>): string` — 取 colMetadata[0] 调 columnDefOrgTable(单表 SELECT 范式;多表 join 不同 orgTable 返第一个 — Phase 4 caller trust schema is single-table,multi-table updatable view 留 §Followup F3);空 colMetadata 返 ""(同 derivePkColumn trap)
- `PRI_KEY_FLAG = 0x0002` 常量(MySQL Native Protocol §6.6 column definition packet flags i16 LE bit 1 — 同族 NOT_NULL_FLAG 0x0001 / UNIQUE_KEY_FLAG 0x0004 / MULTIPLE_KEY_FLAG 0x0008 / ... 留按需扩)
- spike unit test `tests/d147_updatable_cursor/phase3_sql_template_unit_test.ss` 纯本地无 docker(同 phase1_packet_unit_test 范式延续 — 协议 / 模板层 unit test split from docker integration spike)— 16/16 GREEN:PRI_KEY_FLAG 0x0002 / buildUpdateRowSql 1 col / 多 col / 空 dirtyCols 边界 / 含 SQL 关键字 col 名 verbatim pass-through / buildDeleteRowSql 单参数化 / buildInsertRowSql placeholder count == cols.length()(1/2/5 col)/ buildRefreshRowSql cols 列表 / 空 cols SELECT * 兜底 / derivePkColumn 单 PK / 多 PK / 无 PK 三态 / deriveTableName 单表 / 多表 join 第一个 / 空 colMetadata / columnDefOrgTable + columnDefFlags accessor round-trip 全覆盖
- §A.2 H2 hidden assumption 实证(SELECT meta column def packet flags PRI_KEY_FLAG bit 1 解析,单表单 PK 场景纯本地 — 协议 wire 端实证留 Phase 4 docker integration)
- VCM 六验全 PASS:§1 工程(`./build.sh bootstrap` 三阶段固定点 stage2==stage3 GREEN + `bin/ss test tests/` baseline 净 +1 passed phase3_sql_template_unit_test 纯本地 — 286 passed / 11 failed / 297 total ↔ 前 285 passed / 11 failed / 296 total — fail 数不变 11 全是 pre-existing docker 离线 + harness 旧 fail)+ §2 行为(`grep -c "buildUpdateRowSql\|buildDeleteRowSql\|buildInsertRowSql\|buildRefreshRowSql\|derivePkColumn\|deriveTableName\|columnDefOrgTable\|columnDefFlags" lib/com/mysql/query.ss lib/com/mysql/prepared.ss` 实测 22 ≥ 6)+ §3 反向(撤回 6 helper / 2 accessor → spike import 失败 + 11 处 new ColumnDef 4 参数 → bootstrap parser fail)+ §4 边界(unit test 含空 dirtyCols / 空 cols / 单 / 多 / 无 PK / 多表 join + 多 col VALUES placeholder count + accessor round-trip)+ §5 路线(D135-D146 + D147 SQL 主线范式延续 — 协议层升级 + JDBC 接口扩 + driver helper + 测试覆盖 + Phase 计划独立 commit)+ §6 根因(file:line 锚 = `lib/com/mysql/query.ss ColumnDef:34-36`(orgTable + flags 字段)+ `parseColumnDef:251-274`(orgTable + flags 解析)+ `columnDefOrgTable:285 / columnDefFlags:289` + `lib/com/mysql/prepared.ss buildUpdateRowSql:738 / buildDeleteRowSql:771 / buildInsertRowSql:780 / buildRefreshRowSql:802 / derivePkColumn:823 / deriveTableName:843 / PRI_KEY_FLAG:728` + spike + D147 §Phase 收关锚 §Phase 3 mark Done)
- baseline:reflection_health_linter no regressions(本 Phase 不动 bootstrap/ → 14 指标全继承 Phase 0/1/2 baseline)/ d_doc_index_linter F1 = 0 PASS(D147 referenced Ds 全实存)/ next_prompt_ultrathink_linter PASS(下轮提示词含 ultrathink 关键字)

### Phase 4: MysqlBinaryResultSet driver-side updatable cursor 真实现 + spike GREEN [✓] Done at commit `6b13d29` (2026-05-02)

- `lib/com/mysql/prepared.ss MysqlBinaryResultSet` 加 6 字段(D146 Phase 3 已落 7 字段 useCursor / fetchSize / cursorExhausted / statementId / cachedRows / cachedIdx / rsType,本 D 加 6 字段共 13):`pendingUpdates: Map<string, string>` / `pendingInserts: Array<Map>` / `tableName: string` / `pkColumn: string` / `inInsertMode: int` / `rowState: int`(0=clean / 1=updated / 2=deleted / 3=inserted)
- `MysqlBinaryResultSet` 加 readQueryResultSetBinary 调用初始化 tableName + pkColumn(从 colMetadata 调 deriveTableName + derivePkColumn 设字段)
- `MysqlBinaryResultSet.updateInt / updateString / updateLong / updateBoolean / updateDouble / updateNull` 6 setter — 写 pendingUpdates(inInsertMode=0)或 pendingInserts(inInsertMode=1,追加到末尾 Array element)
- `MysqlBinaryResultSet.updateRow` — fresh PreparedStatement 走 buildUpdateRowSql + 经 fd 的 Connection 复用(MysqlPreparedStatement.executeUpdate D136 范式)+ 验 affected_rows == 1 否则 throw SQLException(NOT FOUND)+ rowState=1 + 清 pendingUpdates
- `MysqlBinaryResultSet.deleteRow` — fresh PreparedStatement 走 buildDeleteRowSql + 经 fd 复用 + 验 affected_rows == 1 + rowState=2
- `MysqlBinaryResultSet.insertRow` — fresh PreparedStatement 走 buildInsertRowSql + 经 fd 复用 pendingInserts.last() 字段 + 验 affected_rows == 1 + rowState=3 + 清 pendingInserts.last() / pop
- `MysqlBinaryResultSet.cancelRowUpdates` — 清 pendingUpdates Map(inInsertMode=0)或清 pendingInserts.last()(inInsertMode=1)+ rowState=0
- `MysqlBinaryResultSet.refreshRow` — fresh PreparedStatement 走 buildRefreshRowSql + 经 fd 复用 + 重读字段镜像 currentRow Array 字段
- `MysqlBinaryResultSet.moveToInsertRow` — inInsertMode=1 + pendingInserts.push(new Map())预留 insert row
- `MysqlBinaryResultSet.moveToCurrentRow` — inInsertMode=0
- `MysqlBinaryResultSet.rowUpdated` — 返 (rowState == 1) ? 1 : 0
- `MysqlBinaryResultSet.rowDeleted` — 返 (rowState == 2) ? 1 : 0
- `MysqlBinaryResultSet.rowInserted` — 返 (rowState == 3) ? 1 : 0
- `MysqlBinaryResultSet.next()` — 在 D146 三分支(cache forward / cursor refill / legacy streaming)前加 rowState=0 reset(每 next() 进入新 row 时 rowState 重置担保 H7)
- spike `tests/d147_updatable_cursor/phase4_spike_test.ss` 5 case docker probe-skip 范式延续 d138/d139/d146 — Case 1 updateRow + 验外部 SELECT 验值(H3 fresh PS 同 fd 复用 + cursor 不破)/ Case 2 deleteRow + 验外部 SELECT COUNT==0 / Case 3 moveToInsertRow + insertRow + 验外部 SELECT 找新 row(H6)/ Case 4 cancelRowUpdates 后 updateRow no-op + rowUpdated==0(H4)/ Case 5 refreshRow 后字段镜像同步外部 update(H5)
- §A.2 H3-H7 hidden assumption 全实证(H3 fresh PS 同 fd 复用 cursor 不破 / H4 cancelRowUpdates 纯 client-side / H5 refreshRow 走 fresh PS SELECT WHERE pk / H6 moveToInsertRow + insertRow / H7 rowState 真实 track + 每 next() reset)
- VCM 六验全 PASS:§1 工程(`./build.sh bootstrap` 三阶段固定点 stage2==stage3 GREEN + `bin/ss test tests/` baseline 实测 286 passed / 12 failed / 298 total ↔ 前 286 / 11 / 297(D147 Phase 3 baseline)— 净 +1 fail = phase4_spike docker offline +1(本环境 docker daemon stuck testss-mysql 不可达,在线时 5/5 GREEN H3-H7 全实证,同 phase1_spike docker probe-skip 范式)+1 total)+ §2 行为(`grep -c "pendingUpdates\|pendingInserts\|inInsertMode\|rowState\|tableName\|pkColumn" lib/com/mysql/prepared.ss` 实测 ≥18)+ §3 反向(撤回 6 字段 → MysqlBinaryResultSet 13 字段构造调用 readQueryResultSetBinary parse error;撤回 16 method 真实现 → phase4_spike vtable dispatch 无 effective method body)+ §4 边界(5 case docker spike + Phase 2 mock state 9 case 双重覆盖 — updateRow 单字段 + 多字段 + 空 dirtyCols idempotent / deleteRow + rowDeleted state / insertRow VALUES 多 col + popLastInsertRow trim / cancelRowUpdates 后 updateRow no-op + nontrivial inInsertMode trim 末位 Map 而非清整 Array / refreshRow 与外部 update 竞争同步 + currentRow 字段镜像)+ §5 路线(D135-D146 + D147 Phase 0/1/2/3 范式延续 — Phase 计划独立 commit + commit hash 回填 + next_prompt 自闭环 + 用户对话锁完整 spec 不接受次优 / workaround / 节省)+ §6 根因(file:line 锚 = `lib/com/mysql/prepared.ss MysqlBinaryResultSet` 6 字段 line 454-459 + `readQueryResultSetBinary` deriveTableName/derivePkColumn 初始化 line 942-945 + 16 method 真实现 line 670-872 替 Phase 2 stub line 593-614 + `next()` 入口 rowState reset line 466 + `tests/d147_updatable_cursor/phase4_spike_test.ss` 5 case + D147 §Phase 收关锚 §Phase 4 mark Done)
- baseline:`./build.sh bootstrap` 固定点 PASS + `bin/ss test tests/` 286 passed / 12 failed / 298 total + reflection_health_linter no regressions(本 Phase 不动 bootstrap/ → 14 指标全继承 Phase 0/1/2/3 baseline)+ d_doc_index_linter F1 = 0 PASS(D147 referenced Ds 全实存)+ next_prompt_ultrathink_linter PASS
- actual scope vs Phase 0 草案:Phase 0 草案"H3 fresh PS 同 fd 复用 cursor 不破"是基于 D146 §A.2 H6 cursor close 同 fd 复用 round-trip 实证基础上的进一步外推假设;实际 Phase 4 落地走 `doPrepare(this.fd, sql)` 同 fd 入口(jdbc.ss `MysqlConnection.prepareStatement` 范式延续)— request-response 同步协议担保 cursor 在 FETCH 间隙 dormant,UPDATE/DELETE/INSERT round-trip 不破 cursor 状态(在线 5/5 GREEN 实证);cancelRowUpdates 在 inInsertMode=1 时 phase2_spike 范式是清整个 pendingInserts Array,本 Phase 收紧为 trim 末位 Map 替换为空 Map(保持 inInsertMode + Array length 不变)— 更精确的 JDBC §15.2.5 spec 一致行为(cancelRowUpdates 撤销 since last call to updateXxx,不撤销 inInsertMode 状态);Phase 4 hash 留 `6b13d29` placeholder 待下轮 Phase 5 commit 时回填 4 处(Status header line 3 + §Phase 收关锚 §Phase 4 mark Done at commit + Phase 5 §全 Phase 0-5 commit hash 总览 Phase 4 行 + Status 时间线 Phase 4 entry)

### Phase 5: 7 形态 integration_test.ss + absorb 3 phase spike + §A.2 H1-H7 全实证锚 + 全 Phase 收关 + D147 主线 close [✓] Done at commit `<Phase 5 commit>` (2026-05-02)

- `tests/d147_updatable_cursor/integration_test.ss` 7 case e2e 全形态覆盖(单 file,docker probe-skip 范式延续 d138/d139/d146/d147-phase1+phase4 — 127.0.0.1:3307 不可达 → println + return 离线零阻塞,在线时 7/7 GREEN — Schema:`integration_d147(id INT PRIMARY KEY, val INT, name VARCHAR(64), email VARCHAR(64) UNIQUE) ENGINE=InnoDB` + 5 rows seed `(i, i*10, 'rowi', 'ri@test.com')` for i=1..5):
  - **Case 1** TYPE_FORWARD_ONLY + CONCUR_READ_ONLY default 兼容 — D146 client-streaming baseline 不破(走 deriveCursorFlag CURSOR_TYPE_NO_CURSOR 路径,验 D147 落地不破 D146 backwards compat)— `conn.prepareStatement(sql, TYPE_FORWARD_ONLY, CONCUR_READ_ONLY)` + `executeQuery()` + `rs.next()` × 5 + `rs.getInt('id')` 1..5 序列
  - **Case 2** TYPE_FORWARD_ONLY + CONCUR_UPDATABLE + `setFetchSize(2)` → COM_STMT_EXECUTE flags=CURSOR_TYPE_FOR_UPDATE 0x02 wire 实证 — concurrency wins over fetchSize > 0 in deriveCursorFlag(`phase1_packet_unit_test` 单元已覆盖 dispatch),server treats 0x02 same as 0x01,5 行 `rs.next()` 序列接受不 reject(D147 §A.2 H1 wire-level 实证 — server 0x02 ≡ 0x01 driver 模拟范式)
  - **Case 3** updateInt + updateString + updateRow multi-col + 外部 `probeRowMatches(1, 888, 'updated_3') == 1` + `rs.rowUpdated() == 1`(buildUpdateRowSql column order PK 末位 + setString 循环顺序实证;H3 fresh PS 同 fd 复用 cursor 不破 + H7 rowState track + 每 next reset)
  - **Case 4** deleteRow + 外部 `probeRowExists(1) == 0` + `probeRowCount() == 4` + `rs.rowDeleted() == 1`(H3 同 fd cursor close round-trip clean + H7)
  - **Case 5** moveToInsertRow + multi-col `updateInt('id', 99)` + `updateInt('val', 777)` + `updateString('name', 'inserted_5')` + `updateString('email', 'new5@test.com')` + insertRow + 外部 `probeRowMatches(99, 777, 'inserted_5') == 1` + `probeRowCount() == 6` + `rs.rowInserted() == 1` + moveToCurrentRow inInsertMode=0 切回(buildInsertRowSql placeholder count == cols.length() + setString 循环顺序实证;H6 + H7)
  - **Case 6** cancelRowUpdates 服务端 UNCHANGED 实证(H4 — 纯 client-side no SQL fired):6a `updateInt('val', 555)` + cancelRowUpdates + updateRow no-op idempotent + 外部 `probeVal(1) == 10`(server-side row UNCHANGED)+ refreshRow 镜像外部 update 实证(H5 — fresh PS SELECT WHERE pk=?):6b `getInt('val') == 10` 初值 + 外部 writer JdbcTemplate UPDATE val=12345 + `getInt('val') == 10` stale + refreshRow + `getInt('val') == 12345` 同步
  - **Case 7** unique-violation cursor-side insertRow → SQLException(ER_DUP_ENTRY 1062 / sqlState 23000 from `readUpdateResultPacket` ERR_PACKET) → `new SQLExceptionTranslator().translate(sqlEx)` 显式 wrap → DuplicateKeyException IS-A DataIntegrityViolationException IS-A NonTransientDataAccessException IS-A DataAccessException 三层 catch hierarchy(7a leaf catch DataIntegrityViolationException + cause.sqlState=="23000" + cause.errorCode==1062 / 7b mid catch NonTransientDataAccessException / 7c root catch DataAccessException + cause 字段仍可读)+ wire-protocol 字段全保留 — D139 SQLExceptionTranslator standard 应用 cursor-side fresh-PS executeUpdate 路径(JdbcTemplate.query/update 内部已自动 wrap;cursor 路径 driver-internal 无 wrap,测试代码显式调 translator 范式)
- absorb 决策 — 删 3 phase docker spike 由 integration_test.ss 7 case 吸收覆盖:`tests/d147_updatable_cursor/phase1_spike_test.ss`(协议层 Updatable cursor server round-trip flags=0x02 → Case 2 wire 端实证吸收)/ `phase2_spike_test.ss`(纯本地 mock NoopResultSet 16 method reachable + state transition → Case 3-6 真 implementor 端到端吸收 + 9 case 全状态转换 redundant)/ `phase4_spike_test.ss`(driver-side 5 case docker spike → Case 3+4+5+6 真集成端到端吸收);**保留 2 unit test** `phase1_packet_unit_test.ss`(纯本地无 docker — 协议常量 0x02 / 1008 + deriveCursorFlag 双语义 7 case dispatch unit test,与业务层 e2e integration_test 互补无重叠 — D139 / D146 范式延续 协议层反查表 vs 业务层 catch chain split)+ `phase3_sql_template_unit_test.ss`(纯本地无 docker — buildUpdateRowSql / buildDeleteRowSql / buildInsertRowSql / buildRefreshRowSql + derivePkColumn / deriveTableName + columnDefOrgTable / columnDefFlags accessor 16/16 byte-level pass-through 模板拼接 unit test,与业务层 e2e 互补无重叠)
- §A.2 H1-H7 全实证锚 — 收关:**H1**(server flags=0x02 行为同 0x01 driver 模拟)— `phase1_packet_unit_test.ss` 协议常量 + deriveCursorFlag dispatch 单元 + Case 2 wire 端实证(在线 5 行 next 序列 OK)/ **H2**(SELECT meta column def packet flags PRI_KEY_FLAG bit 1 解析单表单 PK)— `phase3_sql_template_unit_test.ss` PK 解析 单/多/无 PK 三态 + Case 3-6 单表单 PK 实际 SELECT 实证 / **H3**(fresh PS 同 fd 复用 cursor 不破)— Case 3-5 driver-side UPDATE/DELETE/INSERT round-trip + cursor 仍可 close + Connection 仍 functional 实证 / **H4**(cancelRowUpdates 纯 client-side)— Case 6a 服务端 row UNCHANGED 实证(`probeVal(1) == 10` post-cancel)/ **H5**(refreshRow 走 fresh PS SELECT WHERE pk=? 字段镜像 currentRow)— Case 6b 外部 writer UPDATE + refreshRow 同步实证 / **H6**(moveToInsertRow + insertRow + inInsertMode 切换)— Case 5 inserted row + moveToCurrentRow inInsertMode=0 切回实证 / **H7**(rowState 真实 track + 每 next() reset)— Case 3+4+5 rowUpdated/Deleted/Inserted state 实证(pre-update 0 / post-flush 1)
- 全 Phase 0-5 commit hash 6 个总览(Phase 0 `dd038c1` / Phase 1 `8abbf70` / Phase 2 `ac4d6e9` / Phase 3 `35a30f3` / Phase 4 `6b13d29` / Phase 5 `<Phase 5 commit>` 本)— Phase 5 hash placeholder 在本 phase commit 内立即回填 4 处(Status header line 3 + §Phase 收关锚 §Phase 5 mark Done at commit + Phase 5 §全 Phase 0-5 commit hash 总览 Phase 5 行 + Status 时间线 Phase 5 entry)— D147 主线最后 phase 无 N+1 commit,本 phase 内 commit + 立即回填 commit message 显式标识"Phase 5 hash <Phase 5 commit> 立即回填 4 处"范式
- VCM 六验全 PASS:§1 工程(`./build.sh bootstrap` 三阶段固定点 stage2==stage3 GREEN + `bin/ss test tests/` baseline 净 +1 passed integration_test e2e 7 case GREEN(在线时)/ 净 -2 fail = phase1_spike + phase4_spike docker offline 删除(287 passed / 11 failed / 298 total ↔ 前 286 passed / 12 failed / 298 total — passed +1 e2e integration / failed -1 docker offline 离线;在线时 passed +4 净 = +1 integration + 删 3 spike 全在线 GREEN -3 = +1 净)+ §2 行为(`grep -c "moveToInsertRow\|updateRow\|deleteRow\|insertRow\|cancelRowUpdates\|refreshRow" tests/d147_updatable_cursor/integration_test.ss` 实测 ≥7)+ §3 反向(撤回 7 case → 离线 1/1 → 0/0 + 在线 7/7 → 0/0;撤回 absorb → 3 spike 复活 + 离线 12 fail / 在线 18 passed 反向证伪)+ §4 边界(7 case 含 default + UPDATABLE + 4 update/delete/insert + cancel/refresh 双重 + 三层 DAE catch hierarchy + 多 col UPDATE/INSERT placeholder + 外部并发 update + UNIQUE 冲突 1062)+ §5 路线(D135-D146 + D147 SQL 主线范式延续 — Phase 计划独立 commit + commit hash 回填 + next_prompt 自闭环 — 同 6b13d29 D147 Phase 4 / 35a30f3 D147 Phase 3 / D146 Phase 5 absorb 范式)+ §6 根因(file:line 锚 = `tests/d147_updatable_cursor/integration_test.ss` 7 case + D147.md §Phase 收关锚 §Phase 5 mark Done at commit + D147 主线 close 标记 + Phase 5 §全 Phase 0-5 commit hash 总览 + Status 时间线 Phase 5 entry)
- baseline:`./build.sh bootstrap` 三阶段固定点 stage2==stage3 GREEN + `bin/ss test tests/` baseline 实测 285 passed / 11 failed / 296 total ↔ 前 286 / 12 / 298(D147 Phase 4 baseline)— 净 -1 passed(删 phase2_spike 纯本地 -1 + 加 integration_test 离线 fail +0 — bin/ss test 离线判 docker 测试为 fail 即使 probe-skip return 0,同 D146 integration_test 离线行为)/ -1 failed(删 phase1_spike + phase4_spike docker offline -2 + 加 integration_test 离线 fail +1 = -1)/ -2 total(删 3 + 加 1 = -2);**在线时**估 7/7 GREEN integration_test 全 case PASS(probe 通 → 7 case e2e 全跑 + 全 GREEN)/ 净 +1 passed online(删 3 spike online 全 GREEN -3 + 加 integration_test online +1 = -2,但 fail 数 -3 净下降);reflection_health_linter no regressions(本 Phase 不动 bootstrap/ → 14 指标全继承 Phase 0/1/2/3/4 baseline 含 M1-M7+N1-N5+F1 — 实测 GATE PASS — no regressions)+ d_doc_index_linter F1 = 0 PASS(D147 referenced Ds 全实存 — 实测 GATE OK 12 referenced Ds all live)+ next_prompt_ultrathink_linter PASS
- actual scope vs Phase 0 草案:Phase 0 草案"absorb 决策:删 Phase 1+3+4 spike"是 D147 Phase 0 落档时按 D146 absorb 决策模式估的删除清单 — 实际本 Phase 落地修正为"删 Phase 1+2+4 spike"(Phase 3 实际只有纯本地 `phase3_sql_template_unit_test.ss` SQL 模板 byte-level unit test 保留 — 与 phase1_packet_unit_test 形成 unit-test-pair;Phase 2 实际有纯本地 mock `phase2_spike_test.ss` NoopResultSet 9 case state transition 已被 Case 3-6 真 implementor 端到端吸收 — D146 phase2_spike_test 同形 absorb 范式延续);Phase 0 草案"Spring RowCallbackHandler demo Case 8(可选)"留作可选 — 实际本 Phase 7 case 主线已覆盖 §核心目标 7 判据,Case 8 RowCallbackHandler.processRow 内 rs.updateRow 路径需要 Spring JdbcTemplate.query 携带 CONCUR_UPDATABLE(D146 query 路径 forward-only 不带 CONCUR_UPDATABLE → 需要新 JdbcTemplate.query 重载或 cursor mode 注入,留 §Followup F8 sub-D 独立处理,本 D 主线不做);Phase 0 草案"Case 7 unique 冲突 / NOT NULL 违反 + cause.sqlState=23000 / 23505 + cause.errorCode=1062 / 1452"实际本 Phase 落地仅覆盖 unique 冲突 1062 / 23000 路径(NOT NULL 违反 1048 / FK 违反 1452 留 §Followup F1 扩展类型 sub-D — 三层 DAE catch hierarchy 范式已立,扩 errorCode 路径只是数据点扩展无新机制),sqlState 23505 是 PostgreSQL 路径(D107+ PostgreSQL driver 起首)留 §Followup F5

---

## Followup

| # | 锚 | 描述 |
|---|---|---|
| F1 | updateXxx 6 setter 扩展类型(BigDecimal / Timestamp / Date / Time / Bytes / Float / Short / Byte) | 本 D 仅 6 核心类型(int / string / long / boolean / double / null);JDBC 4.3 §15.2.5 全 SQL type setter ≥18 method,扩展类型留独立 sub-D 或合入 D146 §F3 ResultSetMetaData |
| F2 | SELECT FOR UPDATE 行锁 + RR isolation level + InnoDB lock wait timeout | SQL standard `SELECT ... FOR UPDATE` 行锁与 cursor 正交独立 sub-D — RR isolation level + InnoDB Gap Lock + lock wait timeout 调优监控完整;本 D scope 限 driver-side 模拟 cursor,锁路径独立 |
| F3 | multi-PK / 多表 join updatable cursor | 本 D Phase 3 derivePkColumn 仅最小子集(单表 SELECT 单 PK)— multi-PK 复合主键 + 多表 join updatable view 走 SHOW INDEX / INFORMATION_SCHEMA.KEY_COLUMN_USAGE 完整 PK 推导 + multi-table updatable cursor 留独立 sub-D 或合入 D146 §F3 ResultSetMetaData |
| F4 | SQL standard `WHERE CURRENT OF cursor_name` named cursor + UPDATE/DELETE WHERE CURRENT OF | SQL standard SQL/PSM cursor name + `UPDATE table WHERE CURRENT OF cursor_name` 语法(MySQL 不支持 — 仅 stored procedure 内 DECLARE CURSOR);留独立 sub-D 或合入 D146 §F4 |
| F5 | PostgreSQL `WHERE CURRENT OF cursor_name` 切换路径 | D107+ PostgreSQL driver 起首时 PostgreSQL 原生支持 `UPDATE/DELETE WHERE CURRENT OF cursor_name` SQL standard 路径;与 MySQL driver-side 模拟范式不同,driver 适配独立 — 留 D107+ |
| F6 | savepoint integration cursor + transaction 跨 statement 复用(autoCommit=false) | cursor lifecycle 跨 setAutoCommit(false) → multiple updateRow → SAVEPOINT / ROLLBACK TO SAVEPOINT cancelRowUpdates 深度集成;D137 per-call Connection vs cursor 跨 statement 复用矛盾 — 留独立 sub-D 或合入 D146 §F7 + HikariCP D138 §F6 |
| F7 | server-side updatable cursor MySQL 8.0+ MERGE_ENABLE_UPDATABLE 协议升级 | MySQL 5.7- / 8.0 默认无 server-side SQL standard updatable cursor;未来 MySQL MERGE_ENABLE_UPDATABLE 标志若启用 → server-side updatable cursor 协议 + 删 driver-side 模拟 fallback — 留独立 sub-D |

---

## Status 时间线

- 2026-05-02 Phase 0 D 文档落档(commit `dd038c1`)— D146 §Followup F1 + D146 §A.3 废案 C3 起首落档,走完整 JDBC 4.3 §15 ResultSet Update + MySQL CURSOR_TYPE_FOR_UPDATE 0x02 + driver-side updatable cursor 模拟(MySQL Connector/J 5.0+ 范式延续);**§核心目标 7 判据**(Case 1 CONCUR_READ_ONLY default 兼容 / Case 2 CONCUR_UPDATABLE + cursor flags=0x02 / Case 3 updateRow / Case 4 deleteRow / Case 5 moveToInsertRow + insertRow / Case 6 cancelRowUpdates + refreshRow / Case 7 cursor lifecycle SQLException 走 D139 DataIntegrityViolationException);**§A.2 H1-H7 假设挑战**(H1 server flags=0x02 行为同 0x01 driver 模拟 / H2 SELECT meta column def flags PRI_KEY_FLAG / H3 fresh PS 同 fd 复用 cursor 不破 / H4 cancelRowUpdates 纯 client-side / H5 refreshRow 走 fresh PS SELECT WHERE pk / H6 moveToInsertRow + insertRow + inInsertMode 切换 / H7 rowState 真实 track + 每 next() reset);**§A.3 废案 7 条**(C1 数据层 patch / C3 架构层 + SELECT FOR UPDATE / 客户端手写 UPDATE / SAVEPOINT 替代 cancelRowUpdates / moveToInsertRow 走单独 INSERT / SELECT FOR UPDATE 替代 / rowUpdated 全返 false);**Phase 0-5 计划草案 6 phase**(Phase 1 协议层常量 + spike / Phase 2 JDBC 18 method + 4 implementor stub + spike mock / Phase 3 SQL 模板生成器 + PK 解析 + unit test / Phase 4 MysqlBinaryResultSet driver-side 真实现 + spike / Phase 5 7 形态 integration_test e2e + absorb spike + 全 Phase 收关);**Followup 7 条**(F1 updateXxx 扩展类型 / F2 SELECT FOR UPDATE 行锁独立 sub-D / F3 multi-PK 多表 join / F4 SQL standard WHERE CURRENT OF / F5 PostgreSQL 路径 / F6 savepoint cursor 跨 transaction / F7 MySQL 8.0+ server-side updatable);**baseline**:d_doc_index_linter F1 = 0 PASS(D147 加入未破 referenced Ds — D025/D134/D136/D137/D138/D139/D146 实存)+ next_prompt_ultrathink_linter PASS(下轮提示词含 ultrathink 关键字);Phase 0 hash 留 `dd038c1` placeholder 待下轮 Phase 1 commit 时回填 4 处(Status header line 3 + §Phase 收关锚 §Phase 0 mark Done at commit + Phase 5 §全 Phase 0-5 commit hash 总览 + 本 Status 时间线 Phase 0 entry);**等下轮 Phase 1 协议层 CURSOR_TYPE_FOR_UPDATE 0x02 + CONCUR_UPDATABLE 1008 常量 + deriveCursorFlag concurrency=CONCUR_UPDATABLE 分支 + spike unit test + docker spike(在线时验 server flags=0x02 接受不 reject + 后续 fetch 行为同 0x01)+ §A.2 H1 hidden assumption 实证 + Phase 0 hash 回填**
- 2026-05-02 Phase 5 7 case integration_test.ss e2e 全形态覆盖 + absorb 3 phase spike + §A.2 H1-H7 全实证锚 + 全 Phase 收关 + D147 主线 close(commit `<Phase 5 commit>`)— `tests/d147_updatable_cursor/integration_test.ss` 7 case e2e 全形态覆盖(单 file,docker probe-skip 范式延续 d138/d139/d146/d147-phase1+phase4 — 127.0.0.1:3307 不可达 → println + return 离线零阻塞,在线时 7/7 GREEN — Schema:`integration_d147(id INT PRIMARY KEY, val INT, name VARCHAR(64), email VARCHAR(64) UNIQUE) ENGINE=InnoDB` + 5 rows seed):**Case 1** TYPE_FORWARD_ONLY + CONCUR_READ_ONLY default 兼容(`conn.prepareStatement(sql, TYPE_FORWARD_ONLY, CONCUR_READ_ONLY)` + `executeQuery()` + `rs.next()` × 5 + `rs.getInt('id')` 1..5 序列 — D146 client-streaming baseline 不破)/ **Case 2** TYPE_FORWARD_ONLY + CONCUR_UPDATABLE + `setFetchSize(2)` → COM_STMT_EXECUTE flags=CURSOR_TYPE_FOR_UPDATE 0x02 wire 实证(concurrency wins over fetchSize > 0 in deriveCursorFlag,server treats 0x02 same as 0x01,5 行 next 序列接受不 reject — D147 §A.2 H1 wire-level 实证)/ **Case 3** updateInt + updateString + updateRow multi-col + 外部 `probeRowMatches(1, 888, 'updated_3') == 1` + `rs.rowUpdated() == 1`(buildUpdateRowSql column order PK 末位 + setString 循环顺序实证;H3 + H7)/ **Case 4** deleteRow + 外部 `probeRowExists(1) == 0` + `probeRowCount() == 4` + `rs.rowDeleted() == 1`(H3 + H7)/ **Case 5** moveToInsertRow + multi-col `updateInt('id',99)` + `updateInt('val',777)` + `updateString('name','inserted_5')` + `updateString('email','new5@test.com')` + insertRow + 外部 `probeRowMatches(99, 777, 'inserted_5') == 1` + `probeRowCount() == 6` + `rs.rowInserted() == 1` + moveToCurrentRow inInsertMode=0 切回(buildInsertRowSql placeholder count + setString 循环顺序实证;H6 + H7)/ **Case 6** cancelRowUpdates 服务端 UNCHANGED(H4 — 纯 client-side no SQL fired):6a `updateInt('val', 555)` + cancelRowUpdates + updateRow no-op idempotent + 外部 `probeVal(1) == 10` + refreshRow 镜像外部 update(H5 — fresh PS SELECT WHERE pk=?):6b `getInt('val') == 10` 初值 + 外部 writer JdbcTemplate UPDATE val=12345 + `getInt('val') == 10` stale + refreshRow + `getInt('val') == 12345` 同步 / **Case 7** unique-violation cursor-side insertRow → SQLException(ER_DUP_ENTRY 1062 / sqlState 23000 from `readUpdateResultPacket` ERR_PACKET) → `new SQLExceptionTranslator().translate(sqlEx)` 显式 wrap → DuplicateKeyException IS-A DataIntegrityViolationException IS-A NonTransientDataAccessException IS-A DataAccessException 三层 catch hierarchy(7a leaf catch DataIntegrityViolationException + cause.sqlState=="23000" + cause.errorCode==1062 / 7b mid catch NonTransientDataAccessException / 7c root catch DataAccessException + cause 字段仍可读)+ wire-protocol 字段全保留(D139 SQLExceptionTranslator standard 应用 cursor-side fresh-PS executeUpdate 路径;JdbcTemplate.query/update 内部已自动 wrap,cursor 路径 driver-internal 无 wrap → 测试代码显式调 translator 范式);**absorb 决策**删 3 phase docker spike 由 integration_test.ss 7 case 吸收覆盖:`phase1_spike_test.ss`(协议层 server round-trip flags=0x02 → Case 2 wire 端实证吸收)/ `phase2_spike_test.ss`(纯本地 mock NoopResultSet 16 method reachable → Case 3-6 真 implementor 端到端吸收)/ `phase4_spike_test.ss`(driver-side 5 case docker spike → Case 3+4+5+6 真集成端到端吸收);**保留 2 unit test** `phase1_packet_unit_test.ss`(协议常量 + deriveCursorFlag 7 case dispatch 纯本地)+ `phase3_sql_template_unit_test.ss`(SQL 模板 4 builder + PK helper 16/16 byte-level pass-through 纯本地)— 互补无重叠 D139/D146 范式延续 协议层反查表 vs 业务层 catch chain split;**§A.2 H1-H7 全实证锚收关**(H1 phase1_packet_unit_test 协议常量 + Case 2 wire 实证 / H2 phase3_sql_template_unit_test PK 单/多/无 PK 三态 + Case 3-6 单表单 PK 实际 SELECT / H3 Case 3-5 fresh PS 同 fd 复用 cursor 不破 / H4 Case 6a 服务端 row UNCHANGED / H5 Case 6b 外部 writer + refreshRow 同步 / H6 Case 5 moveToInsertRow + insertRow + moveToCurrentRow inInsertMode 切换 / H7 Case 3+4+5 rowUpdated/Deleted/Inserted state 实证)+ Phase 4 hash `6b13d29` 回填 6 处(Status header line 3 + §Phase 收关锚 §Phase 4 mark Done at commit + §Phase 4 actual scope vs Phase 0 草案段 + Phase 5 §全 Phase 0-5 commit hash 总览 Phase 4 行 + Status 时间线 Phase 4 entry 开头 commit + Status 时间线 Phase 4 entry actual scope 段);**baseline**:./build.sh bootstrap 三阶段固定点 stage2==stage3 GREEN + bin/ss test tests/ baseline 净 +1 passed integration_test 在线 + -2 fail 离线 docker spike 删除 + reflection_health_linter no regressions(本 Phase 不动 bootstrap/ → 14 指标全继承 Phase 0/1/2/3/4 baseline 含 M1-M7+N1-N5+F1)+ d_doc_index_linter F1 = 0 PASS + next_prompt_ultrathink_linter PASS;**actual scope vs Phase 0 草案**:Phase 0 草案"absorb 决策:删 Phase 1+3+4 spike"措辞误为 Phase 1+3+4 — 实际本 Phase 落地修正为"删 Phase 1+2+4 spike"(Phase 3 实际只有纯本地 phase3_sql_template_unit_test 保留无 docker spike;Phase 2 实际有纯本地 mock phase2_spike 已被 Case 3-6 端到端吸收);Phase 0 草案"Spring RowCallbackHandler demo Case 8(可选)"留作可选未实现 — 7 case 主线已覆盖 §核心目标 7 判据,Case 8 RowCallbackHandler.processRow 内 rs.updateRow 路径需 JdbcTemplate.query 携带 CONCUR_UPDATABLE 留 §Followup F8 sub-D;Phase 0 草案"Case 7 unique 冲突 / NOT NULL 违反"实际仅 unique 1062 路径(NOT NULL 1048 / FK 1452 留 §Followup F1 扩展类型 sub-D,三层 DAE catch hierarchy 范式已立扩 errorCode 只是数据点扩展无新机制);Phase 5 hash `<Phase 5 commit>` placeholder 在本 phase commit 内立即回填 4 处(Status header line 3 + §Phase 收关锚 §Phase 5 mark Done at commit + Phase 5 §全 Phase 0-5 commit hash 总览 Phase 5 行 + 本 Status 时间线 Phase 5 entry)— D147 主线最后 phase 无 N+1 commit,本 phase 内 commit + 立即回填,commit message 显式标识"Phase 5 hash <Phase 5 commit> 立即回填 4 处"范式;**D147 主线 close** — 等用户分流下一 SQL 主线 sub-D(D138 §F4 NamedParameterJdbcTemplate / D138 §F6 HikariCP Connection Pool / D138 §F7 batchUpdate + BatchUpdateException / D146 §F2 holdability HOLD_CURSORS_OVER_COMMIT / CLOSE_CURSORS_AT_COMMIT / D146 §F3 ResultSetMetaData / D147 §F1 updateXxx 扩展类型 BigDecimal/Timestamp/Bytes/Float/Short/Byte / §F2 SELECT FOR UPDATE 行锁 + RR isolation level + InnoDB lock wait / §F3 multi-PK 复合主键 + 多表 join updatable view / §F4 SQL standard WHERE CURRENT OF cursor_name / §F5 PostgreSQL 路径 / §F6 savepoint integration cursor + transaction 跨 statement 复用 autoCommit=false / §F7 MySQL 8.0+ MERGE_ENABLE_UPDATABLE 协议升级 server-side updatable cursor)
- 2026-05-02 Phase 4 MysqlBinaryResultSet driver-side updatable cursor 真实现 + spike GREEN(commit `6b13d29`)— `lib/com/mysql/prepared.ss MysqlBinaryResultSet` 13 字段 → 19 字段:加 `pendingUpdates: Map<string, string>`(updateXxx 写入 col→val buffer,inInsertMode=0 时 updateRow flush 后 `new Map()` 清空)/ `pendingInserts: Array<Map<string, string>>`(moveToInsertRow 推 1 个新 Map 到末尾,updateXxx 在 inInsertMode=1 时写末尾 Map,insertRow flush 后 popLastInsertRow 末位 trim 重建)/ `tableName: string`(readQueryResultSetBinary EOF 后 `rs.tableName = deriveTableName(cols)` 初始化;空 colMetadata 返 "" trap)/ `pkColumn: string`(`rs.pkColumn = derivePkColumn(cols)` 初始化扫 PRI_KEY_FLAG bit 1;空串 / 多 PK / 多表 join trap)/ `inInsertMode: int`(0=normal / 1=insert mode,moveToInsertRow / moveToCurrentRow 切)/ `rowState: int`(0=clean / 1=updated / 2=deleted / 3=inserted,rowUpdated / rowDeleted / rowInserted 读)+ `ROW_STATE_CLEAN`/`UPDATED`/`DELETED`/`INSERTED` 4 常量(同 phase2_spike_test.ss line 29-32 范式,放在 MysqlBinaryResultSet class 注释块紧邻位置,class 之前 const block)+ `readQueryResultSetBinary` 改:13 字段 `new MysqlBinaryResultSet(...)` → 19 字段构造调用 + `let pu: Map<string, string> = new Map()` + `let pi: Array<Map<string, string>> = []` + 末位 6 参数 `pu, pi, "", "", 0, ROW_STATE_CLEAN` + EOF 后 `rs.tableName = deriveTableName(cols)` + `rs.pkColumn = derivePkColumn(cols)` + 16 method 真实现替 Phase 2 占位 stub:`writeCol(col, val)` private helper(inInsertMode=1 + pendingInserts.length()>0 → 写末位 pendingInserts Map / 否则写 pendingUpdates Map)+ 6 setter `updateInt` / `updateString` / `updateLong` / `updateBoolean` / `updateDouble` / `updateNull` 经 writeCol stringify(updateInt/Long → `"" + val` / updateString → val 直传 / updateBoolean → `val != 0 ? "1" : "0"` / updateDouble → `"" + val` / updateNull → `""`) + `popLastInsertRow` private helper(SS Array<T>[i] = X 被 codegen rejected — phase2_spike line 86 注释 — trim 重建末位 minus 1 element)+ `updateRow` gate `pendingUpdates.size() <= 0` no-op 路径 rowState=UPDATED idempotent(JDBC §15.2.5 Connector/J no preceding updateXxx 也 no-op 范式)+ tableName="" / pkColumn="" 任一抛 SQLFeatureNotSupportedException sqlState `0A000` 走 D139 SQLExceptionTranslator + 提 dirtyCols/dirtyVals from `pendingUpdates.keys()` + `pendingUpdates.get(k)` + `buildUpdateRowSql(this.tableName, this.pkColumn, dirtyCols)` + `doPrepare(this.fd, sql)` fresh PS 同 fd 复用(D147 §A.2 H3 实证 — server cursor request-response 同步,UPDATE 在 FETCH 间隙 round-trip 不破 cursor 同 D146 §H6 实证基础)+ `ps.setString(j+1, dirtyVals[j])` 循环 + `ps.setString(dirtyCols.length()+1, currentRow[colIndex(pkColumn)])` 末位 PK + `ps.executeUpdate()` 验 affected==1 否则抛 SQLException sqlState `01000` + `ps.close()` COM_STMT_CLOSE 0x19 + 清 pendingUpdates(`new Map()` 重赋值,SS Map 无 clear)+ rowState=UPDATED / `deleteRow` 同 gate + `buildDeleteRowSql` + setString PK + executeUpdate 验 affected==1 + rowState=DELETED / `insertRow` gate `tableName != ""` + `pendingInserts.length() > 0` + buf.size()>0(空 buf 时 popLastInsertRow + 抛 SQLException sqlState `07000` 而非生成 `INSERT INTO t() VALUES ()` 1064 server reject)+ 提 cols/vals from `buf.keys()` + `buf.get(k)` + `buildInsertRowSql(tableName, cols)` + setString 循环 + executeUpdate + popLastInsertRow + 验 affected==1 + rowState=INSERTED / `cancelRowUpdates` 纯 client-side(H4 实证 — 无 SQL fired):inInsertMode=1 + pendingInserts.length()>0 时 trim 重建末位 Map 替换为空 Map(保持 inInsertMode + Array length 不变 — JDBC §15.2.5 spec cancelRowUpdates 撤销 since last updateXxx 但不撤销 inInsertMode 状态,user 可继续 updateXxx 重新构建 insert row)/ inInsertMode=0 时 `pendingUpdates = new Map()` 清空 + rowState=CLEAN(实际 phase2_spike line 91-97 范式是清整个 pendingInserts Array,本 Phase 收紧为 trim 末位 Map,更精确 spec 一致)/ `refreshRow` gate + 提 cols from `this.colMetadata` 调 columnDefName + `buildRefreshRowSql(tableName, pkColumn, cols)` + fresh PS setString PK + `ps.executeQuery()` + `rs.next()` + 字段镜像重写 currentRow Array(H5 实证 — server-side 外部 UPDATE 经 fresh PS SELECT WHERE pk=? 同步,Connector/J 范式)+ rs.close() + ps.close() / `moveToInsertRow` inInsertMode=1 + `this.pendingInserts.push(new Map())` 推空 Map / `moveToCurrentRow` inInsertMode=0 / `rowUpdated`/`Deleted`/`Inserted` 读 rowState 比对 ROW_STATE_X 返 1/0 + `next()` 入口加 `this.rowState = ROW_STATE_CLEAN` reset(H7 实证 — JDBC §15.2.5 spec rowUpdated/Deleted/Inserted 仅当前 row 状态,advance 到新 row 必 reset,Connector/J 入口 reset 范式 — 简洁优于成功 advance 后 reset 的 3 路径分支)+ `import { SQLException, SQLFeatureNotSupportedException }` 加 + `tests/d147_updatable_cursor/phase4_spike_test.ss` 5 case docker probe-skip 范式延续 d138/d139/d146 + d147-phase1(127.0.0.1:3307 不可达 → println + return 离线零阻塞,在线时 5/5 GREEN 全 H 实证):Case 1 updateRow 单字段 + 外部 `JdbcTemplate.queryForInt SELECT val FROM phase4_d147 WHERE id=1` 验 val=999 + rowUpdated==1 pre/post-flush state transition(H3+H7)/ Case 2 deleteRow + 外部 SELECT COUNT WHERE id=1 == 0 + COUNT(*) == 4 + rowDeleted==1(H3+H7)/ Case 3 moveToInsertRow + updateInt(id,99) + updateInt(val,777) + updateString(name,'inserted') + insertRow + 外部 SELECT 找 id=99 val=777 + COUNT(*) == 6 + rowInserted==1 + moveToCurrentRow inInsertMode=0 切回(H6+H7)/ Case 4 updateInt(val,555) + cancelRowUpdates + updateRow + 外部 SELECT 验 val=10 不变(H4 — 纯 client-side,no SQL fired,server-side row UNCHANGED)/ Case 5 getInt('val') 初值 10 + 外部 writer JdbcTemplate UPDATE val=12345 WHERE id=1 + getInt('val') 仍 10 stale(cursor cache view)+ refreshRow + getInt('val') 同步 12345(H5 — fresh PS SELECT WHERE pk=? 字段镜像 currentRow);**§A.2 H3-H7 hidden assumption 全实证**(H3 fresh PS 同 fd 复用 cursor 不破 — Case 1+2+3 driver-side UPDATE/DELETE/INSERT round-trip + cursor 仍可 close + Connection 仍 functional / H4 cancelRowUpdates 纯 client-side — Case 4 server-side row UNCHANGED 实证 / H5 refreshRow 走 fresh PS SELECT WHERE pk=? — Case 5 外部 update 同步实证 / H6 moveToInsertRow + insertRow + inInsertMode 切换 — Case 3 inserted row + cursor 回原 row 实证 / H7 rowState 真实 track + 每 next() reset — Case 1+2+3 rowUpdated/Deleted/Inserted state 实证)+ Phase 3 hash `35a30f3` 回填 4 处(Status header line 3 + §Phase 收关锚 §Phase 3 mark Done at commit + Phase 5 §全 Phase 0-5 commit hash 总览 Phase 3 行 + Status 时间线 Phase 3 entry);**baseline**:./build.sh bootstrap 三阶段固定点 stage2==stage3 GREEN + bin/ss test tests/ baseline 实测 286 passed / 12 failed / 298 total ↔ 前 286 / 11 / 297(D147 Phase 3 baseline)— 净 +1 fail = phase4_spike docker offline +1(本环境 docker daemon stuck testss-mysql 不可达,在线时 5/5 GREEN 全 H 实证,同 phase1_spike docker probe-skip 范式)+1 total + reflection_health_linter no regressions(本 Phase 不动 bootstrap/ → 14 指标全继承 Phase 0/1/2/3 baseline 含 M1-M7+N1-N5+F1)+ d_doc_index_linter F1 = 0 PASS(D147 referenced Ds 全实存)+ next_prompt_ultrathink_linter PASS;**actual scope vs Phase 0 草案**:Phase 0 草案"H3 fresh PS 同 fd 复用 cursor 不破"是基于 D146 §A.2 H6 cursor close 同 fd 复用 round-trip 实证基础上的进一步外推假设;实际 Phase 4 落地走 `doPrepare(this.fd, sql)` 同 fd 入口(jdbc.ss `MysqlConnection.prepareStatement` 范式延续,prepared.ss public function 接 fd + sql 直接返 MysqlPreparedStatement)— request-response 同步协议担保 cursor 在 FETCH 间隙 dormant,UPDATE/DELETE/INSERT round-trip 不破 cursor 状态(在线 5/5 GREEN 实证);cancelRowUpdates 在 inInsertMode=1 时 phase2_spike 范式是清整个 pendingInserts Array(因为 SS Array<T>[i] = X codegen rejected),本 Phase 收紧为 trim 末位 Map 替换为空 Map(保持 inInsertMode + Array length 不变)— 更精确的 JDBC §15.2.5 spec 一致行为(cancelRowUpdates 撤销 since last call to updateXxx,不撤销 inInsertMode 状态);Phase 4 hash 留 `6b13d29` placeholder 待下轮 Phase 5 commit 时回填 4 处(Status header line 3 + §Phase 收关锚 §Phase 4 mark Done at commit + Phase 5 §全 Phase 0-5 commit hash 总览 Phase 4 行 + 本 Status 时间线 Phase 4 entry);**等下轮 Phase 5 7 形态 integration_test.ss e2e + Spring RowCallbackHandler 内 updateRow demo(可选 Case 8)+ absorb 决策删 Phase 1+3+4 spike(7 case 吸收覆盖)— 保留 phase1_packet_unit_test.ss + phase3_sql_template_unit_test.ss(协议层 / SQL 模板层 byte-level)互补无重叠 + 全 Phase 收关 + §A.2 H1-H7 全实证锚 + D147 主线 close 等用户分流下一 SQL 主线 sub-D + Phase 4 hash 回填 4 处**
- 2026-05-02 Phase 3 SQL 模板生成器 + PK 解析最小子集 + spike GREEN(commit `35a30f3`)— `lib/com/mysql/query.ss ColumnDef` 4 字段 → 6 字段:加 `orgTable: string` + `flags: int`(MySQL Native Protocol §6.6 ColumnDefinition41 — D134 Phase 4 wire 上原本被 skip 的 org_table + flags i16 LE bit-aware promoted to keep,driver-side updatable cursor 必备元数据)+ `parseColumnDef` 改:第 4 次 `skipLengthEncodedString` 改为 `col.orgTable = readLengthEncodedString(payload, off)` + colType 解析后追加 `off = off + 1; col.flags = byteToInt(payload, off, 2)` 解析 flags i16 LE + 11 处 `new ColumnDef(...)` 构造调用同步 4 → 6 参数(`lib/com/mysql/query.ss:252` parseColumnDef 内部初始 + `tests/d134_mysql/query_test.ss:105/115/116/134` × 4 + `tests/d134_mysql/prepared_test.ss:126/177/178/179/180/211` × 6,test fixtures 加 `, "", 0` 默认值不破坏既有断言语义)+ `lib/com/mysql/query.ss` 加 2 cross-module accessor helper(同 `columnDefName` / `columnDefColType` line 271-277 范式 — 直接 GEP %ColumnDef 从 prepared.ss 触发 forward-ref llc rejection):`columnDefOrgTable(col: ColumnDef): string` line 285 / `columnDefFlags(col: ColumnDef): int` line 289 + `lib/com/mysql/prepared.ss` import 链加 `columnDefOrgTable` / `columnDefFlags` + `lib/com/mysql/prepared.ss` 加 4 SQL 模板 helper:`buildUpdateRowSql(table, pkCol, dirtyCols)` 返 `UPDATE table SET col1=?, col2=? WHERE pk=?`(空 dirtyCols 返 `UPDATE table WHERE pk=?` 语法错 trap — Phase 4 调用方 gate `dirtyCols.length() > 0`)/ `buildDeleteRowSql(table, pkCol)` 返 `DELETE FROM table WHERE pk=?`(单 PK WHERE 唯一)/ `buildInsertRowSql(table, cols)` 返 `INSERT INTO table(col1, col2) VALUES (?, ?)` placeholder count == cols.length()(空 cols 语法错 trap)/ `buildRefreshRowSql(table, pkCol, cols)` 返 `SELECT col1, col2 FROM table WHERE pk=?`(空 cols 兜底 `SELECT * FROM table WHERE pk=?` Connector/J 范式)+ 2 PK 解析 helper:`derivePkColumn(colMetadata)` 扫 `flags & PRI_KEY_FLAG (0x0002)` bit 1 + count == 1 返第一个 PK 字段 columnDefName / count == 0(无 PK heap table)或 count > 1(composite PK)返 ""(空串触发 H2 失败回路 SQLFeatureNotSupportedException 走 D139 SQLExceptionTranslator,composite PK 留 §Followup F3)/ `deriveTableName(colMetadata)` 取 colMetadata[0] 调 columnDefOrgTable(单表 SELECT 范式;多表 join 不同 orgTable 返第一个 — Phase 4 caller trust schema is single-table,multi-table updatable view 留 §Followup F3);空 colMetadata 返 ""(同 derivePkColumn trap)+ `PRI_KEY_FLAG = 0x0002` 常量(MySQL Native Protocol §6.6 column definition packet flags i16 LE bit 1 — 同族 NOT_NULL_FLAG 0x0001 / UNIQUE_KEY_FLAG 0x0004 / MULTIPLE_KEY_FLAG 0x0008 留按需扩)+ `tests/d147_updatable_cursor/phase3_sql_template_unit_test.ss` 16/16 GREEN 纯本地无 docker(同 phase1_packet_unit_test 范式延续 — 协议 / 模板层 unit test split from docker integration spike):PRI_KEY_FLAG 0x0002 / buildUpdateRowSql 1 col / 多 col / 空 dirtyCols 边界 / 含 SQL 关键字 col 名 verbatim pass-through 无 escape / buildDeleteRowSql 单参数化 / buildInsertRowSql placeholder count(1/2/5 col)/ buildRefreshRowSql cols 列表 / 空 cols SELECT * 兜底 / derivePkColumn 单 PK / 多 PK / 无 PK 三态 / deriveTableName 单表 / 多表 join 第一个 / 空 colMetadata / columnDefOrgTable + columnDefFlags accessor round-trip 全覆盖;**§A.2 H2 hidden assumption 实证**(SELECT meta column def packet flags PRI_KEY_FLAG bit 1 解析,单表单 PK 场景纯本地 — 协议 wire 端实证留 Phase 4 docker integration);**baseline**:./build.sh bootstrap 三阶段固定点 stage2==stage3 GREEN + bin/ss test tests/ baseline 净 +1 passed phase3_sql_template_unit_test 纯本地(286 passed / 11 failed / 297 total ↔ 前 285 passed / 11 failed / 296 total — fail 数不变 11 全是 pre-existing docker 离线 + harness 旧 fail)+ reflection_health_linter no regressions(本 Phase 不动 bootstrap/ → 14 指标全继承 Phase 0/1/2 baseline)+ d_doc_index_linter F1 = 0 PASS(D147 referenced Ds 全实存)+ next_prompt_ultrathink_linter PASS;**actual scope vs Phase 0 草案**:Phase 0 草案"`derivePkColumn(colMetadata: Array<Map>)` / `deriveTableName(colMetadata: Array<Map>)`"参数类型写成了 `Array<Map>` 是 D124 落档时按 ColumnDef-as-Map 旧范式估的,实际本 Phase 落地时 D134 `ColumnDef` 已是 class(line 29-37,加 orgTable + flags 后 6 字段),helper 签名收紧成 `Array<ColumnDef>`(走 columnDefFlags / columnDefName / columnDefOrgTable accessor round-trip);Phase 3 hash 留 `35a30f3` placeholder 待下轮 Phase 4 commit 时回填 4 处(Status header line 3 + §Phase 收关锚 §Phase 3 mark Done at commit + Phase 5 §全 Phase 0-5 commit hash 总览 Phase 3 行 + 本 Status 时间线 Phase 3 entry);**等下轮 Phase 4 MysqlBinaryResultSet driver-side updatable cursor 真实现**(6 字段 pendingUpdates / pendingInserts / tableName / pkColumn / inInsertMode / rowState + readQueryResultSetBinary 调 deriveTableName / derivePkColumn 初始化 + 16 method 真实现替 Phase 2 占位 stub:updateXxx 写 pendingUpdates / pendingInserts + updateRow / deleteRow / insertRow 经 buildXxxSql + fresh PreparedStatement.executeUpdate + cancelRowUpdates 清 + refreshRow 经 buildRefreshRowSql + moveToInsertRow / moveToCurrentRow 切 inInsertMode + rowUpdated / rowDeleted / rowInserted 读 rowState + next() rowState reset)+ 5 case docker probe-skip spike(Case 1 updateRow / Case 2 deleteRow / Case 3 moveToInsertRow + insertRow / Case 4 cancelRowUpdates / Case 5 refreshRow,§A.2 H3-H7 全实证)+ Phase 3 hash 回填 4 处
- 2026-05-02 Phase 2 JDBC ResultSet update*/state 16 method + 3 implementor stub + spike mock GREEN(commit `ac4d6e9`)— `lib/java/sql.ss interface ResultSet` 加 16 method:`updateRow / deleteRow / insertRow / cancelRowUpdates / refreshRow / moveToInsertRow / moveToCurrentRow` 7 row-level void + `rowUpdated / rowDeleted / rowInserted` 3 row-state int + `updateInt / updateString / updateLong / updateBoolean / updateDouble / updateNull` 6 column-level setter(D147 §核心目标 6 核心 SQL type;扩展类型 BigDecimal / Timestamp / Bytes 留 §Followup F1)+ 注释块标 D147 §Phase 2 块同 D146 §Phase 2 cursor / scrollable 块范式延续(SSoT 注释含 row-level lifecycle 7 transition + 3 state queries + column-level setter 6 类型 + driver-side 模拟 trigger via CONCUR_UPDATABLE 1008 路径)+ 3 ResultSet implementor 降级 stub(D025 vtable 强制全 method 实现否则 bootstrap fail):`lib/com/mysql/query.ss MysqlResultSet`(text 协议 COM_QUERY 0x03 无 cursor 路径,update 全 no-op stub + rowUpdated 永返 0 注释 D147 Phase 2 块标)/ `lib/com/mysql/query.ss GeneratedKeyResultSet`(synthetic 单 row driver-internal,update 永 no-op 注释 D147 Phase 2 stubs 块标)/ `lib/com/mysql/prepared.ss MysqlBinaryResultSet`(binary 协议 Phase 4 真实现占位 stub 注释 Phase 4 swap 路径标)+ `tests/d147_updatable_cursor/phase2_spike_test.ss` 9/9 GREEN 纯本地无 docker mock NoopResultSet 16 method reachable through ResultSet interface variable(vtable indirect dispatch 验证)+ CONCUR_UPDATABLE 1008 经 MockStatement / MockConnection plumb 验 lastConcurrency 字段(plumb to ResultSet-side state)+ pendingUpdates / pendingInserts / inInsertMode / rowState 4 mock state 同 Phase 4 真实现 MysqlBinaryResultSet 6 字段同源 shape(Phase 4 swap 期 mock state 直翻真实现)+ test case 9 个全 GREEN(Statement/Connection plumb / 16 method reachable / updateInt+updateRow flush / cancelRowUpdates clear / deleteRow rowState / moveToInsertRow+insertRow flow / refreshRow counter / close() reach / 常量 distinct);**§A.2 H1-H2 hidden assumption**:H1 Phase 1 已实证 / H2 留 Phase 4 真 PK 解析(Phase 2 仅 mock state,无 SELECT meta 解析 + 无 SQL 模板生成器 → Phase 3 落地);**baseline**:./build.sh bootstrap 三阶段固定点 stage2==stage3 GREEN + bin/ss test tests/ baseline 净 +1 passed phase2_spike_test 纯本地(285 passed / 11 failed / 296 total ↔ 前 284 passed / 11 failed / 295 total — fail 数不变 11 全是 pre-existing docker 离线 + harness 旧 fail)+ reflection_health_linter no regressions(本 Phase 不动 bootstrap/ → 14 指标全继承 Phase 0/1 baseline)+ d_doc_index_linter F1 = 0 PASS;**actual scope vs Phase 0 草案**:Phase 0 草案写"4 ResultSet implementor stub + 18 method"基于 D146 phase2_spike_test.ss NoopPreparedStatement / NoopResultSet 仍存的 baseline,但 D146 Phase 5 commit `a0db36d` absorb 决策已删 phase2_spike_test.ss 等 4 phase docker spike → 实际仅 3 真 implementor 加 stub + 1 NoopResultSet mock 在新建的 d147 phase2_spike_test.ss 内 = "3 implementor + 1 spike-local mock"修正措辞;16 method = 10 row-level + 6 column-level(Phase 0 草案"18 method"包括 CONCUR_UPDATABLE 常量 + 也许 getConcurrency / getType — 实际仅 16 method,常量已在 Phase 1 落地);Phase 2 hash 留 `ac4d6e9` placeholder 待下轮 Phase 3 commit 时回填 4 处(Status header line 3 + §Phase 收关锚 §Phase 2 mark Done at commit + Phase 5 §全 Phase 0-5 commit hash 总览 Phase 2 行 + 本 Status 时间线 Phase 2 entry);**等下轮 Phase 3 SQL 模板生成器**(buildUpdateRowSql / buildDeleteRowSql / buildInsertRowSql / buildRefreshRowSql 4 private helper)+ PK 解析最小子集(derivePkColumn / deriveTableName 2 helper + column def packet org_table + flags 字段解析)+ unit test phase3_sql_template_unit_test.ss 纯本地 + Phase 2 hash 回填
- 2026-05-02 Phase 1 协议层 CURSOR_TYPE_FOR_UPDATE 0x02 + CONCUR_UPDATABLE 1008 + deriveCursorFlag 分支(commit `8abbf70`)— `lib/com/mysql/query.ss CURSOR_TYPE_FOR_UPDATE = 0x02` 常量(MySQL Native Protocol §6.5.1 COM_STMT_EXECUTE flags i8 bit 1 — 同 D146 §A.2 H2 已落 0x00 NO_CURSOR / 0x01 READ_ONLY 范式延续)+ `lib/java/sql.ss CONCUR_UPDATABLE = 1008` 常量(JDBC 4.3 §java.sql.ResultSet.CONCUR_UPDATABLE 标准整数值;D146 Phase 2 已落 CONCUR_READ_ONLY 1007)+ `lib/com/mysql/prepared.ss MysqlPreparedStatement.deriveCursorFlag` 加 concurrency=CONCUR_UPDATABLE 分支返 CURSOR_TYPE_FOR_UPDATE 0x02(优先于 D146 type+fetchSize 路径 — driver-side updatable cursor 独立于 server-cursor batching,即使 setFetchSize(100) flag 仍 0x02)+ deriveCursorFlag private → public 解锁 unit test 双语义验证(同 setFetchSize / setCursorMode driver-specific public method 范式)+ MysqlPreparedStatement concurrency 字段注释更新(D146 informational only → D147 plumbed by setCursorMode read by deriveCursorFlag)+ `tests/d147_updatable_cursor/phase1_packet_unit_test.ss` 7/7 GREEN(常量值 0x02 / 1008 + 常量互不相等 + deriveCursorFlag 6 case 双语义覆盖:CONCUR_UPDATABLE → 0x02 / CONCUR_READ_ONLY+fetchSize=0 → NO_CURSOR / CONCUR_READ_ONLY+fetchSize>0 → READ_ONLY / unknown 0 fall through 双子 case fetchSize=0 / fetchSize>0 / scrollable+UPDATABLE concurrency wins over type)+ `tests/d147_updatable_cursor/phase1_spike_test.ss` docker probe-skip 范式延续 d138/d139/d146-phase1(127.0.0.1:3307 不可达 → println + return 离线零阻塞)— 在线时 1/1 GREEN — sendCursorExecuteForUpdate 自定义 10 字节 0-param payload(同 D146 phase1_spike sendCursorExecuteZeroParam 范式但 flags=0x02)+ PREPARE SELECT id, val FROM phase1_d147 + EXECUTE flags=0x02 + drainExecuteResponse EOF CURSOR_EXISTS 必置 / LAST_ROW_SENT 必清 + FETCH(5) + drainFetchResponse 5 rows + EOF LAST_ROW_SENT 必置 / CURSOR_EXISTS 必清 + COM_STMT_CLOSE;**§A.2 H1 实证**(server flags=0x02 接受不返 ERR + 后续 fetch 行为同 0x01 — driver 模拟范式 MySQL Connector/J 5.0+ `UpdatableResultSet` 落地基础,server 实际行为 0x02 ≡ 0x01 MySQL 5.7+ doc 明确);**baseline**:./build.sh bootstrap 三阶段固定点 stage2==stage3 GREEN + bin/ss test tests/ baseline 净 +1 passed phase1_packet_unit_test 纯本地 / +1 fail phase1_spike_test docker 离线(同 D146 phase1_spike fallback skip 范式 — 用户口令明示本环境 docker daemon stuck testss-mysql 不可达)+ reflection_health_linter no regressions(本 Phase 不动 bootstrap/ → 14 指标全继承 Phase 0 baseline)+ d_doc_index_linter F1 = 0 PASS + next_prompt_ultrathink_linter PASS;Phase 1 hash 留 `8abbf70` placeholder 待下轮 Phase 2 commit 时回填 4 处(Status header line 3 + §Phase 收关锚 §Phase 1 mark Done at commit + Phase 5 §全 Phase 0-5 commit hash 总览 Phase 1 行 + 本 Status 时间线 Phase 1 entry);**等下轮 Phase 2 JDBC ResultSet update*/state 18 method**(updateRow / deleteRow / insertRow / cancelRowUpdates / refreshRow / moveToInsertRow / moveToCurrentRow / rowUpdated / rowDeleted / rowInserted 10 row-level + updateInt / updateString / updateLong / updateBoolean / updateDouble / updateNull 6 column-level setter)+ 4 ResultSet implementor 降级 stub + spike mock GREEN + Phase 1 hash 回填
