# D164: SS 编译器 PIR liveness helper fn + while/for loop body + class instance method call 模式下误标 last-use 致 use-after-free segfault 修法

**Status:** [x] Phase 0 D 文档落档 at commit `3e4c00d` + [x] Phase 1 minimum repro 隔离 spike + 4 case RED-as-spike + LLVM IR inspect bug A vs bug B 双根因 isolate(对比 helper fn 路径 vs main() 体内 IR 差异 + spike Case 1/2/3 ASSIGN 形态 vs Case 4 EXPR_STMT 形态)at commit `0c888f5` + [x] Phase 2 bootstrap/pir/pir_lower.ss 双修法(bug B pirLowerAssign LHS 非 class 早退漏扫 RHS USE +1 LOC + bug A 新增 pirEmitBlockUsesAtStmt 辅助 ~58 LOC + WHILE/FOR/FOR_IN/FOR_OF/DO_WHILE 5 处对称 +5 LOC,total +64 LOC pir_lower.ss 285→349)+ 三阶段固定点 stage2==stage3 bit-identical PASS + Phase 1 spike 重跑 4 case 全 GREEN(r=126/35/60/35)+ LLVM IR while.body/for.body/entry 块内 ss_drop=0 全 4 case + bin/ss test tests/ 306/16/322 净 +1 PASS at commit `26d656c` + [/] Phase 3 PARTIAL GREEN — D164 PIR liveness 修法 wire 落地后 D160 §Phase 4 wire 真值反射重启实测在线 docker MySQL 8.0 healthy 后 8 case 6/2 PASS/FAIL(**无 segfault** — D164 主线锚 GREEN:segfault 消除 + d164_spike 4 case 全 GREEN + /tmp/d160_t18.ss exit=0)+ Case 3 INOUT setInt(1,5) + register(1,INT) + executeUpdate + getInt(1) **expected 15 got 0** + Case 5 setInt(1,4) + register(2,INT) + setInt(3,100) + register(3,INT) + pmd.getParameterMode(2) **expected 4 got 262147(0x40003 — 非 garbage,structured corruption)** 揭露独立 root cause #3 D160 wire path INOUT IN-inject 缺失 + PMD paramDirections 反射 corruption + 起首 D165 sub-D `docs/3-decisions/D165-d160-callable-statement-inout-pmd-bug.md` + 跨 D 起首回填 D164 §Phase 2 hash `26d656c` + D164 §A.2 H5 假设破裂记录(从"D164 修法后 D160 §Phase 4 即 GREEN" 修为"D164 修法消除 segfault + 主线锚 GREEN,但 D160 wire 残留独立 root cause #3 → D165 起首脱胎")+ 新加 §Followup F10 D165 起首脱胎锚 at commit `<phase3-hash>` + [ ] Phase 4 D164 主线 close + D163 §F5 重启锚回填 + D160 §F9 重启锚回填 at commit `<phase4-hash>` + [ ] D164 主线 close at commit `<phase4-hash>` — D163 §Phase 2 codegen MEMBER_ACCESS 类型解析修法 wire 落地 commit `ae16073` 后 D160 §Phase 4 wire 真值反射重启实测 8 case 仍 segfault → /tmp/d160_t18.ss 17 行最小 repro 揭露独立 root cause #2 = PIR liveness 在 helper fn + while loop ≥ 2 次 + class instance.method() 模式下误标 last-use 在 while body 内,每次循环 ss_drop class instance,第 2 次循环 use-after-free segfault → D163 §Phase 3 hold + D164 起首脱胎 commit `29a7487`,D135-D163 编译器主线范式延续。修 SS 编译器 PIR liveness analysis 让 helper function 内 class instance 在 while/for loop body 内仅 borrow 不 last-use,drop 推到 while.after 块 — 不接受次优 / workaround / 节省路径(用户对话锁)。

## 起首脱胎
- D163 §Phase 2 修了 root cause #1(`bootstrap/gen/gen_types.ss:249` resolveObjClass MEMBER_ACCESS 分支 +1 行 ifaceMethodsCG.has 守护)wire 落地 at commit `ae16073` — Phase 1 spike 3 case 全 GREEN + 三阶段固定点 stage2==stage3 PASS — 但 D160 §Phase 4 wire 真值反射重启实测 8 case 仍 segfault,推翻 D163 §A.2 H5 假设(修编译器后 D160 wire drain 代码不动即 GREEN)
- D163 §Phase 3 hold 实测在线 docker compose -f tests/d134_mysql/docker-compose.yml up -d --wait MySQL 8.0 healthy 后 `bin/ss test tests/d160_callable_statement/` 全 segfault(test runner 路径 / direct binary `/tmp/d160_int` exit=139)at commit `29a7487`
- 最小 repro 隔离 `/tmp/d160_t18.ss` 17 行 完全脱离 D160 wire 路径:`function dropAllProcs() { const tmpl = new JdbcTemplate(URL); let i = 0; while (i < 2) { tmpl.execute("DROP ..."); i = i + 1 } } function main() { dropAllProcs() }` 实测 segfault(exit=139);改 `while (i < 1)` 1 次循环即 GREEN;改 helper fn 内同样代码搬到 main() 体内即 GREEN(`/tmp/d160_t10.ss` 类似形)— **bug 触发条件 = helper function + while loop ≥ 2 次 + class instance.method() 调用**
- LLVM IR inspect:`bin/ss build /tmp/d160_t18.ss --emit-ir` 显示 dropAllProcs 函数 IR while.body 块内出现 `call void @ss_drop_JdbcTemplate(ptr %tmpl)` — **PIR liveness 误标 tmpl 在 while body 内 last-use**,每次循环 drop tmpl,第 2 次循环重新 load 已 free 的 tmpl pointer 即 use-after-free segfault;对比同样代码在 main() 体内的 IR(t10),drop 正确放在 while.after 块外 — 仅 helper function 路径错
- bug scope:任何 `function helper() { let x = new Class(...); while (...) { x.method(...); } }` 模式触发 — 影响范围远超 D160(stdlib JdbcTemplate / 用户代码 helper fn 任一 class instance loop method call)
- D135-D163 编译器主线范式延续(Phase 计划独立 commit + Status 收关 + hash 回填 + next_prompt 自闭环 + §A.2 隐藏假设挑战 + §A.3 废案 + §Followup)
- Depends on:无新前置依赖(SS 编译器 PIR 修法 + D163 §Phase 2 codegen 修法已就位)— 但本 D close 后回填 D163 §F5 重启锚 + D160 §F9 重启锚 + D163 §Phase 3 重启 + D160 §Phase 4 wire 真值反射重启 GREEN

## 核心目标 (Goal)

落地后:
1. `bootstrap/pir/pir_lower.ss` 或 `bootstrap/pir/pir_opt.ss` liveness analysis 修法,helper function 内 class instance 在 while/for loop body 内 last-use 时,`ss_drop_*` 必须放到 while.after 块外而非 while.body 块
2. minimum repro spike `tests/d164_pir_liveness_helper_fn_loop_class_method/phase1_repro_spike_test.ss` 3 case RED→GREEN(Case 1 helper fn + while loop ≥ 2 次 + simple class.method / Case 2 helper fn + for loop + class.method / Case 3 nested helper fn + nested loop + 多 class instance)
3. `/tmp/d160_t18.ss` 17 行最小 repro 实测 exit=0(从 segfault → GREEN)
4. `./build.sh bootstrap` 三阶段固定点 stage2==stage3 bit-identical PASS
5. **D163 §Phase 3 重启 + D160 §Phase 4 wire 真值反射重启**:在线 `docker compose -f tests/d134_mysql/docker-compose.yml up -d --wait` 后 `bin/ss test tests/d160_callable_statement/` 8 case 全 GREEN(getInt(2)=20 / getInt(1)=15 / 多 OUT / 三态 mode / getString / getDouble / wasNull)
6. baseline 全继承 + reflection_health_linter no regression(尤其 F1 bootstrap/pir/*.ss budget 评估)+ d_doc_index_linter F1=0 + next_prompt_ultrathink_linter PASS

**RED**(本 D 文档落档前实测):
- `/tmp/d160_t18.ss` 17 行(helper fn `dropAllProcs` + while loop ≥ 2 次 + JdbcTemplate.execute)实测 exit=139 segfault
- `/tmp/d160_t18.ss` 改 `while (i < 1)` 1 次循环 → exit=0 GREEN(确认 ≥ 2 次循环触发)
- `/tmp/d160_t18.ss` 改 helper fn 内代码搬 main() 体内 → exit=0 GREEN(确认 helper fn 路径触发)
- `bin/ss test tests/d160_callable_statement/` 8 case 全 segfault(在线 docker MySQL 8.0 healthy 状态)
- LLVM IR inspect:while.body 块出现 `call void @ss_drop_JdbcTemplate(ptr %tmpl)` 每次循环 drop tmpl

## 核心原则 (Principles)

1. **Root Cause 优先 + 不接 workaround / 节省路径**(用户对话锁 / CLAUDE.md "Root Cause 优先 — 不接受次优 / workaround / 节省" 第一法则):**不**改 D160 wire drain 用 closure 模拟,**不**在 driver 拆 helper fn 内 loop body 为单次 single-iteration,**不**在 stdlib/JdbcTemplate 加 sticky retain — 修 SS 编译器 PIR liveness root cause
2. **Java/TS 业界对标必修**:helper function 内 class instance + loop method call 是 Java + TypeScript + Kotlin + Scala 全部支持的标准模式,行为一致(helper fn 内 vs main 内不应有差异);SS 缺它必拖累后续每条 sub-D 用 helper fn + loop + class.method 形态
3. **Perceus IR 范式严守**(reference counting + last-use insertion):while body 内 method call 不应触发 class instance last-use(class instance 仍 borrow,drop 推到 loop after);main() 体内行为正确,helper function 体内必须一致
4. **业界对标 Koka / Roc 等 Perceus-style 语言**:liveness analysis 必正确处理 borrow vs last-use,loop body 内 borrow 不 drop,loop after 一次 drop;helper function vs main function 路径对称
5. **bootstrap 三阶段固定点必走**:任何 `bootstrap/pir/` 修改必走 `./build.sh bootstrap` 三阶段 stage2==stage3 bit-identical 验证(D162 §Phase 2 / D163 §Phase 2 范式延续)— 自举 ≈ 55s
6. **D135-D163 主线范式延续**:Phase 计划独立 commit + Status 收关 + hash 回填 + next_prompt 自闭环 + §A.2 隐藏假设 + §A.3 废案 + §Followup
7. **N 年返工度极高**:不修 → 后续每条用 helper fn + class instance + loop 模式(stdlib JdbcTemplate / 用户代码任一 helper fn 内 loop class.method)全部 segfault → 必修
8. **PIR Map key int 转 string 范式 + visited 守 idempotent 范式**(CLAUDE.md §关键不变量 + D161 §Phase 2 / D162 §Phase 2 visited 范式延续)
9. **scope 限单 sub-D**:本 D 仅修 PIR liveness analysis 在 helper fn + loop body + class instance method call 模式下的 last-use 误标 — 不引入 PIR 表示形态改动 / 全局 PIR Map key 类型化 / 跨 D 通用 control-flow 重写
10. **依赖链不破**:D163 §Phase 2 codegen MEMBER_ACCESS 类型解析修法(commit `ae16073`)+ D162 §F7 vtable + non-0-arg overload child class bug 修法系列(commit `7d91d73`)+ 本 D PIR liveness 修法 三者正交并存,各修各根因,任一回退都破依赖链;Phase 2 修法后 D162/D163 spike 重跑 GREEN 守护(§A.2 H3 实证)

## A.1 主候选评估(§MNK §M §字段 10)

| 候选 | 层次 | 含 | 不含 | 决策 |
|------|------|-----|------|------|
| **C1** | workaround:driver `lib/spring/jdbc.ss` 改用 closure 模拟 / 全局 Map<conn_id, tmpl> 模拟 / helper fn 拆 single-iteration loop body | + 暂时绕过 SS 编译器 PIR liveness bug,driver 端走 closure 捕获 / 全局 state / 单次 iteration unroll workaround | 编译器 bug 不修,后续每条 sub-D 用 helper fn + loop + class.method 形态(常见 stdlib helper / 用户代码 helper fn)全断链 | **不选** — 用户对话锁不接 workaround;违反 ROOT CAUSE 第一法则 |
| **C2** | **编译器 PIR liveness 修法(根因)** | + 修 SS 编译器 `bootstrap/pir/pir_lower.ss` 或 `pir_opt.ss` liveness analysis 让 helper fn 内 class instance 在 while/for loop body 内仅 borrow 不 last-use,drop 推到 while.after 块外 + bootstrap 三阶段固定点 + minimum repro spike GREEN + D163 §Phase 3 重启 + D160 §Phase 4 wire 真值反射重启 8 case docker e2e 全 GREEN | 高级模式(generic class loop / nested helper fn 多 closure capture / async/await control flow / try/catch unwind drop)留 §F1/F2/F3 远期 | **选** — 根因解决:N 年返工度极高 + 业界对标 Java/TS/Kotlin/Scala/Koka/Roc + 与 D162 §F7 / D163 §Phase 2 编译器修法系列同源延续 + 不破依赖链 |
| **C3** | 架构层 refactor:全面审视 Perceus IR 系统(pir.ss + pir_lower.ss + pir_opt.ss)+ 多 Perceus pass 对齐(borrow / last-use / drop reorder / dup elide / reuse hint)+ if/else / try/catch / async/await / generator 等高级控制流统一处理 | + 完整 PIR 体系审查 + 跨 D 通用 control-flow drop semantics + multi-pass 一致性证明 | scope 远超 D164 单 sub-D — 跨 D104 mimalloc REUSE / D060 Perceus 主线 / D018 vtable 等所有 PIR / RC 系统 sub-D 联动 | **不选** — scope 远超 + N 年路径,本 D 仅修当前 corner case 不动整体 PIR 系统 |

**决策行**:**选 C2 编译器 PIR liveness 修法(根因)**(用户对话锁 — 不接次优 / workaround / 节省)— 因 (a) Root Cause 第一法则下编译器 bug 必修;(b) 业界对标 Java / TypeScript / Kotlin / Scala 全部支持 helper fn + loop + class.method 模式且行为与 main 一致,Perceus-style Koka / Roc 同样要求 liveness analysis 正确处理 loop body 内 borrow vs last-use;(c) D162 §F7 vtable + non-0-arg overload child class bug 修法 + D163 §Phase 2 ifaceMethodsCG.has 守护修法系列同源 corner case 延续;(d) D160 §Phase 4 wire 真值反射重启 + D163 §Phase 3 重启依赖此修;(e) **N 年返工度极高** — 后续每条用 helper fn + class instance + loop 模式的 sub-D(常见 ORM helper / wrapper class / adapter class / stdlib helper)全部 segfault;(f) scope 中等可控(单 PIR pass + ~10-30 LOC + Phase 1-3 spike + Phase 4 close 5 commit 切片);(g) D135-D163 主线范式延续。

## A.2 隐藏假设挑战

| H | 假设 | 挑战 | 实证锚 |
|---|------|------|--------|
| H1 | PIR liveness root cause 在 `bootstrap/pir/pir_lower.ss`(初始 schedule 计算阶段 — `pirSchedule[stmtId]` set 入口路径 helper fn vs main 不对称)| pir_lower.ss `genStmt` 后 `pirEmitScheduled()` 发射 release(CLAUDE.md §关键不变量 PIR Map key 段)— 路径在 helper fn 内 while loop body 末尾 stmt 错把 class instance 视为 last-use 在 body schedule 而非 while.after schedule;main() 路径下 release 推到 main 末尾(loop after fall-through 范式) | **Phase 1 LLVM IR inspect 实证命中 + 真根因细化为双 bug**(spike Case 1-3 ASSIGN 形态 = bug B,Case 4 / d164_min / t18 EXPR_STMT 形态 = bug A,二者均在 pir_lower.ss):**bug B** = `pir_lower.ss:137-159 pirLowerAssign` LHS 非 class 早退漏扫 RHS USE → reverse scan 找不到 USE → fallback to defStmt;**bug A** = `pir_lower.ss:43-59` WHILE/FOR/FOR_IN/FOR_OF/DO_WHILE 漏 emit USE 在 loop 整体 stmt id → reverse scan first-hit 在 body 内 stmt → schedule 设到 body 内。Phase 2 修法 = pir_lower.ss 单文件双修(+30~+50 LOC) |
| H2 | PIR liveness root cause 在 `bootstrap/pir/pir_opt.ss`(优化阶段 dead code elim 误判 while body 末尾 = last-use)| pir_opt.ss 走 dup elide / drop reorder pass — 可能 helper fn 路径下 fold 把 while.after 的 drop 提前到 while.body 末尾(优化误判 fall-through 等价性);main() 路径下不触发优化或优化保守 | **Phase 1 假设破裂**:pir_opt.ss `pirLivenessPass` reverse scan 算法本身不错(只是 first-hit-from-back),根因不在 pir_opt 优化 fold 误判而在 pir_lower 输入信号缺失。修法在 pir_lower 单文件,pir_opt 算法本身不动 — loop-aware liveness 完整 refactor 留 §F3 远期(本 D scope 不需要) |
| H3 | 修法对 D162 §F7 vtable + non-0-arg overload child class bug 修法 + D163 §Phase 2 ifaceMethodsCG.has 守护修法兼容 | 本 D 修 PIR pass(中间层),D162 §F7 / D163 §Phase 2 修 codegen pass(下游),正交不冲突;但 PIR 修法影响所有 codegen 路径,需 D162/D163 spike 重跑 GREEN 守护 | Phase 2 修法后跑 D162/D163 spike 重跑 GREEN + 全 `bin/ss test tests/` 净不退化(305/16/321 baseline) |
| H4 | 修法对 reflection scope 14 指标无 regression(尤其 F1 `bootstrap/pir/pir_lower.ss` + `pir_opt.ss` LOC 增量)| Phase 2 修法估 +10~+30 LOC 对 F1 budget 影响小;若超 budget_max 需 `bump` / `bump-group` 申报扩容(D097 §扩容协议) | Phase 2 修法后跑 reflection_health_linter no regression GATE,任一物理指标 > budget_max 阻断 commit |
| H5 | D163 §Phase 3 重启 + D160 §Phase 4 wire 真值反射重启 8 case 全 GREEN — 修 PIR liveness root cause #2 后,D163 §Phase 2 codegen 修法 + D160 wire drain 实现代码不动即 GREEN | 假设 PIR liveness 是 D160 segfault 的唯一独立根因(root cause #2 单一),无 root cause #3;若 Phase 3 docker e2e 仍 fail 需 §A.2 H5 修正记录 + 起首独立 sub-D | **Phase 3 实测部分破裂**(2026-05-09 commit `<phase3-hash>`):D164 PIR liveness 修法**消除 segfault 维度 GREEN**(d164_spike 4 case 全 GREEN + /tmp/d160_t18.ss exit=0 + 在线 docker 8 case 0 segfault),但 **wire 真值反射数据正确性维度 RED**(Case 3 INOUT setInt+register+executeUpdate+getInt got 0 expected 15 + Case 5 三态 mode pmd.getParameterMode(2) got 262147 expected 4)— root cause #2 PIR liveness 单一假设**破裂**,实测发现独立 **root cause #3** = D160 wire path **INOUT IN-inject 缺失**(execute() 漏调 SET @out_<i> := <IN value> 多语句 inject)+ **PMD paramDirections 反射 corruption**(简单 repro GREEN,真实 MysqlCallableStatement 形态某额外维度触发,触发条件待 D165 §Phase 1 真 spike 隔离锁定)→ 起首 D165 sub-D `docs/3-decisions/D165-d160-callable-statement-inout-pmd-bug.md` 修双根因;**Phase 4** D164 主线 close + D163 §F5 / D160 §F9 / 新加 D164 §F10 重启锚回填**等 D165 close 后**(D165 主线 close → D164 §Phase 4 close → D163 §Phase 3 重启 → D160 §Phase 4 重启 跨 D 起首回填范式延续)|
| H6 | 修法对 `bootstrap/pir/pir.ss` PIR 表示形态(`pirKind` / `pirStr*` / `pirInt1` / `pirList` / `pirSchedule` Map int 转 string key 范式)不破坏 | CLAUDE.md §关键不变量 PIR Map key 段:所有 PIR Map 访问必须 `id + ""` 将 int 转 string,否则 key 错配;本 D 修法仅触 schedule 计算逻辑,不改 Map 表示形态 | Phase 2 修法落地后跑 D135-D163 全部已落地 spike 重跑 GREEN(若任一退化必排查 PIR Map key 误用) |
| H7 | helper function 路径与 main() 路径在 PIR 阶段处理对称(genFunc 入口对 main 与 helper 共用同一 codepath,仅差 function name 与 return type)| `bootstrap/gen/methods/gen_methods.ss` + `bootstrap/pir/pir_lower.ss` genFunc 入口已统一处理 user fn(无论 main 还是 helper),但本 D 实测发现 PIR liveness 仅在 helper 路径错 — 暗示 schedule 计算 / 优化 pass 内部某条 branch 隐式区分 helper vs main(或基于 fn local var 个数 / 局部 borrow 集合判定)| **Phase 1 假设破裂 — 路径不对称**:`bootstrap/gen/gen_decls.ss:125 if (name != "main") { pirAnalyzeFunc(...) }` 显式区分 main vs 非 main。main 函数**不**走 PIR analysis,走旧 RC 系统(line 134-136 `pirEmitReturnCleanup` + `emitReleaseFnLocals` + `emitReleaseLocals`)在 ret 前一次性 release 所有 fn locals(`ss_rc_release` 旧 RC 路径 — d164_main.ll line 5212-5213 实证);helper / 非 main 函数走 PIR analysis(`pirAnalyzeFunc` → `pirLowerBlock` + `pirLivenessPass` + `pirMoveAnalysis` + `pirUniquenessPass` + `pirReusePass`),触发本 D bug A + bug B(helper 走 PIR 路径暴露 lower 阶段两个独立信号缺失;main 旧 RC 路径绕过)— PIR vs 旧 RC 双系统是 D060 Perceus 主线分阶段切片产物,本 D 仅修 PIR 路径 lower 阶段两 bug 不动双系统架构 |

## A.3 废案

- **C1 driver workaround**(用户对话锁不接 workaround / 节省路径;违反 ROOT CAUSE 第一法则;`memory/feedback_root_cause_no_cost.md` 第一法则触发)
- **C3 架构层 refactor**(scope 远超 D164 单 sub-D — 完整 PIR 体系审查留 §F3 远期独立 sub-D)
- **改 D160 wire drain 把 helper fn 内 loop 拆为多个 single-iteration**(workaround;违反 §核心原则 1;D160 wire drain 实现代码已就位,不应回调)
- **在 stdlib `lib/spring/jdbc.ss` JdbcTemplate 加 cyclic retain / sticky reference 强保留 tmpl**(workaround;反 RC 范式 + memory leak 风险)
- **标记 helper fn class instance 为 noinline / volatile / captureSingleton**(SS 类型系统不支持 + 反 user-friendly 范式 + 跨 D 范畴破坏)
- **临时跳过 segfault test 留远期处理**(违反核心目标 + 用户对话锁 docker 在线验证 wire 真值是核心目标)
- **改 PIR Map key 类型化**(架构层 refactor — D135-D163 范式延续 PIR Map int 转 string,本 D 不动 PIR 表示形态;留 §F3 远期)
- **强制 SS 标记 class instance 在 helper fn 内为 unsafe / @ScopeStable / @LoopStable annotation**(SS 类型系统不支持 + 反 user-friendly 范式 + 用户对话锁 + memory `feedback_no_derive_workaround.md` annotation 不当根因第一法则触发)
- **D164 与 D163 §Phase 3 重启 + D160 §Phase 4 wire 真值反射重启 同 commit 落地**(scope 失控 — D164 是编译器层 PIR 根因 + D163 §Phase 3 + D160 §Phase 4 是应用层 wire 验证,二者分开切片 — D164 主线 close → D163 §Phase 3 重启 → D160 §Phase 4 重启 跨 D 起首回填范式延续)
- **改 PIR liveness analysis 为保守化全程 retain/release**(在 helper fn 入口先 retain 一次 class instance,helper fn 出口 release 一次 — 不依赖 schedule 计算)— 反 Perceus 范式 + 性能退化 + memory pressure 增加(每 helper fn call 多 1 retain + 1 release pair),违反 D060 Perceus 主线核心目标
- **改 PIR pass 顺序(把 pir_opt 在 pir_lower 前跑)**(架构层 refactor;违反 SS 编译器现有 PIR pass 顺序 lexer→parse→checker→pir_lower→pir_opt→codegen,scope 远超本 D)
- **临时回退 D162 §F7 vtable + non-0-arg overload child class bug 修法 / D163 §Phase 2 ifaceMethodsCG.has 守护修法**(回退已 wire 落地的根因修法 — 违反 ROOT CAUSE 第一法则 + 破依赖链 + scope 失控)

## Phase commit hash 总览

| Phase | 内容 | Commit |
|-------|------|--------|
| 0 | D 文档落档(§核心目标 + §核心原则 + §A.1-A.3 + §Phase 收关锚 Phase 0-4 + §Followup F1-F5 + §Status 时间线)+ 跨 D 起首回填 D163 §Phase 2 hash `ae16073`(root cause #1 wire 落地)+ D163 §Phase 3 hold hash `29a7487`(D164 起首脱胎)至 D164.md ≥3 处实际语义位 | `3e4c00d` |
| 1 | minimum repro `tests/d164_pir_liveness_helper_fn_loop_class_method/phase1_repro_spike_test.ss` 4 case RED-as-spike(Case 1 ASSIGN-form helper + while + simple class.method / Case 2 ASSIGN-form helper + C-style for + class.method / Case 3 nested helper + nested loop + 多 class instance / Case 4 EXPR_STMT-form helper + while + bump global,d164_min/t18 同形)+ LLVM IR inspect 双根因 isolate(spike Case 1 IR line 5365-5368 entry 块立即 drop **bug B** + d164_min.ll line 5197-5200 while.body 内 drop **bug A** + main 路径 d164_main.ll while.after 块外 ss_rc_release **对照** — gen_decls.ss:125 main 不走 PIR 分流揭示)+ Phase 2 修法 target 锁定:bug B = pir_lower.ss:137-159 pirLowerAssign 早退前加 RHS USE 扫描(+1~+3 LOC)+ bug A = pir_lower.ss:43-59 WHILE/FOR/FOR_IN/FOR_OF/DO_WHILE 5 处对称 + 新增辅助 pirEmitBlockUsesAtStmt(+25~+45 LOC)| `0c888f5` |
| 2 | bootstrap/pir/pir_lower.ss 或 pir_opt.ss liveness 修法(基于 Phase 1 H1 vs H2 实证锁定的 PIR pass)+ ./build.sh bootstrap 三阶段固定点 stage2==stage3 bit-identical PASS + Phase 1 spike 重跑 3 case 全 GREEN + LLVM IR 验证 grep ss_drop_<ClassName> 在 while.body 块 = 0(从 ≥1 → 0)+ reflection_health_linter no regression(M1-M7+N1-N5)+ bin/ss test tests/ 净不退化 305/16/321 + 跨 D 起首回填 D164 §Phase 1 hash `0c888f5` 至 D164.md ≥4 处 | `26d656c` |
| 3 | D163 §Phase 3 重启 + D160 §Phase 4 wire 真值反射重启 — `docker compose -f tests/d134_mysql/docker-compose.yml up -d --wait` MySQL 8.0 healthy 后 `bin/ss test tests/d160_callable_statement/` 8 case 全 GREEN(Case 1 H1 + Case 2 OUT INTEGER 真值=20 H4 + Case 3 INOUT 真值=15 H4 + Case 4 multi OUT H4 + Case 5 三态 mode H3 + Case 6 getString H4 + Case 7 getDouble H4 + Case 8 wasNull SQL NULL)+ /tmp/d160_t18.ss 实测 exit=0 + 跨 D 起首回填 D164 §Phase 2 hash `26d656c` 至 D163.md(§Phase 3 重启锚)+ D160.md(§F9 row + §Phase 4 §收关锚) | `<phase3-hash>` |
| 4 | D164 主线 close + D163 §F5 重启锚回填 + D160 §F9 重启锚回填 + D163 主线 close + D160 主线 close 锚链一并清理(由 D163 §Phase 4 / D160 §Phase 4 close commit 处理,本 D 仅锚回填) | `<phase4-hash>` |

## Phase 收关锚

### Phase 0: D 文档落档 [x] Done at commit `3e4c00d`

- 落地 `docs/3-decisions/D164-pir-liveness-helper-fn-loop-class-method-drop.md`(本文件)— ≥150 行 — §核心目标 + §核心原则 + §A.1 候选评估 + §A.2 隐藏假设 H1-H5 + §A.3 废案 + §Phase 收关锚 Phase 0-4 + §Followup F1-F5 + §Status 时间线
- 落地 `.claude/next_prompt.md`(下轮 D164 §Phase 1 起首 — minimum repro spike 移植 + ≥3 case RED-as-spike + LLVM IR inspect 真根因)— 含 ultrathink 关键字 + 不写 docs-only 字面(防 self-recursive spin / next_prompt_ultrathink_linter C4 守护)
- **跨 D 起首回填 D163 §Phase 2 hash `ae16073`(root cause #1 codegen MEMBER_ACCESS 类型解析 wire 落地)+ D163 §Phase 3 hold hash `29a7487`(D164 起首脱胎)至 D164.md ≥3 处实际语义位**(Status header / §起首脱胎 / §Phase commit hash 总览段表 Phase 0 行 / Status 时间线 起首脱胎 entry)
- bootstrap/ + lib/ + tools/ + tests/ diff = 0(本 Phase 纯 docs/ + .claude/next_prompt.md)
- baseline:`d_doc_index_linter` F1 = 0 PASS(D147/D154/D155/D156/D157/D160/D161/D162/D163/D164 全实存,加入 D164 实存)+ `next_prompt_ultrathink_linter` PASS(下轮 D164 §Phase 1 起首含 ultrathink + 不 docs-only)+ 14 reflection 指标全继承 D163 §Phase 2 baseline(本 Phase 不动 bootstrap/ → reflection scope 不触)+ `bin/ss test tests/` 全继承 305/16/321

### Phase 1: minimum repro 隔离 spike + 4 case RED-as-spike + LLVM IR 双根因 isolate(bug A EXPR_STMT-in-loop + bug B ASSIGN LHS 非 class 早退漏扫 RHS USE)[x] Done at commit `0c888f5`

- 落地 `tests/d164_pir_liveness_helper_fn_loop_class_method/phase1_repro_spike_test.ss` 4 case RED-as-spike — Case 1 helper fn + while loop ≥ 2 次 + simple class.method(自定义最小 `class TestBoxD164 { val: int; function getVal(): int { return this.val } }`,无外部 lib 依赖)/ Case 2 helper fn + C-style for loop + class.method(`for (let i = 0; i < 5; i = i + 1)` 形态)/ Case 3 nested helper fn(`loopHelperCase3` → `loopHelperInner`,`loopHelperInner` 内 while loop + 2 class instance box1+box2)/ Case 4 helper fn + while loop + EXPR_STMT class.method bump global(`box.bump()` EXPR_STMT 形态修改 `let countD164 = 0` 顶层全局 — `/tmp/d160_t18.ss` `tmpl.execute(...)` / `/tmp/d164_min.ss` `box.getVal()` 同形)
- 实测 RED 4/4 fail:`Tests: 0 passed, 4 failed, 4 total` — Case 1 expected 126 got -1616928867 / Case 2 expected 35 got 1600085851 / Case 3 expected 60 got 2139062140 / Case 4 expected 35 got 2139062147 + mimalloc double free 报错 4 次(实证 use-after-free + mimalloc poison value 0x7F7F7F7F)
- LLVM IR inspect:`bin/ss build phase1_spike --emit-ir -o /tmp/d164_spike`(spike Case 1-4)+ `bin/ss build /tmp/d164_min.ss --emit-ir -o /tmp/d164_min`(EXPR_STMT 形态)+ `bin/ss build /tmp/d164_main.ss --emit-ir -o /tmp/d164_main`(main 体内对照)三种形态 IR 对比 — **bug B 实证**(spike Case 1 IR `/tmp/d164_spike.ll` line 5365-5368 entry 块 ALLOC `%1 = call ptr @TestBoxD164_new(i32 42)` + 立即 `call void @ss_drop_TestBoxD164(ptr %2)` — drop 在 while loop 之前,第 1 次循环 UAF)+ **bug A 实证**(d164_min.ll line 5197-5200 while.body.154 内 `call void @ss_drop_TestBoxD164(ptr %8)` — drop 每次循环 emit,第 2 次起 UAF)+ **main 路径对照**(d164_main.ll line 5181-5215 while.after.155 块外 `call void @ss_rc_release(ptr %14)` — 旧 RC 路径正确,**因 `bootstrap/gen/gen_decls.ss:125 if (name != "main") { pirAnalyzeFunc(...) }` main 不走 PIR 走旧 RC 系统**)
- **真根因 file:line + 修法 target 双修**:
  - **bug B**(LOC +1~+3):`bootstrap/pir/pir_lower.ss:137-159 pirLowerAssign` line 140 `if (pirIsClass(ssType) == 0) { return buf }` LHS 非 class 早退,RHS 表达式如 `sum + box.getVal()` 中 box METHOD_CALL USE 完全不收集 → `pir_opt.ss:11-117 pirLivenessPass` reverse scan 找不到 box 任何 USE → Step 3(line 91-94)`lastUseStmt.set(vn, defStmt.getString(vn))` fallback 到 VAR_DECL stmt id → `pirSchedule[VAR_DECL stmt id] = "box:TestBoxD164"` → `pirEmitScheduled(stmtId)` 在 VAR_DECL codegen 后立即 emit drop(entry 块内)→ 第 1 次循环 UAF。**修法**:line 140 早退**之前**加 `buf = pirEmitExprUses(nGetI1(id), id, buf)`,LHS 非 class return buf(USE 已扫描),LHS class 走原 RC_DEC/RC_INC 流程
  - **bug A**(LOC +25~+45):`bootstrap/pir/pir_lower.ss:43-59` 处理 WHILE/FOR/FOR_IN/FOR_OF/DO_WHILE 时,lower body 后未 emit 任何 USE 在 loop 整体 stmt id 上,`pirLowerExprStmt` line 188-190 emit USE 在 EXPR_STMT 内 stmt id → reverse scan first-hit-from-back 命中 body 内 stmt → schedule 设到 body 内 stmt id → `pirEmitScheduled(bodyStmtId)` 在 genBlock body 内每条 stmt codegen 后调用 → emit 落 while.body / for.body 块内 → 第 2 次起 UAF。**修法**:WHILE/FOR/FOR_IN/FOR_OF/DO_WHILE 5 处对称改 — lower body 完毕后追加调用新增辅助 `pirEmitBlockUsesAtStmt(bodyId, loopStmtId, buf)` 递归扫 body 内每条 stmt 的 expr USE 但 stmtId 用 loop 整体 stmt id 而非 body 内 stmt id → reverse scan first-hit 是 PIR list 中位置更靠后的 loop 整体 stmt USE → last-use 设为 loop 整体 stmt id → schedule 推到 loop 整体 stmt 后 → `pirEmitScheduled(loopStmtId)` 在 genBlock 处理完 WHILE 整体 stmt 后调用 → emit 落 while.after / for.after 块外。新增辅助 `pirEmitBlockUsesAtStmt(blockId: int, stmtIdOverride: int, buf: string): string` 递归处理 VAR_DECL/ASSIGN/MEMBER_ASSIGN/EXPR_STMT/RETURN/嵌套 WHILE/DO_WHILE/FOR/FOR_IN/FOR_OF/IF 全形态(LOC ~35-45)
  - **守护**:`./build.sh bootstrap` 三阶段固定点 stage2==stage3 + 14 reflection 指标 no regression(F1 `bootstrap/pir/pir_lower.ss` 当前 285 LOC,Phase 2 修法后估 ~315-335 LOC,需 reflection_health_linter 跑 budget 评估,若超 budget_max 用 `bump` 申报扩容 D097 §扩容协议)+ Phase 1 spike 重跑 4 case 全 GREEN(Case 1 r=126 / Case 2 r=35 / Case 3 r=60 / Case 4 r=35)+ LLVM IR `grep -c "ss_drop_TestBoxD164" /tmp/d164_spike.ll` 在 while.body / for.body 块内 = 0(从 ≥4 → 0)+ entry 块内立即 drop = 0(从 spike Case 1-3 立即 drop ≥3 → 0)+ bin/ss test tests/ 净不退化 305/16/321(spike 从 -1 退化到回归 baseline)+ /tmp/d160_t18.ss + /tmp/d164_min.ss + /tmp/d164_main.ss 三形态实测 exit=0 + 无 mimalloc double free 报错
- **H1 vs H2 双根因 isolate 结论**:
  - 表层命中 H2(pir_opt 优化阶段 reverse scan 算 last-use 错)— 但 root cause 在 H1(pir_lower lower 阶段缺 USE 信号致 reverse scan 输入不全)
  - bug B 是 H1 路径 ASSIGN 漏扫 RHS USE → reverse scan 找不到 USE → fallback 到 defStmt(实际 last-use 信号缺)
  - bug A 是 H1 路径 loop 漏 emit USE 在 loop 整体 stmt id → reverse scan first-hit 在 body 内 stmt(实际 last-use 信号位置错)
  - **真正修法在 pir_lower.ss 单文件**(在 pir_opt 之前给 reverse scan 准备好正确的 USE 输入)— pir_opt.ss reverse scan 算法本身不动,loop-aware liveness 留 §F3 远期(本 D scope 内已不需要)
  - 修正 §A.2 H1/H2 假设:H1 真根因细化为"pir_lower.ss 漏 emit USE 信号"(不是"helper fn vs main 不对称",而是"helper fn 走 PIR 路径暴露 lower 阶段已有的两个独立信号缺失 bug,main 不走 PIR 用旧 RC 系统 emitReleaseLocals 路径绕过");H2 假设(pir_opt 误判)在表象上命中但 root cause 不在算法
  - 修正 §A.2 H7 假设:helper function 路径与 main() 路径**不对称** — `gen_decls.ss:125 if (name != "main")` 显式分流,main 不走 PIR analysis 走旧 RC `emitReleaseLocals` 在 ret 前一次性 release 所有 fn locals,helper 走 PIR liveness 触发本 D bug
- 跨 D 起首回填 D164 §Phase 0 hash `3e4c00d` 至 D164.md 4 处实际语义位(Status header `[x] Phase 0 D 文档落档 at commit \`3e4c00d\`` / §Phase commit hash 总览段表 Phase 0 行 `\`3e4c00d\`` / §Phase 0 §收关锚 `[x] Done at commit \`3e4c00d\`` / Status 时间线 Phase 0 entry `(commit \`3e4c00d\`)`)
- bootstrap/ + lib/ + tools/ diff = 0(本 Phase 仅 spike 落地 + docs/ hash 回填 + .claude/next_prompt.md,不动 bootstrap/lib/tools/)— Phase 2 修法保留 commit
- baseline:`d_doc_index_linter` F1 = 0 PASS(D147/D154/D155/D156/D157/D160/D161/D162/D163/D164 全实存)+ `next_prompt_ultrathink_linter` PASS(下轮 D164 §Phase 2 起首含 ultrathink + 不 docs-only)+ 14 reflection 指标全继承 D163 §Phase 2 baseline(本 Phase 不动 bootstrap/ → reflection scope 不触)+ `bin/ss test tests/` 净 +1 file +1 fail = 305/17/322(spike 4 case fail 由 file 级算 1 fail file,RED-as-spike 暂时退化,Phase 2 修法后回归 305/16/321 + spike PASS)

### Phase 2: bootstrap/pir/pir_lower.ss 单文件双修法 + 三阶段固定点 PASS + Phase 1 spike 全 GREEN [x] Done at commit `26d656c`

- **PRIMARY 修法 落地**:`bootstrap/pir/pir_lower.ss` 单文件双修法 — bug B = `pirLowerAssign` line 140 早退**之前**加 `return pirEmitExprUses(nGetI1(id), id, buf)`(LHS 非 class 路径 RHS USE 扫描 +1 LOC + 1 行注释)+ bug A = WHILE/FOR/FOR_IN/FOR_OF/DO_WHILE 5 处对称改 lower body 后追加 `pirEmitBlockUsesAtStmt(bodyId, loopStmtId, buf)` 调用(+5 LOC + 4 行注释)+ 末尾新增 `pirEmitBlockUsesAtStmt(blockId: int, stmtIdOverride: int, buf: string): string` 递归处理 VAR_DECL/ASSIGN/MEMBER_ASSIGN/EXPR_STMT/RETURN/嵌套 IF/WHILE/DO_WHILE/FOR/FOR_IN/FOR_OF 全形态 ~58 LOC — total +64 LOC(pir_lower.ss 285→349 行,远低 600 F1 硬上限)
- `./build.sh bootstrap` 三阶段固定点 stage2==stage3 bit-identical PASS — `Fixed point verified! Stage 2 = Stage 3 — Updated bin/ss`(自举 ≈ 80s,CLAUDE.md §构建与测试 范式延续)
- reflection_health_linter GATE PASS — no regressions(M1-M7+N1-N5 全继承 + M2 cur=83326 within 1% bm=83300 AUTO-DRIFT 软警告 / N2 cur=416630 within 1% bm=416000 AUTO-DRIFT / N3 cur=578015 within 1% bm=575500 AUTO-DRIFT / M3b cur=2014 within 1% bm=2010 AUTO-DRIFT — 全 4 项 AUTO-DRIFT 不阻断;F1 pir_lower.ss 349 < 600 默认豁免不在 baseline)
- Phase 1 spike 重跑 4 case 全 GREEN — `bin/ss test tests/d164_pir_liveness_helper_fn_loop_class_method/` 1 passed / 0 failed / 1 total(单 file 内 4 个 test() 全 GREEN — Case 1 r=126 / Case 2 r=35 / Case 3 r=60 / Case 4 r=35)
- LLVM IR 验证:`bin/ss build tests/d164_.../phase1_repro_spike_test.ss --emit-ir -o /tmp/d164_spike` 后实证 — loopHelperCase1 ss_drop_TestBoxD164 在 `while.after.176` 块内(line 5495-5496)/ loopHelperCase2 在 `for.after.180` 块内(line 5530-5531)/ loopHelperInner ss_drop box1+box2 在 `while.after.183` 块内(line 5569-5572 — multi instance + nested helper)/ loopHelperCase4 ss_drop_CounterD164 在 `while.after.186` 块内(line 5605-5606 — bug A EXPR_STMT 形态)— while.body/for.body/entry 块内 ss_drop = 0 全 4 case
- /tmp/d164_min.ss + /tmp/d164_main.ss 实测 exit=0 无 mimalloc double free 报错(Phase 1 实证锚 GREEN 守护)
- bin/ss test tests/ 306/16/322 净 +1 PASS(从 Phase 1 RED-as-spike 305/17/322 → 306/16/322:spike file 1 fail → 1 pass + 0 regression — D162 §F7 vtable 修法 + D163 §Phase 2 ifaceMethodsCG.has 修法兼容守护 H3 实证)
- d_doc_index_linter F1=0 PASS(D147/D154/D155/D156/D157/D160/D161/D162/D163/D164 全实存)+ next_prompt_ultrathink_linter PASS(下轮 D164 §Phase 3 起首含 ultrathink + 不写 docs-only)
- simplify 4 agent(reuse / quality / efficiency / readability)审本轮 pir_lower.ss 双修法 — readability 5 条 (a)-(e) 守护
- 跨 D 起首回填 D164 §Phase 1 hash `0c888f5` 至 D164.md 5 处实际语义位(Status header line 3 / §Phase commit hash 总览段表 Phase 1 行 line 86 / §Phase 1 §收关锚 line 101 / §Phase 2 §收关锚 line 130 / Status 时间线 Phase 1 entry line 161)

### Phase 3: D164 PIR liveness 修法 wire 落地后 D160 §Phase 4 + D163 §Phase 3 重启 PARTIAL GREEN — 在线 docker MySQL 8.0 healthy 后 8 case 6/2 PASS/FAIL(无 segfault — D164 主线锚 GREEN)+ Case 3 INOUT + Case 5 三态 mode 双 fail 揭露独立 root cause #3 + 起首 D165 sub-D [/] PARTIAL GREEN at commit `<phase3-hash>`

- **D164 主线锚 GREEN(segfault 维度)**:在线 `docker compose -f tests/d134_mysql/docker-compose.yml up -d --wait` MySQL 8.0 healthy 后 `bin/ss test tests/d160_callable_statement/` 8 case 全跑完无 segfault(D164 §Phase 2 PIR liveness 修法成功消除 use-after-free)+ d164_spike 4 case 全 GREEN(r=126/35/60/35)+ /tmp/d160_t18.ss 17 行最小 repro 实测 exit=0(从 segfault → GREEN)
- **wire 真值反射部分 RED(独立 root cause #3)**:8 case 中 6 PASS / 2 FAIL — Case 1 H1 wire ✓ / Case 2 OUT INTEGER 真值=20 H4 ✓ / **Case 3 INOUT setInt(1,5) + register(1,INT) + executeUpdate + getInt(1) expected 15 got 0** ❌ / Case 4 multi OUT H4 ✓ / **Case 5 setInt(1,4) + register(2,INT) + setInt(3,100) + register(3,INT) + pmd.getParameterMode(2) expected 4 got 262147(0x40003 — 非 garbage,structured corruption)** ❌ / Case 6 getString OUT VARCHAR ✓ / Case 7 getDouble OUT DOUBLE ✓ / Case 8 wasNull SQL NULL ✓
- **§A.2 H5 假设部分破裂**:从"D164 修法后 D160 §Phase 4 即 GREEN" 修为"D164 修法消除 segfault 维度 GREEN + D164 主线锚 GREEN,但 D160 wire 残留独立 root cause #3 → D165 起首脱胎"(详 §A.2 H5 行)
- **root cause #3 双根因诊断**:**3a** = lib/com/mysql/prepared.ss MysqlCallableStatement.execute() line 1659-1709 漏调 INOUT IN-inject(应只调 rewriteCallSqlPlaceholders 把 ? → @out_<i>,未在 sendComStmtPrepare 之前 COM_QUERY 多语句 inject SET @out_<i> := <IN value>)致 INOUT 的 IN 值丢失,server 看 @out_<i> 初始 NULL → CALL 内 SET p1 = NULL * 3 = NULL → drain getString = "" → getInt = 0;**3b** = MysqlParameterMetaData getParameterMode reflection corruption(简单 repro `Holder + alias write + Reflector cross-class read` GREEN / extends + 多 own Array<int> + iface impl 形态 GREEN / 18-arg constructor 形态 GREEN — bug 触发条件比简单 cross-class share 复杂,真实 MysqlCallableStatement 形态某额外维度触发,触发条件待 D165 §Phase 1 真 spike 隔离锁定)
- **起首 D165 sub-D**:`docs/3-decisions/D165-d160-callable-statement-inout-pmd-bug.md`(应用层 + 可能编译器层 双根因修法 — root cause 3a INOUT IN-inject + root cause 3b PMD reflection corruption)— 4 Phase 计划(Phase 0 D 文档落档(本 commit)+ Phase 1 minimum repro 隔离 spike 多 case + LLVM IR inspect 真根因 + Phase 2 修法 wire 落地(应用层 + 可能编译器层) + 三阶段固定点(若触及 bootstrap/gen)+ Phase 3 D160 §Phase 4 重启 + D163 §Phase 3 重启 8 case 全 GREEN + Phase 4 D165 主线 close + 跨 D 锚回填)
- **D163 / D160 状态保持 hold 等 D165 close**:D163.md Status header 不动(仍 hold)+ D160.md Status header 不动(仍 hold)+ D163 §Followup F5 row 末尾上下文加 D164 §Phase 2 hash `26d656c` + D165 起首脱胎说明 + D160 §F9 row 末尾上下文加 D164 §Phase 2 hash `26d656c` + D165 起首脱胎说明
- 跨 D 起首回填 D164 §Phase 2 hash `26d656c` 至 D164.md 6 处(replace_all `<phase2-hash>` → `26d656c`)+ Status header Phase 2 [/] → [x] + Phase 3 [ ] → [/] PARTIAL GREEN + §Phase 2 §收关锚 [/] → [x] + §Phase 3 §收关锚 [ ] → [/] PARTIAL GREEN + §A.2 H5 假设破裂记录 + 新加 §Followup F10 D165 起首脱胎锚 + Status 时间线 Phase 3 entry + D163.md(§Followup F5 row 末尾上下文)+ D160.md(§F9 row 末尾上下文 / Status 时间线 D164 §Phase 3 PARTIAL + D165 起首脱胎 entry)

### Phase 4: D164 主线 close + D163 §F5 重启锚回填 + D160 §F9 重启锚回填 [ ] Pending at commit `<phase4-hash>`

- D164 主线 close 锚:Status header `[x] Phase 0-4 + [x] D164 主线 close at commit \`<phase4-hash>\``
- D163 §Followup F5 row 末尾上下文更新 `commit \`<phase4-hash>\` D164 主线 close → D163 §Phase 3 重启 GREEN`
- D160 §Followup F9 row 末尾上下文更新 `commit \`<phase4-hash>\` D164 主线 close → D160 §Phase 4 wire 真值反射重启 GREEN`
- D163 §Phase 3 重启 + D163 主线 close + D160 §Phase 4 + D160 主线 close 一并清理(由 D163 §Phase 4 / D160 §Phase 4 close commit 处理,本 D 仅锚回填)
- 跨 D 起首回填范式延续:本 D 主线 close 后 Phase 4 hash 留 D163 §Phase 4 close + D160 §Phase 4 close commit 起首跨 D 回填(D162 §Phase 4 close → D160 §Phase 3 / D163 close → D160 §Phase 4 同形)

## Followup

| F | 内容 | 范围 |
|---|------|------|
| F1 | generic-over-class own field write loop(`function helper<T>() { let x: T = new T(...); while (...) { x.method() } }`)— 泛型 + helper fn + loop + class instance method | 与 D141-D145 contextual typing sub-D 联动,留独立 sub-D 远期 |
| F2 | nested loop 多重 class instance + nested helper fn(`fn1` calls `fn2`,`fn2` 内 nested while loop + 2+ class instance)| 多层 helper fn nest + 多 class instance — 是否本 Phase 2 修法自动覆盖待 Phase 1 spike Case 3 验证;若不覆盖留独立 sub-D |
| F3 | 完整 PIR liveness 体系审查(包括 if/else 分支 last-use / try/catch unwind drop / async/await suspend / generator yield 等高级控制流)| 跨 D 通用 PIR 主题 — 本 D 仅修当前 corner case(helper fn + while/for body + class instance method),完整体系审查留独立 sub-D 远期 |
| F4 | stage0 seed 编译影响(若 Phase 2 修法 affecting stage1+ codegen,stage0 不动 — 但若 stage1 build 后 stage0 重跑需重 seed)| 自举安全 — 留 §F 远期,Phase 2 三阶段固定点验证已覆盖大部分 |
| F5 | reflection_health_linter F1 budget 评估(`bootstrap/pir/pir_lower.ss` + `pir_opt.ss` 当前 LOC vs F1 budget;若超需 `bump` / `bump-group` 申报扩容 D097 §扩容协议)| 本 D Phase 2 修法 LOC 增量评估 — 估 +10~+30 行 — F1 评估需 reflection_health_linter 跑出 baseline 后 evaluate |
| F6 | mimalloc REUSE hint 在 helper fn + loop body 内的 idempotent 保留(D104 mimalloc 主线范式延续)| 修法不应破坏 REUSE hint 在 loop iteration 间的 alloc 复用,Phase 2 修法后跑 D104 spike 重跑 GREEN 守护 |
| F7 | `bootstrap/pir/pir.ss` PIR 节点 Map 表示形态完整性审查(pirKind / pirStr* / pirInt1 / pirList / pirSchedule Map key 一致性 + 类型化提案)| 跨 D 通用 PIR 主题 — D135-D163 范式延续 PIR Map int 转 string,本 D 不动表示形态;留独立 sub-D 远期 |
| F8 | `pirEmitBlockUsesAtStmt` sentinel-USE dedup 优化:每 class var 在 loop 整体 stmt id 上仅 emit 一次 USE 而非按 mention 数 emit(目前 2x USE node count;dedup 后 ~3-5x 减少 PIR list 长度)— Phase 2 simplify efficiency agent 建议(行为正确 + 5 readability rule a-e 全 pass)| 编译期性能 polish — 不属 D164 bug fix scope,留独立 sub-D 远期(本 Phase 2 spike 4 case + bin/ss test tests/ 306/16/322 全 GREEN,不阻塞)|
| F9 | `pirEmitBlockUsesAtStmt` 处理 BREAK/CONTINUE/THROW/SWITCH/TRY/CATCH 等高级控制流 stmt 形态(目前 silent skip,与 `pirLowerStmt` dispatch 一致)| 跨 D 通用 PIR 主题 — 本 D scope 仅当前 corner case(WHILE/FOR/DO_WHILE 主形 + class instance method),高级控制流留 §F3 远期(本 D 编译器主线没用到这些形态)|
| F10 | **D160 wire path INOUT IN-inject 缺失 + PMD paramDirections 反射 corruption 双根因 → D165 起首脱胎 — D164 §Phase 3 PARTIAL GREEN + 主线 close hold 锚** | D164 §Phase 2 PIR liveness 修法 wire 落地后 D160 §Phase 4 wire 真值反射重启实测发现独立 root cause #3(应用层 wire path INOUT IN-inject 缺失 + 可能应用层或编译器层 PMD reflection corruption — 与 D164 PIR liveness scope 正交不同代码路径,推翻 §A.2 H5 "修 PIR liveness 后 D160 §Phase 4 即 GREEN" 单一假设). D165 4 Phase 计划(Phase 0 D 文档落档(本 commit)+ Phase 1 minimum repro 隔离 spike 多 case 分别 isolate INOUT path + PMD reflection + LLVM IR inspect 真根因 + Phase 2 修法 wire 落地(应用层 + 可能编译器层 双修法)+ 三阶段固定点(若触及 bootstrap/gen)+ Phase 3 D160 §Phase 4 重启 + D163 §Phase 3 重启 8 case 全 GREEN + Phase 4 D165 主线 close + 跨 D 锚回填). Root Cause 优先 + 用户对话锁不接 workaround / 节省路径 — D165 主线 close → D164 §Phase 4 close → D163 §Phase 3 重启 → D160 §Phase 4 重启 跨 D 起首回填范式延续 | D165 起首脱胎 commit `549f8f2` = D164 §Phase 3 commit + **D165 §Phase 1 minimum repro 隔离 spike commit `a227192`** + **D165 §Phase 2 root cause 3a INOUT IN-inject 修法 wire 落地 PARTIAL commit `8e607f3`**(lib/com/mysql/prepared.ss MysqlCallableStatement.execute() line 1692 之前新增 INOUT IN-inject 路径 +22 LOC + d160 在线 8 case 7/1 PASS/FAIL — Case 3 INOUT got 15 GREEN + Case 5 三态 mode 留 §F2 远期 用户对话锁 2026-05-09 接受 JDBC 象梣 API 风格不修 codegen Heisenbug)+ **D165 §Phase 3 D160 §Phase 4 + D163 §Phase 3 重启 PARTIAL commit `5c7e2b2`**(在线 docker MySQL 8.0 healthy 8 case 7/1 PASS/FAIL — Case 5 留 §F2 远期接受 RED 用户对话锁 2026-05-09 — JDBC 象梣 API 历史包袱)+ **D165 §Phase 4 主线 close commit `<phase4-hash>`**(D165 主线 close → D164 §Phase 4 close + D164 主线 close 锚链一并清理由 D164 §Phase 4 close commit 处理 + 跨 D 起首回填 D165 §Phase 3 hash `5c7e2b2` 至 D165.md ≥3 处 + D163 主线 close + D160 主线 close 锚链一并清理 — Case 5 留 §F2 远期接受 RED 不挡 D165/D163/D160 主线 close 用户对话锁 2026-05-09;§F2 PMD codegen PIR liveness Heisenbug 留独立 sub-D 远期编译器主线 PIR liveness corner case 修法系列 — 与 D164 §Phase 2 同源 corner case 但 trigger shape 不同) |

## Status 时间线

- 2026-05-09 **D164 §Phase 3 D164 PIR liveness 修法 wire 落地后 D160 §Phase 4 + D163 §Phase 3 重启 PARTIAL GREEN + 起首 D165 sub-D + 跨 D 起首回填 D164 §Phase 2 hash `26d656c` 至 D164.md/D163.md/D160.md**(commit `<phase3-hash>`)— **D164 主线锚 GREEN(segfault 维度)**:在线 `docker compose -f tests/d134_mysql/docker-compose.yml up -d --wait` MySQL 8.0 healthy 后 `bin/ss test tests/d160_callable_statement/integration_test.ss` 8 case 全跑完 0 segfault(D164 §Phase 2 PIR liveness 修法成功消除 use-after-free)+ d164_spike 4 case 全 GREEN(r=126/35/60/35 不变)+ /tmp/d160_t18.ss 17 行最小 repro 实测 exit=0(从 D163 §Phase 3 hold 时 segfault → GREEN);**wire 真值反射部分 RED(独立 root cause #3)**:8 case 中 6 PASS / 2 FAIL — Case 1 prepareCall + getParameterCount=2 H1 ✓ / Case 2 OUT INTEGER getInt(2)=20 H4 ✓ / **Case 3 INOUT setInt(1,5) + register(1,INT) + executeUpdate + getInt(1) expected 15 got 0** ❌ / Case 4 multi OUT getInt(2)=14 + getInt(3)=21 H4 ✓ / **Case 5 setInt(1,4) + register(2,INT) + setInt(3,100) + register(3,INT) + pmd.getParameterMode(2) expected 4 got 262147(0x40003 — 非 garbage,structured corruption)** ❌ / Case 6 getString(1)='hello' OUT VARCHAR ✓ / Case 7 getDouble(1)≈3.14 OUT DOUBLE ✓ / Case 8 getInt(1)=0 + wasNull()=1 SQL NULL ✓;**§A.2 H5 假设部分破裂**:从"D164 修法后 D160 §Phase 4 即 GREEN" 修为"D164 修法消除 segfault 维度 GREEN + 主线锚 GREEN,但 D160 wire 残留独立 root cause #3 → D165 起首脱胎"(详 §A.2 H5 行实证锚列);**root cause #3 双根因诊断**:**3a** = lib/com/mysql/prepared.ss MysqlCallableStatement.execute() line 1659-1709 漏调 INOUT IN-inject(应只调 rewriteCallSqlPlaceholders 把 ? → @out_<i>,未在 sendComStmtPrepare 之前 COM_QUERY 多语句 inject `SET @out_<i> := <IN value>`)致 INOUT 的 IN 值丢失,server 看 @out_<i> 初始 NULL → CALL p1 = NULL * 3 = NULL → drain getString = "" → getInt = 0;**3b** = MysqlParameterMetaData getParameterMode reflection corruption(简单 repro `Holder + alias write + Reflector cross-class read` GREEN / extends + 多 own Array<int> + iface impl 形态 GREEN / 18-arg constructor 形态 GREEN — bug 触发条件比简单 cross-class share 复杂,真实 MysqlCallableStatement(extends MysqlPreparedStatement : CallableStatement + 18 字段 + iface getParameterMetaData override + Array<ColumnDef> paramDefs)某额外维度触发,留 D165 §Phase 1 真 spike 隔离锁定);**起首 D165 sub-D**:`docs/3-decisions/D165-d160-callable-statement-inout-pmd-bug.md`(应用层 + 可能编译器层 双根因修法 — root cause 3a INOUT IN-inject + root cause 3b PMD reflection corruption)≥150 行 + §核心目标 + §核心原则 P1-P10 + §A.1 候选评估 C1/C2 选/C3 + §A.2 隐藏假设 H1-H8 + §A.3 废案 + §Phase 收关锚 Phase 0-4 + §Followup F1-F7 + §Status 时间线;**回退动作**:D164.md Status header Phase 2 [/]→[x] + Phase 3 [ ]→[/] PARTIAL GREEN + §Phase 2 §收关锚 [/]→[x] + §Phase 3 §收关锚 [ ]→[/] PARTIAL GREEN + §A.2 H5 假设破裂记录(实证锚列重写)+ 新加 §Followup F10 D165 起首脱胎锚 + Status 时间线 Phase 3 entry(本 entry);D163.md Status header 不动(仍 hold 等 D165 close)+ §Followup F5 row 末尾上下文加 D164 §Phase 2 hash `26d656c` + D165 起首脱胎说明;D160.md Status header 不动(仍 hold 等 D165 close)+ §F9 row 末尾上下文加 D164 §Phase 2 hash `26d656c` + D165 起首脱胎说明 + Status 时间线加 D164 §Phase 3 PARTIAL + D165 起首脱胎 entry;**跨 D 起首回填**:D164 §Phase 2 hash `26d656c` 至 D164.md 6 处(`<phase2-hash>` replace_all → `26d656c`)+ Status header 实测说明 + §A.2 H5 + §Phase 3 §收关锚 + §F10 + Status 时间线 Phase 3 entry(本 entry)+ D163.md(§F5 row 末尾上下文)+ D160.md(§F9 row 末尾上下文 + Status 时间线 D164 §Phase 3 + D165 起首脱胎 entry);**baseline**:bootstrap/ + lib/ + tools/ + tests/ diff = 0(本 Phase 仅 docker + 测试 + docs + 起首 D165 D 文档 + .claude/next_prompt.md;不动 bootstrap/lib/tools/tests/ — D164 §Phase 2 PIR liveness 修法 wire 落地代码原样保留 + lib/com/mysql/prepared.ss MysqlCallableStatement wire drain 实现代码原样保留 — D165 修双根因后 8 case 全 GREEN);docker down 后 d_doc_index_linter F1 = 0 PASS(D147/D154/D155/D156/D157/D160/D161/D162/D163/D164/D165 全实存,加入 D165 实存)+ reflection_health_linter no regression GATE PASS(本 Phase 不动 bootstrap/ → reflection scope 不触 → 14 reflection 指标全继承 D164 §Phase 2 baseline)+ next_prompt_ultrathink_linter PASS(下轮 D165 §Phase 1 起首含 ultrathink + 不写 docs-only)+ docker down 后 bin/ss test tests/ 全继承 306/16/322(d160 integration_test file 离线 probe-skip PASS);**simplify 跳过**(本 Phase 仅 docker + 测试 + docs / 不动 bootstrap/lib/tools/,§After Done §1 例外 — 同 D164 §Phase 0/1 / D163 §Phase 0/1/3-hold / D162 §Phase 0/1 / D161 §Phase 0/4 / D160 §Phase 0 / D157 §Phase 0 / D156 §Phase 0 / D154 §Phase 0 / D155 §Phase 0 docs+test-only 范式延续);**§N §6 file:line 锚**:docs/3-decisions/D165-d160-callable-statement-inout-pmd-bug.md(本 D165 落档 ≥150 行)+ docs/3-decisions/D164-pir-liveness-helper-fn-loop-class-method-drop.md(Status header / §A.2 H5 / §Phase 2 §收关锚 / §Phase 3 §收关锚 / §Followup F10 / Status 时间线 Phase 3 entry / `<phase2-hash>` 6 处全部替换为 `26d656c`)+ docs/3-decisions/D163-class-extends-iface-typed-field-write.md(§Followup F5 row 末尾上下文)+ docs/3-decisions/D160-callable-statement-out-inout.md(§F9 row 末尾上下文 + Status 时间线 D164 §Phase 3 + D165 起首脱胎 entry)+ tests/d160_callable_statement/integration_test.ss(8 case 在线实测 6/2 PASS/FAIL,file 不动)+ /tmp/d160_t18.ss + /tmp/d165_min_pmd.ss + /tmp/d165_min_pmd2.ss + /tmp/d165_min_pmd3.ss(spike 探索文件 GREEN — PMD bug 触发条件待 D165 §Phase 1 真 spike 隔离)+ lib/com/mysql/prepared.ss:1659-1709(execute() INOUT IN-inject 缺失 root cause 3a 锚)+ lib/com/mysql/prepared.ss:1786-1788(getParameterMetaData PMD 反射构造 root cause 3b 锚)+ .claude/next_prompt.md(下轮 D165 §Phase 1 起首 ultrathink + 不写 docs-only);**等下轮 D165 §Phase 1** — minimum repro spike 移植 + 多 case RED-as-spike(分别隔离 root cause 3a INOUT path + root cause 3b PMD reflection corruption)+ LLVM IR inspect execute() generated IR(看 INOUT IN 值是否 inject 到 COM_QUERY)+ PMD constructor + getParameterMode IR(看 paramDirections GEP offset / RC retain / share-by-ref 路径)+ Phase 2 修法 target file:line + LOC 估算(应用层 +5~+15 LOC + 可能编译器层 +1~+30 LOC)+ 跨 D 起首回填 D165 §Phase 0 hash 至 D165.md ≥4 处

- 2026-05-09 **D164 §Phase 2 bootstrap/pir/pir_lower.ss 单文件双修法 + 三阶段固定点 stage2==stage3 PASS + Phase 1 spike 4 case 全 GREEN**(commit `26d656c`)— **bug B 修法**:`bootstrap/pir/pir_lower.ss:140 pirLowerAssign` 早退**之前**改 `if (pirIsClass(ssType) == 0) { return pirEmitExprUses(nGetI1(id), id, buf) }`(LHS 非 class 路径 RHS USE 扫描 +1 LOC + 注释)— `sum = sum + box.getVal()` 等 LHS=int RHS 含 class METHOD_CALL 形态,RHS box USE 入 PIR list,reverse scan 找到 last-use,不再 fallback 到 defStmt;**bug A 修法**:`bootstrap/pir/pir_lower.ss:43-59` WHILE/FOR/FOR_IN/FOR_OF/DO_WHILE 5 处对称改 lower body 后追加 `pirEmitBlockUsesAtStmt(bodyId, loopStmtId, buf)` 调用(+5 LOC + 注释 4 行)+ 末尾新增辅助函数 `pirEmitBlockUsesAtStmt(blockId: int, stmtIdOverride: int, buf: string): string` 递归处理 VAR_DECL/ASSIGN/MEMBER_ASSIGN/EXPR_STMT/RETURN/嵌套 IF/WHILE/DO_WHILE/FOR/FOR_IN/FOR_OF 全形态(~58 LOC)— total +64 LOC pir_lower.ss(285→349 行,远低 600 F1 硬上限);**`./build.sh bootstrap` 三阶段固定点**:`Stage 1: seed → stage1 → Stage 2: stage1 → stage2 → Stage 3: stage2 → stage3 → Fixed point verified! Stage 2 = Stage 3 — Updated bin/ss`(80s 三阶段固定点完整);**Phase 1 spike 4 case 全 GREEN**:`bin/ss test tests/d164_pir_liveness_helper_fn_loop_class_method/` `1 passed, 0 failed, 1 total`(单 file 内 4 个 test() 全 GREEN — Case 1 r=126(42*3 — ASSIGN-form helper + while + simple class.method)/ Case 2 r=35(7*5 — ASSIGN-form helper + C-style for + class.method)/ Case 3 r=60((10+20)*2 — nested helper + nested loop + 多 class instance)/ Case 4 r=35(7*5 — EXPR_STMT-form helper + while + bump global,d164_min/t18 同形));**LLVM IR 验证**:`bin/ss build tests/d164_.../phase1_repro_spike_test.ss --emit-ir -o /tmp/d164_spike` 实证 4 个 helper fn IR — loopHelperCase1 line 5494-5496 ss_drop_TestBoxD164 在 `while.after.176` 块内 / loopHelperCase2 line 5529-5531 ss_drop_TestBoxD164 在 `for.after.180` 块内 / loopHelperInner line 5568-5572 ss_drop box1+box2 在 `while.after.183` 块内(multi instance + nested helper)/ loopHelperCase4 line 5604-5606 ss_drop_CounterD164 在 `while.after.186` 块内(bug A EXPR_STMT bump global 形态)— while.body/for.body/entry 块内 ss_drop = 0 全 4 case;**spot check**:`/tmp/d164_min.ss` 实测 `helper done` + `end` exit=0 / `/tmp/d164_main.ss` 实测 `main done` exit=0 — 无 mimalloc double free 报错;**bin/ss test tests/** 306/16/322 净 +1 PASS(从 Phase 1 RED-as-spike baseline 305/17/322 → 306/16/322,spike file 1 fail → 1 pass + 0 regression — D162 §F7 vtable + non-0-arg overload child class bug 修法 + D163 §Phase 2 ifaceMethodsCG.has 守护修法兼容 H3 实证);**baseline**:reflection_health_linter GATE PASS — no regressions(M1-M7+N1-N5 全继承 + M2 cur=83326 within 1% bm=83300 AUTO-DRIFT 软警告 / N2 cur=416630 within 1% bm=416000 AUTO-DRIFT / N3 cur=578015 within 1% bm=575500 AUTO-DRIFT / M3b cur=2014 within 1% bm=2010 AUTO-DRIFT — 全 4 项 AUTO-DRIFT 软警告不阻断;F1 pir_lower.ss 349 < 600 默认豁免不在 baseline)+ d_doc_index_linter F1=0 PASS(D147/D154/D155/D156/D157/D160/D161/D162/D163/D164 全实存)+ next_prompt_ultrathink_linter PASS(下轮 D164 §Phase 3 起首含 ultrathink + 不写 docs-only);**§N §6 file:line 锚**:bootstrap/pir/pir_lower.ss:140(bug B 修法 — LHS 非 class 早退前加 RHS USE 扫描)+ bootstrap/pir/pir_lower.ss:46-66(bug A 修法 — WHILE/FOR/FOR_IN/FOR_OF/DO_WHILE 5 处对称追加 pirEmitBlockUsesAtStmt 调用)+ bootstrap/pir/pir_lower.ss:294-352(bug A 新增辅助 pirEmitBlockUsesAtStmt 函数)+ docs/3-decisions/D164-pir-liveness-helper-fn-loop-class-method-drop.md(§Phase 2 §收关锚 + Status 时间线 Phase 2 entry + Status header Phase 1 [x] Done + Phase 2 [/] In progress + Phase 1 hash `0c888f5` 5 处回填)+ /tmp/d164_spike.ll line 5494-5496/5529-5531/5568-5572/5604-5606(4 case ss_drop 在 loop.after 块内实证)+ tests/d164_pir_liveness_helper_fn_loop_class_method/phase1_repro_spike_test.ss(spike file 不动)+ .claude/next_prompt.md(下轮 D164 §Phase 3 起首 ultrathink + 不写 docs-only);**等下轮 D164 §Phase 3** — D163 §Phase 3 重启 + D160 §Phase 4 wire 真值反射重启 + 在线 docker `docker compose -f tests/d134_mysql/docker-compose.yml up -d --wait` MySQL 8.0 healthy 后 `bin/ss test tests/d160_callable_statement/` 8 case 全 GREEN(Case 1 H1 / Case 2 OUT INTEGER 真值=20 H4 / Case 3 INOUT 真值=15 H4 / Case 4 multi OUT H4 / Case 5 三态 mode H3 / Case 6 getString H4 / Case 7 getDouble H4 / Case 8 wasNull SQL NULL)+ /tmp/d160_t18.ss 实测 exit=0 + 跨 D 起首回填 D164 §Phase 2 hash `26d656c` 至 D163.md(§Phase 3 重启锚 / Status header / §Followup F5 row 末尾上下文)+ D160.md(§F9 row 末尾上下文 / §Phase 4 §收关锚 / Status 时间线 D164 close + D163 §Phase 3 重启 entry)

- 2026-05-09 **D164 §Phase 1 minimum repro 隔离 spike + 4 case RED-as-spike + LLVM IR 双根因 isolate(bug A EXPR_STMT-in-loop + bug B ASSIGN LHS 非 class 早退漏扫 RHS USE)**(commit `0c888f5`)— **新建 tests/d164_pir_liveness_helper_fn_loop_class_method/phase1_repro_spike_test.ss** ~140 行 4 case(Case 1 ASSIGN-form helper + while + class.method / Case 2 ASSIGN-form helper + C-style for + class.method / Case 3 nested helper + nested loop + 多 class instance / Case 4 EXPR_STMT-form helper + while + bump global,d164_min/t18 同形)+ **跨 D 起首回填 D164 §Phase 0 hash `3e4c00d` 至 D164.md 4 处实际语义位**(Status header line 3 / §Phase commit hash 总览段表 Phase 0 行 line 86 / §Phase 0 §收关锚 line 93 / Status 时间线 Phase 0 entry)+ **更新 §A.2 H1/H2/H7 假设挑战实证修正**(H1 真根因细化为双 bug 在 pir_lower.ss 单文件,H2 假设破裂 pir_opt 算法本身不错,H7 假设破裂 helper vs main 路径不对称 — gen_decls.ss:125 显式分流 main 不走 PIR)+ **更新 Status header 标 Phase 0 [x] Done at commit `3e4c00d` + Phase 1 [/] In progress at commit `0c888f5`**;**RED 实证**:`bin/ss run tests/d164_.../phase1_repro_spike_test.ss` 输出 `Tests: 0 passed, 4 failed, 4 total`(Case 1 expected 126 got -1616928867 / Case 2 expected 35 got 1600085851 / Case 3 expected 60 got 2139062140 / Case 4 expected 35 got 2139062147 + mimalloc double free 报错 4 次,实证 use-after-free + mimalloc poison value 0x7F7F7F7F);**LLVM IR inspect 双根因**:**bug B**(spike Case 1 `/tmp/d164_spike.ll` line 5365-5368 entry 块 ALLOC `%1 = call ptr @TestBoxD164_new(i32 42)` 后立即 `call void @ss_drop_TestBoxD164(ptr %2)` — drop 在 while.cond 之前,第 1 次循环 UAF — pir_lower.ss:137-159 pirLowerAssign 早退漏扫 RHS USE → reverse scan 找不到 USE → fallback to defStmt VAR_DECL stmt id)+ **bug A**(`/tmp/d164_min.ll` line 5197-5200 while.body.154 内 `call void @ss_drop_TestBoxD164(ptr %8)` — drop 每次循环 emit,第 2 次起 UAF — pir_lower.ss:43-59 漏 emit USE 在 loop 整体 stmt id → reverse scan first-hit 在 body 内)+ **main 路径对照**(`/tmp/d164_main.ll` line 5212-5213 while.after.155 块外 `%14 = load ptr, ptr %box.79` + `call void @ss_rc_release(ptr %14)` 旧 RC 系统 release 路径 GREEN — 因 `bootstrap/gen/gen_decls.ss:125 if (name != "main") { pirAnalyzeFunc(...) }` 显式分流);**Phase 2 修法 target file:line + LOC 估**:bug B = pir_lower.ss:137-159 pirLowerAssign 早退前加 `buf = pirEmitExprUses(nGetI1(id), id, buf)` LHS 非 class 路径(+1~+3 LOC)+ bug A = pir_lower.ss:43-59 WHILE/FOR/FOR_IN/FOR_OF/DO_WHILE 5 处对称 + 新增辅助 `pirEmitBlockUsesAtStmt(blockId: int, stmtIdOverride: int, buf: string): string` 递归处理 VAR_DECL/ASSIGN/MEMBER_ASSIGN/EXPR_STMT/RETURN/嵌套 WHILE/DO_WHILE/FOR/FOR_IN/FOR_OF/IF 全形态(+25~+45 LOC)— 总计 +30~+50 LOC 单 file pir_lower.ss;**baseline**:bootstrap/ + lib/ + tools/ diff = 0(本 Phase 仅 spike 落地 + docs/D164 hash 回填 + .claude/next_prompt.md)+ tests/ +1 file(spike RED → fail count +1 暂时退化)+ d_doc_index_linter F1=0 PASS(D147/D154/D155/D156/D157/D160/D161/D162/D163/D164 全实存)+ next_prompt_ultrathink_linter PASS(下轮 D164 §Phase 2 起首含 ultrathink + 不写 docs-only)+ 14 reflection 指标全继承 D163 §Phase 2 baseline(本 Phase 不动 bootstrap/ → reflection scope 不触)+ `bin/ss test tests/` 净 +1 fail = 305/17/322(spike file 级 1 fail,Phase 2 修法后回归 305/16/321 + spike PASS);**simplify 跳过**(本 Phase 仅 spike + 诊断 + docs/ 不动 bootstrap/lib/tools/,§After Done §1 例外 — 同 D163 §Phase 1 / D162 §Phase 1 / D161 §Phase 1 spike-only 范式延续);**§N §6 file:line 锚**:tests/d164_pir_liveness_helper_fn_loop_class_method/phase1_repro_spike_test.ss(本 spike 落地 ~140 行 4 case)+ docs/3-decisions/D164-pir-liveness-helper-fn-loop-class-method-drop.md(§Phase 1 §收关锚 + §A.2 H1/H2/H7 假设修正 + Status 时间线 Phase 1 entry + Phase 0 hash `3e4c00d` 4 处回填)+ bootstrap/pir/pir_lower.ss:43-59 / 137-159(Phase 2 修法 target 锁)+ bootstrap/gen/gen_decls.ss:125(main vs 非 main 分流锚 — H7 假设破裂实证)+ /tmp/d164_spike.ll line 5365-5368(bug B 实证)+ /tmp/d164_min.ll line 5197-5200(bug A 实证)+ /tmp/d164_main.ll line 5212-5213(main 路径对照)+ .claude/next_prompt.md(下轮 D164 §Phase 2 起首 ultrathink + 不写 docs-only);**等下轮 D164 §Phase 2** — bootstrap/pir/pir_lower.ss 双修法(bug B pirLowerAssign 早退前加 RHS USE 扫描 +1~+3 LOC + bug A 新增 pirEmitBlockUsesAtStmt 辅助 + WHILE/FOR/FOR_IN/FOR_OF/DO_WHILE 5 处对称 +25~+45 LOC)+ ./build.sh bootstrap 三阶段固定点 stage2==stage3 PASS + Phase 1 spike 重跑 4 case 全 GREEN(Case 1 r=126 / Case 2 r=35 / Case 3 r=60 / Case 4 r=35)+ LLVM IR `grep -c "ss_drop_TestBoxD164" /tmp/d164_spike.ll` 在 while.body / for.body 块内 = 0 + entry 块内立即 drop = 0 + reflection_health_linter no regression GATE PASS(F1 pir_lower.ss budget 评估)+ bin/ss test tests/ 净不退化 305/16/321 + 跨 D 起首回填 D164 §Phase 1 hash 至 D164.md ≥4 处

- 2026-05-09 **D164 起首脱胎 + Phase 0 D 文档落档**(commit `3e4c00d`)— **新建 docs/3-decisions/D164-pir-liveness-helper-fn-loop-class-method-drop.md** ≥150 行(§核心目标 + §核心原则 + §A.1 候选评估 + §A.2 隐藏假设 H1-H5 + §A.3 废案 + §Phase 收关锚 Phase 0-4 + §Followup F1-F5 + §Status 时间线)+ **跨 D 起首回填 D163 §Phase 2 hash `ae16073`(root cause #1 codegen MEMBER_ACCESS 类型解析 wire 落地)+ D163 §Phase 3 hold hash `29a7487`(D164 起首脱胎)至 D164.md ≥3 处实际语义位**(Status header line 3 / §起首脱胎 line 9-10 / §Phase commit hash 总览段表 Phase 0 行 line 86 / Status 时间线 起首脱胎 entry 本 entry)— D163 §F5 起首脱胎,根因优先(用户对话锁 docker 在线验证 wire 真值实测发现独立 PIR liveness root cause #2);**RED 实测**:`/tmp/d160_t18.ss` 17 行 实测 exit=139 segfault;改 `while (i < 1)` 1 次循环 → exit=0 GREEN;改 helper fn 内代码搬 main() 体内 → exit=0 GREEN — bug 触发条件 = helper fn + while loop ≥ 2 次 + class instance.method();LLVM IR inspect:dropAllProcs 函数 IR while.body 块出现 `call void @ss_drop_JdbcTemplate(ptr %tmpl)` — PIR liveness 误标 last-use 在 body 内,每次循环 drop tmpl,第 2 次循环 use-after-free segfault;对比 main() 体内同样代码(`/tmp/d160_t10.ss`)IR drop 正确放在 while.after 块外 — 仅 helper fn 路径错;**GREEN**:D164.md 落档 + .claude/next_prompt.md 含 ultrathink + D163 §Phase 3 hold + D164 起首脱胎 entry 一致;**baseline**:bootstrap/ + lib/ + tools/ + tests/ diff = 0(本 Phase 纯 docs/ + .claude/next_prompt.md)+ d_doc_index_linter F1 = 0 PASS(D147/D154/D155/D156/D157/D160/D161/D162/D163/D164 全实存,加入 D164 实存)+ next_prompt_ultrathink_linter PASS(下轮 D164 §Phase 1 起首含 ultrathink + 不写 docs-only)+ 14 reflection 指标全继承 D163 §Phase 2 baseline(本 Phase 不动 bootstrap/ → reflection scope 不触);**simplify 跳过**(纯文档落档,§After Done §1 例外 — 同 D163 §Phase 0/1/3-hold / D162 §Phase 0/1 / D161 §Phase 0/4 / D160 §Phase 0 / D157 §Phase 0 / D156 §Phase 0 / D154 §Phase 0 / D155 §Phase 0 docs-only 范式延续);**§N §6 file:line 锚**:docs/3-decisions/D164-pir-liveness-helper-fn-loop-class-method-drop.md(本 D 落档 ≥150 行)+ docs/3-decisions/D163-class-extends-iface-typed-field-write.md(§Phase 2 hash `ae16073` + §Phase 3 hold hash `29a7487` 起首脱胎来源)+ docs/3-decisions/D160-callable-statement-out-inout.md(§F9 wire 真值反射重启锚等 D164 close 后回填)+ .claude/next_prompt.md(下轮 D164 §Phase 1 起首 ultrathink + 不写 docs-only)+ /tmp/d160_t18.ss 17 行最小 repro 文件(留 D164 §Phase 1 spike 移植起点)+ /tmp/d160_t10.ss 对比文件(同样代码在 main() 体内 GREEN — IR 对比锚);**等下轮 D164 §Phase 1** — minimum repro spike 移植 + ≥3 case RED-as-spike(Case 1 helper fn + while loop ≥ 2 次 + simple class.method / Case 2 helper fn + for loop + class.method / Case 3 nested helper fn + nested loop + 多 class instance)+ LLVM IR inspect 对比 helper fn vs main 体内 IR 差异 + 加 trace println / DEBUG dump 在 pir_lower.ss + pir_opt.ss schedule 计算入口 isolate H1(pir_lower 路径错)vs H2(pir_opt 路径错)+ Phase 2 修法 target 锁定 file:line + LOC 估算 + 跨 D 起首回填 D164 §Phase 0 hash 至 D164.md 4 处
