# D093: SEMA 单函数 dispatch — Zig 本质一样路径

**Status:** Phase 0 Done at 2983d8a — 2026-05-20 反向倒退 audit + 三类混合分类 + Phase 1 首批起手 spec 落地;**Phase 1 Blocked at 5d36c8c** — §拒绝准则 #1/#3 触发(详 §Phase 1 受阻 + D169),升级 Phase 1.5(D169 起首);**Phase 1.5 §A class 形态 Superseded by D098 §决策 1**(2026-05-20 ultrathink — D169 立项时漏引 D098,§A class 改 D098 §决策 1 纯 int 4 helper);**Phase 1.5a Done at 3710016** — D098 §决策 1 4 helper(mvKnownOf/mvValOf/mvRuntime/mvError)+ comptimeMustBeKnown flag + enter/exit + UNARY POC 4 处 mvRuntime + D169 §POC 失败实证落档;**Phase 1.5b 单点起手 Done at e423591** — UNARY 入口双轨消除(1/5 类 B 处);**Phase 1.5b BINARY 子轮 Done at 4573341** — BINARY 入口 ct-depth 字面消(2/5 类 B 处累计 eval_expr.ss);`grep -c "comptimeDepth > 0" bootstrap/eval/eval_expr.ss` 2 → 1(BINARY case 入口 ct-depth 字面消 + int/bool runtime 走 mv 协议 + comptimeMustBeKnown 第二 read callsite;留 line 76 COMPTIME_EXPR 类 C 边界检查);**§POC N3 实证 落档**(首版按 inferType pre-route 在 comptime 路径引入 17 处 regression,inferType IDENT fallback "int" 致 `acc != ""` 走 int interpAsInt 恒 false → 修正改 1:1 字面 rename + 内部纯 valType dispatch);**Phase 1.5c 起手 Done at c9f8ff6** — TERNARY 子模块入口 ct-depth 字面消(`bootstrap/eval/ternary.ss:10` 单点字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1` + OLD silent null `ctVal(interpNewNull())` 升 loud error `comptimeError("ternary condition not compile-time known", astId)` + `comptimeMustBeKnown == 1` 第三 read callsite 接入);`grep -c "comptimeDepth > 0" bootstrap/eval/ternary.ss` = 1 → 0;**Phase 1.5c short_circuit 子轮 Done at 7d60732** — SHORT_CIRCUIT 子模块(`bootstrap/eval/short_circuit.ss:13`)入口 ct-depth 字面消 + OLD silent null `ctVal(interpNewNull())` 升 loud error `comptimeError(`short-circuit '${op}' operand not compile-time known`, astId)` + `comptimeMustBeKnown == 1` 第五 read callsite 接入;`grep -c "comptimeDepth > 0" bootstrap/eval/short_circuit.ss` 1 → 0;**Phase 1.5c index_access 子轮 Done at 6d6d0ec** — INDEX_ACCESS 子模块(`bootstrap/eval/index_access.ss:18`)入口 ct-depth 字面消(`comptimeDepth > 0 → comptimeMustBeKnown == 1` 纯字面 rename,本子模块已 loud error 形态无 silent→loud 升级 + `comptimeMustBeKnown == 1` 第六 read callsite 接入);`grep -c "comptimeDepth > 0" bootstrap/eval/index_access.ss` 1 → 0;**Phase 1.5c template_lit 子轮 Done at a69e964** — TEMPLATE_LIT 子模块(`bootstrap/eval/template_lit.ss:36`)入口 ct-depth 字面消(`comptimeDepth > 0 → comptimeMustBeKnown == 1` 纯字面 rename,本子模块 line 36 已 loud error 形态无 silent→loud 升级 + `comptimeMustBeKnown == 1` 第七 read callsite 接入);`grep -c "comptimeDepth > 0" bootstrap/eval/template_lit.ss` 1 → 0;**Phase 1.5c member_access 子轮 Done at 659034e** — MEMBER_ACCESS 子模块(`bootstrap/eval/member_access.ss:36`)入口 ct-depth 字面消(`comptimeDepth > 0 → comptimeMustBeKnown == 1` 纯字面 rename,本子模块 line 36 已 loud error 形态无 silent→loud 升级 + `comptimeMustBeKnown == 1` 第八 read callsite 接入);`grep -c "comptimeDepth > 0" bootstrap/eval/member_access.ss` 1 → 0;**Phase 1.5c postfix_inc 子轮 Done at 1a5a824** — POSTFIX_INC 子模块(`bootstrap/eval/postfix_inc.ss:5`)入口 ct-depth 字面消(`comptimeDepth > 0 → comptimeMustBeKnown == 1` 纯字面 rename;**入口双轨 dispatch 非 loud-error guard**,line 28 silent null `ctVal(interpNewNull())` 属正交 error-reporting 关切本轮不升级 + `comptimeMustBeKnown == 1` 第九 read callsite 接入);`grep -c "comptimeDepth > 0" bootstrap/eval/postfix_inc.ss` 1 → 0;**Phase 1.5c array_lit 子轮 Done at 04deb68** — ARRAY_LIT 子模块(`bootstrap/eval/array_lit.ss:7+21`)入口 ct-depth 字面消(`comptimeDepth > 0 → comptimeMustBeKnown == 1` 纯字面 rename ×2,**1.5c 首个多点 full-file 子轮**:line 7 空数组 comptime 分支 + line 21 非空 comptime 分支两处皆纯单 `comptimeDepth > 0` guard 无 compound 条件,整文件 2→0 一轮全迁无过渡混态合 §张力 #4;入口双轨 dispatch 非 loud-error guard 对齐 postfix_inc;line 42 per-element silent null 属正交本轮不碰;无 inferType pre-route 风险 — elemVals 在 line 13-19 loop 先 genVal+isCt 于两 guard 之前)+ `comptimeMustBeKnown == 1` 第十+十一 read callsite 接入;`grep -c "comptimeDepth > 0" bootstrap/eval/array_lit.ss` 2 → 0;**Phase 1.5c new_expr 子轮 Done at 64a6694** — NEW_EXPR 子模块(`bootstrap/eval/new_expr.ss:7+16+51`)入口 ct-depth 字面消(`comptimeDepth > 0 → comptimeMustBeKnown == 1` 纯字面 rename ×3,**1.5c 第二个多点 full-file 子轮**:line 7 泛型类 comptime 注册分支 + line 16 Map/Set comptime 一行直构造 + line 51 主 comptime dispatch `ctNewExprDispatch` 三处皆纯单 `comptimeDepth > 0` guard 无 compound 条件,整文件 3→0 一轮全迁无过渡混态合 §张力 #4;入口双轨 dispatch 非 loud-error guard 对齐 postfix_inc/array_lit;line 36/44 NAMED_ARG/arg 非-ct silent null 属正交本轮不碰;无 inferType pre-route 风险 — line 51 前 arg loop line 32/40 先 genVal+isCt 于 guard 之前、line 7 interpClasses.set + line 16 interpNewMap() 直构造无 inferType)+ `comptimeMustBeKnown == 1` 第十二/十三/十四 read callsite 接入;`grep -c "comptimeDepth > 0" bootstrap/eval/new_expr.ss` 3 → 0;**Phase 1.5c call 子轮 Done at ed7ddaa** — CALL 子模块(`bootstrap/eval/call.ss:10+44+59+77`)入口 ct-depth 字面消(`comptimeDepth > 0 → comptimeMustBeKnown == 1` 纯字面 rename ×4,**1.5c 第三个多点 full-file 子轮 + 余下 3 候选最后一个全纯多点文件**:line 10 泛型函数 comptime 注册 + line 44/59 SPREAD_ELEM comptime loud-error guard ×2 + line 77 主 comptime dispatch `ctCallDispatch` 四处皆纯单 `comptimeDepth > 0` guard 无 compound 条件,整文件 4→0 一轮全迁无过渡混态合 §张力 #4;line 10/77 入口双轨 dispatch 非 loud-error guard 对齐 postfix_inc/array_lit/new_expr,line 44/59 已 loud error 形态;line 36/70 NAMED_ARG/arg silent null 属正交本轮不碰;无 inferType pre-route 风险 — line 77 前 arg loop line 25-76 先 genVal+isCt 于 guard 之前、line 10 ctFuncNodes.set + line 44/59 loud-error guard 无 inferType)+ `comptimeMustBeKnown == 1` 第十五/十六/十七/十八 read callsite 接入;`grep -c "comptimeDepth > 0" bootstrap/eval/call.ss` 4 → 0;**Phase 1.5c ident 子轮 Done at a0e43e2** — IDENT 子模块(`bootstrap/eval/ident.ss:6+31`)入口 ct-depth 字面消(`comptimeDepth > 0 → comptimeMustBeKnown == 1` 纯字面 rename ×2,**1.5c 首个含 compound 子轮**:line 6 ct-scope 复合 guard `comptimeDepth > 0 && ctScopeStack.length() > 0` 只迁 `comptimeDepth > 0` 子式 + line 31 纯单 `comptimeDepth > 0` 入口 dispatch,整文件 2→0 一轮全迁无过渡混态合 §张力 #4;入口双轨 dispatch 非 loud-error guard,两 comptime 分支(line 7-14 ctScopeStack/ctVars lookup / line 32-47 interpFindScopeKey/isKnownClass/genericTypeSubs)未命中即落 line 49 runtime genIdent fallthrough 非 interpNewNull silent null 本轮纯字面 swap 不碰;无 inferType pre-route 风险 — 两分支直读无 genVal/inferType)+ `comptimeMustBeKnown == 1` 第十九/二十 read callsite 接入;`grep -c "comptimeDepth > 0" bootstrap/eval/ident.ss` 2 → 0;**Phase 1.5c method_call 子轮 Done at d789a11** — METHOD_CALL 入口 ct-depth 字面消 ×3(`method_call.ss:10+15+67`,line 10/15 compound 只迁 `> 0` 子式 + line 67 纯单,本文件 3→0 全局 37→34,`comptimeMustBeKnown == 1` 第 21/22/23 read callsite,**1.5c 末个 class-B eval 入口收口转 1.5d**;line 15 触反射 `reflection_health_linter` 前后双跑 GATE PASS;**line 18/23/28 三处 `comptimeDepth == 0` inverse 正交保留** — next_prompt「唯一写端」premise 经 PSM 字段 12 证伪 codegen.ss:281 第二写端,改判 piecemeal 反模式 + member_access:14 先例 + reset 对称 prereq 1.5d → inverse 留 1.5d 统一迁);genBinary/genStringConcat/genValStringCompare 接口扩 + 统一 inverse 迁移 + reset 对称补全留 1.5d;**Phase 1.5d 起手 Done at 68c05f7** — `codegen.ss:281` resetCodegen 补 `comptimeMustBeKnown = 0`(reset 对称落地,memory comptimedepth-reset-asymmetry-codegen281 兑现 — 1.5c 无害但 1.5d mustBeKnown 成唯一 gate 须 reset 对称)+ inverse 4 处统一迁 `comptimeDepth == 0 → comptimeMustBeKnown == 0`(`method_call.ss:18/23/28` + `member_access.ss:14` 一轮全迁不 piecemeal,1.5c 收口 d789a11「inverse 留 1.5d 统一迁 + reset 对称 prereq」承诺兑现)+ ct_driver import 扩;`grep -rn "comptimeDepth == 0" bootstrap` 5 → 1(仅留 `ct_driver:36` 真嵌套 exitComptimeBlock 出栈 reset 写端)+ `grep -rn "comptimeMustBeKnown == 0" bootstrap` 0 → 4(首批 read callsite 接入 method_call:18/23/28 + member_access:14);**Phase 1.5d 剩余 prereq 起首 Done at a6b511e** — 三函数接口扩 take pre-eval reg(`genValStringCompare(op, id, lPreVal: int = 0 - 1, rPreVal: int = 0 - 1)` + `genStringConcat`/`genBinary` 加 `lPreReg/rPreReg: string = ""` 哨兵,SS bootstrap 既有 preReg/preObj 协议 5 函数惯例 — `genExprAsString`/`genIndexAccess`/`genMemberAccess`/`genMethodCall`)+ `eval_expr.ss:128` comptime path 1/4 callsite 迁 `genValStringCompare(op, astId, ctBlv, ctBrv)` 复用 line 116-117 已 eager Value 句柄(InternPool dedup 掩盖 double-eval bug 消除),137/142/146 runtime path callsite 暂保 OLD ast-id 形态(forward-compatible 留主轮第一子步迁);**Phase 1.5d 真单 dispatch 主轮第一子步 Done at 2af65a7** — BINARY runtime path 142/146 callsite 迁(`eval_expr.ss:148-165` eager `genVal(nGetI1/I2)` 上移 + lMv/rMv 反向编码 + **混合 dispatch** `isCt(lRaw) == 1 ? valType(lRaw) : inferType(...)` blt/brt(ct path valType 准确 §POC N3 17 处 ctVars IDENT fallback "int" 错配消除 + runtime path inferType 等价 OLD 兜底)+ 142 callsite `genValStringCompare(op, astId, lRaw, rRaw)` forward Value/regId + 146 callsite `mvRuntime(constVal(genBinary(astId, reg(lRaw), reg(rRaw))))` forward reg → delegate 内 `f()+g()` 副作用 IR emit 两次 double-eval bug 消除);PSM 字段 12 两道假设破裂实证(`valType(neg_mv)` bit-mask InternPool miss + `reg(neg_mv)` bit 30 误判 → `ptr 0` 崩 spike 实测),§POC N3 9 suite GREEN baseline 334/3 持平;**Phase 1.5d 真单 dispatch 主轮第二子步 Done at ba2f445** — 137 callsite 4 op (NullCoalesce/Instanceof/As/Pow) 拆独立 evalExpr case + 死代码消(3 sibling 子文件 `null_coalesce.ss` + `instanceof_as.ss` + `pow_binary.ss` 落地 + `eval_expr.ss` line 101-103 注入 3 case dispatch + OLD ct path 4 op 分支 line 108-115 + runtime fallback line 136-138 物理删 + BINARY 主 case 收口为通用 binop only),sibling ternary/short_circuit/index_access 模式一致,simplify H1 op-aware 合并 Instanceof+As → `evalInstanceofOrAs(op, astId)` (sibling `evalShortCircuit` And/Or 模式,2 fn 7 行重复 → 1 fn),NullCoalesce silent encoding bug incidental fix(OLD `return genVal(nGetI2(astId))` 在 lhs ct null + rhs runtime 时返 positive regId 伪装 mv-known → 修正 `isCt(rv) == 1 ? rv : 0 - rv - 1` mv 编码 forward),baseline 334/3 持平 §POC N3 9 suite GREEN;**Phase 1.5d 真单 dispatch 主轮第三子步 Done at bb0f805** — BINARY 通用 ct path leaf gate 主块消(eager unify lRaw/rRaw 仅 genVal 一次上移到 leaf gate 之前 + 双 ct fold 短路 `isCt(lRaw) == 1 && isCt(rRaw) == 1` + 紧 loud error fallback 单行 sibling 一致对齐 `ternary.ss:10` / `short_circuit.ss:13` + OLD 24 行 wrapper 消)+ `interpNumericBinop(op, lp, rp, lt, rt): int` helper 抽取到 `bootstrap/eval/interp_op.ss`(5 行×2 callsite → 1 helper×2 caller `eval_expr.ss:122` + `pow_binary.ss:16`,simplify Finding 1 / H2 unify 兑现)+ **interpAsStr → interpToStr 根因修**(double 值存 tvD1 列 interpToStr 分发,OLD pre-existing latent bug 仅 comptime context 未触发,本轮 ct fold scope 扩到 non-comptime context 暴露 `abstract_basic.ss` `2.0 * 3.14159 * radius` 折 0.0 → CLAUDE.md §Root Cause 第一法则 一并修);死代码消(lMv/rMv 局部 + int/bool runtime path `mvKnownOf+mvKnownOf ctVal(interpIntOp...)` 分支 — eager ct fold 短路于上 runtime 不可达);PSM 字段 12 假设破裂实证(eager double-eval 防 + ct fold scope 扩到 non-comptime 暴露 latent bug);baseline 334/3 §POC N3 9 suite 全 GREEN 持平;回 Phase 1 类 A 起手 do-while spike + 后续 sibling 子轮 for/while 消除(1.5d 主轮收口子步起手 + 后续 sibling 子轮)+ §0.3 验收 5 项达成 留下下轮
**Depends on:** D088(Zig 路线), D092(双轨 SEMA 实现 — 被本决策替代)
**Spawned:** D169(MaybeVal + comptimeMustBeKnown 协议接口详设 — Phase 1.5 prereq)
**Date:** 2026-04-15
**Last Updated:** 2026-05-20 (Phase 1 受阻 → Phase 1.5 升级 + D169 起首)

## 第一性需求

**编译器里只能有一份求值逻辑。** comptime 与 runtime 不是两个世界,是同一份逻辑对同一段代码的两次提问:「这个值现在就能算出来吗?」能 → fold 成常量,不能 → 发射 runtime 指令。

当前 SS 的 **40 处** `if (comptimeDepth > 0) { genValCt* } else { gen* }` 分岔 + **7 个** `genValCt*` 函数代表的「comptime 专用求值器」设计,是把一份逻辑拆成两份独立实现。两份没有同步机制,人类审阅抓不全差异——过去 4 轮对 D092 代码的架构判断反转,根源都在这里。

消除双轨不是风格偏好,是防假装 bug 的唯一机制。

## 历史语境(2026-04-15)

D093 替代 D092 "半 SEMA + 填洞即合并" 路径,来源于本轮对话的以下发现:

1. **D092 既有代码不可靠** —— 单轮对话中 4 次架构判断反转(全盘否定 → 仅节奏缺陷 → 另起炉灶 → 骨架正确 / interp* 必要),每次新读都被下一次读推翻。根因: D092 实现层是"双轨 + 手工同步",代码在"装修双轨 vs 合并架构"两种意图之间漂移,无一致架构承诺。详见 `docs/3-decisions/D092-sema-architecture.md` §不可靠性声明
2. **dev 分支是废墟** —— `d1816ab` 清零了 `interp.ss` / `gen_reflect.ss` / `gen_annotations.ss` 等文件,但保留了 `gen_assigns.ss` / `gen_exprs.ss` 里 **448 处** undefined function 调用。不是 clean slate,是半拆房 + 引用悬空。任何"reset 到 dev 重做"的提议都会落入比当前更糟的状态,已在 `feedback_verify_reset_target.md` 锚定
3. **Phase 8 commit `25e5760` 是双轨装修** —— `interpIntOp` 分支顺序调整 + `UShr` 补丁 + Q1 最小测试,全部在双轨系统一侧补洞,不动消除双轨的结构。符合 D088 §反模式 "给 interp.ss 抄 handler 加深双轨制"
4. **方法论教训** —— "读代码决定架构" 在假装期代码上必然产生反转,因为前提(代码一致性)不成立。设计阶段必须**从原理出发**,不从代码出发。已在 `feedback_design_no_code_authority.md` 锚定

D093 的 §决策 和 §差距清单 不引用 D092 代码作为架构依据——Zig 原理段完全出自 `src/Sema.zig` / `src/Value.zig` / `src/InternPool.zig`,§差距清单 的 40 / 7 数字只作为**现状快照**(要消除的目标),不作为**设计依据**。

## 决策

**走 Zig SEMA 的单函数 dispatch 路径**:一个 `evalExpr` 函数对任意 AST 节点返回「已知值 / 未知」二元结果,`genExpr` 据此决定 fold 常量还是发射 runtime 指令。comptime 块只是对 `evalExpr` 返回值的强约束 flag,不是独立求值器。

### Zig 原理

纯原理提炼,来源 `src/Sema.zig` / `src/Value.zig` / `src/InternPool.zig` + Mitchell Hashimoto "Zig's Comptime is Bonkers Good"。**不引用任何 SS 既有代码作为权威**。

1. **单函数求值入口**: `Sema.resolveMaybeUndefVal(inst: Air.Inst.Ref) -> ?Value`。任意 ZIR 指令走这一个函数,拿 `?Value` 决定下一步。`Sema.zig` 全文只有一个 `resolveMaybeUndefVal`,没有 comptime 专用副本
2. **`?Value` 二元返回**: 有值 → 编译期已知 → `genConst`;null → 无法编译期确定 → 发射 AIR runtime 指令。**同一 `?Value` 语义覆盖 binop / call / member / index / cast / branch 全部表达式类型**,没有任何 `if comptime then A else B` 分岔
3. **Value 是无类型的**: `Value.zig` 里 Value 只存"这是什么值"(tag + payload),不存"这是什么类型"。类型信息单独存在 AIR 指令或 TypedValue 结构里。Value 的无类型性让 `?Value` 能作为通用容器覆盖一切
4. **Type 本身是一个 Value**: `Value.Tag.ty` —— 类型是一等公民,可以被 evalExpr 返回。这使得泛型单态化、`@TypeOf`、`@typeInfo` 无需独立通道,全部走同一套 dispatch
5. **InternPool 统一去重**: `InternPool.zig` 把所有 Value 和 Type 映射到稳定 index,相同值/类型共享同一 index。Value 相等比较是 O(1) index 比较,而非深度结构比较

### SS 本质一样骨架

```ss
// MaybeVal:SS 没有 nullable,用 class 模拟 Zig ?Value
// known=true  → val 是 Value id(InternPool 索引或 tagged int)
// known=false → val 是 LLVM 寄存器引用占位,无编译期意义
class MaybeVal {
    known: bool
    val: int
}

// 唯一求值入口(comptime 与 runtime 共用)
function evalExpr(astId: int): MaybeVal {
    // binop / call / member / index / cast / literal 共享同一分发表
    // 对每一种 kind:
    //   - 递归 evalExpr 所有子表达式,拿 MaybeVal
    //   - 若所有子项 known=true → 调对应 interp* 常量计算,返回 {known:true, val}
    //   - 否则返回 {known:false, val:emitRuntimeSubExprs(...)}
    // 没有任何 "if comptimeDepth > 0" 分岔
}

// 唯一代码生成入口
function genExpr(astId: int): string {
    let m = evalExpr(astId)
    if (mvKnown(m)) {
        return emitConstFromVal(mvVal(m))    // fold 成 LLVM 常量
    }
    if (comptimeMustBeKnown) {
        error("comptime block contains runtime-only expression")
    }
    return emitRuntimeInst(astId)             // 发射 LLVM 指令
}

// comptime 块语义:flag,不是独立求值器
function enterComptimeBlock() { comptimeMustBeKnown = true }
function exitComptimeBlock()  { comptimeMustBeKnown = false }
// 块内任何 evalExpr 返回 known=false 即 error,不存在"comptime 专用"走法
```

**与 Zig 的名词对齐**:

| Zig | SS | 说明 |
|---|---|---|
| `Sema.resolveMaybeUndefVal` | `evalExpr` | 唯一求值入口 |
| `?Value` | `MaybeVal { known, val }` | 二元返回,SS 无 nullable 用 class 模拟 |
| `Sema.air_instructions` | LLVM IR 发射路径 | runtime 分支落点 |
| `InternPool` | 未补 — §张力 第 2 条 | Value / Type 去重层,待 D094 |
| `Sema.typeOf(inst)` | 独立 type 通道 | Value 层不混 Type,§差距 第 3 条 |

## 与双轨现状的差距清单

每项标 P19 状态。本 D 文档**不**给修复步骤,下一轮 Execute 单独决策。

1. **[ ] Planned** — 消除 40 处 `if (comptimeDepth > 0)` 分岔
   现状: gen_exprs.ss:18 + gen_stmts.ss:15 + gen_decls.ss:4 + gen_assigns.ss:2 + codegen.ss:1 = 40。目标: 分岔数归 0,全部走 `evalExpr` → `genExpr` 两步

2. **[ ] Planned** — 合并 7 个 `genValCt*` 函数到 `evalExpr` 分发表
   现状: `genValCtCall` / `NewExpr` / `MemberAccess` / `MethodCall` / `TemplateLit` / `ArrayLit` / `IndexAccess` 各自独立。目标: `genValCt*` 函数不再存在,逻辑作为 `evalExpr` 各 kind 分支吸收

3. **[ ] Planned** — TypedValue storage 拆成 Value + Type 两层
   现状: `{tvKind, tvI1, tvS1, tvD1, tvList, tvMap}` 把 kind 和 val 混存,名义 TypedValue 实则 Kind-tagged Value。目标: Value 层不带 Type,Type 查询走独立通道(`astType` / `inferType` 或未来 InternPool 的 Type id)

4. **[ ] Planned** — 引入 InternPool 去重 Value / Type
   现状: 无 intern,相同常量每次产生新 id(或通过 tagged int 原地带值)。目标: 实现 InternPool,Value 相等即 id 相等,Type 同理

5. **[ ] Planned** — comptime 块降为 `comptimeMustBeKnown` flag
   现状: comptime 块内触发一整套独立求值器 —— `interpIntOp` / `interpDoubleOp` / `interpCompoundOp` / `interpGetField` / `interpSetField` / `ctVars` / `ctScopeStack` / `interpBreakFlag` / `interpContinueFlag` / `interpReturnFlag` / TypedValue storage,本质是 code + state 两份独立系统。目标: 块内走同一个 `evalExpr`,只在块边界把 flag 置位/复位,`evalExpr` 返回 `known=false` 即 error

## Rejected Alternatives

- **A: 继续双路径手工同步**(现状延长线) —— 每 phase 两改,漏一侧即假装 bug。过去 4 轮架构判断反转即证据
- **B: 抄 Nim 编译期 VM / Crystal macro 系统** —— 独立 VM 本身就是双轨;脱离 D088 §借鉴来源 的 Zig 路线
- **C: D092 sub-a→sub-e 补洞路径** —— 装修双轨而非消除,D088 §核心验证 Q2 "双轨制根因" 两问过不了
- **D: 把 `genValCt*` 合入 `genExpr` 但保留 `if comptimeDepth > 0` 分岔** —— 表面合并底层仍双轨,消除不到根

## 张力

1. **SS 无 nullable** —— `MaybeVal` 用 `{known: bool, val: int}` class 模拟 Zig `?Value`。`known=false` 时 `val` 字段的语义需明确(LLVM 寄存器引用占位 / 哨兵值 / 未设),是骨架的核心编码决策,D094 细化
2. **InternPool 依赖哈希表** —— SS Map 够用但需定制 hash/equal。Value 的结构化比较 vs 引用比较取舍单独立项
3. **Type-as-Value 的编码空间** —— 当前 tagged int `ctVal(id) = id | 1073741824` 只有一个 tag,扩展到同时容纳 int / string literal / Type 句柄 / class instance 需重新设计 payload 编码
4. **渐进迁移还是一次性重构** —— 40 + 7 处差距是一次重构还是分批,本 D 文档不决定,下一轮单独立项。但**不允许**"保留双轨作过渡态" —— 即便分批,每批完成时双轨必须局部消除,不允许长期共存

## Phase 0: 反向倒退 audit + 分类规约 + 首批起手 spec (2026-05-20 起首)

**Status**: [x] Done at 2983d8a

### 0.1 反向倒退 audit

2026-04-15 D093 立项时 baseline = 39 处 `comptimeDepth > 0` 分岔(gen_assigns 2 / gen_decls 4 / gen_exprs 18 / gen_stmts 15)。截至 2026-05-20 = **54 处**(**+15 / +38% 反向倒退**)。

**关键发现**: 旧 4 扁平文件 39 处全部消失,**新 27 子目录文件 54 处全部新增** — D113 SEMA 模块拆分(commit `e3d8262`)只是把双轨分岔搬家,**根因双轨架构未消除**。

| | baseline (2026-04-15) | 现在 (2026-05-20) | delta |
|---|---|---|---|
| 旧扁平 `gen_{exprs,stmts,decls,assigns}.ss` | 39 | 0 (文件消失) | -39 |
| 新 `bootstrap/eval/` (11 文件) | 0 | 25 | +25 |
| 新 `bootstrap/gen/{class,methods,stmts,exprs}/` (16 文件) | 0 | 29 | +29 |
| **总数** | **39** | **54** | **+15** |

**事件链**(2026-05-20 PSM §字段 13 + linter C5 双闸落地的触发链):

```
e3d8262 D113 SEMA 模块拆分 (末次 SEMA 主线 commit, 2025-12)
  → D168 RC system 长线 (2026-01-04 起)
    → SS-LIM-4/5/6 修复 (2026-04)
      → I023-I031 CLI/build/comptime issue (2026-04-05~)
        → Tier1-5 tests refactor (2026-05-12~19, 5 commits)
          → next_prompt Tier 第 6 轮 (本轮被双闸 GATE BLOCKED 拦住)
```

5 个月 SEMA Q1 主线 0 commit。Phase 0 起首本身受同期落地的双闸防护,Phase 1+ 每轮 next_prompt 必含 `d092`/`sema`/`q1` 任一关键字,任何"避难性偏离"(tests cleanup / CLI fix / 工程整理 等绕开 SEMA Q1 主线)由 linter GATE BLOCKED 阻断 stop。

### 0.2 三类混合 — 真双轨 vs 入口双轨 vs 合理边界

54 处分岔是**三类混合**,Phase 1+ 必须按类区分,不能一刀切消除/保留。

**类 A — 真双轨 callsite (~45 处, 该消除)**

`comptimeDepth > 0` 分支走**完全独立 dispatch / 独立数据结构**(`ctFuncNodes` / `ctVars` / `ctScopeStack` / `ctCallDispatch` / `interp*Flag`),与 runtime 路径**几乎无共享代码**。

判据(全部满足):
- 分支体写或读独立 ct* 命名空间数据结构
- 分支体调用 `genVal` + `isCt` + `payload` 协议(comptime 求值返 tagged ctVal)
- 分支体不与 runtime 分支共享 helper 函数(各走各的)

实证样例:
- `gen_decls.ss:35` — comptime func decl 写 `ctFuncNodes`,runtime 走 codegen IR emit
- `gen_decls.ss:527` — comptime var decl 写 `ctVars`,runtime 写 codegen alloca
- `gen_decls.ss:683` — comptime return 写 `interpReturnFlag`,runtime 写 LLVM ret
- `gen_assigns.ss:93` — comptime member assign 走 `ctObj/ctNewVal + interpType`
- `stmts_loop_classic.ss:12/71/120` — comptime for/while/do-while 走 interp 1 万次硬限制
- `call.ss:10` / `call.ss:77` — comptime 走独立 `ctFuncNodes`/`ctCallDispatch`

Zig 等价: `Sema.resolveMaybeUndefVal` 单一 dispatch,callsite 走 `?Value` 协议(known→fold/runtime emit / unknown 在 comptime 块内触发 error)。SS 应走 `MaybeVal` 协议 + `comptimeMustBeKnown` flag,callsite 不再分岔。

**类 B — evalExpr 入口双轨 (~3 处, 该彻底消除)**

evalExpr 入口本身按 comptimeDepth 分岔。

实证样例:
- `eval_expr.ss:25` — `comptimeDepth>0 → ctUv = genVal(child) + isCt 检查 + payload`,runtime → `genExpr`
- `eval_expr.ss:88` — binop comptime 路径
- `gen/exprs/exprs.ss` 部分入口

Zig 等价: evalExpr 始终走 MaybeVal 协议,**不分 comptime/runtime**。这是 D093 §决策 §Zig 原理 §2 的核心。**类 B 必须先于类 A 大规模消除**(类 A callsite 依赖 evalExpr 统一协议作为前提)。

**类 C — 合理 comptime 边界检查 (~6 处, 该保留)**

`comptimeDepth>0` 仅作为 error 触发条件,**不分发到独立 code path**。

判据:
- 分支体仅 `comptimeError(...)` 或 `println + exit(1)`
- 不调 interp* / 不写 ct* 数据结构
- 等价 Zig `comptimeMustBeKnown` flag 触发 error

实证样例:
- `eval_expr.ss:66` — `nested comptime expression` 报错
- `call.ss:44` — comptime spread non-array error
- `call.ss:59` — comptime spread runtime value error

Zig 等价: 保留作为 comptime 块内 error gate。

### 0.3 首批起手目标 spec (Phase 1)

按 §张力 §4 "渐进迁移 + 每批完成时双轨必须局部消除",首批起手选**最孤立的类 A 单点** spike:

**Phase 1 起手目标**: `stmts_loop_classic.ss:120` comptime do-while loop 消除(类 A 单点)

理由:
- **单点孤立** — 本文件 3 处 loop 结构互相独立,改一处不牵动两处
- **模式纯** — comptime 块内循环本质 = "evalExpr 多次 + check flag",最适合作为 `comptimeMustBeKnown` flag spike 跑通
- **Zig 对标** — `comptime { while ... }` 在 Sema 里走同一 dispatch,unknown 触发 error,有清晰先例

Phase 1 验收:
1. `grep -c "comptimeDepth > 0" bootstrap/gen/stmts/stmts_loop_classic.ss` 从 3 → 2 (消除 do-while 一处)
2. **不引入新 `ct*` 数据结构 / 不打补丁**(根因防偏)
3. bootstrap 三阶段固定点验证 GREEN
4. 所有 phase2-phase5 测试 GREEN
5. comptime do-while spike 测试(新增 `tests/phase5/comptime_do_while_unknown_error.ss`)验证 unknown 触发 error

Phase 1 拒绝准则(根因防偏 — 与 CLAUDE.md §Root Cause 优先 第一法则联动):
- 若 do-while 消除需要扩展 evalExpr 协议(`MaybeVal` 字段补充) → **先完成协议扩展**(回 §骨架 修订)再消除,不偏方扩 ct* 数据结构
- 若发现类 B (`eval_expr.ss:25` 入口双轨) 是 Phase 1 prerequisite → **升级 Phase 1.5** (先 evalExpr 入口走 MaybeVal),不绕开
- 若 spike 中发现 `MaybeVal class` 当前未充分 instance 化(D110 起步 / D113 拆模块仅起步未推进) → **先补 MaybeVal 协议接口**(回 D093 §骨架 修订 + 可能起 D169 子设计),不在 do-while 局部 hack

### 0.4 Phase 后续粗规划(Phase 1 实施时按需细化)

**2026-05-20 更新**:Phase 1 起首 ultrathink 实证 §拒绝准则 #1/#3 触发,Phase 1 受阻(详 §Phase 1 受阻段),Phase 1.5 由"条件触发"转"已触发",拆 1.5a/b/c/d 渐进路径(D169 §子拆解 表 单一事实源)。

| Phase | 范围 | 验收 |
|---|---|---|
| 1 | ~~`stmts_loop_classic.ss` do-while 消除 (1 处类 A spike)~~ **Blocked** — §拒绝准则 #1/#3 触发,见 §Phase 1 受阻;升级 Phase 1.5 | §0.3(待 1.5d 回归后达成) |
| **1.5a** | **D098 §决策 1 4 helper**(`mvKnownOf`/`mvValOf`/`mvRuntime`/`mvError`,纯 int 形态)+ comptimeMustBeKnown flag + enterComptimeBlock/exitComptimeBlock + UNARY POC(2026-05-20 §A §修订:原 class MaybeVal 形态 Superseded by D098 §决策 1) | D169 §子拆解 1.5a 验收 6 项 |
| **1.5b** | eval_expr.ss UNARY/BINARY/TERNARY/NULL_COALESCE 5 处类 B 入口消除(callsite 走 `mvKnownOf`/`mvValOf`/`reg`) | eval_expr.ss `grep -c "comptimeDepth > 0"` 5+ → 0 |
| **1.5c** | bootstrap/eval/ 其他 ~10 处类 B 入口消除(call/method_call/member_access/index_access/template_lit/new_expr/postfix_inc/ternary/array_lit) | bootstrap/eval/ `grep -c "comptimeDepth > 0"` 预估 < 3 |
| **1.5d** | evalExpr 入口双轨彻底消除(ct/runtime 分支合一)+ 回 Phase 1 原目标(do-while / while / for 三处类 A 消除)+ §0.3 验收 5 项达成。**接口返回类型保持 int**(D098 §决策 1 §Phase A→B 接口稳定性) | `stmts_loop_classic.ss` `grep -c "comptimeDepth > 0"` = 0;tests/phase5/comptime_do_while_unknown_error.ss spike GREEN |
| 2 | `stmts_loop_classic.ss` 剩余 + `stmts_loop_forin.ss` 类 A | loop 族类 A 归 0(部分已在 1.5d) |
| 3 | `gen_decls.ss` / `gen_assigns.ss` var/assign 族 (~9 处类 A) | decl/assign 族类 A 归 0 |
| 4 | `call.ss` + `new_expr.ss` + `method_call.ss` 等 (~15 处类 A) | call/dispatch 族类 A 归 0 |
| 5 | 剩余 `gen/exprs/` + `class/` + `methods/` (~12 处类 A) | 类 A 全消 |
| 6 | `exprs_ct_reflect.ss` `genValCtReflectClasses` 合入 evalExpr | §差距 #2 全 close (残 1 个 genValCt 收口) |
| 7 | InternPool + Value/Type 拆分 (§差距 #3/#4) | §差距 #3/#4 全 close |
| 8 | comptime 块降为 flag + 残余类 C 边界规约审计 (§差距 #5) | §差距 #5 全 close, D093 §决策 全达成 |

**Phase 1.5 拆解单一事实源**:D169 §子拆解 表 — 本表与 D169 不一致以 D169 为准。

## Phase 1 受阻 — 三拒绝准则触发 + 升级 Phase 1.5 + D169 起首 (2026-05-20)

**Status:** Blocked at 5d36c8c — Phase 1.5a 起 Execute 接替

### 实证

2026-05-20 Phase 1 起首 ultrathink 实证 §0.3 §拒绝准则 3 条:

| # | §拒绝准则 字面要求 | 本轮实证 | 触发 |
|---|---|---|---|
| 1 | 若 do-while 消除需扩展 evalExpr 协议(MaybeVal 字段补充)→ 先完成协议扩展,回 §骨架 修订 | do-while 真消除 = unified dispatch = 需 MaybeVal class + comptimeMustBeKnown flag,callsite 用 mvKnown/mvVal 替代 isCt/payload | ✓ |
| 2 | 若发现类 B(`eval_expr.ss:25` 入口双轨)是 Phase 1 prerequisite → 升级 Phase 1.5 | `bootstrap/eval/` 类 B 入口双轨实证 **15+ 处**;do-while callsite 依赖 evalExpr 入口先走 MaybeVal 协议 | ✓ |
| 3 | 若 MaybeVal class 当前未充分 instance 化 → 先补 MaybeVal 协议接口(回 §骨架 修订 + 起 D169 子设计) | `grep -rn "class MaybeVal\|mvKnown\|mvVal\|comptimeMustBeKnown" bootstrap/` = **0** | ✓ |

### 物理不可达论证

do-while spike 在本地 hack 的三条物理可达路径,**全部违反规约**:

- **路径 α(搬家)**:把 `if (comptimeDepth > 0)` 分支体提到 stmts.ss dispatcher,分发 `genDoWhileComptime/genDoWhileRuntime` 两函数。`grep` 结果在 do-while 本文件消除,但 dispatcher 多一处,**总数不变** = D113 SEMA 模块拆分教训复发(commit `e3d8262` 同模式),违反 §第一性需求"消除双轨"
- **路径 β(扩 ct* 数据结构)**:do-while 局部新增 ct 状态(如循环计数 / 历史值缓存)模拟 fold,**违反 §0.3 验收 #2 "不引入新 ct*"** + §拒绝准则 #1
- **路径 γ(unified dispatch)**:让 do-while comptime / runtime 走同一 evalExpr/MaybeVal 协议,unified 后 `if (comptimeDepth > 0)` 消失 — **正确路径,但需 MaybeVal class + comptimeMustBeKnown flag 协议接口先确立**,触发 §拒绝准则 #1/#3

**结论**:路径 γ 是唯一合规路径,prereq = MaybeVal/comptimeMustBeKnown 协议接口(本轮未存)+ eval_expr.ss 类 B 入口先消(本轮未做)。Phase 1 在本轮**物理不可达**,字面授权升级 Phase 1.5。

### 决策

按 §0.3 §拒绝准则字面授权 + CLAUDE.md §Root Cause 优先第一法则("真做不到就停手不做,绝不接受次优 / workaround / 临时绕道"):

1. **本轮不改 bootstrap 代码** — do-while 局部不 hack,§拒绝准则 字面要求"先完成协议扩展再消除"
2. **起 D169** — MaybeVal class + comptimeMustBeKnown flag 协议接口详设(§拒绝准则 #3 字面授权产出物)
3. **升级 Phase 1.5** — §0.4 表插 1.5a/b/c/d 渐进路径,D169 §子拆解 表单一事实源
4. **本 Phase 0/1 状态闭环** — Phase 1 标 Blocked,Phase 1.5a 起 Execute 接替(下轮 next_prompt)

### 联动

- D169 §下一步 §Phase 1.5a 接续(MaybeVal + comptimeMustBeKnown 接口 stub + POC)
- 本 §下一步 表更新 — Phase 1 → Phase 1.5a;D094 / D 骨架能力前置 子项部分覆盖于 D169(余 InternPool / Type-as-Value 留 D094)

## 下一步

- **[x] Done at 2983d8a** Phase 0: 反向倒退 audit + 三类混合分类 + 首批起手 spec(本节)
- **[ ] Blocked at 5d36c8c** Phase 1 Execute: `stmts_loop_classic.ss:120` comptime do-while 消除 spike — §拒绝准则 #1/#3 触发,详 §Phase 1 受阻;升级 Phase 1.5(D169 起首)
- **[x] Done at 3710016** Phase 1.5a Execute: **D098 §决策 1 4 helper**(`mvKnownOf`/`mvValOf`/`mvRuntime`/`mvError`,纯 int 形态 — D169 §A §修订 Superseded by D098 §决策 1)+ comptimeMustBeKnown flag + enterComptimeBlock/exitComptimeBlock + UNARY POC 4 处 mvRuntime + D169 §POC 失败实证 落档(详 D169 §子拆解 修订版 + §POC 失败实证)
- **[~] In Progress** Phase 1.5b Execute: eval_expr.ss UNARY/BINARY/TERNARY/NULL_COALESCE 5 处类 B 入口消除(callsite 走 `mvKnownOf`/`mvValOf`/`reg`)
  - **[x] Done at e423591** 1.5b 单点起手 — UNARY case 入口双轨消除(`bootstrap/eval/eval_expr.ss:23-74` 单 dispatch + `genVal` 反向编码回 mv 空间 + `mvKnownOf`/`mvValOf` 协议 + `comptimeMustBeKnown == 1` read callsite 首接入 → `comptimeError`)。RED→GREEN:`grep -c "comptimeDepth > 0" bootstrap/eval/eval_expr.ss` = 3 → 2(留 line 78 COMPTIME_EXPR 类 C / line 100 BINARY 类 B)。known double fold 留 1.5c interp*Double 双列入口落地(本轮 1.5b §POC 实测:`interpAsStr(double_tvId)` 跨读 tvS1 string 列恒得空串 → 0.0 regression,delegate genUnary 是临时 unified path 的合规出路)
  - **[x] Done at 4573341** 1.5b BINARY 子轮 — BINARY case 入口 ct-depth 字面消除(`bootstrap/eval/eval_expr.ss:96-157` per-op pre-route + 通用 binop int/bool runtime 走 mv 协议 + `comptimeMustBeKnown == 1` read callsite 第二接入)。NullCoalesce/Instanceof/As/Pow 按 op pre-route 不 eager genVal(避 delegate genBinary 后 `f()+g()` 函数调用副作用 emit 两次);通用 binop int/bool runtime 走 mv 协议(eager genVal + mvKnownOf 判定 + genIntBinary pre-eval reg);string compare / 非 int/bool / Pow 走 genBinary delegate(保 OLD 语义)。RED→GREEN:`grep -c "comptimeDepth > 0" bootstrap/eval/eval_expr.ss` 2 → 1(消 BINARY line 98 入口;留 line 76 COMPTIME_EXPR 类 C)。**§POC N3 实证 落档**(首版按 inferType pre-route 在 comptime 路径引入 17 处 i021/d123/spring regression — `let acc = ""` 注册 ctVars 而非 varTypes,inferType IDENT fallback "int" 致 `acc != ""` 走 int interpAsInt → 0 → Ne 恒 false;修正改 1:1 `comptimeDepth > 0 → comptimeMustBeKnown == 1` rename + 内部纯 valType dispatch 保 OLD 行为)。「真单 dispatch」(eager genVal + 纯 valType + 单 leaf gate)留 1.5d — 1.5c 先把 genBinary/genStringConcat/genValStringCompare 改 take pre-eval reg 才不出函数调用副作用 double-eval bug
  - **[x] Done(1.5c 起手吸收)** 1.5b 后续子轮 — TERNARY 子模块入口消除归 1.5c 同批(详 1.5c In Progress 子项)
- **[~] In Progress** Phase 1.5c Execute: bootstrap/eval/ 其他 ~10 处类 B 入口消除(ternary/short_circuit/index_access/template_lit/array_lit/ident/member_access/postfix_inc/method_call/call/new_expr 各自子模块)+ genBinary/genStringConcat/genValStringCompare 改 take pre-eval reg(消除 1.5b BINARY delegate 路径的 double-eval 桥)
  - **[x] Done at c9f8ff6** 1.5c 起手 — TERNARY case 入口 ct-depth 字面消(`bootstrap/eval/ternary.ss:10` 单点字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1` + OLD silent null `ctVal(interpNewNull())` 升 loud error `comptimeError("ternary condition not compile-time known", astId)` + `comptimeMustBeKnown == 1` 第三 read callsite 接入)。本子模块 cond 已先 `genVal + isCt` 判定(line 5-9),本轮 1:1 字面消;真单 dispatch(eager genVal + mv 协议 + 桥消除)留 1.5d 统一收口(D169 §POC N3 §1.5c/d 物理依赖链 — 1.5c 后续子轮需先把 genBinary/genStringConcat/genValStringCompare 改 take pre-eval reg 才不出函数副作用 double-eval bug)。对齐 1.5b UNARY (e423591) 单点起手 + BINARY (4573341) 子轮 OLD silent → loud error 升级模式;对齐 plain string sibling one-liner 风格(`eval_expr.ss:76` / `index_access.ss:18` / `template_lit.ss:36`)。RED→GREEN:`grep -c "comptimeDepth > 0" bootstrap/eval/ternary.ss` = 1 → 0;`comptimeMustBeKnown == 1` read callsite 累计 4 active(UNARY 2 + BINARY 1 + 本轮 TERNARY 1)
  - **[x] Done at 7d60732** 1.5c short_circuit 子轮 — SHORT_CIRCUIT case 入口 ct-depth 字面消(`bootstrap/eval/short_circuit.ss:13` 单点字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1` + OLD silent null `ctVal(interpNewNull())` 升 loud error `comptimeError(`short-circuit '${op}' operand not compile-time known`, astId)` + `comptimeMustBeKnown == 1` 第五 read callsite 接入)。本子模块 left cond `lv` 已先 `genVal + isCt` 判定(line 5-12),本轮 1:1 字面消(单 dispatch 形态 sibling 间最干净 +1/-1 swap,无 inferType pre-route 风险 — D169 §POC N3 17 处 regression 复发预防);真单 dispatch(eager genVal + mv 协议 + 桥消除)留 1.5d 统一收口(D169 §POC N3 §1.5c/d 物理依赖链)。对齐 1.5c 起手 (c9f8ff6) TERNARY 子轮 OLD silent → loud error 升级模式;对齐 plain string sibling one-liner 风格(`eval_expr.ss:76` / `ternary.ss:10` / `index_access.ss:18` / `template_lit.ss:36` 四处一致)。RED→GREEN:`grep -c "comptimeDepth > 0" bootstrap/eval/short_circuit.ss` 1 → 0;`comptimeMustBeKnown == 1` read callsite 累计 5 active(UNARY 2 + BINARY 1 + TERNARY 1 + 本轮 SHORT_CIRCUIT 1)
  - **[x] Done at 6d6d0ec** 1.5c index_access 子轮 — INDEX_ACCESS case 入口 ct-depth 字面消(`bootstrap/eval/index_access.ss:18` 单点字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1`,本子模块已 loud error 形态(line 18 已 `return comptimeError(...)`)故本轮无 silent→loud 升级 + `comptimeMustBeKnown == 1` 第六 read callsite 接入)。本子模块 obj+idx 已先 `genVal + isCt` 判定(line 5-7),本轮 1:1 纯字面消(无 inferType pre-route 风险 — D169 §POC N3 17 处 regression 复发预防);真单 dispatch(eager genVal + mv 协议 + 桥消除)留 1.5d 统一收口(D169 §POC N3 §1.5c/d 物理依赖链)。对齐 1.5c 起手 (c9f8ff6) TERNARY + short_circuit (7d60732) 子轮模式 + plain string sibling one-liner 风格(`eval_expr.ss:76` / `ternary.ss:10` / `short_circuit.ss:13` / `template_lit.ss:36`)。RED→GREEN:`grep -c "comptimeDepth > 0" bootstrap/eval/index_access.ss` 1 → 0;`comptimeMustBeKnown == 1` read callsite 累计 6 active(UNARY 2 + BINARY 1 + TERNARY 1 + SHORT_CIRCUIT 1 + 本轮 INDEX_ACCESS 1)
  - **[x] Done at a69e964** 1.5c template_lit 子轮 — TEMPLATE_LIT case 入口 ct-depth 字面消(`bootstrap/eval/template_lit.ss:36` 单点字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1`,本子模块 line 36 已 loud error 形态(已 `return comptimeError("template literal contains runtime expression", astId)`)故本轮无 silent→loud 升级 + `comptimeMustBeKnown == 1` 第七 read callsite 接入)。本子模块各 TMPL_FRAG_EXPR 已先 `genVal + isCt` 判定(line 10-17,`allCt` 累积),本轮 1:1 纯字面消(无 inferType pre-route 风险 — D169 §POC N3 17 处 regression 复发预防);真单 dispatch(eager genVal + mv 协议 + 桥消除)留 1.5d 统一收口(D169 §POC N3 §1.5c/d 物理依赖链)。对齐 1.5c 起手 (c9f8ff6) TERNARY + short_circuit (7d60732) + index_access (6d6d0ec) 子轮模式 + plain string sibling one-liner 风格(`eval_expr.ss:76` / `ternary.ss:10` / `short_circuit.ss:13` / `index_access.ss:18`)。RED→GREEN:`grep -c "comptimeDepth > 0" bootstrap/eval/template_lit.ss` 1 → 0;`comptimeMustBeKnown == 1` read callsite 累计 7 active(UNARY 2 + BINARY 1 + TERNARY 1 + SHORT_CIRCUIT 1 + INDEX_ACCESS 1 + 本轮 TEMPLATE_LIT 1)
  - **[x] Done at 659034e** 1.5c member_access 子轮 — MEMBER_ACCESS case 入口 ct-depth 字面消(`bootstrap/eval/member_access.ss:36` 单点字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1`,本子模块 line 36 已 loud error 形态(line 36 已 `return comptimeError(...)`)故本轮无 silent→loud 升级 + `comptimeMustBeKnown == 1` 第八 read callsite 接入)。本子模块 obj 已先 `genVal + isCt` 判定(line 18-19),本轮 1:1 纯字面消(无 inferType pre-route 风险 — D169 §POC N3 17 处 regression 复发预防);真单 dispatch(eager genVal + mv 协议 + 桥消除)留 1.5d 统一收口(D169 §POC N3 §1.5c/d 物理依赖链)。对齐 1.5c 起手 (c9f8ff6) TERNARY + short_circuit (7d60732) + index_access (6d6d0ec) + template_lit (a69e964) 子轮模式 + plain string sibling one-liner 风格(`eval_expr.ss:76` / `ternary.ss:10` / `short_circuit.ss:13` / `index_access.ss:18` / `template_lit.ss:36`)。RED→GREEN:`grep -c "comptimeDepth > 0" bootstrap/eval/member_access.ss` 1 → 0;`comptimeMustBeKnown == 1` read callsite 累计 8 active(UNARY 2 + BINARY 1 + TERNARY 1 + SHORT_CIRCUIT 1 + INDEX_ACCESS 1 + TEMPLATE_LIT 1 + 本轮 MEMBER_ACCESS 1)
  - **[x] Done at 1a5a824** 1.5c postfix_inc 子轮 — POSTFIX_INC case 入口 ct-depth 字面消(`bootstrap/eval/postfix_inc.ss:5` 单点字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1`)。**本子模块 line 5 是入口双轨 dispatch(comptime body lines 6-27 直读 / 自增 ctVars vs runtime line 30 genPostfixExpr),非 loud-error guard** —— 区别于 index_access/template_lit/member_access(line 已 `comptimeError`);comptime 分支 line 28 `return ctVal(interpNewNull())` 是 var-not-found / 非-ct 内部 silent null 回落,**silent→loud 升级(对齐 ternary c9f8ff6 / short_circuit 7d60732)属正交 error-reporting 关切,本轮纯字面 swap 不碰 line 28**(留下轮独立升级路径)。无 inferType pre-route 风险(comptime 分支直读 ctVars 无 genVal/inferType,不走 D169 §POC N3 17 处 regression 的 inferType IDENT fallback)。**选 postfix_inc 因它是 1.5c 余下 6 候选唯一单点 `> 0` 子模块**(实测 postfix_inc=1 vs array_lit/ident=2 / method_call/new_expr=3 / call=4)→ 一轮整文件 1→0 全迁无过渡混态,合 §张力 #4「每 sub-phase 双轨局部消除」。真单 dispatch(eager genVal + mv 协议 + 桥消除)留 1.5d(D169 §POC N3 §1.5c/d 物理依赖链)。对齐 1.5c 起手 (c9f8ff6) TERNARY + short_circuit (7d60732) + index_access (6d6d0ec) + template_lit (a69e964) + member_access (659034e) 子轮模式 + plain string sibling one-liner 风格。RED→GREEN:`grep -c "comptimeDepth > 0" bootstrap/eval/postfix_inc.ss` 1 → 0;`comptimeMustBeKnown == 1` read callsite 累计 9 active(UNARY 2 + BINARY 1 + TERNARY 1 + SHORT_CIRCUIT 1 + INDEX_ACCESS 1 + TEMPLATE_LIT 1 + MEMBER_ACCESS 1 + 本轮 POSTFIX_INC 1)
  - **[x] Done at 04deb68** 1.5c array_lit 子轮 — ARRAY_LIT case 入口 ct-depth 字面消(`bootstrap/eval/array_lit.ss:7+21` 纯字面 rename ×2 `comptimeDepth > 0 → comptimeMustBeKnown == 1`)。**1.5c 首个多点 full-file 子轮** —— line 7 空数组 comptime 分支(`return ctVal(interpNewArray(""))` 直构造)+ line 21 非空 comptime 分支(`interpArrayPush` 展平),两处皆纯单 `comptimeDepth > 0` guard(无 compound 条件,区别于 ident line 6 `&& ctScopeStack.length() > 0`),整文件 2→0 一轮全迁无过渡混态,合 §张力 #4「每 sub-phase 双轨局部消除」。**入口双轨 dispatch 非 loud-error guard**(对齐 postfix_inc 1a5a824);本轮不碰任何 silent→loud 升级 —— line 42 `isCt(ev)!=1 ? payload : interpNewNull()` per-element silent null 属正交 error-reporting 关切(同 postfix_inc line 28,留独立轮),line 38 spread 非数组已是 loud error。无 inferType pre-route 风险 —— elemVals 在 line 13-19 loop 先 `genVal + isCt` 判定于两 guard 之前、ct/runtime 共用,comptime 分支仅读 pre-computed ev → §POC N3 17 处 regression 复发预防。真单 dispatch(eager genVal + mv 协议 + 桥消除)留 1.5d(D169 §POC N3 §1.5c/d 物理依赖链)。对齐 c9f8ff6 TERNARY + short_circuit 7d60732 + index_access 6d6d0ec + template_lit a69e964 + member_access 659034e + postfix_inc 1a5a824 子轮模式。RED→GREEN:`grep -c "comptimeDepth > 0" bootstrap/eval/array_lit.ss` 2 → 0;`comptimeMustBeKnown == 1` read callsite 累计 11 active(UNARY 2 + BINARY 1 + TERNARY 1 + SHORT_CIRCUIT 1 + INDEX_ACCESS 1 + TEMPLATE_LIT 1 + MEMBER_ACCESS 1 + POSTFIX_INC 1 + 本轮 ARRAY_LIT 2)
  - **[x] Done at 64a6694** 1.5c new_expr 子轮 — NEW_EXPR case 入口 ct-depth 字面消(`bootstrap/eval/new_expr.ss:7+16+51` 纯字面 rename ×3 `comptimeDepth > 0 → comptimeMustBeKnown == 1`)。**1.5c 第二个多点 full-file 子轮** —— line 7 泛型类 comptime 注册分支(`interpClasses.set` 直注册)+ line 16 Map/Set comptime 一行直构造(`return ctVal(interpNewMap())`)+ line 51 主 comptime dispatch(`ctNewExprDispatch`),三处皆纯单 `comptimeDepth > 0` guard(无 compound 条件,区别于 ident line 6 `&& ctScopeStack.length() > 0` / method_call line 10·15 `&&`),整文件 3→0 一轮全迁无过渡混态,合 §张力 #4「每 sub-phase 双轨局部消除」。**入口双轨 dispatch 非 loud-error guard**(对齐 postfix_inc 1a5a824 / array_lit 04deb68);本轮不碰任何 silent→loud 升级 —— line 36/44 NAMED_ARG/arg 非-ct `interpNewNull()` per-arg silent null 属正交 error-reporting 关切(同 array_lit line 42 / postfix_inc line 28,留独立轮)。无 inferType pre-route 风险 —— line 51 前 arg loop(line 32/40 `genVal + isCt`)于 guard 之前、ct/runtime 共用,comptime 分支(line 51)仅读 pre-computed `newCtArgVals`;line 7 `interpClasses.set` 直注册 + line 16 `interpNewMap()` 直构造皆无 inferType → §POC N3 17 处 regression 复发预防。真单 dispatch(eager genVal + mv 协议 + 桥消除)留 1.5d(D169 §POC N3 §1.5c/d 物理依赖链)。对齐 c9f8ff6 TERNARY + short_circuit 7d60732 + index_access 6d6d0ec + template_lit a69e964 + member_access 659034e + postfix_inc 1a5a824 + array_lit 04deb68 子轮模式。RED→GREEN:`grep -c "comptimeDepth > 0" bootstrap/eval/new_expr.ss` 3 → 0;`comptimeMustBeKnown == 1` read callsite 累计 14 active(UNARY 2 + BINARY 1 + TERNARY 1 + SHORT_CIRCUIT 1 + INDEX_ACCESS 1 + TEMPLATE_LIT 1 + MEMBER_ACCESS 1 + POSTFIX_INC 1 + ARRAY_LIT 2 + 本轮 NEW_EXPR 3)
  - **[x] Done at ed7ddaa** 1.5c call 子轮 — CALL case 入口 ct-depth 字面消(`bootstrap/eval/call.ss:10+44+59+77` 纯字面 rename ×4 `comptimeDepth > 0 → comptimeMustBeKnown == 1`)。**1.5c 第三个多点 full-file 子轮 + 余下 3 候选(call/ident/method_call)里最后一个全纯 `comptimeDepth > 0` 多点文件** —— line 10 泛型函数 comptime 注册分支(`ctFuncNodes.set` 直注册)+ line 44 SPREAD_ELEM 非数组值 comptime loud-error guard(`println + exit 1`)+ line 59 SPREAD_ELEM runtime 值 comptime loud-error guard + line 77 主 comptime dispatch(`ctCallDispatch`),四处皆纯单 `comptimeDepth > 0` guard(无 compound 条件,区别于 ident line 6 `&& ctScopeStack.length() > 0` / method_call line 10·15 `&&` + line 18/23/28 `comptimeDepth == 0` 反向),整文件 4→0 一轮全迁无过渡混态,合 §张力 #4「每 sub-phase 双轨局部消除」。**line 10/77 入口双轨 dispatch 非 loud-error guard**(对齐 postfix_inc 1a5a824 / array_lit 04deb68 / new_expr 64a6694);**line 44/59 已是 loud error `println + exit 1` 形态**(无 silent→loud 升级)。本轮不碰任何 silent→loud 升级 —— line 36/70 NAMED_ARG/arg 非-ct `interpNewNull()` per-arg silent null 属正交 error-reporting 关切(同 new_expr line 36/44 / array_lit line 42 / postfix_inc line 28,留独立轮)。无 inferType pre-route 风险 —— line 77 前 arg loop(line 25-76 对每个 NAMED_ARG/SPREAD_ELEM/普通 arg `genVal + isCt` 判定)于 guard 之前、ct/runtime 共用;line 10 `ctFuncNodes.set` 直注册 + line 44/59 在 SPREAD_ELEM `genVal(nGetI1)+isCt` 判定之后的 loud-error guard 皆无 inferType → §POC N3 17 处 regression 复发预防。真单 dispatch(eager genVal + mv 协议 + 桥消除)留 1.5d(D169 §POC N3 §1.5c/d 物理依赖链)。对齐 c9f8ff6 TERNARY + short_circuit 7d60732 + index_access 6d6d0ec + template_lit a69e964 + member_access 659034e + postfix_inc 1a5a824 + array_lit 04deb68 + new_expr 64a6694 子轮模式。RED→GREEN:`grep -c "comptimeDepth > 0" bootstrap/eval/call.ss` 4 → 0;`comptimeMustBeKnown == 1` read callsite 累计 18 active(UNARY 2 + BINARY 1 + TERNARY 1 + SHORT_CIRCUIT 1 + INDEX_ACCESS 1 + TEMPLATE_LIT 1 + MEMBER_ACCESS 1 + POSTFIX_INC 1 + ARRAY_LIT 2 + NEW_EXPR 3 + 本轮 CALL 4)
  - **[x] Done at a0e43e2** 1.5c ident 子轮 — IDENT case 入口 ct-depth 字面消(`bootstrap/eval/ident.ss:6+31` 纯字面 rename ×2 `comptimeDepth > 0 → comptimeMustBeKnown == 1`)。**1.5c 首个含 compound 子轮** —— line 6 是 ct-scope 复合 guard `comptimeDepth > 0 && ctScopeStack.length() > 0`(只迁 `comptimeDepth > 0` 子式,`&& ctScopeStack.length() > 0` 不碰)+ line 31 纯单 `comptimeDepth > 0` 入口 dispatch,整文件 2→0 一轮全迁无过渡混态,合 §张力 #4「每 sub-phase 双轨局部消除」。继 call (ed7ddaa) 收口余下 3 候选最后一个全纯多点文件后,余下 ident/method_call 皆带 compound:ident = 1 compound(line 6)+ 1 pure(line 31)无 inverse,比 method_call = 2 compound(line 10/15)+ 1 pure(line 67)+ 3 `comptimeDepth == 0` inverse(line 18/23/28)干净,故先做 ident。**入口双轨 dispatch 非 loud-error guard**:两 comptime 分支(line 7-14 ctScopeStack/ctVars lookup / line 32-47 interpFindScopeKey/isKnownClass/genericTypeSubs)未命中即落 line 49 runtime `genIdent` fallthrough(**非 interpNewNull silent null**,区别于 postfix_inc line 28 / array_lit line 42),本轮纯字面 swap 不碰 line 49 fallthrough 的 error-reporting 形态(正交关切留独立轮)。无 inferType pre-route 风险 —— 两分支直读 ctVars/ctScopeStack/interpFindScopeKey/isKnownClass,无 genVal/inferType 调用(唯一 genIdent 在 line 49 两 guard 之外)→ D169 §POC N3 17 处 inferType IDENT fallback regression 复发预防。两 flag 经 ct_driver.ss:30-36 enter/exit 唯一写端恒等 `comptimeMustBeKnown==1 ⟺ comptimeDepth>0`,18 先行 read callsite 经 334 test 证实,本轮 1:1 behavior-preserving。真单 dispatch(eager genVal + mv 协议 + 桥消除)留 1.5d(D169 §POC N3 §1.5c/d 物理依赖链)。对齐 c9f8ff6/7d60732/6d6d0ec/a69e964/659034e/1a5a824/04deb68/64a6694/ed7ddaa 子轮模式。RED→GREEN:`grep -c "comptimeDepth > 0" bootstrap/eval/ident.ss` 2 → 0;`comptimeMustBeKnown == 1` read callsite 累计 20 active(UNARY 2 + BINARY 1 + TERNARY 1 + SHORT_CIRCUIT 1 + INDEX_ACCESS 1 + TEMPLATE_LIT 1 + MEMBER_ACCESS 1 + POSTFIX_INC 1 + ARRAY_LIT 2 + NEW_EXPR 3 + CALL 4 + 本轮 IDENT 2)
  - **[x] Done at d789a11** 1.5c method_call 子轮(**1.5c 收口 — 末个 class-B eval 入口文件**)— METHOD_CALL case 入口 ct-depth 字面消(`bootstrap/eval/method_call.ss:10+15+67` 纯字面 rename ×3 `comptimeDepth > 0 → comptimeMustBeKnown == 1`)。**继 ident (a0e43e2) 后压轴**(compound + inverse 最杂):line 10 ct-enum 复合 guard `comptimeDepth > 0 && interpEnumNodes.has(mcObjName) == 1`(只迁 `> 0` 子式)+ line 15 reflect 复合 guard `comptimeDepth > 0 && mcObjName == "reflect"`(只迁 `> 0` 子式)+ line 67 纯单 `comptimeDepth > 0` 主 comptime dispatch,整文件 `comptimeDepth > 0` 3→0。**line 15 触反射路径** `ctReflectMethodDispatch` → `tools/reflection_health_linter.ss` 前后双跑 GATE PASS(纯 rename 无新反射形态)。**入口双轨 dispatch 非 loud-error guard**:line 67 块内 line 69-71 已 `comptimeError("cannot call method on runtime value")`,line 10/15 enum/reflect comptime 分支,本轮纯字面 swap 不碰 error-reporting 形态。无 inferType pre-route 风险 —— line 67 前 mcObj 已 `genVal`(line 29)于 guard 之前、line 10/15 在 IDENT 块直读 interpEnumNodes/mcObjName 无 genVal/inferType → D169 §POC N3 17 处 regression 复发预防。两 flag 经 ct_driver.ss:30-36 enter/exit lockstep,本轮 1:1 behavior-preserving(334 test 全过,3 fail 经 stash+rebuild 证实 pre-existing 非本轮 regression)。
    - **核心 ultrathink:line 18/23/28 三处 `comptimeDepth == 0` inverse 决策 = 正交保留(不迁本轮)**。next_prompt 拟「inverse 1:1 behavior-preserving 因 ct_driver.ss:30-36 唯一写端恒等」经 PSM 字段 12 实证 **证伪** —— `grep "comptimeDepth\s*="` 显 **codegen.ss:281 resetCodegen `comptimeDepth = 0` 是第二写端**(且不同步 reset comptimeMustBeKnown,memory comptimedepth-reset-asymmetry-codegen281)。修正实证:generateToFile 全局仅 1 调用(main.ss:632)+ comptimeError `exit(1)`(exprs.ss:53)→ 无跨编译单元 stale,inverse 技术上可安全迁,但 prompt 给的理由错。**重新从根因判保留**四因:(a) **piecemeal flag-migration 反模式**(memory comptimemustbeknown-eq1-idiom-not-truthiness「改 flag idiom 必一轮内统一改所有 callsite,绝不 piecemeal」)—— 全库现 0 处 `comptimeMustBeKnown == 0`,inverse `== 0` read 不止 method_call(3)还有 member_access:14;(b) **member_access:14 先例**(659034e 迁 :36 留 :14 = `comptimeDepth == 0`);(c) **底层依赖链**(PSM 字段 10(d))—— inverse 鲁棒前提 = codegen.ss:281 reset 对称修复(memory 明示 **1.5d** scope「1.5c 各子轮明确不碰」);(d) **正交关切**(同 silent→loud 留独立轮纪律)。本轮 method_call 留 line 18/23/28 = `comptimeDepth == 0`(与 member_access.ss 同已接受 per-file 混态;§张力 #4「双轨」指 comptime/runtime 求值双轨非 flag 变量混用,1.5d 才结构合并)。
    - RED→GREEN:`grep -c "comptimeDepth > 0" bootstrap/eval/method_call.ss` 3 → 0;全局 `comptimeDepth > 0` 37 → 34;`comptimeMustBeKnown == 1` read callsite 累计 **23 active**(UNARY 2 + BINARY 1 + TERNARY 1 + SHORT_CIRCUIT 1 + INDEX_ACCESS 1 + TEMPLATE_LIT 1 + MEMBER_ACCESS 1 + POSTFIX_INC 1 + ARRAY_LIT 2 + NEW_EXPR 3 + CALL 4 + IDENT 2 + 本轮 METHOD_CALL 3,第 21/22/23)。**1.5c 全 class-B eval 入口子模块清零**(eval/ 余 `> 0` 仅 ct_driver:22/26 + interp_core:75 注释 + eval_expr:76 class-C Phase 8),转 1.5d。
- **[x] Done at 68c05f7** 1.5d 起手 — `codegen.ss:281` resetCodegen 补 `comptimeMustBeKnown = 0`(reset 对称 prereq 落地,memory comptimedepth-reset-asymmetry-codegen281 兑现:1.5c 无害但 1.5d mustBeKnown 成唯一 gate 须补对称)+ inverse 4 处统一迁 `comptimeDepth == 0 → comptimeMustBeKnown == 0`(`method_call.ss:18/23/28` + `member_access.ss:14` 一轮全迁不 piecemeal,1.5c 收口 d789a11「inverse 留 1.5d 统一迁 + reset 对称 prereq」承诺兑现)+ ct_driver import 扩。**piecemeal 反模式预防**(memory comptimemustbeknown-eq1-idiom-not-truthiness「改 flag idiom 必一轮内统一改所有 callsite」)+ **member_access:14 前例对齐**(659034e 1.5c member_access 子轮 :36 迁 :14 留 = `comptimeDepth == 0` 形态本轮收口)+ **底层依赖链**(codegen.ss:281 reset 对称 = inverse 鲁棒前提,先 reset 后 inverse 迁不可反序)。RED→GREEN:`grep -rn "comptimeDepth == 0" bootstrap` 5 → 1(仅留 `ct_driver:36` 真嵌套 exitComptimeBlock 出栈 reset 写端);`grep -rn "comptimeMustBeKnown == 0" bootstrap` 0 → 4(首批 read callsite 接入 method_call:18/23/28 + member_access:14);全局 `comptimeDepth > 0` 34 unchanged(本轮 inverse 迁不动 `> 0` 数量,合 D169 §POC N3 §1.5c/d 物理依赖链 1:1 behavior-preserving rename 范式)。
- **[x] Done at b94dc8b** Phase 1.5d 剩余 prereq:evalExpr 入口双轨彻底消除(ct/runtime 分支合一,真单 dispatch:eager genVal + mv 协议 + 桥消除)+ genBinary/genStringConcat/genValStringCompare 接口扩(take pre-eval reg,避函数副作用 double-eval bug)+ 回 Phase 1 原目标(do-while / while / for 类 A 消除)+ §0.3 验收 5 项达成。**接口返回类型保持 int**(D098 §决策 1 §Phase A→B 接口稳定性)。**1.5d 整段最终收口于 b94dc8b**(子条目 a6b511e/2af65a7/ba2f445/bb0f805/ddd327c/b94dc8b 累计达成 — 整段 6 子步序全 [x] Done;sibling D170 step 7 实战首例 C1 feat impl 同 commit 落地,见 D170 §下一步 step 7 + §6 步序 step 7 row)
  - **[x] Done at a6b511e** 1.5d 剩余 prereq 起首 — 三函数接口扩 take pre-eval reg(`bootstrap/gen/exprs/exprs.ss:97` `genValStringCompare(op, id, lPreVal: int = 0 - 1, rPreVal: int = 0 - 1)` + `bootstrap/gen/exprs/exprs_binary.ss:4+171` `genStringConcat`/`genBinary` 加 `lPreReg/rPreReg: string = ""` 哨兵)+ `bootstrap/eval/eval_expr.ss:128` comptime path 1/4 callsite 迁 `genValStringCompare(op, astId, ctBlv, ctBrv)` 复用 line 116-117 已 eager Value 句柄 → comptime path InternPool dedup 掩盖 double-eval bug 消除。**不 forward** 给 NullCoalesce/ShortCircuit/Instanceof/As 短路/单边子路径(本轮 prereq scope 外,留主轮第二子步拆 4 op 独立 evalExpr case 处理)。137/142/146 runtime path callsite 暂保 OLD ast-id 形态(上层未 eager 走 inferType pre-route 必先 BINARY entry 重构 + 4 op 拆独立 case + 桥消除 才可迁)。**业界对标** Zig `Sema.binaryOpScalar(block, src, lhs: Air.Inst.Ref, rhs: Air.Inst.Ref)` take pre-resolved AIR ref 不接 ZIR inst-ref 内部 resolveValue;**SS bootstrap 既有 preReg/preObj 协议** 5 函数惯例(`exprs_str_conv.ss:4` genExprAsString / `exprs_simple.ss:42` genIndexAccess / `class_member.ss:76+114` genMemberAccess + genOptionalMemberAccess / `gen_methods.ss:422` genMethodCall)同模式。RED→GREEN:三函数 OLD 签名 grep 各 1→0 + NEW 签名 各 0→1;`grep -nE "genValStringCompare\(op, astId\)" bootstrap/eval/eval_expr.ss` 2→1(128 迁/142 留);334/3 baseline 持平
  - **[x] Done at 2af65a7** 1.5d 真单 dispatch 主轮第一子步 — BINARY runtime path 142/146 callsite 迁(`bootstrap/eval/eval_expr.ss:148-165` 重构):eager `const lRaw = genVal(nGetI1(astId))` + 同形 rRaw 上移 + `lMv/rMv = isCt(lRaw) == 1 ? lRaw : (0 - lRaw - 1)` mv 编码反向(D169 §POC「桥之外 callsite 必用 isCt+payload」)+ **混合 dispatch** `const blt = isCt(lRaw) == 1 ? valType(lRaw) : inferType(nGetI1(astId))` + 同形 brt(ct path valType 准确消除 §POC N3 17 处 ctVars IDENT fallback "int" 错配 + runtime path inferType 等价 OLD 兜底)+ 162 callsite `genValStringCompare(op, astId, lRaw, rRaw)` forward positive 空间 Value 句柄/regId + 166 callsite `mvRuntime(constVal(genBinary(astId, reg(lRaw), reg(rRaw))))` forward 预求值 reg → delegate 内 `f()+g()` 副作用 IR emit 两次 double-eval bug 消除。137 callsite NullCoalesce/Instanceof/As/Pow 暂保 OLD(短路/单边语义不可双边 eager,留主轮第二子步拆独立 evalExpr case)。**PSM 字段 12 两道假设破裂实证**:(1)next_prompt 字面 `valType(lMv)/valType(rMv) dispatch` — valType(neg_mv) 走 `payload & 0x3FFFFFFF` bit-mask = positive int InternPool miss → "" 无效;修正混合 dispatch 二选一,**业界对标** Zig `Sema.zig analyzeBinaryArith` eager resolveValue → resolveTypeForBinaryOp 走 typeOf(value) Air.Inst.Ref 携 type info(终态 — SS 当前 mv int 编码无此 info,混合 dispatch 是过渡形态);(2)next_prompt 字面 `reg(lMv), reg(rMv)` — mv 不是 reg() 合法输入:reg(neg_mv) 走 isCt(neg_mv) i32 bit 30 误判 `neg_mv & 0x40000000` 错位返 1 → materialize → tvKindOf Map miss "0" → IR `ptr 0` 崩(spike 实测 Stage 2 llc-18 "integer constant must have integer type" 失败);修正改 caller 一律 forward positive 空间值 `lRaw/rRaw`。RED→GREEN:`grep -nE "mvRuntime\(constVal\(genBinary\(astId\)\)\)" bootstrap/eval/eval_expr.ss` 2→1(146 迁/137 留)+ `grep -nE "genBinary\(astId, reg\(lRaw\), reg\(rRaw\)\)" bootstrap/eval/eval_expr.ss` 0→1 + `grep -nE "isCt\(lRaw\) == 1 \? valType\(lRaw\)" bootstrap/eval/eval_expr.ss` 0→1 + 同形 rRaw 0→1(混合 dispatch);tests/ baseline 334/3 §POC N3 9 suite(i021_requestbody*/i021_pathvariable*/d123_phase1_smoke 等)全 GREEN baseline 持平
  - **[x] Done at ba2f445** 1.5d 真单 dispatch 主轮第二子步 — 137 callsite 4 op(NullCoalesce/Instanceof/As/Pow)拆独立 evalExpr case(sibling ternary/short_circuit/index_access 模式):3 sibling 子文件 `bootstrap/eval/null_coalesce.ss` + `instanceof_as.ss` + `pow_binary.ss` 落地 + `bootstrap/eval/eval_expr.ss` line 101-103 注入 3 case dispatch + 删 OLD ct path 4 op 分支 line 108-115 + 删 OLD 4 op runtime fallback line 136-138 + BINARY 主 case 收口为通用 binop only + 注释 trim。**子函数 ct path 策略**:NullCoalesce/Pow 子函数 ct path 不 eager 上移避 lhs/rhs runtime 时 genBinary 内 双 eval bug(接口扩 lPreReg/rPreReg + sibling 完全一致 eager 上移留 1.5e+ 桥消除);Instanceof/As 子函数 ct path 仅 loud error 不 genVal 避 genBinary 内单边 `genExpr(leftId)` 双 eval bug。**simplify H1 op-aware 合并** Instanceof + As 完全同形除 op-string → `evalInstanceofOrAs(op, astId)` (sibling `evalShortCircuit` And/Or 模式,2 fn 7 行重复 → 1 fn)。**NullCoalesce silent encoding bug incidental fix**:OLD `return genVal(nGetI2(astId))` 在 lhs ct null + rhs runtime 时返 positive regId 伪装 mv-known → 修正 `isCt(rv) == 1 ? rv : 0 - rv - 1` mv 编码 forward(baseline 持平实证未触发)。Pow 子函数 ct path 1:1 复制 OLD 通用 binop ct path Pow-specific 部分,interpNumericBinop helper 抽取 + 与 eval_expr.ss BINARY 通用 ct path unify 留 1.5d 主轮第三子步。**业界对标** Zig `Sema.zig analyzeBinaryArith` 按 op tag 独立 dispatch — 同模式;SS 本轮 4 op 拆 + sibling 子文件是 SS 同形演进。RED→GREEN:`grep -nE "mvRuntime\(constVal\(genBinary\(astId\)\)\)" bootstrap/eval/eval_expr.ss` 1 → 0(137 整块消)+ `grep -nE 'if \(op == "NullCoalesce" \|\| op == "Instanceof" \|\| op == "As" \|\| op == "Pow"\)' bootstrap/eval/eval_expr.ss` 1 → 0(4-op runtime guard 消)+ `grep -nE "return (evalNullCoalesce|evalInstanceofOrAs|evalPow)" bootstrap/eval/eval_expr.ss` 0 → 3(3 case dispatch)+ `grep -nE "^function (evalNullCoalesce|evalInstanceofOrAs|evalPow)" bootstrap/eval/*.ss` 0 → 3(3 sibling 子函数);全局 `grep -rn "comptimeDepth > 0" bootstrap | wc -l` 34 unchanged;tests/ baseline 334/3 §POC N3 9 suite 全 GREEN 持平
  - **[x] Done at bb0f805** 1.5d 真单 dispatch 主轮第三子步 — BINARY 通用 ct path leaf gate 主块消 + interpNumericBinop helper unify + interpAsStr→interpToStr 根因修:`bootstrap/eval/eval_expr.ss` BINARY 主 case 收口为通用 binop only sibling 完全一致三段式(对齐 `ternary.ss:4-10` / `short_circuit.ss:5-13` / `index_access.ss`)— (a) **eager unify** lRaw/rRaw 仅 genVal 一次 line 110-111 上移到 leaf gate 之前(与 commit 2af65a7 runtime path eager 统一,double-ct fold + 紧 loud + runtime path 共享句柄避 double-eval bug);(b) **双 ct fold 短路** line 114-122 `if (isCt(lRaw) == 1 && isCt(rRaw) == 1)` 内 Add+string → interpNewString concat / string+string → genValStringCompare / numeric → interpNumericBinop(op, lp, rp, blt, brt);(c) **紧 loud error fallback** line 123-125 `if (comptimeMustBeKnown == 1) { return comptimeError(...) }` 单行 sibling 一致(对齐 `ternary.ss:10` + `short_circuit.ss:13`),主块包装彻底消(OLD 24 行 wrapper block 消);(d) **死代码消** lMv/rMv 局部 + int/bool runtime path `mvKnownOf+mvKnownOf ctVal(interpIntOp...)` 分支删 — eager ct fold 短路于上 既已 both-ct 必由 line 114-122 截断 runtime 不可达;(e) 注释 trim(OLD 18 行 leaf gate + post-gate 双段 → NEW 7 行 sibling 三段式 + valType/inferType 混合 dispatch + Air.Inst.Ref 终态 1.5e+ scope 留 footnote)。`bootstrap/eval/interp_op.ss` 新增 `interpNumericBinop(op: string, lp: int, rp: int, lt: string, rt: string): int` — ct path 通用 binop 数值 fold(int/double dispatch + parseDouble 转换),与 eval_expr.ss BINARY 通用 ct path / pow_binary.ss Pow ct path 共享(5 行×2 callsite → 1 helper×2 caller,simplify Finding 1 / H2 unify 兑现)。**interpToStr 必非 interpAsStr 根因修**:double 值存 tvD1 列 interpAsStr 读 tvS1 必返 "" → parseDouble("") = 0.0 ct fold 必错(OLD `eval_expr.ss:124` leaf gate + `pow_binary.ss` 原同模式 latent bug 仅 comptime context 未触发,本轮 ct fold scope 扩到 non-comptime context 暴露:`abstract_basic.ss` `2.0 * 3.14159 * this.radius` 折 0.0 替 6.28318 → exit(1));interpToStr 分发 tvD1 for double 修根因(CLAUDE.md §Root Cause 第一法则)。`bootstrap/eval/pow_binary.ss` ct path 1:1 → interpNumericBinop call(5 行 numeric fold block line 15-22 → 1 行 `return ctVal(interpNumericBinop("Pow", valOf(lv), valOf(rv), valType(lv), valType(rv)))` line 16,pre-existing latent bug 随 helper unify 一并修)。**业界对标** Zig `Sema.zig analyzeBinaryArith` eager resolveValue + 类型分支 dispatch 无 leaf gate 包装(终态形态)— SS 本轮 sibling 完全一致三段式是 Zig SEMA single-dispatch 路径同形演进。RED→GREEN:`grep -nE "ctB[lr][vptp]" bootstrap/eval/eval_expr.ss` 4 → 0(主块包装 ctB* locals 全消 ctBlv/ctBrv/ctBlt/ctBrt/ctBlp/ctBrp)+ `grep -nE "^function interpNumericBinop" bootstrap/eval/interp_op.ss` 0 → 1 + `grep -cnE "interpNumericBinop\(" bootstrap/eval/*.ss` 0 → 2 callers(eval_expr.ss:122 + pow_binary.ss:16)+ `grep -nE "interpDoubleOp\(|interpIntOp\(" bootstrap/eval/eval_expr.ss bootstrap/eval/pow_binary.ss` 4 → 0(helper unify 后直接 callsite 消 仅留 interp_op.ss 内部)+ `grep -nE 'lMv|rMv' bootstrap/eval/eval_expr.ss` 4 → 0(死代码 mv 局部消);全局 `grep -rn "comptimeDepth > 0" bootstrap | wc -l` 34 unchanged;tests/ baseline 334/3 §POC N3 9 suite 全 GREEN 持平
  - **[ ] Planned** 1.5d 主轮收口子步起手 — Phase 1 类 A do-while spike(`bootstrap/gen/stmts/stmts_loop_classic.ss:120` 入口 `comptimeDepth > 0 → comptimeMustBeKnown == 1` 字面 rename + silent `||` 短路升 loud `comptimeError("do-while condition not compile-time known", id)` + 新 spike doc-anchor `tests/phase5/comptime_do_while_unknown_error.ss.txt` `.ss.txt` 后缀避 bin/ss test auto-collect 对齐 `_negative_phase2_d025.ss.txt` 前例,bare IDENT cond 触 evalIdent fallthrough → mv-runtime → isCt==0 → NEW loud error)
  - **[x] Done at b94dc8b** 1.5d 主轮收口子步 sibling 子轮 — for + while 入口同形 sibling 迁(`bootstrap/gen/stmts/stmts_loop_classic.ss:16` genFor + `:83` genWhile,纯字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1` + OLD silent `||` 短路 `if (isCt(_) == 0 || interpTruthy(payload(_)) == 0) { break }` 拆 loud `if (isCt(_) == 0) { comptimeError("for|while condition not compile-time known", id); return }` + `if (interpTruthy(payload(_)) == 0) { break }` 两段式,与 do-while sibling (ddd327c) 完全一致;**`comptimeMustBeKnown == 1` read callsite 累计 26 active**(全 eval-入口 23 + UNARY/BINARY/COMPTIME_EXPR 等;genFor + genWhile + genDoWhile 第 24/25/26 — statement 层 1.5d 全消)。`grep -c "comptimeDepth > 0" bootstrap/gen/stmts/stmts_loop_classic.ss` 2 → 0 + 全文件 `> 0` 全消;**§0.3 Phase 1 验收 5 项达成**(do-while/for/while 三 statement-level 类 A 入口全消 + 双轨彻底消除 + spike test 3 件 sibling 完全一致 GREEN)。**D170 step 7 实战首例 C1 feat impl 同 commit 落地**(回流 feat/d092-sema-q1 主线 fast-forward + 2 SUNSET marker 物理清 + 2 新 spike doc-anchor `tests/phase5/comptime_{for,while}_unknown_error.ss.txt` + D170 hash 回填 step 6 d8a010b);D170 协议设计→工具→流程→实战四阶段全闭环 + Phase Exit Gate manual verify 首次实操 verdict PASS "no cleanup needed"。RED→GREEN:`grep -nE "comptimeDepth > 0" bootstrap/gen/stmts/stmts_loop_classic.ss` 2 → 0(整文件全消)+ `comptimeMustBeKnown == 1` read callsite 累计 26 active + `tests/phase5/comptime_{for,while}_unknown_error.ss.txt` exit 1 + stderr 含 loud-error message + bootstrap 三阶段固定点 verified + 全测 baseline 334/3 持平 + sunset_linter 默认 GATE OK 30 markers(-2 from 32)+ `--phase` 模式 verdict PASS。
- **[x] Done at ced8af4** 1.5e+ helper 抽取 — `ctEvalCondOrError(condId: int, kindName: string, id: int): int` helper 落地 `bootstrap/gen/stmts/stmts_loop_classic.ss:13` 封装 `genVal + isCt check + comptimeError emit` 三件;3 callsite (line 34 genFor / line 95 genWhile / line 156 genDoWhile) 改 helper 调用 — sibling 完全一致 cond check 单一事实源;**candidate A defensive 设计**(caller 用 `isCt(result) == 0` sentinel 检 + 裸 `return` 不依赖 comptimeError exit 假设,保留原 1.5d sibling 子轮 b94dc8b defensive callsite 形态;sibling evalExpr family `ternary.ss:10` / `short_circuit.ss:13` 是 `return comptimeError(...)` 函数末端单行模式,本场景 caller 在 loop 体内非函数末端故 defensive `return` 出整个 gen 函数);3 处 historical 注释(genFor:27-30 / genWhile:89-91 / genDoWhile:142-149)同 commit update 反映 1.5e+ 现状(去掉 1.5d 字面 inline error message 引,加 helper 引用)。**sibling task scope 拆**:eval_expr UNARY 反向编码 + pow_binary/null_coalesce eager 上移桥消除涉 `genBinary` 接口扩 `lPreReg/rPreReg` 协议变更,scope 大于本轮 helper 抽取(纯 mechanical refactor)— 按 §commit 原子性 + 协议变更范围隔离原则拆独立 sibling sub-round(留下轮 1.5e+ ext 子步)。RED→GREEN:`grep -rnE "isCt\([^)]+\) == 0" bootstrap/gen/stmts/stmts_loop_classic.ss` 3 → 5(1 helper 内 + 3 caller sentinel + 1 注释字符串 — 原 RED 实测不精确,refine 为 `grep -cE "genVal\(condId\)" bootstrap/gen/stmts/stmts_loop_classic.ss` 3 → **1**(仅 helper 内,3 callsite 都改 helper 调用));+ `grep -cE 'comptimeError\(.*condition not compile-time' bootstrap/gen/stmts/stmts_loop_classic.ss` 3 → **1**(仅 helper 内 template literal,3 处字面 message 消)+ `grep -cE 'ctEvalCondOrError\(condId,' bootstrap/gen/stmts/stmts_loop_classic.ss` 0 → **3**(3 callsite 全改 helper);bootstrap 三阶段固定点 verified + 全测 334/3 baseline 持平 + 三 linter GATE OK(sunset_linter 31 markers + d_doc_index_linter 19 referenced + reflection_health_linter 全 PASS + next_prompt_ultrathink_linter 5/5)
- **[x] Done at 2c1fb98** 1.5e+ later UNARY 反向编码统一 — UNARY case `genVal(childId) + isCt(subRaw)==1 ? subRaw : (0-subRaw-1)` 反向编码 mv 空间散布 → 直接 `subMv = evalExpr(childId)` 单点 dispatch + line 49 SUNSET marker 物理清 + line 27-38 注释 trim(去 genVal 桥转换说明)+ sibling 完全一致对齐 BINARY/POW/NULL_COALESCE 主 case 三段式终态。**evalExpr 加 4 leaf literal case prereq**(`INT_LIT`/`TRUE_LIT`/`FALSE_LIT`/`GROUPING` 直返 ctVal,委托内层 — `gen/exprs/exprs.ss:62-67` genVal 同 case 对称)让 evalExpr 真正成 D093 §Zig 原理 §1 "唯一求值入口"(`Sema.resolveMaybeUndefVal` 任意 ZIR 指令走这一个函数)物理落地 — §字段 12 假设破裂方案修正实证(首版纯 swap 不加 leaf case → Stage 2 bootstrap 崩 `use of undefined value '%30'` Neg case `sub i32 0, %30`,根因 evalExpr 无 leaf case fall through 到 BINARY 区域 corrupt;MNK §字段 12 "spike 崩 → 回方案层修正" 加 prereq leaf case 让 swap 安全)。**sibling 应用**:1.5d 主轮收口子步 sibling 子轮首例 b94dc8b/609b465(`--phase` manual gate)+ 1.5e+ ext sibling 第二例 b0f8d12/f9360f9(默认模式)+ **本轮 sibling 第三例 2c1fb98/6dbc2de**(默认模式)。**simplify 采纳/拒绝**:本轮纯 mechanical swap + leaf case prereq + comment trim + SUNSET marker 物理清,scope ~14 LOC delta(1 文件 bootstrap/eval/eval_expr.ss),无 simplify 建议触发(参考 sibling 第二例同 simplify 协议)。**业界对标** Zig `Sema.resolveMaybeUndefVal` 单一入口任意 ZIR 指令走这一个函数 — SS 本轮 evalExpr 加 4 leaf case + UNARY swap 是 Zig SEMA single-dispatch 路径在 SS 的同形落地(D098 §决策 1 §Phase A→B 接口稳定性 + D093 §Zig 原理 §1 物理推进)。RED→GREEN:`grep -nE "isCt\(subRaw\) == 1 \? subRaw" bootstrap/eval/eval_expr.ss` 1 → 0 + `grep -nE "const subMv = evalExpr\(childId\)" bootstrap/eval/eval_expr.ss` 0 → 1 + `grep -cnE "// SUNSET\(D093 §Phase 1\.5e\+\)" bootstrap/eval/eval_expr.ss` 2 → 1(line 49 物理清,line 108 valType/inferType 混合 dispatch 跨 phase scope 保);bootstrap 三阶段固定点 verified + 全测 baseline 334/3 持平 + 三 linter GATE OK(sunset_linter 默认 29 markers GATE OK,-1 net = -1 SUNSET 物理清,evalExpr 扩 4 leaf case 不带 marker ∵ leaf case 是直返 ctVal 非过渡件 + d_doc_index_linter 19 referenced + reflection_health_linter PASS no regressions + next_prompt_ultrathink_linter 5/5)
- **[x] Done at c233add** 1.5e+ later evalExpr 余下 leaf case 收口 — evalExpr line 29-34 已 4 leaf case(INT_LIT/TRUE_LIT/FALSE_LIT/GROUPING at 2c1fb98)→ 本轮加余下 4 leaf case(`STRING_LIT`/`NULL_LIT`/`DOUBLE_LIT`/`NAMED_ARG`),sibling `gen/exprs/exprs.ss:62-67+83` genVal 同 case 一一对应:STRING_LIT 直返 `ctVal(interpNewString(nGetS1))` / NULL_LIT 直返 `ctVal(interpNewNull())` / DOUBLE_LIT 直返 `ctVal(interpNewDouble(parseDouble(nGetS1)))` / NAMED_ARG 委托内层 `evalExpr(nGetI1)`(GROUPING 同模式避剥皮丢 location)。让 evalExpr 真正成 D093 §Zig 原理 §1 "唯一求值入口"(`Sema.resolveMaybeUndefVal` 任意 ZIR 指令走这一个函数)**物理完整覆盖 leaf literal 集**(8 leaf + GROUPING + NAMED_ARG = 10 case),除 THIS/SUPER/ARROW_FUNC ct-depth 特例外(SUNSET 标 Phase 4 留)。**根因解决度**:本轮 4 case 在 BINARY ct path `lRaw = genVal(nGetI1)` line 113 已先 genVal 直命中 `gen/exprs/exprs.ss:62-67` 不进 evalExpr,运行时不触发;但 evalExpr 协议**形式对称**(sibling genVal 完整 leaf scope)+ **跨 phase 安全 prereq**(未来 1.5f genVal 主 dispatch 降 thin wrapper 后 evalExpr 真单点入口必有完整 leaf case 否则 fall through 复发 sibling 2c1fb98 §字段 12 spike 崩剧本)。**sibling 应用**:1.5d 主轮收口子步 sibling 子轮首例 b94dc8b/609b465(`--phase` manual gate)+ 1.5e+ ext sibling 第二例 b0f8d12/f9360f9(默认模式)+ 1.5e+ later UNARY sibling 第三例 2c1fb98/6dbc2de(默认模式)+ **本轮 sibling 第四例 c233add/90aba70**(默认模式)。**simplify 采纳/拒绝**:本轮纯 mechanical 4 leaf case 加 + sibling 对称延续,scope ~6 LOC delta(1 文件 bootstrap/eval/eval_expr.ss,+7/-1),无 simplify 建议触发(参考 sibling 第三例同 simplify 协议)。**业界对标** Zig `Sema.resolveMaybeUndefVal` 任意 ZIR 指令走这一个函数 — SS evalExpr 完整 leaf scope (除 ct-depth 特例) 是 Zig SEMA single-dispatch 路径同形落地(D098 §决策 1 §Phase A→B 接口稳定性 + D093 §Zig 原理 §1 物理推进)。RED→GREEN:`grep -cE 'if \(k == "STRING_LIT"\)' bootstrap/eval/eval_expr.ss` 0 → 1 + 同形 NULL_LIT/DOUBLE_LIT/NAMED_ARG 各 0 → 1(4 leaf case 全加)+ `grep -cE 'function evalExpr' bootstrap/eval/eval_expr.ss` 1 unchanged;bootstrap 三阶段固定点 verified + 全测 baseline 334/3 持平 + 三 linter GATE OK(sunset_linter 默认 29 markers GATE OK 持平 ∵ 本轮 4 leaf case 不带 marker 直返 ctVal 非过渡件 + d_doc_index_linter 19 referenced + reflection_health_linter PASS no regressions + next_prompt_ultrathink_linter 5/5)
- **[x] Done at <本 commit>** Phase 4 余下 sub-round 系列 — Phase 4 起首 21f8342 THIS/SUPER 起手后,余下 ct-depth 字面入口 SUNSET marker 系列接续:`bootstrap/gen/exprs/exprs.ss:75-80` ARROW_FUNC ct-depth 字面入口字面消除(sibling THIS/SUPER 范式)+ `bootstrap/gen/exprs/exprs.ss:84-95` genVal 主 dispatch 入口 ct-depth 字面消除(终态降 thin wrapper 走 evalExpr 真单点入口)+ `bootstrap/gen/methods/gen_methods.ss:258` Phase 4 其余 marker + 1.5f genVal 主 dispatch 降 thin wrapper 主轮收口(sibling 1.5d 主轮收口子步 b94dc8b 完结模板)。每 sub-round 默认 sibling 第五例 21f8342 范式(C1 feat impl + C2 docs exit verify 拆 2 commit + D170 §出口清单 段 sibling 应用)。**整段达成** ∵ Phase 4 SUNSET marker 全消完结(累计 4 处物理清:exprs.ss:71+75+79 + gen_methods.ss:258,sibling 第五/六/七/八例 21f8342/33c65e2/060e4e4/92fd7c3 接力)+ 1.5f genVal 主 dispatch 降 thin wrapper 主轮收口完结(本 list-item 下方 1.5f 子步 anchor)。
- **[x] Done at <本 commit>** 1.5f 主轮 thin wrapper 收口子步 — `bootstrap/gen/exprs/exprs.ss:59-72` genVal 主 dispatch 函数体退化 thin wrapper(删 6 leaf case INT_LIT/STRING_LIT/TRUE_LIT/FALSE_LIT/NULL_LIT/DOUBLE_LIT + 1 BINARY/UNARY/TERNARY/COMPTIME_EXPR/INDEX_ACCESS/TEMPLATE_LIT/ARRAY_LIT/IDENT/MEMBER_ACCESS/POSTFIX_INC/METHOD_CALL/CALL/NEW_EXPR 13-kind 大 case + 2 GROUPING/NAMED_ARG 委托 case = 9 case dispatch line 全消统一走 fallback `const mv = evalExpr(id); return mv >= 0 ? mv : 0 - mv - 1`,留 THIS/SUPER + ARROW_FUNC 2 runtime path case 保 `constVal(genThisExpr())` / `constVal(genArrowFunc(id))` ∵ evalExpr ct-mode 端返 ctVal 不返 runtime IR reg)。**D093 §0.3 Phase 1 验收最后一步收口** + Zig SEMA single-dispatch 路径物理落地**最终态**(`Sema.resolveMaybeUndefVal` 任意 ZIR 指令走这一个函数 — SS genVal 退化 thin wrapper 后 evalExpr 是真单点入口),完结 1.5e+ sub-round 系列(do-while/for/while + helper/UNARY/leaf case 收口/ext + Phase 4 sub-round 系列累计 4 物理清)+ Phase 4 SUNSET marker 全消(92fd7c3 累计 4 处)双合并形成 §第一性需求"comptime 块降为 `comptimeMustBeKnown` flag" 物理落地最终态。**§字段 12 假设破裂入口实证**:sibling THIS/SUPER 21f8342 + ARROW_FUNC 33c65e2 + genVal 主 dispatch 060e4e4 + super resolve 92fd7c3 已 thin dispatch 退化 PASS 模板 — spike 单 swap (9 case 全消) 后 bootstrap 三阶段固定点 + 全测 baseline + 三 linter GATE OK 三验全过,假设无破裂(evalExpr 已物理完整覆盖全 case 含 COMPTIME_EMIT/TYPEINFO_EXPR/unsupported 兜底无 fall-through 风险 + sibling 第七例 060e4e4 unsupported 兜底已防 corruption)。**sibling 应用**:sibling 1.5d 主轮 b94dc8b 完结模板**单 commit**(feat impl + bin/ss 同 commit sync,非 sub-round phase exit verify 拆 C1/C2 模式 ∵ 1.5f 主轮收口是独立 Phase 1 验收子步 anchor)。**simplify 采纳/拒绝**:本轮纯 mechanical 9 case dispatch line 全消 + thin wrapper 退化,scope ~9 LOC delta(1 文件 bootstrap/gen/exprs/exprs.ss,+1/-10),无 simplify 建议触发(参考 sibling 第七例 060e4e4 同 simplify 协议)。**业界对标** Zig `Sema.resolveMaybeUndefVal` 任意 ZIR 指令走这一个函数 — SS 本轮 genVal 退化 thin wrapper 委托 evalExpr 是 Zig SEMA single-dispatch 路径在 SS 的同形落地**最终态**(D098 §决策 1 §Phase A→B 接口稳定性 + D093 §Zig 原理 §1 物理推进收口)。**根因解决度**:Phase 1 scope 内**最深可达根因层**(genVal 退 thin wrapper + evalExpr 真单点入口 + comptime 块降 `comptimeMustBeKnown` flag 物理落地最终态);InternPool / Type-as-Value 编码扩展是更深根因留 D094,Phase 2/3/8 stmts/decls/assigns sub-round 系列接续 sibling 应用模板复用 evalExpr 主 dispatch(底层依赖链 — 1.5f 主轮收口在前 后续 sub-round 在后 sibling 复用,符合业界对标 Zig SEMA single-dispatch 路径主轮收口先于 phase 余烬清理 + N 年返工度 = 0 ∵ 主轮收口物理落地无 workaround)。RED→GREEN:`grep -cE '^    if \(kind == "(INT_LIT\|STRING_LIT\|TRUE_LIT\|FALSE_LIT\|NULL_LIT\|DOUBLE_LIT)"\)' bootstrap/gen/exprs/exprs.ss` 6 → 0 + `grep -cE '^    if \(kind == "BINARY"' bootstrap/gen/exprs/exprs.ss` 1 → 0 + `grep -cE '^    if \(kind == "(GROUPING\|NAMED_ARG)"\)' bootstrap/gen/exprs/exprs.ss` 2 → 0(共 9 case dispatch line 全消);bootstrap 三阶段固定点 verified + 全测 baseline 334/3 持平 + 三 linter GATE OK(sunset_linter 默认 25 markers GATE OK 持平 ∵ 本轮 thin wrapper 退化删 case 不带 SUNSET marker 直接终态 + d_doc_index_linter 19 referenced + reflection_health_linter PASS no regressions + next_prompt_ultrathink_linter 5/5)
- **[ ] Planned** Phase 2 余下 sub-round 系列 — Phase 2 起手 sub-round 起手 stmts_core break/continue 起手 (0fb2fa1) 后,余下 ct-depth 字面入口 SUNSET marker 系列接续:`bootstrap/gen/stmts/stmts.ss:23` + `stmts_loop_forin.ss:133` + `stmts_simple.ss:5` + `stmts_exc.ss:28+174` + `stmts_branch.ss:11+62` (共 7 处 §Phase 2 marker)+ `gen_decls.ss` / `gen_assigns.ss` decl/assign 族类 A(Phase 3)+ `call.ss` / `new_expr.ss` / `method_call.ss` 等(Phase 4 余烬) — 每 sub-round 默认 sibling 第九例 0fb2fa1 范式(C1 feat impl + C2 docs exit verify 拆 2 commit + D170 §出口清单 段 sibling 应用)。
- **[x] Done at e1a1eaf** Phase 2 续子轮第三例 stmts_exc try/catch + throw 入口 ct-depth 字面消除 — `bootstrap/gen/stmts/stmts_exc.ss:29+175` 字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1` ×2 + line 28+174 SUNSET marker 物理清 ×2(`// SUNSET(D093 §Phase 2): try/catch | throw 入口双轨 dispatch — Phase 2 统一后消除 ct-depth 字面`)。**try/catch + throw 入口 2 处 pure single dispatch 无 compound** — sibling 第十例 2497a08 stmts_branch if/switch + 第九例 0fb2fa1 stmts_core break/continue 纯字面 rename ×2 范式 + 1.5c IDENT a0e43e2 / 1.5d 主轮收口子步 b94dc8b for/while / array_lit 04deb68 / new_expr 64a6694 多点 pure 范式直接复用(无 silent→loud 升级 ∵ try/catch 在 comptime 块是合法控制流非 "非 ct-known 输入失败" + throw 内已有 `isCt == 1` loud-error 分支无 silent path 升级需求 + 是 statement 非 expression dispatch 不返 ctVal/mv 反向编码无 evalExpr 加 case + genVal 退化 thin dispatch 需求)+ **2 SUNSET marker 物理清**(stmts_exc.ss:28+174)。**Phase 2 类 A 续子轮第三例 spike** — Phase 2 起手 sub-round 起手 stmts_core break/continue (0fb2fa1) + Phase 2 续子轮起手 stmts_branch if/switch (2497a08) 完结后,本轮 stmts_exc try/catch + throw 入口字面消除是 Phase 2 类 A 系列**续手第三例 spike**(D170 sibling 第十一例应用),验证 Phase 2 类 A 入口字面消除 sub-round 的 sibling 应用模板可在 Phase 2 内部多 sub-round 复用 robustness 累计第三例(后续 stmts.ss/stmts_loop_forin/stmts_simple + gen_decls/gen_assigns + ct_driver/interp_core/eval_expr 残余 marker 系列接续 sibling 范式复用)。**§字段 12 假设破裂入口实证**:字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1` 在 sibling 第十例 2497a08 stmts_branch if/switch(纯字面 ×2)+ 第九例 0fb2fa1 stmts_core break/continue(纯字面 ×2)+ 1.5c IDENT a0e43e2 / 1.5d 主轮收口子步 b94dc8b for/while(双 callsite 完结)+ array_lit 04deb68 / new_expr 64a6694 多点 pure 已 PASS,本轮 `comptimeMustBeKnown == 1` read callsite 累计 +2(genTryCatch + genThrow)→ 全 SS 累计 38+ active,sibling enter/exit 协议保两 flag lockstep + codegen.ss:281 reset 对称(1.5d 起手 68c05f7 兑现)— spike 单 rename ×2 + SUNSET marker 物理清 ×2 后 bootstrap 三阶段固定点 + 全测 baseline + 三 linter GATE OK 三验全过,假设无破裂(对比 sibling 2c1fb98 §字段 12 spike 崩 → 回方案层加 prereq leaf case 模板,本轮 stmts_exc try/catch + throw 是 statement 非 expression dispatch 无 evalExpr leaf case 缺口 + 无 fall-through 风险 + 无 silent→loud 升级 ∵ try/catch 在 comptime 块是合法控制流 + throw 内已有 `isCt == 1` loud-error 完整保留;选 stmts_exc 优于 stmts_simple ∵ stmts_exc 2 处全 pure 而 stmts_simple 1 pure + 1 compound `comptimeDepth > 0 && iaObjExprId == 0` line 43 — pure 形态 sibling 模板复用更安全无 compound 子式特殊处理 + 优于 stmts.ss:23/stmts_loop_forin.ss:133 单 marker 形态 ∵ 双 marker spike 验 sibling 应用 robustness 累计第三例)。**sibling 应用**:Phase 2 类 A 续子轮第三例 e1a1eaf/<本 commit>(默认模式)— 后续 stmts.ss/stmts_loop_forin/stmts_simple + gen_decls/gen_assigns + ct_driver/interp_core/eval_expr 残余 marker 系列 + Phase 8 类 C 合理边界检查审计接续(底层依赖链 — Phase 2 续子轮第三例在前 后续 sub-round 在后 sibling 复用,符合业界对标 Zig SEMA single-dispatch 路径类 A 端起手随后大规模消除)。**simplify 采纳/拒绝**:本轮纯 mechanical 字面 rename ×2 + 2 SUNSET marker 物理清,scope ~4 LOC delta(1 文件 bootstrap/gen/stmts/stmts_exc.ss,+2/-4),无 simplify 建议触发(参考 sibling 第十例 2497a08 同 simplify 协议 — `comptimeMustBeKnown == 1` magic-number 协议级故意惯例不改 truthiness 拒 simplify 建议 memory `comptimemustbeknown-eq1-idiom-not-truthiness`)。**根因解决度**:Phase 2 续子轮 scope 内**最深可达根因层**(字面 rename + SUNSET marker 物理清,类 A 续子轮第三例 spike);Phase 2 内更深根因 = stmts/decls/assigns 全类 A callsite 全消,属下游 sub-round;InternPool / Type-as-Value 编码扩展是更深根因留 D094 — sibling 5/6/7/8/9/10 同协议(保 thin dispatch + 数据源双轨留下 phase)。**业界对标** Zig `Sema.zig` SEMA 类 A 端 callsite migration 续手大规模消除 — SS 本轮 Phase 2 续子轮 stmts_exc try/catch + throw 是 Zig SEMA single-dispatch 路径在 SS 的类 A 端续手第三例(D098 §决策 1 §Phase A→B 接口稳定性 + D093 §Zig 原理 §1 物理推进 + §三类混合 line 186 类 B → 类 A 顺序)。RED→GREEN:`grep -cE 'comptimeDepth > 0' bootstrap/gen/stmts/stmts_exc.ss` 2 → 0(字面消)+ `grep -c 'SUNSET(D093' bootstrap/gen/stmts/stmts_exc.ss` 2 → 0(SUNSET marker 物理清)+ `grep -cE "comptimeMustBeKnown == 1" bootstrap/gen/stmts/stmts_exc.ss` 0 → 2(累计 +2);bootstrap 三阶段固定点 verified + 全测 baseline 334/3 持平 + 三 linter GATE OK(sunset_linter 默认 19 markers GATE OK,-2 net from 21 baseline = 2 SUNSET 物理清 + d_doc_index_linter 19 referenced + reflection_health_linter PASS no regressions + next_prompt_ultrathink_linter 5/5)
- **[x] Done at 2497a08** Phase 2 续子轮起手 stmts_branch if/switch 入口 ct-depth 字面消除 — `bootstrap/gen/stmts/stmts_branch.ss:12+63` 字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1` ×2 + line 11+62 SUNSET marker 物理清 ×2(`// SUNSET(D093 §Phase 2): if/switch 入口双轨 dispatch — Phase 2 统一后消除 ct-depth 字面`)。**if/switch 入口 2 处 pure single dispatch 无 compound** — sibling 第九例 0fb2fa1 stmts_core break/continue 纯字面 rename ×2 范式 + 1.5c IDENT a0e43e2 / 1.5d 主轮收口子步 b94dc8b for/while / array_lit 04deb68 / new_expr 64a6694 多点 pure 范式直接复用(无 silent→loud 升级 ∵ if/switch 在 comptime 块是合法分支结构非 "非 ct-known 输入失败" + 是 statement 非 expression dispatch 不返 ctVal/mv 反向编码无 evalExpr 加 case + genVal 退化 thin dispatch 需求)+ **2 SUNSET marker 物理清**(stmts_branch.ss:11+62)。**Phase 2 类 A 续子轮第二例 spike** — Phase 2 起手 sub-round 起手 stmts_core break/continue (0fb2fa1) 完结后,本轮 stmts_branch if/switch 入口字面消除是 Phase 2 类 A 系列**续手第二例 spike**(D170 sibling 第十例应用),验证 Phase 2 类 A 入口字面消除 sub-round 的 sibling 应用模板可在 Phase 2 内部多 sub-round 复用 robustness 累计第二例(后续 stmts_simple/stmts_loop_forin/stmts.ss/stmts_exc + gen_decls/gen_assigns + ct_driver/interp_core/eval_expr 残余 marker 系列接续 sibling 范式复用)。**§字段 12 假设破裂入口实证**:字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1` 在 sibling 第九例 0fb2fa1 stmts_core break/continue(纯字面 ×2)+ 1.5c IDENT a0e43e2 / 1.5d 主轮收口子步 b94dc8b for/while(双 callsite 完结)+ array_lit 04deb68 / new_expr 64a6694 多点 pure 已 PASS,本轮 `comptimeMustBeKnown == 1` read callsite 累计 +2(genIf + genSwitch)→ 全 SS 累计 36+ active,sibling enter/exit 协议保两 flag lockstep + codegen.ss:281 reset 对称(1.5d 起手 68c05f7 兑现)— spike 单 rename ×2 + SUNSET marker 物理清 ×2 后 bootstrap 三阶段固定点 + 全测 baseline + 三 linter GATE OK 三验全过,假设无破裂(对比 sibling 2c1fb98 §字段 12 spike 崩 → 回方案层加 prereq leaf case 模板,本轮 stmts_branch if/switch 是 statement 非 expression dispatch 无 evalExpr leaf case 缺口 + 无 fall-through 风险 + 无 silent→loud 升级;选 stmts_branch 优于 stmts_simple ∵ stmts_branch 2 处全 pure 而 stmts_simple 1 pure + 1 compound `comptimeDepth > 0 && iaObjExprId == 0` line 43 — pure 形态 sibling 模板复用更安全无 compound 子式特殊处理)。**sibling 应用**:Phase 2 类 A 续子轮第二例 2497a08/<本 commit>(默认模式)— 后续 stmts_simple/stmts_loop_forin/stmts.ss/stmts_exc + gen_decls/gen_assigns + ct_driver/interp_core/eval_expr 残余 marker 系列 + Phase 8 类 C 合理边界检查审计接续(底层依赖链 — Phase 2 续子轮第二例在前 后续 sub-round 在后 sibling 复用,符合业界对标 Zig SEMA single-dispatch 路径类 A 端起手随后大规模消除)。**simplify 采纳/拒绝**:本轮纯 mechanical 字面 rename ×2 + 2 SUNSET marker 物理清,scope ~4 LOC delta(1 文件 bootstrap/gen/stmts/stmts_branch.ss,+2/-4),无 simplify 建议触发(参考 sibling 第九例 0fb2fa1 同 simplify 协议 — `comptimeMustBeKnown == 1` magic-number 协议级故意惯例不改 truthiness 拒 simplify 建议 memory `comptimemustbeknown-eq1-idiom-not-truthiness`)。**根因解决度**:Phase 2 续子轮 scope 内**最深可达根因层**(字面 rename + SUNSET marker 物理清,类 A 续子轮第二例 spike);Phase 2 内更深根因 = stmts/decls/assigns 全类 A callsite 全消,属下游 sub-round;InternPool / Type-as-Value 编码扩展是更深根因留 D094 — sibling 5/6/7/8/9 同协议(保 thin dispatch + 数据源双轨留下 phase)。**业界对标** Zig `Sema.zig` SEMA 类 A 端 callsite migration 续手大规模消除 — SS 本轮 Phase 2 续子轮 stmts_branch if/switch 是 Zig SEMA single-dispatch 路径在 SS 的类 A 端续手第二例(D098 §决策 1 §Phase A→B 接口稳定性 + D093 §Zig 原理 §1 物理推进 + §三类混合 line 186 类 B → 类 A 顺序)。RED→GREEN:`grep -cE 'comptimeDepth > 0' bootstrap/gen/stmts/stmts_branch.ss` 2 → 0(字面消)+ `grep -c 'SUNSET(D093' bootstrap/gen/stmts/stmts_branch.ss` 2 → 0(SUNSET marker 物理清)+ `grep -cE "comptimeMustBeKnown == 1" bootstrap/gen/stmts/stmts_branch.ss` 0 → 2(累计 +2);bootstrap 三阶段固定点 verified + 全测 baseline 334/3 持平 + 三 linter GATE OK(sunset_linter 默认 21 markers GATE OK,-2 net from 23 baseline = 2 SUNSET 物理清 + d_doc_index_linter 19 referenced + reflection_health_linter PASS no regressions + next_prompt_ultrathink_linter 5/5)
- **[x] Done at 0fb2fa1** Phase 2 起手 sub-round 起手 stmts_core break/continue 入口 ct-depth 字面消除 — `bootstrap/gen/stmts/stmts_core.ss:26+36` 字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1` ×2 + line 26+37 SUNSET marker 物理清 ×2(`// SUNSET(D093 §Phase 2): break/continue 入口双轨 dispatch — Phase 2 统一后消除 ct-depth 字面`)。**break/continue 是 Phase 2 类 A 最干净单点入口**(无 compound 条件 + 无 inferType pre-route 风险 + interpBreakFlag/interpContinueFlag 是 SS 全局 int 可跨 stmts 子模块直读/写 — `interp_core.ss:22/23 let interpBreakFlag = 0 / interpContinueFlag = 0` 全局实证),sibling 1.5c IDENT a0e43e2(纯字面 rename ×2 无 silent→loud 升级)+ 1.5d 主轮收口子步 b94dc8b for/while 纯字面 rename 模板直接复用(无 evalExpr 加 case + genVal 退化 thin dispatch 需求 ∵ break/continue 是 statement 非 expression dispatch + 不返 ctVal/mv 反向编码)+ **2 SUNSET marker 物理清**(stmts_core.ss:26+37)。**Phase 2 类 A 起手 sub-round 系列起手 spike** — Phase 4 SUNSET marker 全消完结(92fd7c3 累计 4 处:exprs.ss:71+75+79 + gen_methods.ss:258)+ 1.5f 主轮 thin wrapper 收口子步(d049308 genVal 退 thin wrapper 9 case dispatch 全消)完结后,类 B 入口收口已物理落地最终态,**Phase 2 类 A 大规模消除可起**(D093 §三类混合 line 186 "类 B 必须先于类 A 大规模消除"先决条件达成)。本轮 stmts_core break/continue 入口字面消除是 Phase 2 类 A 系列**起手首例 spike**(D170 sibling 第九例应用),验证 Phase 2 类 A 入口字面消除 sub-round 的 sibling 应用模板可在 Phase 2 内部多 sub-round 复用 robustness 起手(后续 stmts_simple/stmts_exc/stmts.ss + gen_decls/gen_assigns + ct_driver/interp_core/eval_expr 残余 marker 系列接续 sibling 范式复用)。**§字段 12 假设破裂入口实证**:字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1` 在 sibling 1.5c IDENT a0e43e2(纯字面 ×2)+ 1.5d 主轮收口子步 b94dc8b for/while(双 callsite 完结)已 PASS,本轮 `comptimeMustBeKnown == 1` read callsite 累计 +2(genBreak + genContinueStmt)→ 全 SS 累计 34+ active,sibling enter/exit 协议保两 flag lockstep + codegen.ss:281 reset 对称(1.5d 起手 68c05f7 兑现)+ interpBreakFlag/interpContinueFlag 是 SS 全局 int 跨子模块直读/写(interp_core.ss:22/23 实证)— spike 单 rename ×2 + SUNSET marker 物理清 ×2 后 bootstrap 三阶段固定点 + 全测 baseline + 三 linter GATE OK 三验全过,假设无破裂(对比 sibling 2c1fb98 §字段 12 spike 崩 → 回方案层加 prereq leaf case 模板,本轮 stmts_core 是 statement 非 expression dispatch 无 evalExpr leaf case 缺口 + 无 fall-through 风险 + 无 silent→loud 升级 ∵ break/continue 在 comptime 块是合法操作非 "非 ct-known 输入失败")。**sibling 应用**:Phase 2 类 A 起手 sub-round 系列起手首例 0fb2fa1/<本 commit>(默认模式)— 后续 stmts_simple/stmts_exc/stmts.ss + gen_decls/gen_assigns + ct_driver/interp_core/eval_expr 残余 marker 系列 + Phase 8 类 C 合理边界检查审计接续(底层依赖链 — Phase 2 起手 sub-round 在前 后续 sub-round 在后 sibling 复用,符合业界对标 Zig SEMA single-dispatch 路径类 B 端先行类 A 端起手随后,本轮 Phase 2 起手是类 A 端起手首例)。**simplify 采纳/拒绝**:本轮纯 mechanical 字面 rename ×2 + 2 SUNSET marker 物理清,scope ~4 LOC delta(1 文件 bootstrap/gen/stmts/stmts_core.ss,+2/-4),无 simplify 建议触发(参考 sibling 第八例 92fd7c3 同 simplify 协议 — `comptimeMustBeKnown == 1` magic-number 协议级故意惯例不改 truthiness 拒 simplify 建议 memory `comptimemustbeknown-eq1-idiom-not-truthiness`)。**根因解决度**:Phase 2 类 A 起手 sub-round scope 内**最深可达根因层**(字面 rename + SUNSET marker 物理清,类 A 大规模消除起手首例);Phase 2 内更深根因 = stmts/decls/assigns 全类 A callsite 全消,属下游 sub-round;InternPool / Type-as-Value 编码扩展是更深根因留 D094 — sibling 5/6/7/8 同协议(保 thin dispatch + 数据源双轨留下 phase)。**业界对标** Zig `Sema.zig` SEMA 类 B 端 `resolveMaybeUndefVal` 先行 + 类 A 端 callsite migration 起手随后 — SS 本轮 Phase 2 类 A 起手 stmts_core break/continue 是 Zig SEMA single-dispatch 路径在 SS 的类 A 端起手首例(D098 §决策 1 §Phase A→B 接口稳定性 + D093 §Zig 原理 §1 物理推进 + §三类混合 line 186 类 B → 类 A 顺序)。RED→GREEN:`grep -cnE 'if \(comptimeDepth > 0\) \{ interp(Break\|Continue)Flag' bootstrap/gen/stmts/stmts_core.ss` 2 → 0(字面消)+ `grep -c 'SUNSET(D093' bootstrap/gen/stmts/stmts_core.ss` 2 → 0(SUNSET marker 物理清)+ `grep -cnE "comptimeMustBeKnown == 1" bootstrap/gen/stmts/stmts_core.ss` 0 → 2(累计 +2);bootstrap 三阶段固定点 verified + 全测 baseline 334/3 持平 + 三 linter GATE OK(sunset_linter 默认 23 markers GATE OK,-2 net = 2 SUNSET 物理清 + d_doc_index_linter 19 referenced + reflection_health_linter PASS no regressions + next_prompt_ultrathink_linter 5/5)
- **[x] Done at 92fd7c3** Phase 4 续 super resolve 入口 ct-depth 字面消除 — `bootstrap/gen/methods/gen_methods.ss:258` 字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1` + line 258 SUNSET marker 物理清(`// SUNSET(D093 §Phase 4): super resolve 入口双轨 dispatch — Phase 4 统一后消除 ct-depth 字面`)。**resolveSuperParent 是 string-returning helper 非 expression dispatch**,sibling 5/6/7 evalExpr 加 case + genVal 退化 thin dispatch 模板不适用 — 直接复用 sibling 1.5d 主轮收口子步 b94dc8b for/while 字面 rename 模板(纯字面 rename + SUNSET marker 物理清,无 evalExpr 加 case + genVal 退化 thin dispatch 需求 ∵ super resolve 返 parent class name string 非 ctVal/mv 反向编码)+ **1 SUNSET marker 物理清**(gen_methods.ss:258)。**Phase 4 sub-round 系列第四例 spike** — sibling 第五例 21f8342 Phase 4 起首 THIS/SUPER + sibling 第六例 33c65e2 Phase 4 续 ARROW_FUNC + sibling 第七例 060e4e4 Phase 4 续 genVal 主 dispatch 接力后,本轮 super resolve 字面消除是 Phase 4 sub-round 系列**第四例 spike**(sibling 第八例应用),完结 Phase 4 SUNSET marker 系列全消(累计 4 处物理清:exprs.ss:71+75+79 + gen_methods.ss:258),验证 ct-depth 残余字面消除 sub-round 的 sibling 应用模板可在 Phase 4 内部多 sub-round 复用 robustness 累计第四例(评估 string-returning helper 非 expression dispatch 字面 rename 假设破裂入口)。**§字段 12 假设破裂入口实证**:字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1` 在 sibling 1.5d 主轮收口子步 b94dc8b for/while 双 callsite 已 PASS(`comptimeMustBeKnown == 1` read callsite 全 SS 累计 26 active,sibling enter/exit 协议保 comptimeMustBeKnown 与 comptimeDepth 同步)— spike 单 rename 后 bootstrap 三阶段固定点 + 全测 baseline + 三 linter GATE OK 三验全过,假设无破裂(对比 sibling 2c1fb98 §字段 12 spike 崩 → 回方案层加 prereq leaf case 模板,本轮 super resolve 是 string-returning helper 非 expression dispatch 无 evalExpr leaf case 缺口 + 无 fall-through 风险)。**sibling 应用**:Phase 4 sub-round 系列第四例 92fd7c3/<本 commit>(默认模式)— 完结 Phase 4 SUNSET marker 系列全消 + 后续 ct_driver:22/26 + interp_core:75 等余下 SUNSET marker 系列 + 1.5f genVal 主 dispatch 降 thin wrapper 主轮收口直接 prereq(Phase 4 SUNSET marker 全消完结后,evalExpr 已物理完整覆盖 leaf literal + COMPTIME_EMIT/TYPEINFO_EXPR/unsupported 兜底 + THIS/SUPER/ARROW_FUNC ct-depth 特例 + super resolve 字面消 — 1.5f 主轮 thin wrapper 化主轮收口 ready)。**simplify 采纳/拒绝**:本轮纯 mechanical 字面 rename + 1 SUNSET marker 物理清,scope ~2 LOC delta(1 文件 bootstrap/gen/methods/gen_methods.ss,+1/-2),无 simplify 建议触发(参考 sibling 第五/六/七例同 simplify 协议 — `comptimeMustBeKnown == 1` magic-number 协议级故意惯例不改 truthiness 拒 simplify 建议 memory `comptimemustbeknown-eq1-idiom-not-truthiness`)。**根因解决度**:Phase 4 scope 内最深可达根因层(字面消 + SUNSET marker 物理清 + Phase 4 SUNSET marker 系列全消完结);interpClassParents vs classParents 数据源统一是更深根因,留 1.5f 主轮收口或更深 phase scope — sibling 5/6/7 同协议(保 thin dispatch + 数据源双轨留下 phase)。**业界对标** Zig `Sema.resolveMaybeUndefVal` 单点 dispatch — SS 本轮 super resolve 字面消除是 Zig SEMA single-dispatch 路径在 SS 的同形落地 ct-mode 端口收口(D098 §决策 1 §Phase A→B 接口稳定性 + D093 §Zig 原理 §1 物理推进)。RED→GREEN:`grep -cnE "// SUNSET\(D093 §Phase 4\)" bootstrap/gen/methods/gen_methods.ss` 1 → 0(line 258 SUNSET 物理清)+ `grep -cnE "comptimeDepth > 0" bootstrap/gen/methods/gen_methods.ss` 1 → 0(字面消)+ `grep -cnE "comptimeMustBeKnown == 1" bootstrap/gen/methods/gen_methods.ss` 0 → 1(累计 +1);bootstrap 三阶段固定点 verified + 全测 baseline 334/3 持平 + 三 linter GATE OK(sunset_linter 默认 25 markers GATE OK,-1 net = -1 line 258 SUNSET 物理清,Phase 4 SUNSET marker 系列全消完结 + d_doc_index_linter 19 referenced + reflection_health_linter PASS no regressions + next_prompt_ultrathink_linter 5/5)
- **[x] Done at 060e4e4** Phase 4 续 genVal 主 dispatch 入口 ct-depth 字面消除 — `bootstrap/eval/eval_expr.ss` 加 COMPTIME_EMIT case `comptimeSS = \`${comptimeSS}${interpAsStr(valOf(ctEmitMv))}\`` cross-module side-effect 直写(sibling THIS/SUPER 21f8342 interpThisVal cross-module access 模板复用 — interp_core.ss:79 全局 `let comptimeSS = ""` 可跨 eval 子模块直读/写;mvKnownOf/valOf 替 OLD genVal:81 isCt/payload mv 空间对称)+ 加 TYPEINFO_EXPR case 直返 `ctVal(interpBuildTypeInfo(nGetS1(astId)))` 单态直构(sibling ARROW_FUNC 33c65e2 interpNewVal 同模板;interpBuildTypeInfo 经 interp_obj.ss:215 接受 string 参数直返,sibling member_access.ss:32 + exprs_ct_obj.ss:61 + exprs_ct_call.ss:70 + exprs_ct_reflect.ss:40 多处已用证明 cross-module 合法)+ 加 unsupported 兜底 BINARY 主 case 之前 kind 白名单守门(`if (k != "BINARY") { if (comptimeMustBeKnown == 1) loud error else silent null }`)— sibling THIS/SUPER 21f8342 comptimeMustBeKnown==1 loud-error path 模板;§字段 12 假设破裂方案修正 prereq(防 unsupported kind fall-through 到 BINARY ct path 致 corrupt,sibling 2c1fb98 §字段 12 spike 崩剧本本轮预防)+ `bootstrap/gen/exprs/exprs.ss:79-92` genVal 主 dispatch 入口 ct-depth 字面双轨 dispatch 整段退化 thin dispatch(SUNSET marker line 79 + comptimeDepth > 0 ct-depth dispatch line 80-90 + unknown kind silent error line 91-92 共 14 行整段删,替换为 `const mv = evalExpr(id); return mv >= 0 ? mv : 0 - mv - 1` sibling 完全一致 BINARY/UNARY 等 mv 反向编码 pattern + 走 evalExpr 主 case 兜底 COMPTIME_EMIT/TYPEINFO_EXPR/unsupported)+ **1 SUNSET marker 物理清**(exprs.ss:79 `// SUNSET(D093 §Phase 4): genVal 主 dispatch 入口 ct-depth — Phase 4 统一 evalExpr 主 dispatch 后消除 ct-depth 字面`)。**Phase 4 sub-round 系列第三例 spike** — sibling 第五例 21f8342 Phase 4 起首 THIS/SUPER + sibling 第六例 33c65e2 Phase 4 续 ARROW_FUNC 起手后,本轮 genVal 主 dispatch 入口 ct-depth 字面消除是 Phase 4 sub-round 系列**第三例 spike**(sibling 第七例应用),验证 ct-depth 残余字面消除 sub-round 的 sibling 应用模板可在 Phase 4 内部多 sub-round 复用(评估 COMPTIME_EMIT cross-module side-effect + TYPEINFO_EXPR 直构 + unsupported 兜底 三 case 同时迁假设破裂入口)。**§字段 12 假设破裂入口实证**:COMPTIME_EMIT case `comptimeSS = \`${comptimeSS}${interpAsStr(valOf(ctEmitMv))}\`` cross-module side-effect 全局直写(grep 实证 `interp_core.ss:79 let comptimeSS = ""` 全局可跨 eval 子模块直读/写 + sibling THIS/SUPER 21f8342 interpThisVal cross-module access 模板已 PASS)+ TYPEINFO_EXPR `interpBuildTypeInfo` 直构造无外部状态依赖(grep 实证 `interp_obj.ss:215 function interpBuildTypeInfo(typeName: string): int` + sibling member_access.ss:32 / exprs_ct_obj.ss:61 / exprs_ct_call.ss:70 / exprs_ct_reflect.ss:40 多处已用)+ unsupported 兜底 BINARY 主 case 之前 kind 白名单守门避 fall-through corruption(sibling 2c1fb98 §字段 12 spike 崩 → 回方案层加 prereq leaf case 模板本轮 explicit 复用,evalExpr 加 3 case + genVal 整段退化 + unsupported 兜底一次性 swap 后 bootstrap 三阶段固定点 + 全测 baseline + 三 linter GATE OK 三验全过,假设无破裂)。**sibling 应用**:Phase 4 sub-round 系列第三例 060e4e4/5118424(默认模式)— 后续 ct_driver:22/26 + interp_core:75 + gen_methods.ss:258 等余下 SUNSET marker 系列 + 1.5f genVal 主 dispatch 降 thin wrapper 主轮收口接续(genVal 现已剩 thin dispatch 各 case ready 1.5f 主轮 thin wrapper 化主轮收口)。**simplify 采纳/拒绝**:本轮纯 mechanical evalExpr 加 3 case + genVal:79-92 整段退化 thin dispatch + 1 SUNSET marker 物理清,scope ~32 LOC delta(2 文件 bootstrap/eval/eval_expr.ss +18/-0 + bootstrap/gen/exprs/exprs.ss +1/-14),无 simplify 建议触发(参考 sibling 第六例 33c65e2 同 simplify 协议 — `comptimeMustBeKnown == 1` magic-number 协议级故意惯例不改 truthiness 拒 simplify 建议 memory `comptimemustbeknown-eq1-idiom-not-truthiness`;`if (k != "BINARY")` 白名单守门是 explicit 防御性 prereq 不可消)。**业界对标** Zig `Sema.resolveMaybeUndefVal` 任意 ZIR 指令走这一个函数 — SS 本轮 evalExpr 加 3 case + genVal 整段退化是 Zig SEMA single-dispatch 路径在 SS 的同形落地最终形态(genVal 现已纯 thin dispatch 各 case ready 1.5f 主轮 thin wrapper 化,evalExpr 成 D093 §Zig 原理 §1 "唯一求值入口" 物理完整覆盖含 COMPTIME_EMIT/TYPEINFO_EXPR/unsupported 兜底 — D098 §决策 1 §Phase A→B 接口稳定性 + D093 §Zig 原理 §1 物理推进)。RED→GREEN:`grep -cnE "// SUNSET\(D093 §Phase 4\): genVal 主 dispatch" bootstrap/gen/exprs/exprs.ss` 1 → 0(line 79 SUNSET 物理清)+ `grep -cnE 'k == "COMPTIME_EMIT"' bootstrap/eval/eval_expr.ss` 0 → 1(evalExpr 加 COMPTIME_EMIT case)+ `grep -cnE 'k == "TYPEINFO_EXPR"' bootstrap/eval/eval_expr.ss` 0 → 1(evalExpr 加 TYPEINFO_EXPR case)+ `grep -cnE 'function evalExpr' bootstrap/eval/eval_expr.ss` 1 unchanged;bootstrap 三阶段固定点 verified + 全测 baseline 334/3 持平 + 三 linter GATE OK(sunset_linter 默认 26 markers GATE OK,-1 net = -1 line 79 SUNSET 物理清,evalExpr 加 3 case 不带 marker ∵ 直接终态非过渡件 + d_doc_index_linter 19 referenced + reflection_health_linter PASS no regressions + next_prompt_ultrathink_linter 5/5)
- **[x] Done at 33c65e2** Phase 4 续 ARROW_FUNC ct-depth 入口字面消除 — `bootstrap/eval/eval_expr.ss` 加 ARROW_FUNC case 直返 `ctVal(interpNewVal("fn", \`${astId}\`))` — 直构造 ct value 无外部状态依赖,sibling c233add 第四例 leaf case (STRING_LIT/NULL_LIT/DOUBLE_LIT/NAMED_ARG) 直返 ctVal 同形单行 case(sibling THIS/SUPER 21f8342 多态 case 不适用 ∵ interpNewVal 永远成功无 loud-error path 需求)+ `bootstrap/gen/exprs/exprs.ss:74-77` ARROW_FUNC 入口 ct-depth 字面双轨 dispatch 退化 thin dispatch(`comptimeDepth > 0` 时 `const mv = evalExpr(id); return mv >= 0 ? mv : 0 - mv - 1` sibling 完全一致 THIS/SUPER 21f8342 + BINARY/UNARY 等 mv 反向编码 pattern + runtime 路径保 `constVal(genArrowFunc(id))`)+ **1 SUNSET marker 物理清**(exprs.ss:75 `// SUNSET(D093 §Phase 4): ARROW_FUNC expr 入口双轨 dispatch — Phase 4 统一后消除 ct-depth 字面`)。**Phase 4 sub-round 系列第二例 spike** — sibling 第五例 21f8342 Phase 4 起首 THIS/SUPER 起手后,本轮 ARROW_FUNC 入口 ct-depth 字面消除是 Phase 4 sub-round 系列**第二例 spike**,验证 ct-depth 残余字面消除 sub-round 的 sibling 应用模板可在 Phase 4 内部多次复用(评估 ARROW_FUNC 入口 interpNewVal 直构造 ct value 无外部状态依赖假设破裂入口)。**§字段 12 假设破裂入口实证**:ARROW_FUNC 入口 `interpNewVal("fn", \`${id}\`)` 是直构造 ct value 无外部状态依赖(grep 实证 `interp_obj.ss:94 function interpNewVal(kind: string, payload: string): int` 接受 string 参数直返,无 cross-module 依赖)+ sibling THIS/SUPER 21f8342 spike 同模式已 PASS — spike 单 swap 后 bootstrap 三阶段固定点 + 全测 baseline + 三 linter GATE OK 三验全过,假设无破裂(对比 sibling 2c1fb98 §字段 12 spike 崩 → 回方案层加 prereq leaf case 模板,本轮已 c233add leaf case 完整收口故无 fall-through 风险)。**sibling 应用**:Phase 4 sub-round 系列第二例 33c65e2/7c0bbcf(默认模式)— 后续 genVal:84 主 dispatch ct-depth + ct_driver:22/26 + interp_core:75 + gen_methods.ss:258 等余下 SUNSET marker 系列接续。**simplify 采纳/拒绝**:本轮纯 mechanical ARROW_FUNC 入口 evalExpr 加 case + genVal:74-77 退化 thin dispatch + 1 SUNSET marker 物理清,scope ~4 LOC delta(2 文件 bootstrap/eval/eval_expr.ss +1/-0 + bootstrap/gen/exprs/exprs.ss +1/-3),无 simplify 建议触发(参考 sibling 第五例 21f8342 同 simplify 协议 — 保留 genVal:84 主 dispatch ct-depth 字面是 1.5f 主轮收口才能彻底消的形态,跨 phase scope 留 Phase 4 后续 sub-round)。**业界对标** Zig `Sema.resolveMaybeUndefVal` 任意 ZIR 指令走这一个函数 — SS 本轮 ARROW_FUNC 入口 ct-depth 字面消除 + sibling 退化 thin dispatch 是 Zig SEMA single-dispatch 路径在 SS 的同形落地(D098 §决策 1 §Phase A→B 接口稳定性 + D093 §Zig 原理 §1 物理推进)。RED→GREEN:`grep -cnE "// SUNSET\(D093 §Phase 4\): ARROW_FUNC" bootstrap/gen/exprs/exprs.ss` 1 → 0(line 75 SUNSET 物理清)+ `grep -cnE 'k == "ARROW_FUNC"' bootstrap/eval/eval_expr.ss` 0 → 1(evalExpr 加 ARROW_FUNC case)+ `grep -cnE 'function evalExpr' bootstrap/eval/eval_expr.ss` 1 unchanged;bootstrap 三阶段固定点 verified + 全测 baseline 334/3 持平 + 三 linter GATE OK(sunset_linter 默认 27 markers GATE OK,-1 net = -1 line 75 SUNSET 物理清,evalExpr 加 ARROW_FUNC case 不带 marker ∵ 直接终态非过渡件 + d_doc_index_linter 19 referenced + reflection_health_linter PASS no regressions + next_prompt_ultrathink_linter 5/5)
- **[x] Done at 21f8342** Phase 4 起首 THIS/SUPER ct-depth 入口字面消除 — `bootstrap/eval/eval_expr.ss` 加 THIS/SUPER case 直返 ctVal(interpThisVal > 0 时 ctVal(interpThisVal) / comptimeMustBeKnown == 1 时 loud error `'X' not bound in comptime context` / 否则 OLD silent `ctVal(interpNewNull())` 保 sibling 行为一致)+ `bootstrap/gen/exprs/exprs.ss:70-73` THIS/SUPER 入口 ct-depth 字面双轨 dispatch 退化 thin dispatch(`comptimeDepth > 0` 时 `const mv = evalExpr(id); return mv >= 0 ? mv : 0 - mv - 1` sibling 完全一致 BINARY/UNARY 等 mv 反向编码 pattern + runtime 路径保 `constVal(genThisExpr())`)+ **1 SUNSET marker 物理清**(exprs.ss:71 `// SUNSET(D093 §Phase 4): THIS/SUPER expr 入口双轨 dispatch — Phase 4 统一后消除 ct-depth 字面`)。**Phase 4 sub-round 系列起手首例 spike** — sibling 第四例 c233add evalExpr leaf case 收口后,本轮 THIS/SUPER 入口 ct-depth 字面消除是 Phase 4 sub-round 系列**首例 spike**,验证 ct-depth 残余字面消除 sub-round 的 sibling 应用模板(评估 evalExpr 加 case 直读 interpThisVal cross-module access 假设破裂入口)。**§字段 12 假设破裂入口实证**:THIS/SUPER 入口 `interpThisVal` 是 SS 全局 int 可跨 eval 子模块直读(grep 实证 `interp_core.ss:105 let interpThisVal = 0` + 5 处直读/写 `gen/exprs/exprs.ss:73` + `exprs_ct_obj.ss:82/91/131`)+ SUPER 与 THIS 同 branch 处理(sibling `exprs.ss:70 if kind == "THIS" \|\| kind == "SUPER"`)— spike 单 swap 后 bootstrap 三阶段固定点 + 全测 baseline + 三 linter GATE OK 三验全过,假设无破裂(对比 sibling 2c1fb98 §字段 12 spike 崩 → 回方案层加 prereq leaf case 模板)。**sibling 应用**:Phase 4 sub-round 系列起手首例 21f8342/7d66eb4(默认模式)— 后续 ARROW_FUNC ct-depth 特例 + genVal:84 主 dispatch ct-depth + ct_driver:22/26 + interp_core:75 等余下 SUNSET marker 系列接续。**simplify 采纳/拒绝**:本轮纯 mechanical THIS/SUPER 入口 evalExpr 加 case + genVal:70-73 退化 thin dispatch + 1 SUNSET marker 物理清,scope ~6 LOC delta(2 文件 bootstrap/eval/eval_expr.ss +5/-0 + bootstrap/gen/exprs/exprs.ss +1/-5),无 simplify 建议触发(参考 sibling 第三例同 simplify 协议 — 保留 ct-depth 字面是 1.5f genVal 主 dispatch 降 thin wrapper 后才能彻底消的形态,跨 phase scope 留 Phase 4 后续 sub-round)。**业界对标** Zig `Sema.resolveMaybeUndefVal` 任意 ZIR 指令走这一个函数 — SS 本轮 THIS/SUPER 入口 ct-depth 字面消除 + sibling 退化 thin dispatch 是 Zig SEMA single-dispatch 路径在 SS 的同形落地(D098 §决策 1 §Phase A→B 接口稳定性 + D093 §Zig 原理 §1 物理推进)。RED→GREEN:`grep -cnE "// SUNSET\(D093 §Phase 4\): THIS/SUPER" bootstrap/gen/exprs/exprs.ss` 1 → 0(line 71 SUNSET 物理清)+ `grep -cnE 'if \(k == "THIS"' bootstrap/eval/eval_expr.ss` 0 → 1(evalExpr 加 THIS/SUPER case)+ `grep -cnE 'function evalExpr' bootstrap/eval/eval_expr.ss` 1 unchanged;bootstrap 三阶段固定点 verified + 全测 baseline 334/3 持平 + 三 linter GATE OK(sunset_linter 默认 28 markers GATE OK,-1 net = -1 line 71 SUNSET 物理清,evalExpr 加 THIS/SUPER case 不带 marker ∵ 直接终态非过渡件 + d_doc_index_linter 19 referenced + reflection_health_linter PASS no regressions + next_prompt_ultrathink_linter 5/5)
- **[x] Done at b0f8d12** 1.5e+ ext — sibling tasks(`pow_binary.ss` / `null_coalesce.ss` eager 上移桥消除 + `genNullCoalesce` 接口扩 `lPreReg/rPreReg` 哨兵 + `genBinary` line 198 dispatch forward + 2 SUNSET marker 物理清,sibling D170 step 7 实战首例 sibling 应用 — C1 feat impl):**pow_binary.ss** 重构三段式对齐 BINARY 主 case bb0f805(eager unify lRaw/rRaw → 段 1 ct fold 仅 integer 指数 + fractional fallback runtime path ss_pow libc → 段 2 紧 loud → 段 3 runtime forward reg);**null_coalesce.ss** 重构三段式保单边短路(eager lRaw → 段 1 ct 短路 → 段 2 紧 loud + 双 operand 区分 lhs/rhs 错误消息 → 段 3 runtime forward `reg(lRaw)` + 空 rPreReg "" 哨兵 PERMANENT 保 rhs lazy 由 genNullCoalesce 内 br then 块惰性 eval);**genNullCoalesce 接口扩** `lPreReg/rPreReg: string = ""` 哨兵 + genBinary dispatch line 198 forward — sibling 接口范式对齐 genStringConcat/genBinary。**fractional ct fold 根因**:SS bootstrap 无 ct-side libc pow/exp/log,扩 interpDoubleOp 需 Newton iteration / Taylor 展开 scope 远超本轮 — sustainable 中间态留 1.5e+ later 同 valType 终态批次。**simplify 采纳/拒绝**:采纳 pow_binary.ss rt 移入 ct-fold guard 简化(死路径计算消)+ SUNSET marker D170 protocol 合规 + exprs_binary.ss line 179 stale comment 更新(NullCoalesce 现已 forward)+ null_coalesce.ss inline `""` rhs lazy PERMANENT 注释;拒绝 regOrEval helper 抽取(scope 外留 1.5e+ later 收口 5+ callsite)+ must-known eager early-check 重构(破 sibling 三段式)+ genBinary 入口 dispatch reorder(scope 外)。RED→GREEN:`grep -nE "return mvRuntime\(constVal\(genBinary\(astId\)\)\)" bootstrap/eval/pow_binary.ss bootstrap/eval/null_coalesce.ss` 2 → 0 + `grep -nE "function genNullCoalesce" bootstrap/gen/exprs/exprs_binary.ss` 接口扩 `lPreReg/rPreReg: string = ""` 哨兵 + `grep -nE "genNullCoalesce\(leftId, rightId, lPreReg, rPreReg\)" bootstrap/gen/exprs/exprs_binary.ss` 0 → 1(dispatch forward);bootstrap 三阶段固定点 verified + 全测 334/3 baseline 持平 + 三 linter GATE OK(sunset_linter 默认 30 markers GATE OK,-1 net = -2 SUNSET 物理清 + 1 SUNSET valType 混合 dispatch 加锚同 eval_expr.ss:116 sibling + d_doc_index_linter 19 referenced + reflection_health_linter 全 PASS + next_prompt_ultrathink_linter 5/5)

## Phase 1.5d 主轮收口子步 sibling 子轮 §出口清单 (D170 §决策 C exit 动作 §2 落地 — 首例实战)

D170 协议**实战首例** — Phase 1.5d sibling 子轮 phase 完结后,2 SUNSET markers 经 Manual gate `sunset_linter --phase "1.5d 主轮收口子步 sibling 子轮"` verify 显式 resolve(本段是 D170 §决策 C exit 动作清单 §2 落地的首例 §出口清单,后续 phase exit 模板):

| # | marker file:line | resolve 标签 | commit hash |
|---|---|---|---|
| 1 | `bootstrap/gen/stmts/stmts_loop_classic.ss:12` (for 入口 SUNSET) | `[clean]` 代码已删 — for 入口字面 rename + silent→loud 完成,SUNSET marker 物理清(D170 §决策 A canonical marker 模板 #5 — phase 完结后 marker 不再需要) | b94dc8b |
| 2 | `bootstrap/gen/stmts/stmts_loop_classic.ss:72` (while 入口 SUNSET) | `[clean]` 代码已删 — while 入口字面 rename + silent→loud 完成,SUNSET marker 物理清(sibling do-while ddd327c + 同段 #1 for 完全一致) | b94dc8b |

**Manual gate verify 实测**:`bin/ss run tools/sunset_linter.ss --phase "1.5d 主轮收口子步 sibling 子轮"`(本 commit 后跑)→
- C1 collection: 30 SUNSET markers (-2 from 32,本 phase markers 已物理清)
- Phase Exit verdict: **PASS** — no cleanup needed for phase '1.5d 主轮收口子步 sibling 子轮'(0 markers + phase status post-C2 转 [x] Done)
- exit 0 + GATE OK
- (next_prompt 字面 "verdict 应为 BLOCK (phase 已 [x] Done 但 2 markers 残留)" 描述反向操作场景;本轮实战正向路径**先清 marker C1,后 [x] Done + exit verify C2** verdict 实际是 PASS — 两 path 都是 manual gate 合法 use case,D170 §决策 C Phase Exit Gate 双轨触发设计预期之内)

## Phase 1.5e+ ext §出口清单 (D170 §决策 C exit 动作 §2 sibling 落地 — step 7 实战首例 sibling 第二例应用)

D170 协议 step 7 实战首例 (`1.5d 主轮收口子步 sibling 子轮 §出口清单` b94dc8b/609b465) 之后 **sibling 第二例应用** — Phase 1.5e+ ext 完结后,2 SUNSET markers 经 `sunset_linter` 默认模式 verify 显式 resolve(本段是 D170 §决策 C exit 动作清单 §2 落地 sibling 第二例,后续 sibling phase exit 沿用本格式):

| # | marker file:line | resolve 标签 | commit hash |
|---|---|---|---|
| 1 | `bootstrap/eval/pow_binary.ss:6` (Pow eager genVal 上移 + 接口扩 SUNSET) | `[clean]` 代码已删 — eager unify lRaw/rRaw 上移 + 三段式 sibling 完全一致(对齐 BINARY 主 case bb0f805)+ runtime path forward `reg(lRaw)/reg(rRaw)` 完成,SUNSET marker 物理清(D170 §决策 A canonical marker 模板 — phase 完结后 marker 不再需要) | b0f8d12 |
| 2 | `bootstrap/eval/null_coalesce.ss:6` (NullCoalesce eager 上移 + genNullCoalesce 接口扩 SUNSET) | `[clean]` 代码已删 — eager lRaw 上移 + 三段式(保单边短路)+ genNullCoalesce 接口扩 `lPreReg/rPreReg` 哨兵 + runtime path forward `reg(lRaw)` + 空 rPreReg "" 哨兵 PERMANENT 保 rhs lazy 完成,SUNSET marker 物理清(sibling pow_binary 同段 #1;NullCoalesce 短路语义差异详 §决策 #1 注 — rPreReg 哨兵 PERMANENT 不可消) | b0f8d12 |

**Manual gate verify 实测**:`bin/ss run tools/sunset_linter.ss`(本 commit 后跑)→
- C1 collection: 30 SUNSET markers (-1 net from 31,本 phase 2 markers 物理清 + 1 新 SUNSET 加在 `pow_binary.ss:18` valType/inferType 混合 dispatch — Air.Inst.Ref 终态后收口 sibling 锚同 `eval_expr.ss:116`)
- 默认模式 GATE OK + exit 0
- C5/C6/C8/C9 软警告维持 baseline (C5 0 / C6 0 / C8 43 / C9 3)
- (sibling 1.5d 子轮 §出口清单 用 `--phase` manual gate;本 sibling 第二例用默认模式 verify ∵ 2 marker 物理清后无残留触发 BLOCK,默认模式即可 verify GATE OK;两 path 均 D170 §决策 C 双轨触发设计预期内)

**D170 step 7 实战首例 sibling 范式实战完结**(2 sub-round 系列:b94dc8b/609b465 + b0f8d12/f9360f9):D170 协议**实战首例 + 第二例 sibling 闭环** — phase exit verify 双轨(manual gate `--phase X` + 默认模式 0-marker)均落地实战,sibling 应用模板可重复;后续每个 phase exit 沿用本 §出口清单 表格格式(`# | marker file:line | resolve 标签 | commit hash` 三列)

## Phase 1.5e+ later UNARY 反向编码统一 §出口清单 (D170 §决策 C exit 动作 §2 sibling 第三例应用)

D170 协议 step 7 实战首例 (b94dc8b/609b465) → sibling 第二例 (b0f8d12/f9360f9) → **本轮 sibling 第三例应用** — Phase 1.5e+ later UNARY 反向编码统一完结后,1 SUNSET marker 经 `sunset_linter` 默认模式 verify 显式 resolve(本段是 D170 §决策 C exit 动作清单 §2 落地 sibling 第三例,后续 sibling phase exit 沿用本格式):

| # | marker file:line | resolve 标签 | commit hash |
|---|---|---|---|
| 1 | `bootstrap/eval/eval_expr.ss:49` (UNARY genVal 桥反向编码 SUNSET) | `[clean]` 代码已删 — UNARY case `subMv = evalExpr(childId)` 单点 dispatch + 注释 trim 完成,SUNSET marker 物理清(D170 §决策 A canonical marker 模板 — phase 完结后 marker 不再需要;sibling pow_binary.ss:6 / null_coalesce.ss:6 b0f8d12 同模式) | 2c1fb98 |

**Manual gate verify 实测**:`bin/ss run tools/sunset_linter.ss`(本 commit 后跑)→
- C1 collection: 29 SUNSET markers (-1 net from 30,本 phase 1 marker 物理清;evalExpr 扩 4 leaf case 不带 marker ∵ leaf case 是直返 ctVal 非过渡件)
- 默认模式 GATE OK + exit 0
- C5/C6/C8/C9 软警告维持 baseline (C5 0 / C6 0 / C8 43 / C9 3)
- (sibling 1.5e+ ext b0f8d12/f9360f9 默认模式 verify 同 path;1.5d 主轮收口子步 sibling 子轮 b94dc8b/609b465 用 `--phase` manual gate;两 path 均 D170 §决策 C 双轨触发设计预期内)

**D170 step 7 实战首例 sibling 范式实战完结升级**(3 sub-round 系列:b94dc8b/609b465 + b0f8d12/f9360f9 + 2c1fb98/6dbc2de):D170 协议**实战首例 + 第二例 + 第三例 sibling 闭环** — phase exit verify 双轨(manual gate `--phase X` + 默认模式 0-marker)三轮均落地实战,sibling 应用模板可重复;**§字段 12 假设破裂方案修正**也作为 D170 sibling 应用模板的 robustness validator(首版照 next_prompt 字面 swap 不 prereq → spike 崩 → 回方案层加 prereq leaf case → swap 安全 — 后续 sibling 应用也应预期假设破裂入口)

## Phase 1.5e+ later evalExpr 余下 leaf case 收口 §出口清单 (D170 §决策 C exit 动作 §2 sibling 第四例应用)

D170 协议 step 7 实战首例 (b94dc8b/609b465) → sibling 第二例 (b0f8d12/f9360f9) → sibling 第三例 (2c1fb98/6dbc2de) → **本轮 sibling 第四例应用** — Phase 1.5e+ later evalExpr 余下 leaf case 收口完结后,**0 SUNSET marker 物理清**(本 sub-round 4 leaf case `STRING_LIT`/`NULL_LIT`/`DOUBLE_LIT`/`NAMED_ARG` 直返 ctVal / 委托内层,**非过渡件**故不带 SUNSET marker),`sunset_linter` 默认模式 verify GATE OK 持平 baseline 29 markers(本段是 D170 §决策 C exit 动作清单 §2 落地 sibling 第四例,后续 sibling phase exit 沿用本格式):

| # | marker file:line | resolve 标签 | commit hash |
|---|---|---|---|
| — | (本 sub-round NULL marker — 4 leaf case 直返 ctVal / 委托内层非过渡件) | `[no-marker]` — sibling genVal:62-67+83 完整对称形式 prereq,运行时 BINARY ct path `genVal(nGetI1) line 113` 直命中 exprs.ss:62-67 不触 evalExpr fall-through;**跨 phase 安全 prereq** 留 1.5f genVal 主 dispatch 降 thin wrapper 后 evalExpr 真单点入口 | c233add |

**Manual gate verify 实测**:`bin/ss run tools/sunset_linter.ss`(本 commit 后跑)→
- C1 collection: 29 SUNSET markers 持平(本 sub-round 0 marker 物理清,evalExpr 4 leaf case 直返 ctVal 非过渡件 sibling 第三例 2c1fb98 同协议)
- 默认模式 GATE OK + exit 0
- C5/C6/C8/C9 软警告维持 baseline (C5 0 / C6 0 / C8 43 / C9 3)
- (sibling 1.5e+ later UNARY 2c1fb98/6dbc2de + 1.5e+ ext b0f8d12/f9360f9 默认模式 verify 同 path;1.5d 主轮收口子步 sibling 子轮 b94dc8b/609b465 用 `--phase` manual gate;两 path 均 D170 §决策 C 双轨触发设计预期内)

**D170 step 7 实战首例 sibling 范式实战完结升级**(4 sub-round 系列:b94dc8b/609b465 + b0f8d12/f9360f9 + 2c1fb98/6dbc2de + c233add/90aba70):D170 协议**实战首例 + 第二例 + 第三例 + 第四例 sibling 闭环** — phase exit verify 双轨(manual gate `--phase X` + 默认模式 0-marker)四轮均落地实战,sibling 应用模板可重复;**§字段 12 假设破裂方案修正**也作为 D170 sibling 应用模板的 robustness validator(首版照 next_prompt 字面 swap 不 prereq → spike 崩 → 回方案层加 prereq leaf case → swap 安全 — 后续 sibling 应用也应预期假设破裂入口);**0-marker sibling 应用首例**:本轮 sibling 第四例首次落地 NULL-marker §出口清单 段(4 leaf case 直返 ctVal 非过渡件),拓展 sibling 应用模板的 "marker 物理清" 维度由 ≥1 marker 缩到 0 marker — D170 §决策 C exit 动作 §2 sibling 应用 scope 覆盖 0-marker 形态 leaf-completeness 收口子轮

## Phase 4 起首 THIS/SUPER ct-depth 入口字面消除 §出口清单 (D170 §决策 C exit 动作 §2 sibling 第五例应用)

D170 协议 step 7 实战首例 (b94dc8b/609b465) → sibling 第二例 (b0f8d12/f9360f9) → sibling 第三例 (2c1fb98/6dbc2de) → sibling 第四例 (c233add/90aba70) → **本轮 sibling 第五例应用** — Phase 4 起首 THIS/SUPER ct-depth 入口字面消除完结后,**1 SUNSET marker 物理清**(exprs.ss:71 THIS/SUPER ct-depth 字面),`sunset_linter` 默认模式 verify GATE OK 28 markers(-1 net from 29 baseline,sibling 应用模板 ≥1 marker 形态恢复对齐 sibling 第三例 2c1fb98 模式)。本段是 D170 §决策 C exit 动作清单 §2 落地 sibling 第五例,后续 sibling phase exit 沿用本格式:

| # | marker file:line | resolve 标签 | commit hash |
|---|---|---|---|
| 1 | `bootstrap/gen/exprs/exprs.ss:71` (THIS/SUPER ct-depth 入口双轨 SUNSET) | `[clean]` 代码已删 — THIS/SUPER 入口 ct-depth 字面双轨 dispatch 退化 thin dispatch + evalExpr 加 THIS/SUPER case prereq,SUNSET marker 物理清(D170 §决策 A canonical marker 模板 — phase 完结后 marker 不再需要;sibling eval_expr.ss:49 UNARY 2c1fb98 同模式) | 21f8342 |

**Manual gate verify 实测**:`bin/ss run tools/sunset_linter.ss`(本 commit 后跑)→
- C1 collection: 28 SUNSET markers (-1 net from 29,本 phase 1 marker 物理清;evalExpr 加 THIS/SUPER case 不带 marker ∵ 直接终态非过渡件)
- 默认模式 GATE OK + exit 0
- C5/C6/C8/C9 软警告维持 baseline (C5 0 / C6 0 / C8 43 / C9 3)
- (sibling 1.5e+ later evalExpr 余下 leaf case 收口 c233add/90aba70 + 1.5e+ later UNARY 2c1fb98/6dbc2de + 1.5e+ ext b0f8d12/f9360f9 默认模式 verify 同 path;1.5d 主轮收口子步 sibling 子轮 b94dc8b/609b465 用 `--phase` manual gate;两 path 均 D170 §决策 C 双轨触发设计预期内)

**D170 step 7 实战首例 sibling 范式实战完结升级**(5 sub-round 系列:b94dc8b/609b465 + b0f8d12/f9360f9 + 2c1fb98/6dbc2de + c233add/90aba70 + 21f8342/7d66eb4):D170 协议**实战首例 + 第二例 + 第三例 + 第四例 + 第五例 sibling 闭环** — phase exit verify 双轨(manual gate `--phase X` + 默认模式)五轮均落地实战,sibling 应用模板可重复;**Phase 4 sub-round 系列起手首例**:本轮 sibling 第五例首次跨入 Phase 4 sub-round 系列(前四例皆 1.5d/1.5e+ scope),验证 ct-depth 残余字面消除 sub-round 的 sibling 应用模板可跨 phase 复用 — D093 §下一步 line 107 §第一性需求"comptime 块降为 `comptimeMustBeKnown` flag" 物理落地最终态 + Zig SEMA single-dispatch 路径同形落地接力推进(后续 ARROW_FUNC ct-depth + genVal:84 主 dispatch ct-depth + ct_driver:22/26 + interp_core:75 等余下 SUNSET marker 系列接续)

## Phase 4 续 ARROW_FUNC ct-depth 入口字面消除 §出口清单 (D170 §决策 C exit 动作 §2 sibling 第六例应用)

D170 协议 step 7 实战首例 (b94dc8b/609b465) → sibling 第二例 (b0f8d12/f9360f9) → sibling 第三例 (2c1fb98/6dbc2de) → sibling 第四例 (c233add/90aba70) → sibling 第五例 (21f8342/7d66eb4) → **本轮 sibling 第六例应用** — Phase 4 续 ARROW_FUNC ct-depth 入口字面消除完结后,**1 SUNSET marker 物理清**(exprs.ss:75 ARROW_FUNC ct-depth 字面),`sunset_linter` 默认模式 verify GATE OK 27 markers(-1 net from 28 baseline,sibling 应用模板 ≥1 marker 形态延续 sibling 第三/五例同模式)。本段是 D170 §决策 C exit 动作清单 §2 落地 sibling 第六例,后续 sibling phase exit 沿用本格式:

| # | marker file:line | resolve 标签 | commit hash |
|---|---|---|---|
| 1 | `bootstrap/gen/exprs/exprs.ss:75` (ARROW_FUNC ct-depth 入口双轨 SUNSET) | `[clean]` 代码已删 — ARROW_FUNC 入口 ct-depth 字面双轨 dispatch 退化 thin dispatch + evalExpr 加 ARROW_FUNC case prereq,SUNSET marker 物理清(D170 §决策 A canonical marker 模板 — phase sub-round 完结后 marker 不再需要;sibling exprs.ss:71 THIS/SUPER 21f8342 同模式) | 33c65e2 |

**Manual gate verify 实测**:`bin/ss run tools/sunset_linter.ss`(本 commit 后跑)→
- C1 collection: 27 SUNSET markers (-1 net from 28,本 sub-round 1 marker 物理清;evalExpr 加 ARROW_FUNC case 不带 marker ∵ 直接终态非过渡件)
- 默认模式 GATE OK + exit 0
- C5/C6/C8/C9 软警告维持 baseline (C5 0 / C6 0 / C8 43 / C9 3)
- (sibling Phase 4 起首 THIS/SUPER 21f8342/7d66eb4 + 1.5e+ later evalExpr 余下 leaf case 收口 c233add/90aba70 + 1.5e+ later UNARY 2c1fb98/6dbc2de + 1.5e+ ext b0f8d12/f9360f9 默认模式 verify 同 path;1.5d 主轮收口子步 sibling 子轮 b94dc8b/609b465 用 `--phase` manual gate;两 path 均 D170 §决策 C 双轨触发设计预期内)

**D170 step 7 实战首例 sibling 范式实战完结升级**(6 sub-round 系列:b94dc8b/609b465 + b0f8d12/f9360f9 + 2c1fb98/6dbc2de + c233add/90aba70 + 21f8342/7d66eb4 + 33c65e2/7c0bbcf):D170 协议**实战首例 + 第二例 + 第三例 + 第四例 + 第五例 + 第六例 sibling 闭环** — phase exit verify 双轨(manual gate `--phase X` + 默认模式)六轮均落地实战,sibling 应用模板可重复;**Phase 4 sub-round 系列第二例**:本轮 sibling 第六例是 Phase 4 sub-round 系列**第二例**(sibling 第五例 21f8342 起首后),验证 ct-depth 残余字面消除 sub-round 的 sibling 应用模板可在 Phase 4 内部多次复用(sibling 第五/六例同 phase 接力 — D170 协议 sibling 应用模板 Phase 内复用 robustness 跨 sub-round 累计验证)— D093 §下一步 line 107 §第一性需求"comptime 块降为 `comptimeMustBeKnown` flag" 物理落地最终态 + Zig SEMA single-dispatch 路径同形落地接力推进(后续 genVal:84 主 dispatch ct-depth + ct_driver:22/26 + interp_core:75 + gen_methods.ss:258 等余下 SUNSET marker 系列接续)

## Phase 4 续 genVal 主 dispatch 入口 ct-depth 字面消除 §出口清单 (D170 §决策 C exit 动作 §2 sibling 第七例应用)

D170 协议 step 7 实战首例 (b94dc8b/609b465) → sibling 第二例 (b0f8d12/f9360f9) → sibling 第三例 (2c1fb98/6dbc2de) → sibling 第四例 (c233add/90aba70) → sibling 第五例 (21f8342/7d66eb4) → sibling 第六例 (33c65e2/7c0bbcf) → **本轮 sibling 第七例应用** — Phase 4 续 genVal 主 dispatch 入口 ct-depth 字面消除完结后,**1 SUNSET marker 物理清**(exprs.ss:79 genVal 主 dispatch ct-depth 字面),`sunset_linter` 默认模式 verify GATE OK 26 markers(-1 net from 27 baseline,sibling 应用模板 ≥1 marker 形态延续 sibling 第三/五/六例同模式)。本段是 D170 §决策 C exit 动作清单 §2 落地 sibling 第七例,后续 sibling phase exit 沿用本格式:

| # | marker file:line | resolve 标签 | commit hash |
|---|---|---|---|
| 1 | `bootstrap/gen/exprs/exprs.ss:79` (genVal 主 dispatch 入口 ct-depth 字面 SUNSET) | `[clean]` 代码已删 — genVal:79-92 整段退化 thin dispatch + evalExpr 加 COMPTIME_EMIT/TYPEINFO_EXPR/unsupported 兜底 3 case prereq,SUNSET marker 物理清(D170 §决策 A canonical marker 模板 — phase sub-round 完结后 marker 不再需要;sibling exprs.ss:71 THIS/SUPER 21f8342 + exprs.ss:75 ARROW_FUNC 33c65e2 同模式) | 060e4e4 |

**Manual gate verify 实测**:`bin/ss run tools/sunset_linter.ss`(本 commit 后跑)→
- C1 collection: 26 SUNSET markers (-1 net from 27,本 sub-round 1 marker 物理清;evalExpr 加 3 case 不带 marker ∵ 直接终态非过渡件)
- 默认模式 GATE OK + exit 0
- C5/C6/C8/C9 软警告维持 baseline (C5 0 / C6 0 / C8 43 / C9 3)
- (sibling Phase 4 续 ARROW_FUNC 33c65e2/7c0bbcf + Phase 4 起首 THIS/SUPER 21f8342/7d66eb4 + 1.5e+ later evalExpr 余下 leaf case 收口 c233add/90aba70 + 1.5e+ later UNARY 2c1fb98/6dbc2de + 1.5e+ ext b0f8d12/f9360f9 默认模式 verify 同 path;1.5d 主轮收口子步 sibling 子轮 b94dc8b/609b465 用 `--phase` manual gate;两 path 均 D170 §决策 C 双轨触发设计预期内)

**D170 step 7 实战首例 sibling 范式实战完结升级**(7 sub-round 系列:b94dc8b/609b465 + b0f8d12/f9360f9 + 2c1fb98/6dbc2de + c233add/90aba70 + 21f8342/7d66eb4 + 33c65e2/7c0bbcf + 060e4e4/5118424):D170 协议**实战首例 + 第二例 + 第三例 + 第四例 + 第五例 + 第六例 + 第七例 sibling 闭环** — phase exit verify 双轨(manual gate `--phase X` + 默认模式)七轮均落地实战,sibling 应用模板可重复;**Phase 4 sub-round 系列第三例**:本轮 sibling 第七例是 Phase 4 sub-round 系列**第三例**(sibling 第五例 21f8342 起首 + 第六例 33c65e2 ARROW_FUNC 接力后),验证 ct-depth 残余字面消除 sub-round 的 sibling 应用模板可在 Phase 4 内部多 sub-round 复用 robustness 累计第三例(sibling 第五/六/七例同 phase 接力 — D170 协议 sibling 应用模板 Phase 内复用 robustness 跨 sub-round 累计验证升级)— D093 §下一步 line 107 §第一性需求"comptime 块降为 `comptimeMustBeKnown` flag" 物理落地最终态 + Zig SEMA single-dispatch 路径同形落地接力推进(genVal 主 dispatch ct-depth 字面消除后 genVal 已剩 thin dispatch 各 case ready 1.5f 主轮 thin wrapper 化主轮收口 — evalExpr 成 D093 §Zig 原理 §1 "唯一求值入口" 物理完整覆盖含 COMPTIME_EMIT/TYPEINFO_EXPR/unsupported 兜底;后续 ct_driver:22/26 + interp_core:75 + gen_methods.ss:258 等余下 SUNSET marker 系列 + 1.5f genVal 主 dispatch 降 thin wrapper 主轮收口接续)

## Phase 4 续 super resolve 入口 ct-depth 字面消除 §出口清单 (D170 §决策 C exit 动作 §2 sibling 第八例应用)

D170 协议 step 7 实战首例 (b94dc8b/609b465) → sibling 第二例 (b0f8d12/f9360f9) → sibling 第三例 (2c1fb98/6dbc2de) → sibling 第四例 (c233add/90aba70) → sibling 第五例 (21f8342/7d66eb4) → sibling 第六例 (33c65e2/7c0bbcf) → sibling 第七例 (060e4e4/5118424) → **本轮 sibling 第八例应用** — Phase 4 续 super resolve 入口 ct-depth 字面消除完结后,**1 SUNSET marker 物理清**(gen_methods.ss:258 super resolve ct-depth 字面),`sunset_linter` 默认模式 verify GATE OK 25 markers(-1 net from 26 baseline,sibling 应用模板 ≥1 marker 形态延续 sibling 第三/五/六/七例同模式)。本段是 D170 §决策 C exit 动作清单 §2 落地 sibling 第八例,后续 sibling phase exit 沿用本格式:

| # | marker file:line | resolve 标签 | commit hash |
|---|---|---|---|
| 1 | `bootstrap/gen/methods/gen_methods.ss:258` (super resolve 入口 ct-depth 字面 SUNSET) | `[clean]` 代码已删 — line 258 SUNSET marker 删 + line 259 字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1`,SUNSET marker 物理清(D170 §决策 A canonical marker 模板 — phase sub-round 完结后 marker 不再需要;sibling exprs.ss:71 THIS/SUPER 21f8342 + exprs.ss:75 ARROW_FUNC 33c65e2 + exprs.ss:79 genVal 主 dispatch 060e4e4 同模式,Phase 4 SUNSET marker 系列累计 4 物理清完结) | 92fd7c3 |

**Manual gate verify 实测**:`bin/ss run tools/sunset_linter.ss`(本 commit 后跑)→
- C1 collection: 25 SUNSET markers (-1 net from 26,本 sub-round 1 marker 物理清;Phase 4 SUNSET marker 系列累计 4 物理清完结)
- 默认模式 GATE OK + exit 0
- C5/C6/C8/C9 软警告维持 baseline (C5 0 / C6 0 / C8 43 / C9 3)
- (sibling Phase 4 续 genVal 主 dispatch 060e4e4/5118424 + Phase 4 续 ARROW_FUNC 33c65e2/7c0bbcf + Phase 4 起首 THIS/SUPER 21f8342/7d66eb4 + 1.5e+ later evalExpr 余下 leaf case 收口 c233add/90aba70 + 1.5e+ later UNARY 2c1fb98/6dbc2de + 1.5e+ ext b0f8d12/f9360f9 默认模式 verify 同 path;1.5d 主轮收口子步 sibling 子轮 b94dc8b/609b465 用 `--phase` manual gate;两 path 均 D170 §决策 C 双轨触发设计预期内)

**D170 step 7 实战首例 sibling 范式实战完结升级**(8 sub-round 系列:b94dc8b/609b465 + b0f8d12/f9360f9 + 2c1fb98/6dbc2de + c233add/90aba70 + 21f8342/7d66eb4 + 33c65e2/7c0bbcf + 060e4e4/5118424 + 92fd7c3/<本 commit>):D170 协议**实战首例 + 第二例 + 第三例 + 第四例 + 第五例 + 第六例 + 第七例 + 第八例 sibling 闭环** — phase exit verify 双轨(manual gate `--phase X` + 默认模式)八轮均落地实战,sibling 应用模板可重复;**Phase 4 sub-round 系列第四例 + Phase 4 SUNSET marker 全消完结**:本轮 sibling 第八例是 Phase 4 sub-round 系列**第四例**(sibling 第五例 21f8342 起首 + 第六例 33c65e2 ARROW_FUNC + 第七例 060e4e4 genVal 主 dispatch 接力后),完结 Phase 4 SUNSET marker 系列全消(累计 4 物理清:exprs.ss:71+75+79 + gen_methods.ss:258)验证 ct-depth 残余字面消除 sub-round 的 sibling 应用模板可在 Phase 4 内部多 sub-round 复用 robustness 累计第四例 + Phase 4 完结(sibling 第五/六/七/八例同 phase 接力 — D170 协议 sibling 应用模板 Phase 内复用 robustness 跨 sub-round 累计验证升级 + Phase 4 SUNSET marker 全消)— D093 §下一步 line 107 §第一性需求"comptime 块降为 `comptimeMustBeKnown` flag" 物理落地最终态 + Zig SEMA single-dispatch 路径同形落地接力推进(Phase 4 SUNSET marker 全消完结后 evalExpr 已物理完整覆盖 leaf literal + COMPTIME_EMIT/TYPEINFO_EXPR/unsupported 兜底 + THIS/SUPER/ARROW_FUNC ct-depth 特例 + super resolve 字面消 — 1.5f 主轮 thin wrapper 化主轮收口直接 prereq;后续 ct_driver:22/26 + interp_core:75 等余下 SUNSET marker 系列 + 1.5f genVal 主 dispatch 降 thin wrapper 主轮收口接续)

## Phase 2 起手 sub-round 起手 stmts_core break/continue 入口 ct-depth 字面消除 §出口清单 (D170 §决策 C exit 动作 §2 sibling 第九例应用)

D170 协议 step 7 实战首例 (b94dc8b/609b465) → sibling 第二例 (b0f8d12/f9360f9) → sibling 第三例 (2c1fb98/6dbc2de) → sibling 第四例 (c233add/90aba70) → sibling 第五例 (21f8342/7d66eb4) → sibling 第六例 (33c65e2/7c0bbcf) → sibling 第七例 (060e4e4/5118424) → sibling 第八例 (92fd7c3/eedbfa0) → **本轮 sibling 第九例应用** — Phase 2 起手 sub-round 起手 stmts_core break/continue 入口 ct-depth 字面消除完结后,**2 SUNSET marker 物理清**(stmts_core.ss:26+37 break/continue 入口 ct-depth 字面),`sunset_linter` 默认模式 verify GATE OK 23 markers(-2 net from 25 baseline,sibling 应用模板 ≥1 marker 形态延续 sibling 第三/五/六/七/八例同模式 + Phase 2 起手 sub-round 首例 SUNSET 物理清 2 处)。本段是 D170 §决策 C exit 动作清单 §2 落地 sibling 第九例,后续 sibling phase exit 沿用本格式:

| # | marker file:line | resolve 标签 | commit hash |
|---|---|---|---|
| 1 | `bootstrap/gen/stmts/stmts_core.ss:26` (break 入口 ct-depth 字面 SUNSET) | `[clean]` 代码已删 — line 26 SUNSET marker 删 + line 27 字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1`,SUNSET marker 物理清(D170 §决策 A canonical marker 模板 — Phase 2 sub-round 完结后 marker 不再需要;sibling exprs.ss:71 THIS/SUPER 21f8342 + exprs.ss:75 ARROW_FUNC 33c65e2 + exprs.ss:79 genVal 主 dispatch 060e4e4 + gen_methods.ss:258 super resolve 92fd7c3 同模式) | 0fb2fa1 |
| 2 | `bootstrap/gen/stmts/stmts_core.ss:37` (continue 入口 ct-depth 字面 SUNSET) | `[clean]` 代码已删 — line 37 SUNSET marker 删 + line 38 字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1`,SUNSET marker 物理清(D170 §决策 A canonical marker 模板 — Phase 2 sub-round 完结后 marker 不再需要;sibling break 同模式 + Phase 2 起手 sub-round 首例 SUNSET 物理清累计 2 处) | 0fb2fa1 |

**Manual gate verify 实测**:`bin/ss run tools/sunset_linter.ss`(本 commit 后跑)→
- C1 collection: 23 SUNSET markers (-2 net from 25,本 sub-round 2 marker 物理清;Phase 2 起手 sub-round 首例 SUNSET 物理清累计 2 处)
- 默认模式 GATE OK + exit 0
- C5/C6/C8/C9 软警告维持 baseline (C5 0 / C6 0 / C8 43 / C9 3)
- (sibling Phase 4 续 super resolve 92fd7c3/eedbfa0 + Phase 4 续 genVal 主 dispatch 060e4e4/5118424 + Phase 4 续 ARROW_FUNC 33c65e2/7c0bbcf + Phase 4 起首 THIS/SUPER 21f8342/7d66eb4 + 1.5e+ later evalExpr 余下 leaf case 收口 c233add/90aba70 + 1.5e+ later UNARY 2c1fb98/6dbc2de + 1.5e+ ext b0f8d12/f9360f9 默认模式 verify 同 path;1.5d 主轮收口子步 sibling 子轮 b94dc8b/609b465 用 `--phase` manual gate;两 path 均 D170 §决策 C 双轨触发设计预期内)

**D170 step 7 实战首例 sibling 范式实战完结升级**(9 sub-round 系列:b94dc8b/609b465 + b0f8d12/f9360f9 + 2c1fb98/6dbc2de + c233add/90aba70 + 21f8342/7d66eb4 + 33c65e2/7c0bbcf + 060e4e4/5118424 + 92fd7c3/eedbfa0 + 0fb2fa1/<本 commit>):D170 协议**实战首例 + 第二例 + 第三例 + 第四例 + 第五例 + 第六例 + 第七例 + 第八例 + 第九例 sibling 闭环** — phase exit verify 双轨(manual gate `--phase X` + 默认模式)九轮均落地实战,sibling 应用模板可重复;**Phase 2 sub-round 系列起手首例 + 跨 phase sibling 模板复用 robustness 起手验证**:本轮 sibling 第九例是 Phase 2 sub-round 系列**起手首例**(Phase 4 sub-round 系列完结后 — sibling 第五/六/七/八例 21f8342/33c65e2/060e4e4/92fd7c3 + 1.5f 主轮收口 d049308),验证 Phase 2 类 A 入口字面消除 sub-round 的 sibling 应用模板可起手 robustness 起手首例(sibling 第五-八例 Phase 4 接力 + 本轮 Phase 2 起手 — D170 协议 sibling 应用模板跨 phase 复用 robustness 跨 Phase 2 起手验证升级)— D093 §第一性需求 "comptime 块降为 `comptimeMustBeKnown` flag" 物理落地最终态 + Zig SEMA single-dispatch 路径同形落地接力推进类 A 端起手(类 B 端 1.5f 完结后类 A 端起手 — D093 §三类混合 line 186 "类 B 必须先于类 A 大规模消除"先决条件达成;后续 stmts_simple/stmts_exc/stmts.ss + gen_decls/gen_assigns + ct_driver/interp_core/eval_expr 残余 marker 系列 + Phase 8 类 C 合理边界检查审计接续)

## Phase 2 续子轮起手 stmts_branch if/switch 入口 ct-depth 字面消除 §出口清单 (D170 §决策 C exit 动作 §2 sibling 第十例应用)

D170 协议 step 7 实战首例 (b94dc8b/609b465) → sibling 第二例 (b0f8d12/f9360f9) → sibling 第三例 (2c1fb98/6dbc2de) → sibling 第四例 (c233add/90aba70) → sibling 第五例 (21f8342/7d66eb4) → sibling 第六例 (33c65e2/7c0bbcf) → sibling 第七例 (060e4e4/5118424) → sibling 第八例 (92fd7c3/eedbfa0) → sibling 第九例 (0fb2fa1/8f45c3e) → **本轮 sibling 第十例应用** — Phase 2 续子轮起手 stmts_branch if/switch 入口 ct-depth 字面消除完结后,**2 SUNSET marker 物理清**(stmts_branch.ss:11+62 if/switch 入口 ct-depth 字面),`sunset_linter` 默认模式 verify GATE OK 21 markers(-2 net from 23 baseline,sibling 应用模板 ≥1 marker 形态延续 sibling 第三/五/六/七/八/九例同模式 + Phase 2 续子轮第二例 SUNSET 物理清 2 处)。本段是 D170 §决策 C exit 动作清单 §2 落地 sibling 第十例,后续 sibling phase exit 沿用本格式:

| # | marker file:line | resolve 标签 | commit hash |
|---|---|---|---|
| 1 | `bootstrap/gen/stmts/stmts_branch.ss:11` (if 入口 ct-depth 字面 SUNSET) | `[clean]` 代码已删 — line 11 SUNSET marker 删 + line 12 字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1`,SUNSET marker 物理清(D170 §决策 A canonical marker 模板 — Phase 2 sub-round 完结后 marker 不再需要;sibling stmts_core.ss:26+37 break/continue 0fb2fa1 + exprs.ss:71 THIS/SUPER 21f8342 + exprs.ss:75 ARROW_FUNC 33c65e2 + exprs.ss:79 genVal 主 dispatch 060e4e4 + gen_methods.ss:258 super resolve 92fd7c3 同模式) | 2497a08 |
| 2 | `bootstrap/gen/stmts/stmts_branch.ss:62` (switch 入口 ct-depth 字面 SUNSET) | `[clean]` 代码已删 — line 62 SUNSET marker 删 + line 63 字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1`,SUNSET marker 物理清(D170 §决策 A canonical marker 模板 — Phase 2 sub-round 完结后 marker 不再需要;sibling if 同模式 + Phase 2 续子轮第二例 SUNSET 物理清累计 2 处) | 2497a08 |

**Manual gate verify 实测**:`bin/ss run tools/sunset_linter.ss`(本 commit 后跑)→
- C1 collection: 21 SUNSET markers (-2 net from 23,本 sub-round 2 marker 物理清;Phase 2 续子轮第二例 SUNSET 物理清累计 2 处)
- 默认模式 GATE OK + exit 0
- C5/C6/C8/C9 软警告维持 baseline (C5 0 / C6 0 / C8 43 / C9 3)
- (sibling Phase 2 起手 sub-round stmts_core 0fb2fa1/8f45c3e + Phase 4 续 super resolve 92fd7c3/eedbfa0 + Phase 4 续 genVal 主 dispatch 060e4e4/5118424 + Phase 4 续 ARROW_FUNC 33c65e2/7c0bbcf + Phase 4 起首 THIS/SUPER 21f8342/7d66eb4 + 1.5e+ later evalExpr 余下 leaf case 收口 c233add/90aba70 + 1.5e+ later UNARY 2c1fb98/6dbc2de + 1.5e+ ext b0f8d12/f9360f9 默认模式 verify 同 path;1.5d 主轮收口子步 sibling 子轮 b94dc8b/609b465 用 `--phase` manual gate;两 path 均 D170 §决策 C 双轨触发设计预期内)

**D170 step 7 实战首例 sibling 范式实战完结升级**(10 sub-round 系列:b94dc8b/609b465 + b0f8d12/f9360f9 + 2c1fb98/6dbc2de + c233add/90aba70 + 21f8342/7d66eb4 + 33c65e2/7c0bbcf + 060e4e4/5118424 + 92fd7c3/eedbfa0 + 0fb2fa1/8f45c3e + 2497a08/<本 commit>):D170 协议**实战首例 + 第二例 + 第三例 + 第四例 + 第五例 + 第六例 + 第七例 + 第八例 + 第九例 + 第十例 sibling 闭环** — phase exit verify 双轨(manual gate `--phase X` + 默认模式)十轮均落地实战,sibling 应用模板可重复;**Phase 2 sub-round 系列续手第二例 + Phase 2 内部多 sub-round 复用 robustness 累计第二例**:本轮 sibling 第十例是 Phase 2 sub-round 系列**续手第二例**(sibling 第九例 0fb2fa1 stmts_core 起手后),验证 Phase 2 类 A 入口字面消除 sub-round 的 sibling 应用模板可在 Phase 2 内部多 sub-round 复用 robustness 累计第二例(sibling 第九例 Phase 2 起手 + 本轮 Phase 2 续子轮 — D170 协议 sibling 应用模板 Phase 内复用 robustness 跨 sub-round 累计验证升级)— D093 §第一性需求 "comptime 块降为 `comptimeMustBeKnown` flag" 物理落地最终态 + Zig SEMA single-dispatch 路径同形落地接力推进类 A 端大规模消除(类 A 端起手随后续手 — D093 §三类混合 line 186 "类 B 必须先于类 A 大规模消除" 先决条件已达成 0fb2fa1;后续 stmts_simple/stmts_loop_forin/stmts.ss/stmts_exc + gen_decls/gen_assigns + ct_driver/interp_core/eval_expr 残余 marker 系列 + Phase 8 类 C 合理边界检查审计接续)

## Phase 2 续子轮第三例 stmts_exc try/catch + throw 入口 ct-depth 字面消除 §出口清单 (D170 §决策 C exit 动作 §2 sibling 第十一例应用)

D170 协议 step 7 实战首例 (b94dc8b/609b465) → sibling 第二例 (b0f8d12/f9360f9) → sibling 第三例 (2c1fb98/6dbc2de) → sibling 第四例 (c233add/90aba70) → sibling 第五例 (21f8342/7d66eb4) → sibling 第六例 (33c65e2/7c0bbcf) → sibling 第七例 (060e4e4/5118424) → sibling 第八例 (92fd7c3/eedbfa0) → sibling 第九例 (0fb2fa1/8f45c3e) → sibling 第十例 (2497a08/daad6d4) → **本轮 sibling 第十一例应用** — Phase 2 续子轮第三例 stmts_exc try/catch + throw 入口 ct-depth 字面消除完结后,**2 SUNSET marker 物理清**(stmts_exc.ss:28+174 try/catch + throw 入口 ct-depth 字面),`sunset_linter` 默认模式 verify GATE OK 19 markers(-2 net from 21 baseline,sibling 应用模板 ≥1 marker 形态延续 sibling 第三/五/六/七/八/九/十例同模式 + Phase 2 续子轮第三例 SUNSET 物理清 2 处)。本段是 D170 §决策 C exit 动作清单 §2 落地 sibling 第十一例,后续 sibling phase exit 沿用本格式:

| # | marker file:line | resolve 标签 | commit hash |
|---|---|---|---|
| 1 | `bootstrap/gen/stmts/stmts_exc.ss:28` (try/catch 入口 ct-depth 字面 SUNSET) | `[clean]` 代码已删 — line 28 SUNSET marker 删 + line 29 字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1`,SUNSET marker 物理清(D170 §决策 A canonical marker 模板 — Phase 2 sub-round 完结后 marker 不再需要;sibling stmts_branch.ss:11+62 if/switch 2497a08 + stmts_core.ss:26+37 break/continue 0fb2fa1 + exprs.ss:71 THIS/SUPER 21f8342 + exprs.ss:75 ARROW_FUNC 33c65e2 + exprs.ss:79 genVal 主 dispatch 060e4e4 + gen_methods.ss:258 super resolve 92fd7c3 同模式) | e1a1eaf |
| 2 | `bootstrap/gen/stmts/stmts_exc.ss:174` (throw 入口 ct-depth 字面 SUNSET) | `[clean]` 代码已删 — line 174 SUNSET marker 删 + line 175 字面 rename `comptimeDepth > 0 → comptimeMustBeKnown == 1`,SUNSET marker 物理清(D170 §决策 A canonical marker 模板 — Phase 2 sub-round 完结后 marker 不再需要;sibling try/catch 同模式 + Phase 2 续子轮第三例 SUNSET 物理清累计 2 处 + throw 内已有 `isCt == 1` loud-error 分支完整保留无 silent path 升级需求) | e1a1eaf |

**Manual gate verify 实测**:`bin/ss run tools/sunset_linter.ss`(本 commit 后跑)→
- C1 collection: 19 SUNSET markers (-2 net from 21,本 sub-round 2 marker 物理清;Phase 2 续子轮第三例 SUNSET 物理清累计 2 处)
- 默认模式 GATE OK + exit 0
- C5/C6/C8/C9 软警告维持 baseline (C5 0 / C6 0 / C8 43 / C9 3)
- (sibling Phase 2 续子轮起手 stmts_branch 2497a08/daad6d4 + Phase 2 起手 sub-round stmts_core 0fb2fa1/8f45c3e + Phase 4 续 super resolve 92fd7c3/eedbfa0 + Phase 4 续 genVal 主 dispatch 060e4e4/5118424 + Phase 4 续 ARROW_FUNC 33c65e2/7c0bbcf + Phase 4 起首 THIS/SUPER 21f8342/7d66eb4 + 1.5e+ later evalExpr 余下 leaf case 收口 c233add/90aba70 + 1.5e+ later UNARY 2c1fb98/6dbc2de + 1.5e+ ext b0f8d12/f9360f9 默认模式 verify 同 path;1.5d 主轮收口子步 sibling 子轮 b94dc8b/609b465 用 `--phase` manual gate;两 path 均 D170 §决策 C 双轨触发设计预期内)

**D170 step 7 实战首例 sibling 范式实战完结升级**(11 sub-round 系列:b94dc8b/609b465 + b0f8d12/f9360f9 + 2c1fb98/6dbc2de + c233add/90aba70 + 21f8342/7d66eb4 + 33c65e2/7c0bbcf + 060e4e4/5118424 + 92fd7c3/eedbfa0 + 0fb2fa1/8f45c3e + 2497a08/daad6d4 + e1a1eaf/<本 commit>):D170 协议**实战首例 + 第二例 + 第三例 + 第四例 + 第五例 + 第六例 + 第七例 + 第八例 + 第九例 + 第十例 + 第十一例 sibling 闭环** — phase exit verify 双轨(manual gate `--phase X` + 默认模式)十一轮均落地实战,sibling 应用模板可重复;**Phase 2 sub-round 系列续手第三例 + Phase 2 内部多 sub-round 复用 robustness 累计第三例**:本轮 sibling 第十一例是 Phase 2 sub-round 系列**续手第三例**(sibling 第九例 0fb2fa1 stmts_core 起手 + 第十例 2497a08 stmts_branch 续手后),验证 Phase 2 类 A 入口字面消除 sub-round 的 sibling 应用模板可在 Phase 2 内部多 sub-round 复用 robustness 累计第三例(sibling 第九例 Phase 2 起手 + 第十例 Phase 2 续子轮 + 本轮 Phase 2 续子轮第三例 — D170 协议 sibling 应用模板 Phase 内复用 robustness 跨 sub-round 累计验证升级)— D093 §第一性需求 "comptime 块降为 `comptimeMustBeKnown` flag" 物理落地最终态 + Zig SEMA single-dispatch 路径同形落地接力推进类 A 端大规模消除(类 A 端起手随后续手 — D093 §三类混合 line 186 "类 B 必须先于类 A 大规模消除" 先决条件已达成 0fb2fa1;后续 stmts.ss/stmts_loop_forin/stmts_simple + gen_decls/gen_assigns + ct_driver/interp_core/eval_expr 残余 marker 系列 + Phase 8 类 C 合理边界检查审计接续)

- **[x] Done at D098(2026-04-18)+ D099(2026-04-18)+ D111(2026-04-18)+ D112(2026-04-20)+ D117(2026-04-21)** D093 §张力 1-3 "MaybeVal / InternPool / Type-as-Value 详设" — 由 D098 §决策 1/2/3 集中决策,各 Phase 已落地:D098 §决策 1 §Phase A 编码 → D099 落 eval_expr.ss:1-5 + gen/gen_maybeval.ss;D098 §决策 2 §Phase B Meta InternPool → D117 落;D098 §决策 3 Phase A `comptimeTypeAliases` 消除 → D112 落。**D094 由 D098 承接 supersede**,InternPool/Type-as-Value 不再另起 D094
- **[x] Done at 2026-05-20** 验证 D093 骨架所需的 SS 语言能力缺口 — 见 D169 §SS 语言能力前置实证修订版(全局 int flag / error 机制 / 顶层 helper 函数 / int 符号位运算 全部齐备)

**本节落档后,Phase 1.5a 起 Execute 轮**;每 sub-phase 完成时双轨必须**局部消除**(不保留过渡态,§张力 #4 联动)。
