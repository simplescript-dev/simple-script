# D162: class extends class + interface impl — D025 walk classParents 漏检修根因

**Status:** [x] Phase 0 D 文档落档 at commit `418fe5d` + [x] Phase 1 parser 验证 extends + : 组合语法 + ≥3 case spike GREEN at commit `f866c11` + [x] Phase 2 checker walk classParents 修法 + ≥6 case codegen spike GREEN at commit `d6e1851` + [x] Phase 3 D160 Phase 2 真 extends 形态回退验证 + SS 编译器 vtable + non-0-arg overload child class bug 修法(F7 wire 落地)+ ≥12 case spike GREEN at commit `7d91d73` + [x] Phase 4 D162 主线 close + D160 §F8 跨 D 起首回填(D162 Phase 3 hash `7d91d73` 回填 D162.md 5 处 + D160.md Phase 2 hold 锚解锁 + §F8 row 回填 + Status 时间线 entry)at commit `<phase4-hash>` + [x] D162 主线 close at commit `<phase4-hash>` — D161 主线 close → D160 Phase 1 重启 → D162 起首脱胎,D135-D161 编译器/SQL 主线范式延续。修 SS 编译器 `bootstrap/checker/check_class.ss:62-94 checkInterfaceImpl` D025 vtable check 漏 walk classParents 链根因 — `class MysqlCallableStatement extends MysqlPreparedStatement : CallableStatement` 触发 12 处 `missing method 'executeUpdate' / 'executeQuery' / ...` 假阳报错(父类 MysqlPreparedStatement 已实现 13 PreparedStatement method,checkInterfaceImpl 不 walk classParents 不识别)— 不接受 re-declare 13 method workaround / composition pattern 替代 / D025 check 跳过 walk 节省。

## 起首脱胎
- D161 主线 close at commit `6cfb300`(interface extends 编译器路径)→ D160 Phase 1 重启 at commit `3bd4d61`(interface CallableStatement extends PreparedStatement 21 own method 形态)→ D160 Phase 2 落地实测发现 `class C extends Parent : Interface` 触发 D025 vtable check 假阳 → D162 起首脱胎(class extends class + interface impl 编译器路径)
- D135-D161 编译器层 + SQL 主线范式延续(Phase 计划独立 commit + Status 收关 + hash 回填 + next_prompt 自闭环 + §A.2 隐藏假设挑战 + §A.3 废案 + §Followup)
- Depends on:D161(interface extends 编译器路径主线 close — parser parseInterfaceDecl 加 EXTENDS + ifaceParents Map + walk parent chain merge)+ D025(vtable check 范式 — check_class.ss checkInterfaceImpl 强制 implementor class 必须实现 interface methods)+ D071(abstract method walk classParents 修法范式 — check_class.ss checkAbstractImpl 已 walk classParents 链合并 concrete method 集合,本 D 是其在 interface vtable check 端的姊妹修法)
- D160 Phase 2 hold 等本 D 主线 close 后回头落地 — `class MysqlCallableStatement extends MysqlPreparedStatement : CallableStatement` 真 extends 形态(继承父类 14 字段 + 13 PreparedStatement method,加 3 own 字段 + 21 own method + getParameterMetaData override),自动满足 D025 vtable check

## 核心目标 (Goal)

落地后:
1. `bootstrap/checker/check_class.ss:62-94 checkInterfaceImpl` 加 walk classParents 链 — 子类 implementor class 走 `class C extends Parent : Interface` 时,checkerError 假阳消除(父类 own methods 自动满足子类 interface vtable)
2. `class C extends Parent : Interface` 组合语法 parser 范式锚定(parseClassDecl line 427-441 已支持,但仓内首次系统性使用 — D162 §A.2 H1 实证锚)
3. 多层 chain(`class GrandChild extends Child : Interface` 其中 Child extends Parent)走 classParents 链全集传递(Phase 2 ≥6 case codegen spike 验证)
4. 子类 override 父类 method 时 D025 走子类签名(子赢 — Phase 2 §A.2 H3 实证)
5. 父类已实现 method 子类不必 re-declare 即满足 D025(`class C extends Parent : Interface` 的根因解决路径,与 D161 walk-ifaceParents 同形 — 本 D 是 walk-classParents 的姊妹修法)
6. D160 Phase 2 真 extends 形态回退验证(`class MysqlCallableStatement extends MysqlPreparedStatement : CallableStatement` 编译通过 + Phase 2 spike ≥12 case GREEN + bin/ss test tests/ baseline 全继承)
7. D025 negative anchor 验证(`class BadChild extends Parent : Interface { 仅父类不实现 + 子类不实现的 method }` → exit=1 stderr 含 missing method 错误 — 走 walk classParents 后仍强制全集实现)
8. `./build.sh bootstrap` 三阶段固定点 stage2==stage3(checker.ss 改动必走 self-bootstrap 验证 — §A.2 H5)+ `bin/ss test tests/` baseline 全继承 + reflection_health_linter 全 14 指标无 regression + d_doc_index_linter F1 = 0 + next_prompt_ultrathink_linter PASS

**RED**(本 D 文档落档前实测):
- `ls docs/3-decisions/D162-class-extends-interface-impl.md` = ENOENT(D162 不存在,起首必新建)
- `bin/ss build prepared.ss with class MysqlCallableStatement extends MysqlPreparedStatement : CallableStatement` 实测 = exit=1 stderr 含 12 处 `error: class 'MysqlCallableStatement' missing method '<X>' required by interface 'CallableStatement'`(executeUpdate / executeQuery / getGeneratedKeys / getLastInsertId / close / setInt / setLong / setString / setDouble / setBoolean / setNull / setFetchSize 全 13 inherited PreparedStatement method 假阳报错 — 父类已全实现)
- `grep -nE "function checkInterfaceImpl" bootstrap/checker/check_class.ss` 实测 = 62(line 62-94 主体,classMethods 仅本类 own,无 walk classParents)
- `grep -c "<phase4-hash>" docs/3-decisions/D161-interface-extends.md` 实测 = 0(D161 Phase 4 hash `6cfb300` 已回填,本 D 起首不需回填 D161)
- `grep -c "<D162-phase0-hash>" docs/3-decisions/D160-callable-statement-out-inout.md` 实测 ≥1(D160 Status 时间线 D162 起首脱胎 entry 待本 Phase 0 commit 回填)

## 核心原则 (Principles)

1. **修编译器根因不接 workaround / 节省路径**(用户对话锁不接次优;CLAUDE.md "Root Cause 优先 — 不接受次优 / workaround / 节省" 第一法则):D160 Phase 2 不允许 re-declare 13 method workaround,不允许走 composition pattern 规避 extends,不允许跳过 D025 check 让 walk 假错放过 — 真 extends 形态走 D025 walk classParents 修根因
2. **复用 D161 walk-ifaceParents 修法范式**:D161 §Phase 2 加 walk ifaceParents 链 merge ifaceMethods 是子接口走父接口 method 集合的修法;本 D 是其镜像 — implementor class 走 walk classParents 链 merge ownMethods 满足 interface vtable
3. **复用 D071 walk-classParents 修法范式**:`bootstrap/checker/check_class.ss:97 checkAbstractImpl` 已实现 walk classParents 合并 concrete methods 满足 abstract method 强制实现要求,本 D 是其在 interface vtable check 端的姊妹修法 — checkInterfaceImpl 应同步 walk
4. **bootstrap fixed-point 自举安全**:checker 改动必走 self-bootstrap 三阶段 stage2==stage3 bit-identical(§A.2 H5)— 本 D 不动 codegen / parser,仅改 checker 单文件,自举影响范围小
5. **dispatch 时已 walk classParents,本 D 仅补 check 假阳**:实际 dispatch 路径(method_call.ss)走 classParents chain 命中父类 method,wire 端可执行;D025 check 端假阳是 check_class.ss checkInterfaceImpl 单点缺陷 — 本 D 仅修 check 端,不动 dispatch 端
6. **D135-D161 主线范式延续**:Phase 计划独立 commit + Status 收关 + hash 回填 + next_prompt 自闭环 + §A.2 隐藏假设 + §A.3 废案 + §Followup
7. **D025 negative anchor 不削弱 walk 后**:walk classParents 合并后,父类 + 子类全集仍未实现 interface method 必须 exit=1 — 不允许 walk 让真错放过,Phase 2 spike _negative test doc-anchor 验证
8. **scope 限单 sub-D**:本 D 仅修 checkInterfaceImpl 单函数 + 加 helper walk(~15-30 LOC),不引入 codegen 改动 / parser 改动 / 全局 method-collection refactor — `class C extends A, B`(多继承)留 §Followup F2 远期
9. **业界对标 Java + TS extends + implements 必须正确处理父类 method 满足子类 interface**:Java `class C extends Parent implements I` 父类 own methods 自动满足子类 interface vtable;TS `class C extends Parent implements I` 同形;Kotlin `class C : Parent(), I` 同形 — SS `class C extends Parent : Interface` 必须达到同样语义,本 D 是必需的根因补齐(N 年返工度 100%,后续每条 sub-D class extends class + : interface 必依赖)

## A.1 主候选评估(§MNK §M §字段 10)

| 候选 | 层次 | 含 | 不含 | 决策 |
|------|------|-----|------|------|
| **C1** | 数据层 patch | + 在 D160 内 re-declare 13 PreparedStatement method workaround(子类显式重新声明每个 method 复制父类同体)| 编译器层无修;后续每条 `class C extends Parent : Interface` 必继续 re-declare workaround;CLAUDE.md "Root Cause 优先 — 同一个 workaround 出现第二次必须停下修根因" 第一法则触发(D160 Phase 2 是首次,但单 D 内重复 13 次 ≫ 2 次门槛 + 类型已知必复 5+ 次后续 sub-D)| **不选** — 用户对话锁不接次优 / workaround / 节省;CLAUDE.md 第一法则触发;Java/TS 业界对标必修编译器 |
| **C2** | **接口层 trap(根因)** | + bootstrap/checker/check_class.ss checkInterfaceImpl 加 walk classParents 链合并 ownMethods + parentMethods 集合后再校验 interface vtable + Phase 1 parser 验证 extends + : 组合语法可执行 + Phase 2 ≥6 case codegen spike(单层 + 多层 chain + override + 同名不同 paramSig + multi-iface + D025 negative anchor)+ Phase 3 D160 Phase 2 真 extends 形态回退验证 ≥12 case GREEN | 多继承 `class C extends A, B`(SS 不支持多继承,留 §F2 远期)+ codegen 端显式 merge methods(method_call.ss 已 walk chain dispatch,本 D 仅修 check 端假阳,留 §F1 远期)+ abstract method walk(D071 已修,本 D 仅 interface vtable 端补齐) | **选** — 根因解决:CLAUDE.md "Root Cause 优先" 第一法则 + Java/TS 业界对标必修(`extends Parent implements I` 父类 own methods 自动满足子类 interface vtable)+ D161 walk-ifaceParents 修法范式延续(本 D 是其镜像 — walk-classParents)+ D071 walk-classParents 修法范式延续(checkAbstractImpl 已 walk,本 D 在 checkInterfaceImpl 同步)+ N 年返工度 100%(后续每条 sub-D class extends class + : interface 必依赖)+ scope 中等可控(单函数 + 单 helper ~15-30 LOC + Phase 1-3 spike + Phase 4 close)+ D135-D161 主线范式延续 |
| **C3** | 架构层 refactor | + checkInterfaceImpl 完整 + 全局统一 method-collection 抽象(walk classParents + walk ifaceParents 一并处理)+ codegen 端显式 merge methods + multi-extends `class C extends A, B` 多继承支持 + D025 check 跳过 walk 让 abstract / final 修饰符控制 | spec 100%;Java + TS + Kotlin + Scala 全语法套件;多继承 + Wrapper.unwrap 跨 D 通用 + abstract / final 修饰符全套 | **不选** — scope 远超 D162 单 sub-D(LOC > 1000;多继承独立 sub-D + 全局抽象 + abstract/final 修饰符独立 sub-D + Wrapper.unwrap 跨 D 通用) |

**决策行**:**选 C2 接口层 trap(根因)** — 因 (a) CLAUDE.md "Root Cause 优先" 第一法则 + 不接次优 / workaround / 节省路径(D160 Phase 2 不允许 re-declare 13 method workaround);(b) Java/TS 业界对标(`class C extends Parent implements I` 父类 own methods 自动满足子类 interface vtable 是 Java + TS + Kotlin + Scala 全部支持的核心特性,SS 缺它必拖累后续每条 sub-D 用 class extends class + : interface 形态);(c) D161 walk-ifaceParents 修法范式镜像(D161 §Phase 2 加 walk ifaceParents 链 merge ifaceMethods 是子接口走父接口 method 集合的修法;本 D 是其姊妹 — implementor class 走 walk classParents 链 merge ownMethods 满足 interface vtable);(d) D071 walk-classParents 修法范式延续(checkAbstractImpl 已实现 walk classParents 合并 concrete methods 满足 abstract method 强制实现要求,本 D 是其在 interface vtable check 端的姊妹修法 — checkInterfaceImpl 应同步 walk);(e) **N 年返工度 100%**:不做 → 每条 `class C extends Parent : Interface` 形态全部 re-declare workaround,SQL 主线 D160 / D154 / D155 driver class 演化路径必拖累;(f) scope 中等可控(单函数 ~15-30 LOC + Phase 1-3 spike 验证 + Phase 4 close,5 commit 切片);(g) D135-D161 主线范式延续(Phase 计划独立 commit + Status 收关 + hash 回填 + next_prompt 自闭环 + §A.2 隐藏假设 + §A.3 废案 + §Followup)。**为何不选 C1**:CLAUDE.md 第一法则明确触发(D160 Phase 2 单 D 内重复 13 次 + 后续 sub-D 必复 5+ 次),用户对话锁不接次优 / workaround / 节省。**为何不选 C3**:scope 远超 D162 单 sub-D — 多继承 / 全局 method-collection 抽象 / abstract/final 修饰符全套 / Wrapper.unwrap 跨 D 通用 各留 §Followup 独立 sub-D。

## A.2 隐藏假设挑战

| H | 假设 | 挑战 | 实证锚 |
|---|------|------|--------|
| H1 | parser parseClassDecl 已支持 `class C extends Parent : Interface` 组合语法(line 427-441 EXTENDS + COLON 双子句解析)| parseClassDecl line 427-431 解析 EXTENDS 子句存 extendsName,line 435-441 解析 COLON 子句存 implList,二者顺序固定(EXTENDS 在前 COLON 在后)— 本 D 仓内首次系统性使用此组合形态,需 Phase 1 parser 节点 unit test 验证 nGetS2(extendsName)+ nGetS3(implList)双 slot 全填 | Phase 1 ≥3 case spike — Case 1 `class C extends Parent : I` 双 slot 全填 / Case 2 `class C extends Parent : I, J` 多 interface impl / Case 3 `class C extends Parent` 仅 extends 无 implList 兼容路径 |
| H2 | classMethodNames Map 已存父类 own methods(class_register.ss 已 set classMethodNames[parentName])— checkInterfaceImpl 加 walk classParents 后可直接复用,不需要 codegen 层改 | bootstrap/gen/class/class_register.ss line 198 resolveInheritance() 已为 classParents 链每个父类 set classMethodNames,本 D Phase 2 修法仅在 checkInterfaceImpl 端 walk classParents 链查 classMethodNames 合并 ownMethods 即可,不动 codegen / parser | Phase 2 spike Case 1 单层 `class Child extends Parent : I` 父类 method 走 D025 通路 + Case 2 多层 chain(GrandChild → Child → Parent)父+祖父 method 全集合并 |
| H3 | 多层 chain(`class GrandChild extends Child : Interface` 其中 Child extends Parent)walk 链全集传递 + idempotent | Phase 2 修法 walk classParents 链直到 ""(根类),visited 守 self-loop hang(同 D161 §Phase 2 visited Map 防御范式)— 真实场景 SS 不支持多继承,链结构是树而非 DAG,但 visited 守仍必要防 `class A extends A` 编程错误 self-loop | Phase 2 spike Case 2 多层 chain — `class GrandChild extends Child : I` 其中 Child extends Parent,Parent 实现 I 的部分 method,Child 实现剩余,GrandChild 不 re-declare 任何 method 走 D025 通路 + visited 守不 hang |
| H4 | 子类 override 父类 method 时 D025 走子类签名(子赢)| 子类 own method 与父类 method 同名同签名 → ownMethods 集合包含子类 method,walk 父类时 dedupe(已在 ownMethods 中跳过 add)— 子赢自然 fall-through;同名不同签名 → 双条 method 不 dedupe(method overloading,各自 mangled key 不同) | Phase 2 spike Case 3 子类 override `class Child extends Parent : I { function foo(): int { return 42 } }` 父类 foo 返 0,子类 foo 返 42 — D025 通路 + Case 4 同名不同 sig overload 不 dedupe(`function foo()` vs `function foo(x: int)` 父子各持) |
| H5 | bootstrap fixed-point 自举安全 — checker.ss 改动必走 self-bootstrap 三阶段 stage2==stage3 bit-identical 验证 | check_class.ss 改动直接影响 bootstrap 自身编译过程(bootstrap/ 内 class 用 D025 vtable check),修法必须 idempotent + 不改 method merge 顺序破坏现有 bootstrap;`./build.sh bootstrap` 三阶段固定点必走过 | Phase 2 修法落地后 `./build.sh bootstrap` GREEN(stage2 md5 == stage3 md5);Phase 1 parser 不改不需自举验证(parser 已支持组合语法,Phase 1 仅 spike test 验证) |

## A.3 废案

- **C1 数据层 patch**(用户对话锁不接次优;D160 Phase 2 单 D 内重复 re-declare 13 method workaround + 后续 sub-D 必复 5+ 次,CLAUDE.md "同一个 workaround 出现第二次必须停下修根因" 第一法则触发)
- **C3 架构层 refactor + 多继承 + 全局 method-collection 抽象 + abstract/final 修饰符全套**(scope 远超 D162 单 sub-D — 各独立 sub-D 留 §Followup F1-F4)
- **D025 check 跳过 walk 让 abstract / final 修饰符控制**(违反 §核心原则 7 — walk 不削弱 D025,真错仍 exit=1;跳过 walk 是放任假阳兼隐藏真错,不可接受)
- **composition pattern 替代 extends**(违反 §核心原则 1 — 用户对话锁不接 workaround;`class MysqlCallableStatement : CallableStatement { inner: MysqlPreparedStatement; ... 13 method delegations }` 是 OO composition over inheritance,但回避了 SS 编译器 D025 check 缺陷,不修根因;本 D 走真 extends 形态走根因修法)
- **D162 与 D160 Phase 2 同 commit 落地**(scope 失控 — D162 是编译器层根因 + D160 Phase 2 是应用层 driver class 真实现,二者分开切片;D162 主线 close 后 D160 Phase 2 重启 commit 跨 D 起首回填范式延续)
- **walk classParents 也加 walk ifaceParents on impl side**(scope 远超 — D161 §Phase 2 已修 walk-ifaceParents 子接口走父接口,本 D 仅修 walk-classParents 子类走父类;两个独立修法,合并会破 scope)
- **codegen 端显式 merge methods**(method_call.ss 已通过 classParents chain dispatch 命中父类 method,wire 端已可执行;本 D 仅修 check 端假阳,codegen 不动 — 留 §Followup F1 远期评估)
- **parser parseClassDecl 改支持 EXTENDS 在 COLON 后**(parser 历史顺序固定 EXTENDS 在 COLON 前,改顺序破坏向后兼容 — 留 §Followup F3 远期独立 sub-D)
- **abstract method walk classParents 重写**(D071 已实现 checkAbstractImpl walk classParents 合并 concrete methods 集合,本 D 不重写 D071 修法 — 仅在 checkInterfaceImpl 同步 walk 范式;D071 范式直接复用)
- **强制把已有 `: Interface` 单 implList 形态全部回退到 `extends Parent : Interface`**(scope 失控 — 现有 lib/ 仓内多处 `class MysqlConnection : Connection` / `class MysqlStatement : Statement` 等单 implList 形态稳定;本 D 仅启用 `extends Parent : Interface` 形态可行不强制回退;留 §Followup F4 simplify 评估远期)

## Phase commit hash 总览

| Phase | 内容 | Commit |
|-------|------|--------|
| 0 | D 文档落档(§核心目标 + §核心原则 + §A.1-A.3 + §Phase 收关锚 Phase 0-4 + §Followup F1-F4)+ D161 §F8 entry 末尾"等下轮"指向 D162 + D160.md Status header Phase 2 hold 锚 + D160.md §Followup F8 row + D160.md Status 时间线 D162 起首脱胎 entry | `418fe5d` |
| 1 | parser 验证 extends + : 组合语法 + ≥3 case spike GREEN(class extends class + : interface 同时存在的 parser 节点验证 — nGetS2/extendsName + nGetS3/implList 双 slot 全填) | `f866c11` |
| 2 | bootstrap/checker/check_class.ss checkInterfaceImpl walk classParents 链合并 ownMethods + parentMethods 集合(inline walk + visited 守 self-loop)+ 6 case codegen spike GREEN(单层 + 多层 chain + 子赢 override + overload 不 dedupe + multi-iface + D160 mini)+ D025 negative anchor + ./build.sh bootstrap 三阶段固定点 stage2==stage3 PASS + 跨 D 起首回填 D162 Phase 1 hash `f866c11` 至 D162.md ≥4 处实际语义位 | `d6e1851` |
| 3 | D160 Phase 2 真 extends 形态回退验证(`class MysqlCallableStatement extends MysqlPreparedStatement : CallableStatement` + paramDirections / outRow / lastWasNull 3 own 字段 + 19 own method 真实现 + 1 override getParameterMetaData + injectOutVarSetters / appendOutVarSelectors / rewriteCallSqlPlaceholders SQL 重写 helper + buildCallableStatement / doPrepareCall factory + MysqlConnection.prepareCall stub → doPrepareCall + NoopDate / NoopTime move 至 driver_types.ss + MysqlParameterMetaData 升级 paramDirections 字段 + getParameterMode 三态反射 + ≥12 case spike GREEN)+ **F7 SS 编译器 vtable + non-0-arg overload child class bug 修法 wire 落地**(bootstrap/gen/class/class.ss 加 classMethodHasPlainFn Map state + bootstrap/gen/gen_registry.ss registerClassMethodRetType 加 mSig=="" set logic + bootstrap/gen/gen_type_ops.ss buildVtableForClass 加 isOverloaded + classMethodHasPlainFn.has=0 skip slot logic + bootstrap/gen/methods/gen_methods.ss vtable indirect dispatch 改 raw `ptr` GEP 避开 struct type 前向引用 + ./build.sh bootstrap 三阶段固定点 stage2==stage3 PASS)| `7d91d73` |
| 4 | D162 主线 close 锚 + D160 §F8 跨 D 起首回填(D162 主线 close hash 回填至 D160.md §F8 row + D160.md Status 时间线 D162 起首脱胎 entry hash) | `<phase4-hash>` |

## Phase 收关锚

### Phase 0: D 文档落档 [x] Done at commit `418fe5d`

- 落地 `docs/3-decisions/D162-class-extends-interface-impl.md`(本文件)— §核心目标 + §核心原则 + §A.1 候选评估 + §A.2 隐藏假设 H1-H5 + §A.3 废案 + §Phase 收关锚 Phase 0-4 + §Followup F1-F4 + §Status 时间线
- D161.md F8 entry 末尾"等下轮"指向 D162 起首脱胎(已在本 Phase 同 commit 修)+ D160.md Status header Phase 2 hold 锚 + D160.md §Followup F8 row + D160.md Status 时间线 D162 起首脱胎 entry
- 落地 `.claude/next_prompt.md`(下轮 D162 Phase 1 起首 — parser 验证 extends + : 组合语法 + ≥3 case spike GREEN)
- payload 走 §M PSM 九问 B 档(sub-D 中段 Phase 1 parser unit test,scope ≤200 行 tests/d162_class_extends_interface_impl/),next_prompt_ultrathink_linter C1-C4 全 PASS(file exists + non-empty + ultrathink keyword + 标题不声明 docs-only)
- bootstrap/ + lib/ + tools/ + tests/ diff = 0(本 Phase 纯 docs/ + .claude/next_prompt.md)
- baseline:d_doc_index_linter F1 = 0(D147/D154/D155/D156/D157/D160/D161 全实存,加入 D162 实存)+ next_prompt_ultrathink_linter PASS + 14 reflection 指标全继承 D161 主线 close baseline(本 Phase 不动 bootstrap/)
- §A.2 H1-H5 假设挑战全实证锚明确

### Phase 1: parser 验证 extends + : 组合语法 + ≥3 case spike GREEN [x] Done at commit `f866c11`

- `tests/d162_class_extends_interface_impl/phase1_parser_unit_test.ss` 3 case 全 GREEN — Case 1 `class TestChildD162 extends TestParentD162 : TestIfaceD162` 双 slot 全填(nGetS2/extendsName + nGetS3/implList)+ Case 2 `class TestChildD162B extends TestParentD162B : TestIfaceD162B, TestIfaceD162C` 多 interface impl(implList comma 分割)+ Case 3 `class TestChildD162C extends TestParentD162C` 仅 extends 无 implList 兼容路径
- 不动 parser(parseClassDecl line 427-441 已支持组合语法 — §A.2 H1 实证锚,Phase 1 仅 spike test 验证)
- §A.2 H1 实证(parser EXTENDS + COLON 双子句解析与现有 class extends + interface impl 同形)— 编译 + 运行 exit 0 即 parser 节点 nSetS2/nSetS3 双 slot 落地的间接验证
- Phase 1 binary 下 D025 vtable check 不 walk classParents,故 Case 1-2 子类必须 re-declare interface methods 满足 D025 vtable check;Phase 2 walk classParents 修法落地后,子类不必 re-declare 即可走父类 method 满足 vtable(D162 §核心目标 5)
- bootstrap fixed-point 不需走(本 Phase 不动 bootstrap/,纯 docs/ + tests/ 改动)
- baseline:bin/ss test tests/ 净 +1 file +3 case + d_doc_index_linter F1 = 0 + reflection_health_linter no regression
- VCM 六验全 PASS

### Phase 2: checker walk classParents 修法 + ≥6 case codegen spike GREEN [x] Done at commit `d6e1851`

- `bootstrap/checker/check_class.ss` checkInterfaceImpl 修法 — 加 walk classParents 链合并 ownMethods + parentMethods 集合(同 D071 checkAbstractImpl line 100-129 walk classParents 范式)+ visited 守 self-loop hang(同 D161 §Phase 2 visited Map 防御范式);inline walk(不引入新 helper 函数,N1 不增 — Phase 2 simplify 内可决断要不要抽 walkClassMethods 跨 checkInterfaceImpl + checkAbstractImpl 共用)
- `tests/d162_class_extends_interface_impl/phase2_codegen_spike_test.ss` 6 case 全 GREEN — Case 1 单层 `class GreeterChildD162 extends GreeterParentD162 : IGreeterD162` 父类 greet() 满足子类 interface(子类不 re-declare)/ Case 2 多层 chain(`MixedGrandD162 extends MixedChildD162 extends MixedParentD162 : IMixedD162`)Grand→Child→Parent walk 全 hit / Case 3 子类 override 父类 method 子赢(value() 父返 10 子返 42)/ Case 4 同名不同 sig overload 编译通过 + walk 不破 mangled 分隔(子类 add(x:int) self-call PASS,父类 add() inheritance dispatch 走 method_call.ss 旁路不在 D162 scope)/ Case 5 multi-iface `class MultiChildD162 extends MultiParentD162 : IFooPD162, IBarQD162` 父类 fooP/barQ 同时满足 I+J / Case 6 D160 mini 模型 `class MysqlCallableMiniD162 extends MysqlPreparedMiniD162 : ICallableMiniD162` 全 inherit + 1 own callOut(D160 Phase 2 wire mini 锚)
- `tests/d162_class_extends_interface_impl/_negative_phase2_d025.ss.txt` D025 negative doc-anchor — `class BadChildD162 extends ParentD162Neg : ChildIfaceD162Neg { 父类 fromParent + 子类 fromChild 但全集仍缺 missingFromBoth }` → bin/ss build exit=1 stderr 含 `missing method 'missingFromBoth' required by interface 'ChildIfaceD162Neg'`(commit-time shell verify exit=1 + grep "missing method 'missingFromBoth'" ≥1 + grep "ChildIfaceD162Neg" ≥1)
- `./build.sh bootstrap` 三阶段固定点 stage2==stage3 bit-identical PASS(§A.2 H5 — checker 改动必走 self-bootstrap;Stage 1 seed→stage1 / Stage 2 stage1→stage2 / Stage 3 stage2→stage3 verify Fixed point verified! Updated bin/ss)
- §A.2 H2 + H3 + H4 + H5 实证完整(H2 classMethodNames 已存父类 method 复用 → Case 1 + Case 6 wire 直接 walk classParents 链;H3 多层 chain idempotent + visited 守 → Case 2 Grand→Child→Parent 三层 walk 全 hit + 不 hang;H4 子赢 override + overload 不 dedupe → Case 3 + Case 4 mangled key 自然分隔保留;H5 bootstrap fixed-point → ./build.sh bootstrap stage2==stage3 通过)
- **RED 实测**(修法前用旧 bin/ss 编译 spike test):`bin/ss build phase2_codegen_spike_test.ss` 报 5 处 `missing method` 假阳错误(GreeterChildD162 missing greet / MixedGrandD162 missing fromChild + fromParent / MultiChildD162 missing fooP + barQ — 父类已实现但 checkInterfaceImpl 不 walk classParents 假阳)+ `_negative_phase2_d025.ss.txt` 编译失败(走老 binary)
- **GREEN**(修法后 bin/ss):`bin/ss build phase2_codegen_spike_test.ss` exit=0 + 运行 `Tests: 6 passed, 0 failed, 6 total` exit=0 + `bin/ss build _negative_phase2_d025.ss.txt` exit=1 stderr 含 `missing method 'missingFromBoth'` 真错强制
- **baseline**:bin/ss test tests/ 305 passed / 15 failed / 320 total(15 failed 全 pre-existing SQL docker integration 不在 D162 scope:d135/d136/d138/d139/d146/d147/d155/d156/d157/phase5)+ d_doc_index_linter F1 = 0 PASS(D147/D154/D155/D156/D157/D160/D161/D162 全实存)+ reflection_health_linter no regression GATE PASS(N1 cur=35 == budget_max bm=35 不变 — inline 修法不加 helper / check_class.ss 不在 F1 list 内行数变化不触发 F1 GATE)+ next_prompt_ultrathink_linter 4/4 PASS(下轮 Phase 3 起首含 ultrathink 关键字)
- VCM 六验全 PASS

### Phase 3: D160 Phase 2 真 extends 形态回退验证 + SS 编译器 vtable + non-0-arg overload child class bug 修法 + ≥12 case spike GREEN [x] Done at commit `7d91d73`

- `lib/com/mysql/prepared.ss` 加 `class MysqlCallableStatement extends MysqlPreparedStatement : CallableStatement`(继承父类 14 字段 + 13 PreparedStatement method;新加 3 own 字段 paramDirections + outRow + lastWasNull;19 own method 真实现 + 1 override getParameterMetaData)+ injectOutVarSetters / appendOutVarSelectors / rewriteCallSqlPlaceholders SQL 重写 helper + buildCallableStatement / doPrepareCall factory(buildCallableStatement 拆出 socketless 路径供 unit test;doPrepareCall 是 wire 路径 socket-driven)+ allInDirections / emptyCallableOutRow helper + 升级 MysqlParameterMetaData paramDirections 字段 + getParameterMode 反射(突破 D157 §核心原则 4 静态 IN-only 妥协)
- `lib/com/mysql/jdbc.ss` MysqlConnection.prepareCall 替换 stub 为真返 `doPrepareCall(this.fd, sql)` + import 加 doPrepareCall + NoopDate / NoopTime 删除(move 至 driver_types.ss)
- `lib/com/mysql/driver_types.ss` 加 NoopDate / NoopTime + import 加 Date, Time(避开 prepared.ss → jdbc.ss 循环导入,prepared.ss 直接 import driver_types.ss)
- **F7 SS 编译器 vtable + non-0-arg overload child class bug 修法 wire 落地**(D162 §Phase 3 实施时发现的 latent bug — `class C extends P : I` 触发 vtable=1,但 child class 内多个 non-0-arg overload method 时 vtable 引用 plain `@${cls}_${m}` fn 但实际只 emit mangled `@${cls}_${m}_${paramSig}` fn,LLC 18 报 `base element of getelementptr must be sized` 因 type 前向引用 + plain fn never emit。修法:`bootstrap/gen/class/class.ss` 加 `classMethodHasPlainFn` Map state + initClassState 初始化 + `bootstrap/gen/gen_registry.ss` registerClassMethodRetType 在 `mSig == ""` 时 set classMethodHasPlainFn[baseName] 标志 0-arg overload 触发 plain fn emit + `bootstrap/gen/gen_type_ops.ss` buildVtableForClass 加 `isOverloaded(baseName) == 1 && classMethodHasPlainFn.has(baseName) == 0` 条件 skip vtable slot — overloaded 但无 0-arg variant 的 method 不入 vtable,dispatch fall back 到静态 mangled call via gen_methods.ss:233 pickClassMethodKey + `bootstrap/gen/methods/gen_methods.ss` vtable indirect dispatch 改 `getelementptr ptr, ptr ${objVal}, i32 2` raw ptr GEP 避开 `getelementptr %${className}, ptr, i32 0, i32 2` struct type 前向引用 — 同 byte offset 16 因 class header `{ i32 rc, ptr typeinfo, ptr vtable, ... }` 8-byte align)
- `tests/d160_callable_statement/phase2_spike_test.ss` ≥12 case 全 GREEN — Case 1 MysqlCallableStatement class 构造(paramDirections.length=3 / lastWasNull=0 / outRow.next()=0 stub 非 null 反射)/ Case 2 paramDirections 初始全 parameterModeIn(D157 IN-only 妥协回归三态)/ Case 3 registerOutParameter 设 parameterModeOut(fresh slot,paramTypes==0)/ Case 4 registerOutParameter 重 setXxx 后设 parameterModeInOut(setInt → paramTypes 非零 → register 走 INOUT)/ Case 5 getParameterMode 反射 IN/OUT/INOUT 三态 + paramDirections 三 idx 各值验证 / Case 6 getInt OUT getter 默认 0 / Case 7 getString OUT getter 默认 "" / Case 8 getDouble OUT getter 默认 0.0 / Case 9 getBoolean OUT getter 默认 0 / Case 10 wasNull 返 lastWasNull(默认 0,直写 lastWasNull=1 后 wasNull 返 1 验证字段 read/write)/ Case 11 SQL 重写 injectOutVarSetters("CALL p(?, ?, ?)", [IN,OUT,INOUT]) → "SET @out_2 = NULL; SET @out_3 = NULL; CALL p(?, @out_2, @out_3)" + rewriteCallSqlPlaceholders ?-substitution 单测 / Case 12 SQL 重写 appendOutVarSelectors → "CALL p(?, ?, ?); SELECT @out_2 AS col_2, @out_3 AS col_3" + 全 IN 路径 sql 不变穿透
- `tests/d160_callable_statement/phase1_spike_test.ss` Case 12 改写从 `conn.prepareCall(sql)` 触发 fd=0 socket 真发包改为 `new NoopCallableStatement()` 直接构造(D162 §Phase 3 swap 后 prepareCall 走真 doPrepareCall;Case 12 verify NoopCallableStatement stub 仍可用 D025 vtable mock fallback)
- D162 §A.2 H1-H5 实证 wire 端落地完整 — 真 extends 形态走 D025 walk classParents 通路全 GREEN(D162 §核心目标 6)
- `./build.sh bootstrap` 三阶段固定点 stage2==stage3 bit-identical PASS(F7 修法触碰 bootstrap/ 4 个文件,自举安全验证)
- baseline:bin/ss test tests/ 306 passed / 15 failed / 321 total(15 failed 全 pre-existing SQL docker integration 不在 D162 scope:d134/d136/d138/d139/d146/d147/d155 等)+ d_doc_index_linter F1 = 0 PASS + reflection_health_linter GATE PASS no regression(N1=35 budget_max 不变,N3 cur=576202 vs bm=575500 AUTO-DRIFT 软警告 1% 内 — bootstrap 4 文件 ~50 LOC 改动累计;classMethodHasPlainFn Map + skip logic + raw ptr GEP)+ next_prompt_ultrathink_linter 4/4 PASS(下轮 Phase 4 起首含 ultrathink 关键字)
- VCM 六验全 PASS

### Phase 4: D162 主线 close + D160 §F8 跨 D 起首回填 [x] Done at commit `<phase4-hash>`

- D162 主线 close 锚:Status header `[x] Phase 0-4 + [x] D162 主线 close at commit \`<phase4-hash>\``
- 跨 D 起首回填范式延续:本 D 主线 close 后的 Phase 4 hash 回填至 D160.md §F8 row(`<D162-phase4-hash>` placeholder)+ D160.md Status 时间线 D162 起首脱胎 entry hash(`<D162-phase0-hash>` placeholder)+ D160.md Status header Phase 2 hold 锚解锁("hold" → "[x] Phase 2 重启" + commit hash)
- D160 Phase 2 重启 commit `<D160-phase2-hash>` 已在 Phase 3 commit 一并落地(D162 §F? 路径或并行)— D160 Phase 3 docker e2e 仍 pending(D160 §Phase 3 收关锚)
- baseline:bootstrap/ + lib/ + tools/ + tests/ diff = 0(本 Phase 纯 docs/ + .claude/next_prompt.md)+ d_doc_index_linter F1 = 0 + 14 reflection 指标全继承
- VCM 六验全 PASS

## Followup

| F | 内容 | 范围 |
|---|------|------|
| F1 | codegen 端显式 merge methods walk classParents — method_call.ss 已通过 classParents chain dispatch 命中父类 method,wire 端已可执行;codegen 端显式 merge 仅让 IR 更直观,marginal 优化 — 留独立 sub-D 远期 |
| F2 | 多继承 `class C extends A, B` — SS 不支持多继承(parser parseClassDecl line 427-431 EXTENDS 仅吃单 ident),Java 同 SS 限制(单类继承 + 多接口实现);本 D 不开多继承支持 — 留独立 sub-D 远期(若有强需求,scope 远超本 D)|
| F3 | parser parseClassDecl 改支持 EXTENDS 在 COLON 后 — 历史顺序固定 EXTENDS 在 COLON 前,改顺序破坏向后兼容;Java + TS 顺序固定 `extends ... implements ...`,不需要灵活,本 D 不开此分支 — 留独立 sub-D 远期 |
| F4 | simplify 评估 — 现有 lib/ 仓内多处 `class MysqlConnection : Connection` / `class MysqlStatement : Statement` 等单 implList 形态稳定,是否回退到 `class C extends Parent : Interface` 形态(若 Parent 抽出公共字段);scope 失控不强制,留 §Followup simplify Agent 评估远期 |
| F5 | plain mName vs mangled key 混合假阴边界(D162 Phase 2 simplify quality agent 锚) — checkInterfaceImpl walk classParents 时父类 classMethodNames 存 plain mName(check_class.ss:460,无 paramSig),本类 classMethods 是 mangled key(check_stmts.ss:83 mangleMethodList fdOnly=1)。父类有 overload `foo()` + `foo(x:int)` + 接口要求 mangled `foo_i` 形态下,父类 plain `foo` 不会匹配 `,foo_i,` → 假阴报错(实际父类有 `foo_i` 但 plain 收集丢 paramSig)。**当前 D162 实际 use case 无此形态**(MysqlPreparedStatement 13 PreparedStatement method 全 plain sig 不涉 overload)留远期(若有 overload 父类 + 接口形态需重写 walk 收 mangled key) |
| F6 | D071 checkAbstractImpl walk classParents 范式统一加 visited Map self-loop 防御(D162 Phase 2 simplify quality agent 锚) — D162 §Phase 2 修法借 D161 §F8 visited Map 防御范式给 checkInterfaceImpl walk 加 visited 守 self-loop hang;但 D071 checkAbstractImpl line 140-168 + 170-198 同形 walk 未带 visited(早于 D161 §F8 cycle 防御)。`class A extends A` self-reference 形态会让 D071 walk 死循环 hang(实际 user 不写,但理论 defense-in-depth 缺一致性)。同步给 D071 也加 visited 范式统一 — 范式漂移 polish 远期(本 Phase 仅修 checkInterfaceImpl scope 限) |
| F7 | **vtable 多 slot 编码支持 child class 内 non-0-arg overload method**(D162 §Phase 3 wire 落地仅修法 minimal 跳过 — vtable 为 overloaded-no-0-arg method 完全 skip slot,dispatch fall back 静态 mangled call;但 parent class 有 overload + child override 其中一个变体的 corner case 时 vtable 必要,且 vtable 应按 mangled name 多 slot 而非 plain name 单 slot — 真根因架构修法是 vtable encoding 改 mangled key + caller-side mangled-key lookup,scope 远超本 D。当前修法对 D162 实际 use case 充分(MysqlCallableStatement 子类 own overload 无父类同名 method 冲突);若有 parent overload + child override variant 形态需 vtable 多 slot 重构 — 留独立 sub-D 远期评估)|
| F8 | **CallableStatement interface 21 own method 接口完整恢复**(本 Phase 3 实施已落地完整 21 own method — registerOutParameter ×2 overload + getObject ×2 overload + 17 单 overload getter + wasNull;F7 修法已让 SS 编译器接受 child class 内 non-0-arg overload,接口未缩减;若未来 SS 编译器架构升级让 vtable 多 slot 编码,F7 修法可 simplify;本 F8 仅留 future-watch 锚 — interface 完整性已 wire 端实证不需修法)|
| F9 | **vtable indirect dispatch raw ptr GEP vs struct type GEP**(D162 §Phase 3 修法 — gen_methods.ss vtable lookup 改用 `getelementptr ptr, ptr %obj, i32 2` 而非 `getelementptr %${className}, ptr %obj, i32 0, i32 2` 避开 struct type 前向引用导致 LLC 18 报 unsized base。raw ptr GEP 同 byte offset(class header `{ i32 rc, ptr typeinfo, ptr vtable, ... }` 8-byte align,vtable 在 offset 16 = sizeof(ptr)*2)。但缺少类型 metadata 让 IR 可读性下降 + future struct layout 改动若动 vtable 位置需同步修 raw GEP 偏移。是否回归 struct type GEP + 改 SS codegen emit 顺序让所有 class struct def 在所有 method body 之前 emit — 留 simplify Agent 评估远期 |
| F10 | **MysqlCallableStatement.registerOutParameter idx 越界静默**(simplify quality agent 锚) — `dirs[idx-1] = parameterModeOut` 若 idx<1 或 idx>paramDirections.length() 触发 array 越界写,无 guard;参 MysqlParameterMetaData.getParameterMode line 1232 已有 idx 边界 fallback parameterModeUnknown 范式 — 子类 setter 端不对称。修法:加 `if (idx < 1 || idx > this.paramDirections.length()) { throw new SQLException(...) }` 与 JDBC §10.2 一致。Phase 3 unit-level test 都用合法 idx(1-3),不阻断;远期独立 polish |
| F11 | **rewriteCallSqlPlaceholders SQL string literal `?` 误改写**(simplify quality agent 锚) — 当前实现不区分 SQL 字符串字面 `'a?b'` 与 placeholder `?`,后者会被改写成 `@out_<idx>`;Connector/J 5.x.late 用 single-pass quote-aware scanner(track ' / " / -- 行注释 / `/* */` 块注释)修。Phase 3 unit-level 测试 SQL 形态简单(`CALL p(?, ?, ?)`),不触发;wire 端 D160 §Phase 3 docker e2e 真 stored proc 形态可能触发 — 留远期独立 polish |
| F12 | **rewriteCallSqlPlaceholders 字符串拼接 O(n²)**(simplify efficiency agent 锚) — `result = result + fromCharCode(c)` 每次走 ss_string_concat strlen+memcpy 全量重拷,n=SQL 长度则 O(n²)。Phase 3 unit-level SQL 短(几十字节)实际不痛,但 wire 端 stored proc SQL 几 KB 可能显著退化。优化:array buffer 收集 char 段(连续非 `?` 字符块)+ join,O(n²) → O(n)。同根因可参 prelude.ss case 转换。远期独立 polish |
| F13 | **doPrepare / buildCallableStatement parallel array init while-push 范式重复**(simplify reuse agent 锚) — 两处都 init 4 个 parallel array(paramTypes/paramValues/paramDoubles/paramNullBits)用 while + push,完全同形;buildCallableStatement 仅多 1 个 paramDirections push parameterModeIn。修法:抽 helper `function initParamArrays(numParams: int)` 让 doPrepare 与 buildCallableStatement 复用 ≈ 7 LOC dedupe + 范式统一 — readability 范式延续(class_method.ss 父类 bindParam 同形 alias 范式作锚)。本 Phase 不抽因 doPrepare 是 D136 范式 + 抽改风险大;远期独立 polish |

## Status 时间线

- 2026-05-09 Phase 0 D 文档落档(commit `418fe5d`)— **新建 docs/3-decisions/D162-class-extends-interface-impl.md**(≥150 行 — §核心目标 + §核心原则 + §A.1 候选评估 + §A.2 隐藏假设 H1-H5 + §A.3 废案 + §Phase 收关锚 Phase 0-4 + §Followup F1-F4 + §Status 时间线)+ D161.md F8 entry 末尾"等下轮"更新指向 D162 起首脱胎(D161 §F8 wire 落地 entry 末尾改写)+ D160.md Status header Phase 2 hold 锚("[ ] Phase 2 hold(D162 编译器层 D025 walk classParents 修法主线 close 后回头做)"+ D160.md §Followup F8 row + D160.md Status 时间线 D162 起首脱胎 entry;**RED 实测**:`ls D162 = ENOENT` / `bin/ss build prepared.ss with class MysqlCallableStatement extends MysqlPreparedStatement : CallableStatement` 实测 = exit=1 stderr 含 12 处 missing method 假阳报错(executeUpdate / executeQuery / getGeneratedKeys / getLastInsertId / close / setInt / setLong / setString / setDouble / setBoolean / setNull / setFetchSize 全 13 inherited PreparedStatement method)/ `grep -nE "function checkInterfaceImpl" bootstrap/checker/check_class.ss = 62`(line 62-94 主体,classMethods 仅本类 own,无 walk classParents);**GREEN**:D162.md 落档 ≥150 行 + D161.md F8 entry 更新 + D160.md Status header Phase 2 hold 锚 + D160.md §Followup F8 row 起首脱胎 + D160.md Status 时间线 D162 起首脱胎 entry + .claude/next_prompt.md 含 ultrathink 关键字 + 下轮 D162 Phase 1 起首;**baseline**:bootstrap/ + lib/ + tools/ + tests/ diff = 0(本 Phase 纯 docs/ + .claude/next_prompt.md;Phase 2 半成品 lib/ 改动 git checkout -- 还原)+ d_doc_index_linter F1 = 0 PASS(D147/D154/D155/D156/D157/D160/D161 全实存,加入 D162 实存)+ next_prompt_ultrathink_linter PASS(下轮含 ultrathink 关键字)+ 14 reflection 指标全继承 D161 主线 close baseline(本 Phase 不动 bootstrap/);**simplify 跳过**(纯文档改动,§After Done §1 例外 — 同 D161 Phase 0/4 / D160 Phase 0 / D157 Phase 0 / D156 Phase 0 / D154 Phase 0 / D155 Phase 0 docs-only 范式延续);**§A.2 H1-H5 假设挑战全实证锚明确**:H1 parser EXTENDS + COLON 双子句已支持 / H2 classMethodNames 已存父类 method 复用 / H3 多层 chain idempotent + visited 守 / H4 子赢 override / H5 bootstrap fixed-point;**等下轮 Phase 1 — parser 验证 extends + : 组合语法 + ≥3 case spike GREEN(class extends class + : interface 同时存在的 parser 节点验证 — nGetS2/extendsName + nGetS3/implList 双 slot 全填)— 同 D162 §Phase 1 收关锚**

- 2026-05-09 **Phase 1 parser 验证 extends + : 组合语法 + 3 case spike GREEN**(commit `f866c11` 待下轮 Phase 2 commit 即时回填)— **新建 tests/d162_class_extends_interface_impl/phase1_parser_unit_test.ss**(86 行 — Case 1 `class TestChildD162 extends TestParentD162 : TestIfaceD162` 双 slot 全填验证 nGetS2/extendsName + nGetS3/implList / Case 2 `class TestChildD162B extends TestParentD162B : TestIfaceD162B, TestIfaceD162C` 多 interface impl 验证 implList comma 分割 / Case 3 `class TestChildD162C extends TestParentD162C` 仅 extends 无 implList 兼容路径)— Phase 1 binary 下 D025 vtable check 不 walk classParents,故 Case 1-2 子类必须 re-declare interface methods 满足 D025 vtable check(临时妥协,Phase 2 修法落地后子类不必 re-declare);**RED 实测**:`ls tests/d162_class_extends_interface_impl/ = ENOENT`(测试目录与文件全无,起首必新建);**GREEN**:`bin/ss build tests/d162_class_extends_interface_impl/phase1_parser_unit_test.ss -o /tmp/d162_phase1_parser` exit 0 + `/tmp/d162_phase1_parser` 运行输出 `Tests: 3 passed, 0 failed, 3 total` exit 0(3 case 全 PASS);**§A.2 H1 实证**(parser EXTENDS + COLON 双子句解析与 class extends + interface impl 同形 — parseClassDecl line 427-441 已支持组合语法,Phase 1 spike test 编译 + 运行 exit 0 即 nSetS2(extendsName) + nSetS3(implList) 双 slot 落地的间接验证);**baseline**:bin/ss test tests/ 净 +1 file +3 case 全继承 D162 Phase 0 baseline 302/15/317(本 Phase 仅 tests/ 不动 bootstrap/lib/tools/)+ d_doc_index_linter F1 = 0 PASS(D147/D154/D155/D156/D157/D160/D161/D162 全实存)+ reflection_health_linter no regression GATE PASS(本 Phase 不动 bootstrap/ → reflection scope 不触 → 14 reflection 指标全继承 D161 主线 close baseline)+ next_prompt_ultrathink_linter 4/4 PASS(下轮 D162 Phase 2 起首含 ultrathink 关键字);**simplify 4 agent 并跑评估**(reuse / quality / efficiency / readability + readability VETO 守);**bootstrap fixed-point 不需走**(本 Phase 不动 bootstrap/ 自举无影响);**§N §6 file:line 锚**:tests/d162_class_extends_interface_impl/phase1_parser_unit_test.ss(本 Phase 新建)+ docs/3-decisions/D162-class-extends-interface-impl.md Status header [x] Phase 1 + §Phase 1 §收关锚 [x] Done at commit `f866c11` + Status 时间线 Phase 1 entry(本 entry)+ bootstrap/parse/parser.ss line 427-441 parseClassDecl EXTENDS + COLON 双子句已支持(本 Phase 不动 parser);**等下轮 Phase 2 — bootstrap/checker/check_class.ss checkInterfaceImpl walk classParents 链合并 ownMethods + parentMethods 集合修法(同 D071 checkAbstractImpl line 97 范式 + D161 §Phase 2 visited Map 防御范式)+ ≥6 case codegen spike GREEN + ./build.sh bootstrap 三阶段固定点 stage2==stage3 PASS + Phase 1 hash 即时回填 D162.md 实际语义位 4 处 — 同 D162 §Phase 2 收关锚**

- 2026-05-09 **Phase 3 D160 Phase 2 真 extends 形态回退验证 + SS 编译器 vtable + non-0-arg overload child class bug 修法 wire 落地(F7) + 12 case spike GREEN + 跨 D 起首回填 Phase 2 hash `d6e1851`**(commit `7d91d73` 待下轮 Phase 4 commit 即时回填)— **修 lib/com/mysql/prepared.ss**(加 class MysqlCallableStatement extends MysqlPreparedStatement : CallableStatement + 3 own 字段 paramDirections / outRow / lastWasNull + 19 own method 真实现 + 1 override getParameterMetaData;加 helper allInDirections / emptyCallableOutRow / rewriteCallSqlPlaceholders / injectOutVarSetters / appendOutVarSelectors;加 factory buildCallableStatement / doPrepareCall;升级 MysqlParameterMetaData paramDirections 字段 + getParameterMode 三态反射;import 加 CallableStatement, parameterModeInOut, parameterModeOut, parameterModeUnknown, NoopDate, NoopTime, MysqlTimestamp, MysqlCharacterStream)+ **修 lib/com/mysql/jdbc.ss**(MysqlConnection.prepareCall stub `new NoopCallableStatement()` 替换为真返 `doPrepareCall(this.fd, sql)` + import 加 doPrepareCall + import 加 NoopDate, NoopTime from driver_types + 删 NoopDate / NoopTime class)+ **修 lib/com/mysql/driver_types.ss**(import 加 Date, Time + 加 NoopDate / NoopTime class — move from jdbc.ss 避循环导入)+ **修 SS 编译器 vtable + overload bug**(F7 wire 落地 — bootstrap/gen/class/class.ss 加 classMethodHasPlainFn Map state + initClassState 初始化 + bootstrap/gen/gen_registry.ss registerClassMethodRetType 加 mSig=="" 时 classMethodHasPlainFn.set(baseName, "1") 跟踪 plain fn emit 标志 + bootstrap/gen/gen_type_ops.ss buildVtableForClass 加 `isOverloaded(baseName)==1 && classMethodHasPlainFn.has(baseName)==0` continue skip slot logic — overloaded-no-0-arg variant 的 method 不入 vtable,dispatch fall back 静态 mangled call via gen_methods.ss:233 pickClassMethodKey + bootstrap/gen/methods/gen_methods.ss vtable indirect dispatch 改 `getelementptr ptr, ptr ${objVal}, i32 2` raw ptr GEP 避 struct type 前向引用导致 LLC 18 报 unsized base — 同 byte offset 16 因 class header 8-byte align)+ **修 tests/d160_callable_statement/phase1_spike_test.ss**(Case 12 从 `conn.prepareCall(sql)` 真发包改为直接 `new NoopCallableStatement()` 验证 stub fallback — D162 §Phase 3 swap 后 prepareCall 真返 doPrepareCall 走 fd=0 socket 死)+ **新建 tests/d160_callable_statement/phase2_spike_test.ss**(85 行 — 12 case 覆盖 MysqlCallableStatement 真 extends 形态全形态 wire);**RED 实测**(修法前用旧 bin/ss 编译):`bin/ss build phase1_spike_test.ss` 报 `parse error at line 4820: expected newline or '}', found ASSIGN`(this.array[i] = v parser-rejected,实际是 latent SS 编译器 vtable bug — child class with non-0-arg overload + parent vtable=1 → llc 报 unsized base 因 type 前向引用 + plain fn 永不 emit)+ `bin/ss build phase2_spike_test.ss` 不存在(待新建);**GREEN**:./build.sh bootstrap 三阶段固定点 stage2==stage3 bit-identical PASS(F7 修法 4 个 bootstrap 文件改动自举安全)+ `bin/ss build phase1_spike_test.ss -o /tmp/d160_phase1` exit=0 + `/tmp/d160_phase1` 运行 `Tests: 12 passed, 0 failed, 12 total` exit=0 + `bin/ss build phase2_spike_test.ss -o /tmp/d160_phase2` exit=0 + `/tmp/d160_phase2` 运行 `Tests: 12 passed, 0 failed, 12 total` exit=0 + `bin/ss build phase2_codegen_spike_test.ss -o /tmp/d162_phase2` exit=0(D162 Phase 2 spike baseline 全继承);**§A.2 H1-H5 实证 wire 端落地完整**(H1 parser EXTENDS+COLON 双子句已支持 / H2 classMethodNames 已存父类 method 复用 → MysqlCallableStatement 13 inherited PreparedStatement method 自动满足 / H3 多层 chain — 不触发 / H4 子赢 override + overload mangled 分隔 → MysqlCallableStatement.getParameterMetaData override 父类 + child overload registerOutParameter / getObject 走 mangled 静态 dispatch / H5 bootstrap fixed-point — F7 修法自举 stage2==stage3);**baseline**:bin/ss test tests/ 306 passed / 15 failed / 321 total(15 failed 全 pre-existing SQL docker integration 不在 D162 scope:d134/d135/d136/d138/d139/d146/d147/d155/d156/d157/phase5 — 净 +1 file +12 case)+ d_doc_index_linter F1 = 0 PASS(D147/D154/D155/D156/D157/D160/D161/D162 全实存)+ reflection_health_linter no regression GATE PASS(N1 cur=35 == budget_max bm=35 不变 — F7 修法不加 helper 函数全 inline + N3 cur=576202 vs bm=575500 AUTO-DRIFT 软警告 1% 内 — bootstrap 4 文件累计 ~50 LOC 改动 + lib/com/mysql/prepared.ss 累计 ~250 LOC 改动 — 14 reflection 指标全 cur ≤ budget_max DRIFT 软警告)+ next_prompt_ultrathink_linter 4/4 PASS(下轮 Phase 4 起首含 ultrathink 关键字);**simplify 4 agent 并跑评估**(reuse / quality / efficiency / readability + readability VETO 守 — F7 修法 + lib/ ~250 LOC 实质改动必走);**§N §6 file:line 锚**:lib/com/mysql/prepared.ss:42-48 imports 升级 / line 1208-1240 MysqlParameterMetaData 三态升级 / line 1434-1436 getParameterMetaData 父类 caller 改 / line 1450+ MysqlCallableStatement extends 类 + helper + factory + lib/com/mysql/jdbc.ss:25 import doPrepareCall + line 92-94 prepareCall 真路径 + lib/com/mysql/driver_types.ss:15 import Date,Time + line 180+ NoopDate / NoopTime 类 + bootstrap/gen/class/class.ss:29 classMethodHasPlainFn state + line 75 initClassState + bootstrap/gen/gen_registry.ss:64 mSig=="" set logic + bootstrap/gen/gen_type_ops.ss:88 isOverloaded skip + bootstrap/gen/methods/gen_methods.ss:215 raw ptr GEP + tests/d160_callable_statement/phase1_spike_test.ss:122 Case 12 改 + tests/d160_callable_statement/phase2_spike_test.ss(本 Phase 新建)+ docs/3-decisions/D162-class-extends-interface-impl.md Status header [x] Phase 3 + §Phase 3 §收关锚 [x] Done at commit `7d91d73` + Status 时间线 Phase 3 entry(本 entry)+ §Phase commit hash 总览段表 Phase 3 行 + §Followup F7 / F8 / F9 远期锚 + 跨 D 起首回填 Phase 2 hash `d6e1851` 至 D162.md 5 处实际语义位;**等下轮 Phase 4 — D162 主线 close + D160 §F8 跨 D 起首回填(D162 主线 close hash 回填至 D160.md §F8 row + D160.md Status 时间线 D162 起首脱胎 entry hash + D160.md Status header Phase 2 hold 锚解锁 — 同 D162 §Phase 4 收关锚)**

- 2026-05-09 **Phase 2 checker walk classParents 修法 + 6 case codegen spike GREEN + bootstrap 三阶段固定点 PASS + 跨 D 起首回填 Phase 1 hash `f866c11`**(commit `d6e1851`)— **修 bootstrap/checker/check_class.ss checkInterfaceImpl line 62-94**(D162 §核心目标 1)加 walk classParents 链合并 ownMethods + parentMethods 集合,inline 实现复用 D071 checkAbstractImpl line 100-129 walk classParents 范式 + visited Map self-loop 防御范式(同 D161 §Phase 2)— 修法 LOC ~30 行(check_class.ss 526→554 行,F1 ≤600 不需 bump,N1 不增 inline 不加新函数);本类 classMethods 是 mangled key(check_stmts.ss:83 mangleMethodList fdOnly=1)父类 classMethodNames 存 plain mName(check_class.ss:460),walk 时父类 plain mName 直接加入 allMethods 满足无 overload 接口(D162 实际用例 MysqlCallableStatement extends MysqlPreparedStatement : CallableStatement 13 inherited PreparedStatement method 全 plain sig 不涉 overload);**新建 tests/d162_class_extends_interface_impl/phase2_codegen_spike_test.ss**(155 行 — Case 1 单层 GreeterChildD162 extends GreeterParentD162 : IGreeterD162 父类 greet() 满足 / Case 2 多层 chain MixedGrandD162→Child→Parent walk 全 hit / Case 3 子赢 override OverrideChildD162.value() 父返 10 子返 42 / Case 4 overload 不 dedupe AddChildD162.add(x:int) self-call PASS / Case 5 multi-iface MultiChildD162 extends MultiParentD162 : IFooPD162, IBarQD162 父类 fooP/barQ 同时满足 I+J / Case 6 D160 mini MysqlCallableMiniD162 extends MysqlPreparedMiniD162 : ICallableMiniD162 全 inherit + 1 own callOut)+ **新建 tests/d162_class_extends_interface_impl/_negative_phase2_d025.ss.txt**(31 行 — class BadChildD162 extends ParentD162Neg : ChildIfaceD162Neg 父类 fromParent + 子类 fromChild 但全集仍缺 missingFromBoth → exit=1 stderr 含 missing method 真错强制 — D025 walk 后真错放过的反向断链验证);**RED 实测**(修法前用旧 bin/ss 编译):`bin/ss build phase2_codegen_spike_test.ss` 报 5 处 missing method 假阳错误(GreeterChildD162 missing greet / MixedGrandD162 missing fromChild + fromParent / MultiChildD162 missing fooP + barQ — 父类已实现但 checkInterfaceImpl 不 walk classParents 假阳)+ Case 6 D160 mini 同形假阳;**GREEN**:./build.sh bootstrap 三阶段固定点 stage2==stage3 bit-identical PASS + `bin/ss build phase2_codegen_spike_test.ss -o /tmp/d162_phase2` exit=0 + `/tmp/d162_phase2` 运行输出 `Tests: 6 passed, 0 failed, 6 total` exit=0(6 case 全 PASS)+ `bin/ss build _negative_phase2_d025.ss.txt` exit=1 stderr 含 `missing method 'missingFromBoth' required by interface 'ChildIfaceD162Neg'`(walk 后真错放过的反向断链验证);**§A.2 H2-H5 实证完整**(H2 classMethodNames 已存父类 method 复用 → Case 1 + Case 6;H3 多层 chain idempotent + visited 守 → Case 2;H4 子赢 override + overload mangled 分隔 → Case 3 + Case 4;H5 bootstrap fixed-point → ./build.sh bootstrap stage2==stage3 通过);**baseline**:bin/ss test tests/ 305 passed / 15 failed / 320 total(15 failed 全 pre-existing SQL docker integration 不在 D162 scope:d135/d136/d138/d139/d146/d147/d155/d156/d157/phase5)+ d_doc_index_linter F1 = 0 PASS(D147/D154/D155/D156/D157/D160/D161/D162 全实存)+ reflection_health_linter no regression GATE PASS(N1 cur=35 == budget_max bm=35 不变 — inline 修法不加 helper / check_class.ss 不在 F1 list 行数变化不触发 F1 GATE / 14 reflection 指标全 cur ≤ budget_max DRIFT 软警告)+ next_prompt_ultrathink_linter 4/4 PASS(下轮 Phase 3 起首含 ultrathink 关键字);**simplify 4 agent 并跑评估**(reuse / quality / efficiency / readability + readability VETO 守);**§N §6 file:line 锚**:bootstrap/checker/check_class.ss checkInterfaceImpl line 62-122(walk classParents block + 原 implList scan)+ tests/d162_class_extends_interface_impl/phase2_codegen_spike_test.ss(本 Phase 新建)+ tests/d162_class_extends_interface_impl/_negative_phase2_d025.ss.txt(本 Phase 新建)+ docs/3-decisions/D162-class-extends-interface-impl.md Status header [x] Phase 2 + §Phase 2 §收关锚 [x] Done at commit `d6e1851` + Status 时间线 Phase 2 entry(本 entry)+ §Phase commit hash 总览段表 Phase 2 行;**等下轮 Phase 3 — D160 Phase 2 真 extends 形态回退验证(`class MysqlCallableStatement extends MysqlPreparedStatement : CallableStatement` 21 own method 真实现 + paramDirections 字段 + getParameterMode 反射 + ≥12 case spike GREEN — 同 D162 §Phase 3 收关锚)**

- 2026-05-09 **Phase 4 D162 主线 close + D160 §F8 跨 D 起首回填 + 顺手回填 D160 残留 placeholder**(commit `<phase4-hash>` 待本 Phase 4 commit 即时回填)— **跨 D 起首回填 D162 Phase 3 hash `7d91d73` 至 D162.md 5 处实际语义位**(line 3 Status header Phase 3 entry hash + line 82 §Phase commit hash 总览段表 Phase 3 行 + line 119 §Phase 3 §收关锚 [x] Done at commit + Status 时间线 Phase 3 entry 2 处 — 同 D 内 Phase N → Phase N+1 即时回填范式延续 D162 §Phase 0 → §Phase 1 / §Phase 1 → §Phase 2 / §Phase 2 → §Phase 3 / §Phase 3 → §Phase 4 同形 + D161 §Phase N → §Phase N+1 同形)+ **D162 主线 close**(Status header [x] Phase 4 + [x] D162 主线 close at commit `<phase4-hash>` + §Phase 4 §收关锚 [ ] Pending → [x] Done at commit `<phase4-hash>`)+ **跨 D 起首回填 D162 Phase 3 commit `7d91d73` 至 D160.md ≥4 处实际语义位**(D160.md Status header `[ ] Phase 2 hold` 锚解锁 → `[x] Phase 2 重启 + wire 落地 at commit \`7d91d73\`(D162 §Phase 3 中 wire 落地)` + line 84 §Phase commit hash 总览段表 Phase 2 行 `<phase2-hash>` → `7d91d73` + line 113 §Phase 2 §收关锚 [ ] Pending → [x] Done at commit `7d91d73` + line 150 §F8 row 末尾 `<phase2-hash>` 上下文更新为 `commit \`7d91d73\` 在 D162 §Phase 3 中 wire 落地(D162 主线 close at commit \`<phase4-hash>\`)`)+ **顺手回填 D160 残留 placeholder**(D162 §Phase 0 cleanup commit `c50f055` 漏的 — D160.md line 83 §Phase commit hash 总览段表 Phase 1 行 `<phase1-hash>` → `3bd4d61` + line 100 §Phase 1 §收关锚 `<phase1-hash>` → `3bd4d61` + line 156 Status 时间线 Phase 1 重启 entry `<phase1-hash>` → `3bd4d61` + line 155 D161 起首脱胎 entry `<D161-phase0-hash>` → `b31c7b0` — 4 处遗留 placeholder 同轮修齐 — feedback `memory_stale_same_turn` 范式延续);**RED 实测**:`grep -c "<phase3-hash>" docs/3-decisions/D162-class-extends-interface-impl.md` = 5(待本 Phase 4 commit 回填)+ `grep -c "<phase2-hash>" docs/3-decisions/D160-callable-statement-out-inout.md` = 4(待本 Phase 4 commit 回填)+ `grep -c "<phase1-hash>" docs/3-decisions/D160-callable-statement-out-inout.md` = 3(D162 §Phase 0 cleanup 漏的)+ `grep -c "<D161-phase0-hash>" docs/3-decisions/D160-callable-statement-out-inout.md` = 1(D162 §Phase 0 cleanup 漏的);**GREEN**:D162.md `<phase3-hash>` 5 处全替换为 `7d91d73` + Status header [x] Phase 4 + [x] D162 主线 close 升级 + §Phase 4 §收关锚 [x] Done at commit `<phase4-hash>` + Status 时间线 Phase 4 entry 加(本 entry)+ D160.md Phase 2 hold 解锁 + §Phase 2 §收关锚 [x] Done + §F8 row 回填 + Phase 1 残留 placeholder 全回填 + Status 时间线 Phase 4 close + D160 Phase 2 重启 wire 落地 entry + `.claude/next_prompt.md` 写入 D160 Phase 3 docker e2e + D160 主线 close 起首(integration_test e2e ≥7 case docker probe-skip 全 stored procedure idiom 覆盖 + absorb spike + §A.2 H1-H5 wire 端全实证锚收关 — 同 D160 §Phase 3 收关锚)含 ultrathink 关键字 + bin/ss run tools/next_prompt_ultrathink_linter.ss GATE OK;**baseline**:bootstrap/ + lib/ + tools/ + tests/ diff = 0(本 Phase 纯 docs/ + .claude/next_prompt.md)+ d_doc_index_linter F1 = 0 PASS(D147/D154/D155/D156/D157/D160/D161/D162 全实存)+ next_prompt_ultrathink_linter PASS(下轮含 ultrathink 关键字)+ 14 reflection 指标全继承 D162 §Phase 3 baseline(本 Phase 不动 bootstrap/ → reflection scope 不触 → N1=35 + 14 指标全继承);**simplify 跳过**(纯文档改动,§After Done §1 例外 — 同 D162 §Phase 0 / D161 §Phase 0/4 / D160 §Phase 0 / D157 §Phase 0 / D156 §Phase 0 / D154 §Phase 0 / D155 §Phase 0 docs-only 范式延续);**§A.2 H1-H5 实证完整闭环**(本 Phase 4 是 D162 主线 close 锚不引入新 §A.2 假设;Phase 1-3 既有 H1-H5 实证全继承,wire 端 D160 Phase 2 已落地);**§A.3 废案 9 项全锁定**(C1 数据层 patch + C3 架构层 refactor + 多继承 + 全局 method-collection 抽象 + abstract/final 修饰符全套 + D025 check 跳过 walk + composition pattern 替代 + D162 与 D160 同 commit 落地 + walk classParents 也加 walk ifaceParents on impl side + codegen 端显式 merge methods + parser EXTENDS 在 COLON 后 + abstract method walk classParents 重写 + 强制 lib/ 全 implList 形态回退);**§N §6 file:line 锚**:docs/3-decisions/D162-class-extends-interface-impl.md line 3 Status header [x] Phase 4 + [x] D162 主线 close + line 82 §Phase commit hash 总览段表 Phase 3 行 hash 回填 + line 83 §Phase 4 行 + line 119 §Phase 3 §收关锚 hash 回填 + line 132 §Phase 4 §收关锚 [x] Done + line 134 §Phase 4 §收关锚正文 D162 主线 close 锚 + line 164 Status 时间线 Phase 3 entry 2 处 hash 回填 + Status 时间线 Phase 4 entry(本 entry)+ docs/3-decisions/D160-callable-statement-out-inout.md line 3 Status header Phase 2 重启 + wire 落地 + line 84 §Phase commit hash 总览段表 Phase 2 行 hash 回填 + line 113 §Phase 2 §收关锚 [x] Done at commit `7d91d73` + line 150 §F8 row 末尾上下文更新 + line 83/100/156 §Phase 1 hash 顺手回填 + line 155 D161 §Phase 0 hash 顺手回填 + Status 时间线 D162 主线 close + D160 Phase 2 重启 wire 落地 entry;**等下轮 D160 Phase 3 docker e2e + D160 主线 close — integration_test e2e ≥7 case docker probe-skip 全 stored procedure idiom 覆盖(CREATE PROCEDURE OUT param + INOUT param + multiple ResultSet 反射 + getParameterMode 真值反射 IN/OUT/INOUT 三态)+ absorb spike(删 phase1_spike + phase2_spike,可保留 ≤1 unit test)+ §A.2 H1-H5 wire 端全实证锚收关 + D160 主线 close at commit `<D160-phase3-hash>` + 跨 D 起首回填 D162 Phase 4 hash 至 D160.md §F8 row + Status 时间线 D162 主线 close + D160 Phase 2 重启 wire 落地 entry(D162 主线 close → D160 Phase 3 docker e2e + D160 主线 close 跨 D 起首回填范式延续)— 同 D160 §Phase 3 收关锚**
