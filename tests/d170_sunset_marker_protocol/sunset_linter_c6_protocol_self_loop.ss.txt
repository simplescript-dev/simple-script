// D170 sunset_linter C6 软警告 spike test — 协议自循环 (D170 §决策 A.2 + B C6)
//
// 验证 C6 (协议自循环) — 默认模式扫 docs/3-decisions/D*.md 找
// "长期可考虑" 类 forward-looking commitment — D 文档自身 forward-looking commitment
// 必有对应工具/代码侧 SUNSET 反向锚(元-level 一致性,§第一性需求 "过渡债不漂移"
// 的递归应用,协议自己也是漂移源)。
//
// 软警告 — N candidate > 0 不 BLOCK commit;允许跨 commit 累积 audit。
//
// 实证 sibling (step 9 (a) 实测 + step 9 (b.1) 精化后):
//   step 9 (a) 实测 6 处 "长期可考虑" (D170 自身) — 全在 ## 决策 ### A. canonical marker
//   模板 #### A.2 协议自循环过渡件 spec 段(line 118/124/130/135)+ ## 与现有机制关系
//   ### 复用 段(line 179/242),协议引"长期可考虑 X" pattern 作 forward-looking commitment
//   示例文本,非真 commitment。其中 line 242 "长期可考虑共享 helper" 是真 commitment 已由
//   tools/sunset_linter.ss 顶 SUNSET(D170 §step 9) 反向锚 cover(step 8 就近示范)。
//
//   step 9 (b.1) sub-context 精化后:6 → 0 — ## 决策 / ## 与现有机制关系 全 skip(spec
//   段 false positive),真 commitment 进 §下一步 / §Followup 段 currently 0 库存。
//   未来若 spec 段外 (§下一步 / §Followup 段)出现"长期可考虑 X"真 commitment 则 C6 命中。
//
// commit-time shell verify (`.ss.txt` 后缀避 bin/ss test auto-collect):
//   bin/ss run tools/sunset_linter.ss 2>&1 | grep -c "C6 (协议自循环 "  # 1 (汇总行存在)
//   bin/ss run tools/sunset_linter.ss --audit 2>&1 | grep -cE "\[长期可考虑\]"  # 0 (精化后 spec 段全 skip,真 commitment 暂无)

function main() {
    // doc-anchor only — sunset_linter 默认模式扫 docs/3-decisions/D*.md 触发 C6
}
