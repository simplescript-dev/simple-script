// D170 sunset_linter `--phase X` mode spike test — active phase PASS
//
// 验证 Phase Exit Gate manual gate (D170 §决策 C) — `--phase X` 模式当 X 在
// D 文档 §下一步 仍 [ ] Planned (active) 时,linter PASS 软提示报告 + exit 0,
// markers awaiting future cleanup OK to remain。
//
// 触发场景 (sibling step 5a 落地的真实 markers):
//   D093 §1.5d 主轮收口子步 sibling 子轮 = D093 §下一步 line 181 [ ] Planned —
//   stmts_loop_classic.ss:12 for + :72 while 2 markers 待 sibling 子轮 sweep。
//
// commit-time shell verify (`.ss.txt` 后缀避 bin/ss test auto-collect):
//   bin/ss run tools/sunset_linter.ss --phase "1.5d 主轮收口子步 sibling 子轮" ; echo $?  # 0
//   bin/ss run tools/sunset_linter.ss --phase "1.5d 主轮收口子步 sibling 子轮" 2>&1 | grep -c "Phase Exit verdict: PASS"  # 1
//   bin/ss run tools/sunset_linter.ss --phase "1.5d 主轮收口子步 sibling 子轮" 2>&1 | grep -cE "marker: .+stmts_loop_classic\.ss:"  # 2
//   bin/ss run tools/sunset_linter.ss --phase "1.5d 主轮收口子步 sibling 子轮" 2>&1 | grep -c "phase active \[ \] Planned"  # 1

function main() {
    // doc-anchor only — sunset_linter --phase X 模式默认扫 bootstrap/lib/tools,
    // 本测试不需要自身内嵌 marker,通过 shell verify 跑 sunset_linter 对比 expected
    // output 验证 --phase X mode 行为
}
