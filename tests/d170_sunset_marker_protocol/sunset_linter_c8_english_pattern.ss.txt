// D170 sunset_linter C8 软警告 spike test — 英文模式 (D170 §决策 B C8)
//
// 验证 C8 (英文模式) — 默认模式扫 bootstrap/ lib/ tools/ .ss 找 5 个高 signal
// keyword: "workaround" / "FIXME" / "TBD" / "deferred" / "not yet"
// (原 D170 §历史语境表 audit #3 — 立项实证表只 catch 中文「留 X+」漏英文同义模式).
//
// v0.1 keyword 选定 (spike 实证):case-sensitive,排除 stub (31 命中,
// 多是合法 "stub function" 词义噪声) + placeholder (18 命中类似词义噪声) +
// "later use" + "future use" (短语 boundary 模糊).case-insensitive + 高假阳
// keyword 升级留 v0.2.
//
// 软警告 — N candidate > 0 不 BLOCK commit;allow audit-driven backfill (step 9 (b)).
//
// commit-time shell verify (`.ss.txt` 后缀避 bin/ss test auto-collect):
//   bin/ss run tools/sunset_linter.ss 2>&1 | grep -c "C8 (英文模式 "  # 1
//   bin/ss run tools/sunset_linter.ss --audit 2>&1 | grep -cE "\[workaround\]|\[FIXME\]|\[TBD\]|\[deferred\]|\[not yet\]"  # ≥ 1

function main() {
    // doc-anchor only — sunset_linter 默认模式扫 bootstrap/lib/tools .ss 触发 C8
}
