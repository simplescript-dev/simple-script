// D170 sunset_linter C9 软警告 spike test — IR-emit-side TODO (D170 §决策 B C9)
//
// 验证 C9 (IR-emit-side TODO) — 默认模式扫 bootstrap/gen/ 找 `emitIR(...)`
// 内含 TODO/FIXME/XXX 的运行时 IR 携带过渡件 (D170 §历史语境 audit #4 —
// 立项 scope 只看源码注释漏 codegen 输出端 emitIR("... TODO ...") 形态).
//
// 软警告 — N candidate > 0 不 BLOCK commit;允许 audit-driven backfill (step 9 (b)).
//
// 实证 sibling (本轮 spike 实测):bootstrap/gen/methods/gen_methods.ss:565
//   `emitIR(\`  ; TODO: method call .\${method}\`)` 1 处真 IR-emit TODO 过渡件 —
//   是 emitIR 输出端 placeholder method call,运行时 IR 携带的过渡 placeholder.
//
// commit-time shell verify (`.ss.txt` 后缀避 bin/ss test auto-collect):
//   bin/ss run tools/sunset_linter.ss 2>&1 | grep -c "C9 (IR-emit-side "  # 1
//   bin/ss run tools/sunset_linter.ss --audit 2>&1 | grep -cE "\[emitIR \+ TODO\]|\[emitIR \+ FIXME\]|\[emitIR \+ XXX\]"  # ≥ 1

function main() {
    // doc-anchor only — sunset_linter 默认模式扫 bootstrap/gen/ 触发 C9
}
