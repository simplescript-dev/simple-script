# I028 — 运行时 IR 缓存子系统死代码 — 修复方案对比(MNK §轨 1)

**bug**: `buildRuntimeCache()`(`bootstrap/gen/rt/gen_rt_cache.ss:11-33`)在每次 `bin/ss test` 起手构建一份运行时 LLVM IR 缓存 `/tmp/ss_rt_cache.{o,decls}`。该子系统**双重失效**:(层 1)喂给 `llc-18` 的 IR 由 `emitRuntimeDefs()` 产出,只定义 `%TypeInfo`/`%ObjHeader`,**不定义** `%String`/`%Array`/`%Map`;而运行时 IR 含 `getelementptr %Array, ...` → llc verifier `base element of getelementptr must be sized` 确定性失败;(层 2)即使 llc 成功,产出的 `.o`/`.decls` 也**无人读取** —— 读取分支(`codegen.ss:332`、`main.ss:640`)gated on `useRuntimeCache==1`,而 `useRuntimeCache` 仅 `cmdTest` 父进程(`main.ss:429`)置 1,父进程置 1 后不在进程内 `compile()`(只 `system()` 拼脚本);真正编译的 `bin/ss build` 子进程恒 `useRuntimeCache==0`。

**父约束**: `docs/4-issues/I028-runtime-cache-dead-subsystem.md`(独立 root cause,父决策无;与 I026/I027「固定共享 /tmp 路径并发 race」**不同 bug 类** —— 经实测证伪并发假设,真根因是「确定性 llc 失败 + 子系统无消费者」)。

**结论**: **选 C —— 删整个 dead 运行时 IR 缓存子系统**(见 §4 决策行)。对标 I027 `b0e272c`「删多余机制」根层手法。

---

## 1. 根因机理(坐实,两层)

**层 1 — 确定性 llc 失败(直接症状)**
`buildRuntimeCache()` 把 `emitRuntimeDefs()` 输出加 `; ModuleID` 头直接喂 `llc-18 -filetype=obj`。`emitRuntimeDefs`(`gen_runtime.ss:13`)只 emit `%TypeInfo`(`:191`)/`%ObjHeader`(`:194`);`%String`/`%Array`/`%Map` 的唯一定义点是 `generateToFile` 的 `typeDecl` 常量(`codegen.ss:352`),`buildRuntimeCache` **不 prepend 它**。运行时 IR 含 `getelementptr %Array, ptr %p, i32 0, i32 2` 等(实测 rtLL:375)→ GEP base 是未定义/opaque 类型 → llc verifier 报 `base element of getelementptr must be sized`,llc 非零退出。普通 `bin/ss build` 无此问题:`generateToFile` 在 `codegen.ss:352-353` 显式 prepend `%String/%Array/%Map`。

**层 2 — 子系统是死代码(根因)**
即使层 1 修好,`/tmp/ss_rt_cache.{o,decls}` 仍无消费者:`generateToFile`(`codegen.ss:332`)与 `compile()` 链接步(`main.ss:640`)的缓存读取分支均 gated on `useRuntimeCache==1`。`useRuntimeCache` 全仓仅 `main.ss:429` 置 1 —— 那是 `cmdTest` 父进程;父进程置 1 后**不在本进程 `compile()`**,只 `writeFile` 拼并行脚本 `system("bash ...")` 之。脚本里每个 job 是独立 `bin/ss build` 子进程(`main.ss:442`),`cmdBuild`(`main.ss:317-341`)从不置 `useRuntimeCache` → 子进程恒 0 → 缓存读取分支恒不取。`gen_rt_cache.ss:1` 注释提的 "CLI `--rt-cache` driver" 全仓不存在(`grep -rn -- --rt-cache` 仅命中该注释自身)。∴ 缓存每次构建、从未被读。

## 2. 假设破裂入口

`buildRuntimeCache` + `useRuntimeCache` 子系统隐含两条未校验假设,**均破裂**:

- **假设①「`emitRuntimeDefs()` 的 IR 自包含、可独立喂 llc」** —— **假设破裂**:运行时 IR 对 `%String`/`%Array`/`%Map` 的 GEP 引用依赖 `generateToFile` 才 prepend 的 `typeDecl`;`buildRuntimeCache` 独立成模块时缺这三个 typedecl,llc 确定性失败。这是「层 1」的破裂入口。
- **假设②「构建出的缓存 `.o`/`.decls` 会被某个编译路径消费」** —— **假设破裂**:消费者全部 gated on `useRuntimeCache==1`,而该旗标只在不执行 `compile()` 的 `cmdTest` 父进程置 1;执行 `compile()` 的 `bin/ss build` 子进程恒 0。生产者与消费者**永不在同一进程**。这是「层 2」根因的破裂入口 —— 也是 A/B 两候选与 C 的分水岭:A 只补假设①(让它能 build),假设②仍破裂;C 同时消除两者(无子系统 → 无假设)。

## 3. 候选方案对比

| 候选 | 层次 | 改动 | 长久 / 演化评估 | 致命缺陷 |
|---|---|---|---|---|
| A | 数据层 patch | `buildRuntimeCache` 里 prepend `%String/%Array/%Map` typeDecl,让 llc 成功 | N 年返工度**高**:只补假设①,假设②仍破裂 —— 缓存能 build 但仍无人读;下一轮死代码巡检必再次命中。业界对标:成熟编译器删失效的 build-cache,不给它续命 | 给一个**确认无消费者**的子系统续命 —— 修了 llc 噪音症状,根因(死代码)原样保留;字段 9 判「表面」 |
| B | 接口层 trap | 给 `cmdBuild` 加 `--rt-cache` flag,`cmdTest` 拼脚本时令子进程 `bin/ss build --rt-cache` 传入 → 子进程置 `useRuntimeCache=1` 读缓存 | 底层依赖链**倒置**:B 是「上层接活缓存」,却依赖一个未落地的下层前提 —— 「运行时 IR 缓存确有性能价值」。业界对标:ccache / 编译缓存都是 profiling 证明瓶颈**之后**才加;本项目 bootstrap ~55s、`bin/ss test` 已 nproc 并行,无证据表明运行时 IR 重复 emit 是瓶颈。先做 B = 先做上层补下层 | 把一个 bug 修复扩成**价值未证的新特性**;且层 1 不修 B 也不能工作。本质是「为正当化死代码而造消费者」,方向倒置 |
| C | 架构层 refactor | 删 `gen_rt_cache.ss` 整文件 + `codegen.ss:11` import + `codegen.ss:332-337` 缓存分支(保留 `emitRuntimeDefs()` else 行为)+ `main.ss:428-429` + `main.ss:640` `rtObj` + `main.ss:595` clean glob 的 `ss_rt_cache.*` | 零底层依赖、终态:无更基础候选需先做。业界对标:删除死 / 坏子系统是标准做法。N 年返工度**低**:若日后确需编译缓存,会按真实 profiling 重新设计「生产者—消费者同进程」的正确架构,与 C 删的这套断链实现无继承关系,非返工 | 无 —— 子系统已确认既死(无消费者)又坏(确定性 llc 失败),删除连带消除 llc 噪音 + I026/I027-family 并发面 |

## 4. 决策行

选 C 因 该子系统经实测与静态分析坐实「既死(生产者—消费者永不同进程,假设②破裂)又坏(缺 typedecl,假设①破裂,确定性 llc 失败)」,删除是消除根因的架构层手法 —— A 只补假设①属表面(字段 9「表面」),B 倒置依赖链(为死代码造消费者、且依赖未证的缓存价值前提)。C 是最深可达层:更深的「死代码为何累积」属流程问题非本 bug 的代码根;`cmdTest` 自身 `/tmp/ss_test_*` 固定路径并发隐患是 I028 §关联观察 明列的**独立 bug**、不在本 scope。

## 5. §实证(MNK §字段 12)

### 5.1 根因定位 — grep / read 证据

全仓引用面(`grep -rn 'useRuntimeCache|runtimeCacheObj|runtimeCacheDecls|buildRuntimeCache|ss_rt_cache' bootstrap/`):

```
main.ss:428    if (fileExists(runtimeCacheObj) == 0) { buildRuntimeCache() }
main.ss:429    useRuntimeCache = 1
main.ss:595    rm -f ... /tmp/ss_rt_cache.*
main.ss:640    if (useRuntimeCache == 1) { rtObj = runtimeCacheObj }
codegen.ss:11    import { buildRuntimeCache, runtimeCacheObj, runtimeCacheDecls, useRuntimeCache } from "./rt/gen_rt_cache"
codegen.ss:332   if (useRuntimeCache == 1 && fileExists(runtimeCacheDecls)==1 && fileExists(runtimeCacheObj)==1) {
codegen.ss:333       appendFile(outFile, readFile(runtimeCacheDecls))
codegen.ss:336       useRuntimeCache = 0
gen_rt_cache.ss  子系统定义体本身(33 行)
```

`grep -rn -- --rt-cache`:仅命中 `gen_rt_cache.ss:1` 注释自身 → 「CLI driver」不存在,确认无消费者入口。
`emitRuntimeDefs` → `emitRuntimeGlobals`(`gen_runtime.ss:151-196`)只 emit `%TypeInfo`/`%ObjHeader`;`%String/%Array/%Map` 唯一定义点 `codegen.ss:352` `typeDecl` 常量 —— 确认假设①破裂。

RED 实测(`rm -f /tmp/ss_rt_cache.*; bin/ss test tests/phase2 2>&1 | grep 'getelementptr must be sized'`):

```
llc-18: error: /tmp/ss_rt_cache.ll:375:34: error: base element of getelementptr must be sized
```

命中 → 确定性 llc 失败坐实(单进程零并发即重现 → 与 next_prompt 的并发假设无关)。

### 5.2 最危险假设 + 最小 spike / 试切

**最危险假设**:「删 `codegen.ss:332-337` 的 `if useRuntimeCache` 分支、退化为单行 `emitRuntimeDefs()`,不改变任何真实编译路径行为」。一旦此假设错(某路径真的依赖缓存分支),删除即 regression。

**试切验证**:`generateToFile` 的唯一调用方是 `compile()`(`main.ss:626`),`compile()` 由 `cmdBuild`/`cmdRun`/`cmdBuildLegacy` 调用 —— 这三条路径都不置 `useRuntimeCache`(仅 `cmdTest` 置,而 `cmdTest` 不调 `compile()`)。∴ `codegen.ss:332` 的 `useRuntimeCache==1` 首个合取项在任何执行 `compile()` 的进程里**恒 false** → `else` 分支(`emitRuntimeDefs()`)**恒被取**。把 if/else 整体替换为 `emitRuntimeDefs()` 对每条真实路径**逐字节等价**。`main.ss:640` 同理:`rtObj` 恒 `""`,`${rtObj}` 在 musl-gcc 命令里恒空。spike 即整体删除后 `./build.sh bootstrap` 三阶段固定点 —— 删除是纯死分支移除,固定点必然保持(若有活引用,bootstrap 立即断,届时回滚;静态分析已穷尽引用面,无活引用)。

**same_pattern 排查**:「死缓存子系统」形态在 `bootstrap/` 仅 `gen_rt_cache.ss` 一处;`cmdTest` 的 `/tmp/ss_test_*` 固定路径是「活代码并发 race」另一 bug 类(I028 §关联观察),非「死子系统」同模式 → same_pattern_count = 0。
