ultrathink D144 Phase 1 RED 复现 + 信息源探查轮 — D141 §Followup F3 + D142 §Followup F2 + D143 §Followup F1 ternary contextual typing 同模式合并候选入口实施(Phase 0 commit `<TBD>` 落档后启 Phase 1 调研 — D135/D136/D137/D140/D141/D142/D143 范式延续 / D141 Phase 1 commit `bb96e27` + D142 Phase 1 commit `eb26644` + D143 Phase 1 commit `f089738` 同模式)。§任务清单(每条 RED 实测 + 探查 fact 入 §A.2 假设挑战表 + Phase 1 commit message 收关):(1) 写 `/tmp/spike_ternary_red.ss` 7 形态(null+T 反推 T? / T+T 显式两分支同类型 / 嵌套 ternary / class 实例 ternary / interface upcast / 多层 nullable / fn 返回 ternary)+ `bin/ss run /tmp/spike_ternary_red.ss 2>&1 | grep -c <pattern>` IR 层 RED 铁证(预期硬错或 silent miscompile);(2) §A.2 H1 同模式实证(funcParamTypes `T?` / 结构化字符串 codegen 阶段满载 — `bootstrap/gen/codegen.ss:110-112 + gen_registry.ss:56-57` register 时机 + `codegen.ss:326 registerAllDecls(rootId)` 在 `emitGlobalsAndCode(rootId)` 之前实证)+ §A.2 H10 cross-D 反思继承(eval pre-eval 时序 — outer call site `eval/method_call.ss + eval/call.ss + eval/new_expr.ss` 三处通用 pre-eval `genVal(argId)` 之前反推前移,不仅 grep 字面 handler `evalTernary` — D143 H10 cross-D 教训 memory `feedback_h10_cross_d_verify.md`);(3) TERNARY 节点 slot 占用探查(`bootstrap/parse/parse_exprs.ss` `parseTernary` 后 s1/s2/s3/i1-i4 槽位)— 选 nSetS? 存 branchType(D141 ARROW_FUNC PARAM s2 + D142 ARRAY_LIT s2 + D143 OBJ_LITERAL s2 同范式);(4) TERNARY kind dispatch site 全 grep(check_exprs.ss + check_types.ss + gen_exprs.ss / gen/exprs/ + parse_exprs.ss + eval/)+ Phase 2 改入口 / 反推插桩点 / D067 nullable 调用前移点全锁;(5) Phase 1 commit message 含 RED 实证 + 节点 slot 探查 PASS + §A.2 H1/H10 实证 PASS + **Phase 0 commit hash 回填占位符**(D144 §Phase 0 收关锚 + Status 时间线 共 2 处 `<TBD>` 替换 Phase 0 实际 commit hash — D135/D136/D137/D140/D141/D142/D143 范式延续);D144 主线 sub-D 范围(C2 接口层 trap 选 + G1 D141/D142/D143 同模式复刻 + §A.2 H1-H13 + §A.3 废案锁定 + Phase 0-5 计划草案)。
