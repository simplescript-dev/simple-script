ultrathink D142 §Phase 1 RED 复现 + 信息源探查 — D141 §Followup F1 array literal contextual typing 候选入口 Phase 1 起首,RED 实测 `bin/ss build /tmp/spike_array_lit_untyped.ss --emit-ir` 看 IR 层是否走错路径(单元素 / 多元素同质 / 空数组 / 嵌套 array `[[1,2],[3,4]]` / interface upcast 五形态各跑一遍,锁定 RED 入口);信息源探查 funcParamTypes 在 codegen 阶段已 ready(D141 H1 已实证 codegen.ss:326 registerAllDecls 在 check(root) 之后,D142 同模式假设大概率成立 — 走个过场实证记录) + ARRAY_LIT 节点 slot 当前占用情况(`bootstrap/parse/parse_exprs.ss:496` newNode + nSetList 后,s1/s2/s3 是否空 — Phase 2 选 slot 命名)+ evalArrayLit pre-eval 时序确认(对照 D141 H10 同形 — `genVal(elemId)` 在 `genArrayLit` 之前,反推必须前移到 eval pre-eval 之前);§A.2 H1/H10 实证记录 + 新假设(若有)入档;Phase 1 收关条件 = (a) RED 铁证(IR 层对比 typed/untyped 双路径)成立 (b) 时序探查就位写入 §1 必读清单 + §A.2 (c) 隐藏假设 H1/H10 实证 PASS — 满足后 Phase 1 commit 落锚 + Status 收关 + commit hash 回填 + next_prompt 指向 Phase 2(G1 路径实施);D135/D136/D137/D140/D141 范式延续(每 Phase 独立 commit 大改档).
