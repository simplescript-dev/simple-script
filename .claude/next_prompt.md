ultrathink D144 Phase 3 测试覆盖 + §A.2 H1-H8 实证 + tests/d144_ternary_inference 6+ 形态测试 — D135/D136/D137/D140/D141/D142/D143 范式延续(D143 Phase 3 commit `15f8dfe` 同模式)。§前置就绪(Phase 2 commit `<PHASE2_HASH>` 已锁):checker check_exprs.ss + check_types.ss + gen_types.ss inferType + resolveObjClass GROUPING/TERNARY case + helper 5 函数(isNullableType / extractInnerType / unifyBranchTypes / propagateTernaryBranchType / inferTernaryBranchType)+ gen_calls.ss / gen/methods/gen_methods.ss args 循环 + eval pre-eval 三处前移(method_call.ss / call.ss / new_expr.ss)+ ternary.ss phi llType 反推 / spike X4-1 + X mismatch RED→GREEN 双铁证 / bootstrap 三阶段固定点 PASS / tests/ 276/4/280 baseline 不降 / reflection_health GATE PASS 扩容申报 D144#扩容申报-Phase2 11 metric bump / 关键 bug 修复(checker 排除 `"null"` 同类型回填 + helper 完整递归 H3 嵌套反推链路)。§任务清单(Phase 3 实施 — 测试覆盖 + 隐藏假设全 PASS):(1) **`tests/d144_ternary_inference/` 新增 6+ 测试**:`null_plus_T.ss`(null+`User?` 反推 — `takesNullableUser((cond)?null:new User("X"))` H1 反推 nullable + H2 两分支类型 widen)/ `T_plus_T_explicit.ss`(T+T 显式 — `takesInt((cond)?1:2)` H6 显式优先 / `nested.ss`(嵌套 ternary — `takesNullableUser((c1)?null:((c2)?null:new User("A")))` H3 嵌套反推 capture 链)/ `class_instance.ss`(class 实例 ternary — `takesNullableUser((cond)?null:new User("X",18))` callee `User?` H4)/ `interface_upcast.ss`(`takesShape((cond)?new Circle(1):new Square(2))` callee `IShape` H5 vtable indirect dispatch)/ `multi_level_nullable.ss`(多层 nullable — `(cond)?null:((c2)?null:new User("X"))` H3 嵌套 + null 多层)/ `explicit_override.ss`(`let n: User? = (cond)?null:new User("X"); takesFn(n)` D084 rewrite + D067 既有路径 H6 显式注解优先级)/ `mismatch_hard_error.ss`(`takesInt((cond)?1:"X")` checker 硬错 — H4 严格模式 + H13 失败粒度 expect-error 测试)/ `field_access.ss`(`((cond)?u1:u2).name` X4-1 字段访问 GREEN — Phase 2 resolveObjClass GROUPING/TERNARY case 修复入口);(2) **§A.2 H1-H8 隐藏假设全 PASS**:H1 funcParamTypes 时序 PASS(Phase 1 实证)/ H2 两分支类型 widen / H3 嵌套反推 capture 链 / H4 null literal D067 路径复用 / H5 interface upcast / H6 显式优先 / H7 tests baseline 不降 / H8 reflection GATE 不破;H9 var binding callee OOD scope / H10 eval pre-eval 时序(Phase 1 + Phase 2 实证)/ H11 callee `T?` 已就绪无升级前置 / H12 parser 不需扩 / H13 反推失败硬错粒度全标 OOD / 弱化;(3) **VCM 六验全跑**:bootstrap 三阶段固定点 PASS + tests/ d144_ternary_inference 全绿 + tests/ 276/4/280 baseline 不降 + reflection_health GATE PASS no regressions + d_doc_index_linter F1 = 0 + ultrathink_linter PASS + simplify;(4) **commit `feat(D144): Phase 3 — 测试覆盖 + §A.2 H1-H8 实证 + tests/d144_ternary_inference 9 形态全绿 + Phase 2 hash 回填 + Status 行收关 + next_prompt 指向 Phase 4`**;(5) **next_prompt 指向 Phase 4**(workaround cleanup — grep + 删现存 fn 实参 ternary 临时变量绑定 workaround,若有 `let n: User? = (cond)?null:new User("X"); takesFn(n)` 形态 — Phase 1 grep 实测后定计数,预估 lib/ + tests/d067/ 多处)+ Phase 1 完结 commit `17a1573` 锚回顾 + Phase 2 完结 commit `<PHASE2_HASH>` 锚回顾。**根因优先**(CLAUDE.md §项目技术规则):TERNARY 反推机制是编译器主线能力,主线信息源破裂走根因修复,不接受 annotation / handler 旁路;**根因解决度 + 第一性需求覆盖度**第一,LOC / 工程量 / bootstrap 轮数不是次优排序依据。
