ultrathink D144 Phase 2 codegen 阶段反推实施轮 — G1 D141/D142/D143 同模式复刻(callee PARAM 已结构化 SSoT 复用 + codegen 阶段反推 + eval pre-eval 时序前移 + funcParamTypes 信息源单点)— D135/D136/D137/D140/D141/D142/D143 范式延续(D143 Phase 2 commit `5eb722e` 同模式)。§前置就绪(Phase 1 commit `17a1573` 已锁):H1/H10 实证 PASS(funcParamTypes 时序 + outer call site 通用 pre-eval `genVal(argId)` 反推前移 D141/D142/D143 同点) / TERNARY 节点 slot 选 nSetS2 锁定(parse_exprs.ss:13-16 parseExpr ternary 占 i1/i2/i3,s1/s2/s3+i4+nList 全空闲 — 与 D141 ARROW_FUNC PARAM s2 + D142 ARRAY_LIT s2 + D143 OBJ_LITERAL s2 同范式) / TERNARY 8 dispatch site 全 grep PASS(parse_exprs.ss:13 parser + check_exprs.ss:304 checker case + check_types.ss:201 checker inferType + gen/exprs/exprs.ss:68 genVal dispatch + gen_types.ss:541 codegen inferType + eval/eval_expr.ss:64 evalExpr dispatch + eval/ternary.ss:4-33 evalTernary handler + pir/pir_lower.ss:252 PIR use 分析,PIR 仅递归收集 use 不破反推链路) / D144 §第一性需求 motivating example `int?` 不合法修正已落档(D067 primitive nullable 硬错 `bootstrap/checker/check_narrow.ss:6`,改用 `User?`)+ 真 RED 形态另立(X4-1 字段访问 LLC `'%14' defined with type 'ptr' but expected 'i32'` + X mismatch 两分支类型不一致 LLC `global variable reference must have pointer type` 双铁证)。§任务清单(Phase 2 实施 — bootstrap 改 + spike RED→GREEN 凭据 + bootstrap 三阶段固定点):(1) **checker check_exprs.ss:304-309 TERNARY case 加两分支类型一致性检查** — 当前仅依次 checkExpr 三子节点 silent miscompile,新增 `inferType(thenId)` vs `inferType(elseId)` mismatch 时硬错(参考 D143 §A.1.1 G1 接口层 trap 同模式 — `check_exprs.ss:285` OBJ_LITERAL untyped 放行风格)+ 节点 nSetS2 优先回填 unified branchType(对齐 D141 ARROW_FUNC PARAM s2 + D142 ARRAY_LIT elemType s2 + D143 OBJ_LITERAL className s2)+ checker 端硬错粒度精确化锚 §A.2 H4 决策行严格模式继承 D143;(2) **checker check_types.ss:201 TERNARY inferType case 加优先读 nGetS2 fallback `inferType(nGetI2(id))`** — 当前单一返 then 分支,改为 `let stored = nGetS2(id); if (stored != "") return stored; return inferType(nGetI2(id));`(参考 D143 inferType OBJ_LITERAL case fallback "auto" 同模式 + D142 ARRAY_LIT case fallback elemType 同模式);(3) **gen_types.ss:541 TERNARY inferType case 加优先读 nGetS2 fallback then 分支** — 与 checker inferType 对偶,**信息源单点**(参考 D141/D142/D143 G1 4 落点同模式)+ gen_types.ss 加 helper 三函数 — `isNullableType(t)` 判 `T?` 形态(D067 nullable 路径复用) / `extractInnerType(t)` 取 `T?` 的 `T`(去 `?` 后缀) / `unifyBranchTypes(thenT, elseT)` 两分支类型统一(both ptr → ptr / both prim 同 → 同 / mismatch → 硬错)— 对偶 D141 `isFnType` + D142 `isArrayType` + D143 `isClassType` helper 同范式;(4) **gen_calls.ss + gen/methods/gen_methods.ss args 循环加 inferTernaryBranchType(argId, callee, idx)** — D141/D142/D143 G1 4 落点同模式(callee PARAM 期望读取 + nSetS2 回填 unified branchType)+ 嵌套 ternary 内层 ternary 反推链路(类比 D143 H3 嵌套反推 `class/class_method.ss:genNamedConstructorArgs` 用 classFieldTypes 反推 inner OBJ_LITERAL,D144 用 callee PARAM 反推 inner TERNARY,递归同模式);(5) **eval pre-eval 前移三处** — `eval/method_call.ss line 65 之后` + `eval/call.ss line 39 之后` + `eval/new_expr.ss` 同点(D141/D142/D143 反推前移落锚 line 61-65/33-39/27-41)追加 `inferTernaryBranchType(argId, callee, idx)` 第 4 行;**§A.2 H10 cross-D 反思继承**(D143 教训):**不仅 grep 字面 handler `evalTernary`**,outer call site 通用 pre-eval `genVal(argId)` 路过 TERNARY 走 phi llType 错位,Phase 2 修正三处 pre-eval 之前同模式前移 inferTernaryBranchType — 与 D141 H10 + D142 H10 + D143 H10 完全同形;(6) **eval/ternary.ss line 12 phi llType 优先读反推 branchType** — 当前 phi llType 单一推导,改为 `let stored = nGetS2(id); let llType = stored != "" ? mapTypeToLLVMType(stored) : phi 默认推导;`(对偶 D143 NEW_EXPR + vtable indirect H5b interface upcast 路径同模式);(7) **spike RED→GREEN 凭据**:(a) `/tmp/spike_d144_phase2_x4_1.ss` X4-1 字段访问 RED→GREEN(callee `function takesUser(u: User?): void` + `((cond)?u1:u2).name` 实际 `u1.name` 输出 — 对照 RED `'%14' defined with type 'ptr' but expected 'i32'` LLC error)+ (b) `/tmp/spike_d144_phase2_x_mismatch.ss` X mismatch 类型一致性 RED→硬错 GREEN(`takesInt((cond)?1:"X")` checker 硬错 mismatch 而非 LLC silent miscompile — 对照 RED `global variable reference must have pointer type` LLC silent error);(8) **VCM 六验全跑**:bootstrap 三阶段固定点 PASS + tests/ baseline 不降(d141/d142/d143 反推机制不破)+ reflection_health_linter GATE PASS(若 bootstrap 增量需扩容申报-Phase2 同模式 6 metric bump,LOC 增量预估 ~100 行 helper + ~30 行 args 循环 + ~12 行 pre-eval 三处)+ d_doc_index_linter F1 = 0 + ultrathink_linter PASS + simplify;(9) **commit `feat(D144): Phase 2 — codegen 阶段反推实施 + G1 D141/D142/D143 同模式复刻 — checker check_exprs.ss + check_types.ss:201 + gen_types.ss:541 优先读 nGetS2 fallback then 分支 + helper isNullableType/extractInnerType/unifyBranchTypes + gen_calls/gen_methods args 循环 + eval pre-eval 前移三处 + ternary.ss phi llType 反推 + spike X4-1 + X mismatch RED→GREEN 双铁证`;(10) **next_prompt 指向 Phase 3**(测试覆盖 + §A.2 H1-H13 全 PASS 实证 + tests/d144_ternary_inference 6 形态测试 — D143 Phase 3 commit `15f8dfe` 同模式)+ Phase 1 完结 commit `17a1573` 锚回顾。**根因优先**(CLAUDE.md §项目技术规则):TERNARY 反推机制是编译器主线能力,主线信息源破裂走根因修复,不接受 annotation / handler 旁路;**根因解决度 + 第一性需求覆盖度**第一,LOC / 工程量 / bootstrap 轮数不是次优排序依据。
