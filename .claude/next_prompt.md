ultrathink 用户审本轮 I021 §下轮升根 P1 候选巡检结论 — 按 CLAUDE.md §Root Cause 优先 修订版 (d) 子条按**根因解决度**(非工程量)重排:I021b V=int + I021c V=double **同根因**(invoke sentinel `method_call.ss:107` hardcode `ptr ${aReg}` + `gen_registry.ss:42-58 registerClassMethodRetType` 漏注册 `funcParamTypes`,V 维度两破裂表现);I021d V=class **不属同根因**(query string→User 无 deserializer,@RequestBody / @ModelAttribute 域语义混淆,I021 §下轮 list "I020c mirror" 描述与 @RequestParam 域语义破裂)。挑定后立子决策档 + 同轮回写 I021 主 issue doc status `Planned` → `Done at lib/spring/boot/application.ss:37-79 + bootstrap/eval/interp_obj.ss MethodMeta.params + bootstrap/parse/prelude.ss ParamMeta.annotations + commit 75f0516 + 8844e5f` + reflection_linter stale REGRESSION (gen_decls.ss cur=730 vs bm=708) 在同一 commit 一并 bump 扩容申报登 D123 §扩容申报。
