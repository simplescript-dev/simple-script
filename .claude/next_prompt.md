ultrathink D138 Phase 0 commit hash 回填 `<本轮 commit>` + Status 行收关(Phase 0 完结)+ next_prompt 指向 Phase 1(协议层 OkPacket 完整 parse + GeneratedKeyResultSet)— D135/D136/D137 SQL 主线范式延续(不是 D141-D145 docs-heavy 衍生链)。

**用户对话锁(2026-04-28)**:
- SQL 主线 — D138 是 D137 §F8 generated keys 直接续接
- **最优最佳,不考虑成本,不 workaround,不节省** — 完整 JDBC 4.3 + Spring KeyHolder 标准
- D138 5 Phase close 后**停**,不自动衍生 D139+(Followup F1-F7 仅作锚记不预设下个 D 入口)

§前置就绪(D138 Phase 0 commit `<本轮回填>` 已锁):
— D138 §Phase 0 章节 [✓] Done — 332 行 D 文档落档(完整 JDBC 4.3 + Spring KeyHolder 标准设计 + §A.1 C2 选 + §A.2 H1-H8 + Phase 0-5 计划)
— d_doc_index F1=0 PASS + reflection_health GATE PASS

§任务清单(D138 Phase 0 hash 回填 + Phase 1 起首):

(1) **Phase 0 commit hash 回填**:
  - `git log --oneline | head -3` 找最新 D138 Phase 0 commit hash → Edit `docs/3-decisions/D138-mysql-generated-keys.md` 3 处 `<placeholder>`:
    - line 3 Status 行 Phase 0 部分
    - §Phase 收关锚 §Phase 0 章节标题
    - Status 时间线起首行

(2) **Phase 1 实施 — 协议层 OkPacket 完整 parse**(D135/D136/D137 SQL 主线范式):
  - `lib/com/mysql/query.ss` 加 `class OkPacket { affectedRows: int, lastInsertId: int, statusFlags: int, warnings: int }`(D136 PrepareOk class 同模式)
  - `lib/com/mysql/query.ss` 加 `parseOkPacket(payload, payloadLen): OkPacket` 完整 4 字段 parse — header byte + lenenc int affected_rows + lenenc int last_insert_id(用 lengthEncodedIntSize 推 offset)+ 2 byte LE status_flags + 2 byte LE warnings
  - `lib/com/mysql/query.ss` 加 `readUpdateResultPacket(fd): OkPacket` — 完整返 OkPacket(commit/rollback/setAutoCommit 等内部消费方零破坏前提下)
  - `lib/com/mysql/query.ss` `readUpdateResult(fd): int` 改 thin wrapper — 调 `readUpdateResultPacket` 取 affectedRows 字段返 int(向后兼容 D134/D135/D136 调用方)
  - `lib/com/mysql/query.ss` `parseOkPacketAffectedRows` 改 thin wrapper(向后兼容)
  - `lib/com/mysql/query.ss` 加 `class GeneratedKeyResultSet : ResultSet`(单行单列)— 字段 `key: int` + `firstAccessed: int` + 实现 next() / getString() / getInt() / getLong() / getDouble() / getBoolean() / close() — 列名 `"GENERATED_KEY"` 是 MySQL Connector/J 标准
  - **不动**:OK packet `info string`(SESSION_TRACK 路径)留 §Followup F1
  - VCM 验证:bootstrap 三阶段固定点 PASS + tests/d134_mysql + tests/d135_caching_sha2 + tests/d136_prepared_statement baseline 不降 + tests/ 全 baseline 不降

(3) **commit `feat(D138): Phase 0 hash 回填 <Phase 0 hash> + Phase 1 — 协议层 OkPacket 完整 parse + GeneratedKeyResultSet — class OkPacket 4 字段(affectedRows/lastInsertId/statusFlags/warnings)+ parseOkPacket + readUpdateResultPacket + readUpdateResult/parseOkPacketAffectedRows thin wrapper 向后兼容 + class GeneratedKeyResultSet : ResultSet(单行单列 GENERATED_KEY 列)— D135/D136/D137 SQL 主线范式延续`**
  - 仅 stage `lib/com/mysql/query.ss` + `docs/3-decisions/D138-*.md` + `.claude/next_prompt.md`

(4) **next_prompt 指向 Phase 2**:JDBC API + MySQL 实现完整(`lib/java/sql.ss` interface 扩展 + MysqlStatement / MysqlPreparedStatement 字段扩 + executeUpdate 改 + getGeneratedKeys / getLastInsertId 实现 + MysqlConnection.prepareStatement(sql, autoGeneratedKeys) 重载)

§根因优先(CLAUDE.md §项目技术规则):
— 完整 JDBC 4.3 + Spring KeyHolder 标准 — 不裁剪 / 不 workaround / 不节省路径
— OK packet 全 4 字段 parse — 无 skip / 无 silent abandon
— readUpdateResult thin wrapper 向后兼容 — 既有调用方零破坏,扩展走 readUpdateResultPacket 新出口
— D135/D136/D137 SQL 主线范式 — 每 Phase 独立 commit + Status 收关 + hash 回填 + next_prompt 自闭环

§D135/D136/D137 commit hash 回填轮同形参考:
— D135 Phase 0 commit + Phase 1 实施同 commit 模式
— D136 Phase 0 + Phase 1 同模式
— D137 Phase 0 commit `bafc25a` + Phase 1 commit `d2df1ba` 同模式
— D138 Phase 0 commit `<本轮 commit>` + Phase 1 commit(下轮)
