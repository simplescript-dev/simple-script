ultrathink D135 Phase 2 起立 — docker retcon + tests/d135_caching_sha2/ e2e + D134 §Status superseded 锚回填(承 commit `d34e0a7` D135 Phase 1 caching_sha2_password fast-path 实施完成 + 6/6 handshake_test vec PASS + 256/260 全集 baseline 不降 + bootstrap stage2==stage3 byte-identical + 7 协议字节命名常量 simplify 采纳)

改 tests/d134_mysql/docker-compose.yml 删 `command: --default-authentication-plugin=mysql_native_password` 行(MySQL 8 默认 caching_sha2 接管,L24 + L6 注释段同步);改 tests/d134_mysql/integration_test.ss L10 注释 `mysql_native_password globally; D134 Phase 3 scramble path` retcon → caching_sha2 描述;新建 tests/d135_caching_sha2/integration_test.ss ~150 LOC / 4 e2e(test 1 connect + isClosed + close 基础 caching_sha2 fast-path / test 2 第二次 connect server cache hit 0x01 0x03 fast_auth_success 路径 / test 3 wrong password 0xFF ERR / test 4 server perform_full_authentication 0x01 0x04 拒绝路径 — 用 FLUSH PRIVILEGES + ALTER USER 清 cache 触发,降级 unit mock 写死 0x01 0x04 byte sequence);改 docs/3-decisions/D134-jdbc-mysql-wire-protocol.md §Status 加 superseded 锚 `D135 superseded mysql_native_password 实施层 at commit <Phase 2 hash> (2026-04-26)` + §Phase 3 + §Phase 6 docker-compose 段加 superseded 注脚指向 D135;改 docs/3-decisions/D135-mysql-caching-sha2-fast-path.md §附录 B Phase 2 [✓] Done at commit <Phase 2 hash>(承 D134 二阶段回填范式 — 第一 commit 落代码 + 第二 commit 回填 hash 防 self-reference 循环 + 不 amend)。

RED 实测:`grep -rcE "mysqlNativePasswordScramble|mysql_native_password" tests/d134_mysql/docker-compose.yml tests/d134_mysql/integration_test.ss` = 2+1 = 3(Phase 1 已删 lib/com/mysql/ + handshake_test.ss 主线;残 docker-compose 2 + integration_test.ss 1 注释,Phase 2 清零 docker-compose + integration_test.ss 注释 retcon);`ls tests/d135_caching_sha2/` = "No such file or directory"(Phase 2 新建)。GREEN:docker-compose 行删 + integration_test.ss 注释 retcon → tests/d134_mysql/ 整树 mysql_native_password 残留 = 0 + docker compose -f tests/d134_mysql/docker-compose.yml up -d --wait MySQL 8 caching_sha2 默认接管 + bin/ss test tests/d134_mysql/ 4 file pass / 0 fail(e2e 8 case 全绿,plugin 透明)+ bin/ss test tests/d135_caching_sha2/ 4 case 全绿 + bin/ss run tools/d_doc_index_linter.ss F1 = 0(D134 §Status superseded 锚回填后 D135 § 引用挂上,F2 orphan D135 清零)+ axiom 红线 grep / nm = 0 永久维持。档位大改 — MNK §M 九问 + §N 六验 + /simplify 3 agent + commit "feat(D135): Phase 2 ..." + 下轮 D135 全 Phase 收关报告 next_prompt + 等用户指定下任务 二阶段范式回填本轮 Phase 2 commit hash(承 D134 全 Phase 收关 commit 范式 e509be1 → 3b91839)。
