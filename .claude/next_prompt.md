ultrathink D135 Phase 1 起立 — caching_sha2_password 实施替换(承 commit `571e54d` D135 Phase 0 D 文档骨架落盘)

改 lib/com/mysql/handshake.ss:(a) 删 mysqlNativePasswordScramble + 加 cachingSha2Scramble(SHA256(pwd) XOR SHA256(SHA256(SHA256(pwd)) ‖ nonce),32-byte 64 hex chars,走 lib/crypto.ss:239 sha256flex dataHex 路径,注意 stage2 ‖ nonce 顺序与 mysql_native 的 nonce ‖ stage2 相反)(b) sendHandshakeResponse41 plugin 字面值 mysql_native_password → caching_sha2_password + auth-response length 20 → 32 + payloadLen 计算更新 (c) mysqlConnect 处理 server fast-path 响应 0x00 OK / 0x01 0x03 fast_auth_success / 0x01 0x04 perform_full_authentication ERR(显式拒绝)/ 0xFE AuthSwitchRequest ERR / 0xFF ERR;tests/d134_mysql/handshake_test.ss 删 4 mysql_native vec test + 加 4 caching_sha2 vec test(Python hashlib SHA-256 离线算 + MySQL sha2_password_common.cc generate_auth_string_sha256() 反查 stage2 在前 nonce 在后串接顺序确认)。

RED 实测:grep -nE "cachingSha2Scramble|caching_sha2_password|fast_auth_success" lib/com/mysql/handshake.ss = 0(当前 handshake.ss:244 仅 errMsg "server requested AuthSwitch (unsupported in Phase 3)")。GREEN:RED ≥ 5 + grep -rnE "mysqlNativePasswordScramble|mysql_native_password" lib/com/mysql/ tests/d134_mysql/handshake_test.ss 接近 0(docker-compose 留 Phase 2 删)+ bin/ss test tests/d134_mysql/handshake_test.ss 4 vec 全绿 + ./build.sh bootstrap 三阶段固定点 stage2 == stage3 byte-identical(纯 lib 不动 bootstrap)。档位大改 — MNK §M 九问 + §N 六验 + /simplify 3 agent + commit "feat(D135): Phase 1 ..." + 下轮 Phase 2 起立 next_prompt 回填本轮 commit hash 二阶段范式(承 D134 Phase 1-6 范式)。
