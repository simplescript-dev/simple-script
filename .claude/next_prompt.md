ultrathink D136 Phase 1 起立 — lib/java/sql.ss 加 `interface PreparedStatement extends Statement`(setInt/setString/setLong/setDouble/setBoolean/setNull/重载 executeQuery() executeUpdate())+ `Connection.prepareStatement(sql)` + lib/com/mysql/prepared.ss(新)~250 LOC(协议常量 COM_STMT_PREPARE 0x16 / EXECUTE 0x17 / CLOSE 0x19 + MYSQL_TYPE_LONG/DOUBLE/LONGLONG/NULL/VAR_STRING + class PrepareOk + sendComStmtPrepare + parsePrepareOk + readPrepareOk + readParamDef + readColumnDefList + class MysqlPreparedStatement 框架占位)+ lib/com/mysql/jdbc.ss 加 MysqlConnection.prepareStatement + tests/d134_mysql/prepared_test.ss(新)~150 LOC 4 vec test,严格按 D136 §Phase 1 + §A.2 + §A.5 + §核心原则 1-13 + 反模式 + R1-R5 执行。RED `grep -rnE "COM_STMT_PREPARE|prepareStatement|PreparedStatement|sendComStmtPrepare" lib/com/mysql/ lib/java/sql.ss | wc -l` = 0 改前实测 ✓(本轮 Phase 0 已跑),改后 ≥ 5 + bin/ss test tests/d134_mysql/prepared_test.ss 全绿(4 vec)+ ./build.sh bootstrap stage2==stage3 byte-identical + bin/ss test tests/ 256+/260 不降 GREEN 收敛。Phase 1 commit 落地后下轮 Phase 2(EXECUTE + binary result set + CLOSE)起立由 terman claude-next preset 30s 窗口注入,本轮 Phase 0 commit hash 在 Phase 1 起立首步二阶段回填到 D136 §Status / §附录 B Phase 0 placeholder。
