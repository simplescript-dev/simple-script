ultrathink D138 Phase 1 commit hash 回填 `<本轮 commit>` + Status 行收关(Phase 1 完结)+ next_prompt 指向 Phase 2(JDBC API + MySQL 实现完整)— D135/D136/D137/D138 Phase 0/Phase 1 SQL 主线范式延续(不是 D141-D145 docs-heavy 衍生链)。

**用户对话锁(2026-04-28)**:
- SQL 主线 — D138 是 D137 §F8 generated keys 直接续接
- **最优最佳,不考虑成本,不 workaround,不节省** — 完整 JDBC 4.3 + Spring KeyHolder 标准
- D138 5 Phase close 后**停**,不自动衍生 D139+(Followup F1-F7 仅作锚记不预设下个 D 入口)

§前置就绪(D138 Phase 1 commit `<本轮回填>` 已锁):
— D138 Phase 0 hash `d47a05d` 已落档 + Phase 1 hash `<placeholder>` 待本轮回填
— Phase 1 实施代码已落地:`lib/com/mysql/query.ss` `class OkPacket`(4 字段)+ `parseOkPacket` + `readUpdateResultPacket` 单一信息源出口 + `readUpdateResult/parseOkPacketAffectedRows` thin wrapper 向后兼容 + `class GeneratedKeyResultSet : ResultSet`(单行单列 GENERATED_KEY)
— d_doc_index F1=0 PASS + reflection_health GATE PASS no regressions + bootstrap 三阶段固定点 PASS + d134_mysql 5/0/5 + d135_caching_sha2 1/0/1 + d136_prepared_statement 1/0/1 + d141-d144 反推 5/6/6/8 全绿 + 全 tests/ 284/4(4 fail 全 pre-existing — git stash 反证无 mysql/query 引用)

§任务清单(D138 Phase 1 hash 回填 + Phase 2 起首):

(1) **Phase 1 commit hash 回填**:
  - `git log --oneline | head -3` 找最新 D138 Phase 1 commit hash → Edit `docs/3-decisions/D138-mysql-generated-keys.md` 3 处 `<placeholder>`:
    - line 3 Status 行 Phase 1 部分
    - §Phase 收关锚 §Phase 1 章节标题
    - Status 时间线 Phase 1 行

(2) **Phase 2 实施 — JDBC API + MySQL 实现完整**(D135/D136/D137/D138 Phase 1 SQL 主线范式):
  - `lib/java/sql.ss` `interface Statement` 加 `getGeneratedKeys(): ResultSet` + `getLastInsertId(): int` 2 method
  - `lib/java/sql.ss` `interface PreparedStatement` 同位 2 method(`getGeneratedKeys` + `getLastInsertId`)
  - `lib/java/sql.ss` `interface Connection` 加 `prepareStatement(sql: string, autoGeneratedKeys: int): PreparedStatement` 重载(MySQL 等价 no-op 但保 JDBC spec 一致 — D138 §A.2 H4)
  - `lib/java/sql.ss` 加 `const RETURN_GENERATED_KEYS = 1` + `const NO_GENERATED_KEYS = 2`(JDBC 4.3 标准常量)
  - `lib/com/mysql/jdbc.ss` `class MysqlStatement` 加 `lastInsertId: int` 字段(default 0)+ executeUpdate 改 — 用 `readUpdateResultPacket`(query.ss Phase 1 单一信息源出口)→ 仅在 `pkt.affectedRows >= 0` 时写 `this.lastInsertId = pkt.lastInsertId`(D138 §A.2 H7 ERR 防御 — getLastInsertId 返 0)+ 返 `pkt.affectedRows`
  - `lib/com/mysql/jdbc.ss` `class MysqlStatement` 加 `getLastInsertId(): int` 返字段 + `getGeneratedKeys(): ResultSet` 返 `new GeneratedKeyResultSet(this.lastInsertId, 0)`
  - `lib/com/mysql/jdbc.ss` `class MysqlConnection` 加 `prepareStatement(sql: string, autoGeneratedKeys: int): PreparedStatement` 重载 — thin delegate `return this.prepareStatement(sql)`(autoGeneratedKeys 对 MySQL 协议无运行时影响)
  - `lib/com/mysql/prepared.ss` `class MysqlPreparedStatement` 加 `lastInsertId: int` 字段 + executeUpdate 改用 `readUpdateResultPacket`(同模式)+ `getLastInsertId` / `getGeneratedKeys` 实现
  - `lib/com/mysql/prepared.ss` `doPrepare` 构造 `new MysqlPreparedStatement(...)` 加 lastInsertId 初始值 0
  - `lib/com/mysql/jdbc.ss` `createStatement` 构造 `new MysqlStatement(this.fd)` 改 `new MysqlStatement(this.fd, 0)` 加 lastInsertId 初始值 0
  - **不动**:Spring KeyHolder(留 Phase 3)/ Phase 1 query.ss 路径(向后兼容 thin wrapper 不破)
  - VCM 验证:bootstrap 三阶段固定点 PASS + tests/d134_mysql / d135 / d136 baseline 不破 + tests/d141-d144 baseline 不破 + 全 tests/ baseline 不降 + reflection_health GATE PASS + d_doc_index PASS + spike `stmt.getLastInsertId()` / `stmt.getGeneratedKeys()` 编译 GREEN(完整集成测试 Phase 4)

(3) **commit `feat(D138): Phase 1 hash 回填 <Phase 1 hash> + Phase 2 — JDBC API + MySQL 实现完整 — interface Statement/PreparedStatement 加 getGeneratedKeys/getLastInsertId 2 method + interface Connection 加 prepareStatement(sql, autoGeneratedKeys) 重载 + const RETURN_GENERATED_KEYS/NO_GENERATED_KEYS + MysqlStatement/MysqlPreparedStatement 加 lastInsertId 字段 + executeUpdate 改用 readUpdateResultPacket 存 lastInsertId(D138 §A.2 H7 ERR 防御)+ getLastInsertId/getGeneratedKeys 实现 + MysqlConnection.prepareStatement(sql, autoGeneratedKeys) 重载(MySQL 等价 no-op — D138 §A.2 H4)— D135/D136/D137/D138 Phase 1 SQL 主线范式延续`**
  - 仅 stage `lib/java/sql.ss` + `lib/com/mysql/jdbc.ss` + `lib/com/mysql/prepared.ss` + `docs/3-decisions/D138-*.md` + `.claude/next_prompt.md`

(4) **next_prompt 指向 Phase 3**:Spring KeyHolder 完整 — `lib/spring/jdbc.ss` 加 `interface KeyHolder { getKey(): int; getKeyAsLong(): int; getKeys(): Map<string,int>; getKeyList(): Array<Map<string,int>> }` 4 method + `class GeneratedKeyHolder : KeyHolder` impl(内部 `keyList: Array<Map<string,int>>` 完整结构 — multi-row + multi-column 扩展位)+ `JdbcTemplate.update(sql, setter, keyHolder: KeyHolder): int` 重载(D137 第 6 method 重载)

§根因优先(CLAUDE.md §项目技术规则):
— 完整 JDBC 4.3 + Spring KeyHolder 标准 — 不裁剪 / 不 workaround / 不节省路径
— readUpdateResultPacket 单一信息源出口 — 接口层 trap,非数据层 patch(D138 §核心原则 4)
— readUpdateResult thin wrapper 向后兼容 — 既有调用方零破坏
— D135/D136/D137/D138 Phase 1 SQL 主线范式 — 每 Phase 独立 commit + Status 收关 + hash 回填 + next_prompt 自闭环

§D135/D136/D137/D138 Phase 1 commit hash 回填轮同形参考:
— D135 Phase 0 commit + Phase 1 实施同 commit 模式
— D136 Phase 0 commit `9326b9f` + Phase 1 commit `c854778` 同模式
— D137 Phase 0 commit `bafc25a` + Phase 1 commit `d2df1ba` 同模式
— D138 Phase 0 commit `d47a05d` + Phase 1 commit `<本轮回填>` + Phase 2 commit(下轮)同模式
