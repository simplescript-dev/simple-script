ultrathink Execute D131 + I021-requestbody-nested-optional-inner v0 ship 同轮(承本轮 D131 D 文档 Decided + I021 Plan 起立子档 §Execute 第一步实测验证 D130 SSoT 假设破裂记录 + 升根 D131 锚定 — **本轮 scope** D131 §4 谓词层 stripNullableCG inner 修 + I021 v0 7 case tests + spring-parity OrderTagsArr/OrderTagsMap fixture + Java oracle byte-identical + RC stress 50 次混合循环);**Decision 已锁不需重 Plan 直接 Execute**(承 memory `feedback_execute_when_doc_locked.md` D 文档锁过子决策 RED→改代码→GREEN 不再巡检)。RED before:`bin/ss build /tmp/t_optional_inner.ss --emit-ir -o /tmp/t.ll > /tmp/t.ll && grep -cE '@jnIsNullOrMissing' /tmp/t.ll` = 1(仅 lib 函数定义)+ `grep -cE 'opt_present\|opt_done' /tmp/t.ll` = 0 + `ls tests/phase5/i021_requestbody_nested_optional_inner.ss 2>&1 | grep -c "No such"` = 1 + `grep -cE 'class Tag\|Array<Tag\?>' examples/spring-parity/hello/ss/HelloController.ss` = 0。**改动 1**(`bootstrap/gen/gen_deserialize.ss:12-23 isArrayDeserializable`):extractContainerElemType 后加 `const etStripped = stripNullableCG(et)`,后续 isUserClass/Array/Map/primitive 判定换用 etStripped(D131 §4.1)。**改动 2**(`:25-34 isMapDeserializable`):同模式 vt → vtStripped(D131 §4.2)。**改动 3** 不动 emitArrayDeserializeInto/emitMapDeserializeInto/emitDeserializeForType 主体(extractContainerElemType + line 174+199 / 222+254 透传保留 `?` 既有正确;nullable case 复用 commit 29c3148 主路径)。**改动 4**(`tests/phase5/i021_requestbody_nested_optional_inner.ss`):新建 ~140 行 7 case — case 1 `Array<Tag?>` 元素 [{...},null,{...}] → tags=2,nulls=1;case 2 `Array<Tag?>` 全 null → tags=0,nulls=N;case 3 `Array<Tag?>` 全非 null → tags=N,nulls=0;case 4 `Map<string, Tag?>` value null vs object 双路径分流;case 5 `Map<string, Tag?>` 全 null value → 全 ptr null 走 ss_release isnull guard 不 segfault;case 6 全链路 raw HTTP POST `/orders/tags` 三场景 byte-identical Java;case 7 RC stress 50 次混合循环 no leak/segfault。**改动 5**(`examples/spring-parity/hello/ss/HelloController.ss` + `examples/spring-parity/hello/java/.../HelloController.java`):加 `class Tag { name: string }` + `class OrderTagsArr { customer: string; tags: Array<Tag?> }` + `class OrderTagsMap { customer: string; items: Map<string, Tag?> }` + `@PostMapping("/orders/tags") createOrderTags` + `@PostMapping("/orders/items") createOrderItems`;Java oracle `List<Tag>` / `Map<String, Tag>` 元素默认 nullable 对称。GREEN after:`grep -cE '@jnIsNullOrMissing' /tmp/t.ll` ≥ 3(lib + Array element + Map value)+ `grep -cE 'opt_present\|opt_done' /tmp/t.ll` ≥ 4 + `grep -nA 30 'OrderTagsArr_deserialize' /tmp/t.ll | grep -c 'jnArrayLen\|arr_loop'` ≥ 1 + `grep -nA 30 'OrderTagsMap_deserialize' /tmp/t.ll | grep -c 'jnObjectKeys\|map_loop'` ≥ 1 + `bin/ss test tests/phase5/i021_requestbody_nested_optional_inner.ss` 7 case 全 PASS + bootstrap 三阶段固定点 PASS(stage2 == stage3)+ reflection_health_linter GATE PASS no regressions + 全套 phase4/5 无 regression(对比 commit c68d443 baseline)+ spring-parity hello /orders/tags + /orders/items byte-identical Java oracle + Valgrind/mimalloc no-leak。**风险锚**(D131 §9 + I021 子档 §风险 6 条):1) emitPendingDeserializers BFS 字段扫描配合 stripNullableCG inner 实测验证(若 `class Outer { tags: Array<Tag?> }` BFS 未把 Tag 入队 → 谓词修后字段路径触发 emitArrayDeserializeInto → 子 deserialize 调 ss_Tag_deserialize 发现 Tag 未生成 deserializer → 链接错误 → 需补 BFS strip);2) ss_release isnull guard 同源验证(D018 + D023 + 父档 commit 29c3148 §风险 1 line 289-290 实测同源,Array element/Map value ptr null 不触发 mi_free);3) Map retain isnull(retain 自带 isnull guard);4) D067 narrow for-in body 维度限 IDENT(用户层 `let a = t; if (a != null) { a.name }` 绕);5) D131 修法对 stripped et = "int|string|double|bool" 配 nullable 维度路径(`Array<int?>` / `Map<string, int?>`)谓词修后**会返回 1**,但 nullable primitive boxing/sentinel 涉独立 codegen 路径 → 留 -optional-inner-primitive 子档 v0 不在本轮 — **本轮测试 case 仅用 user class `Tag?` 不混 primitive nullable**,Plan 验证 -optional-inner-primitive 子档已锚 out-of-scope;6) D131 修法递归自动 cover N=2 双层 `Array<Array<Tag?>>` / `Map<string, Map<string,Tag?>>` —— 本轮**不测**(scope 留 -deep-optional 子档),仅本轮 v0 cover N=1 单层 inner nullable。**simplify 4 agent**:reuse / quality / efficiency / readability(按 [feedback_human_readable_code](../../../.claude/projects/-root-code-simplescript-dev-simple-script/memory/feedback_human_readable_code.md) 5 rubric a-e — 本轮谓词内 +2 行 stripNullableCG inner 改动微小,simplify 复审形态;readability 复审 7 case 测试 + spring-parity DTO 命名陌生人秒懂)。**commit format**:`feat(I021-requestbody-nested-optional-inner,D067,D123,D129,D130,D131): 容器 inner nullable Array<T?> / Map<string,V?> v0 端到端反序列化 — D130 SSoT 假设破裂转 D131 谓词层 stripNullableCG inner 升根修 + 7 case 测试 + spring-parity OrderTagsArr/OrderTagsMap fixture + RC stress 50 次循环 — Phase 4 §247 第二支柱嵌套深化第八轮 Execute 落地轮`。
