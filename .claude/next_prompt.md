ultrathink D137 Phase 1 实施 — lib/spring/jdbc.ss 加 5 method callback 重载(execute / update / queryForList / queryForString / queryForInt 各加 (sql, setter: fn[, column]) 重载,内部 prepareStatement + setter(stmt) + executeUpdate/executeQuery + close,既有 5 method 签名零破坏 DDL/无参数 SELECT 路径保留)。RED `grep -c "prepareStatement" lib/spring/jdbc.ss` = 0 当前 → Phase 1 后期望 > 0;Phase 1 早期 spike 实测 SS fn 多语句 lambda body `(s) => { s.setInt(1, 1); s.setString(2, "X"); s.setInt(3, 30) }`(H1 假设挑战);GREEN tests/d134_mysql 8 case + tests/d135_caching_sha2 4 case + tests/d136_prepared_statement 5 case 全绿 + ./build.sh bootstrap 三阶段固定点;严格按 D137 §核心原则 + docs/3-MNK.md §M PSM 九问 + §N VCM 六验执行。
