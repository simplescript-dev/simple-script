# D168 P2.3 数组 RC 全栈统一 — 实施方案对比(Execute 轮蓝图)

**bug**: 数组 RC 半迁移 —— retain `ss_retain`(新)/ release `ss_rc_release`(旧,对新布局 no-op)不一致 → over-retain 泄漏(I024 §残留)+ codegen 非确定 miscompilation(I025)+ d095 类 SIGSEGV 根因常驻。
**父决策**: D168《统一双 RC 系统为 Perceus 主线》§C.9-exec(P2.3);axiom C4「Deterministic memory management only」。
**issue**: `I024`(Resolved — Candidate A 表面修)/ `I025`(Planned — 同根)/ `I023`(依赖 P2.3 完成的干净 baseline)。
**结论**: **选 C — 系统审计 + 一致切换**(详见 §4 决策行)。本文件是 P2.3 Execute 轮蓝图,非本轮实施。

---

## 1. 根因机理(坐实)

数组 / 容器 codegen-local RC 的精确半迁移状态:

| 路径 | retain | release | 配对 |
|---|---|---|---|
| Array `let` 局部(`genVarDecl` gen_decls.ss:652-660) | borrowed→`emitRetainForType`→`ss_retain`(新) | `emitReleaseVarList`→`ss_rc_release`(旧,新布局 magic-guard 失配 = no-op) | ✗ borrowed over-retain 泄漏 / owned 不释放泄漏 |
| Array 字段赋值(`gen_assigns.ss:177/180` 等) | `emitRetainForType`→`ss_retain` | `emitReleaseForType`→`ss_release` | ✓ 一致 |
| string `let` 局部 | borrowed→`ss_rc_retain`(gen_decls.ss:658,新布局 no-op) | `emitReleaseVarList`→`ss_rc_release`(no-op) | ⚠ 都 no-op:borrowed 平衡、owned string 泄漏 |
| ref 数组 `.push()` 元素(`gen_builtins.ss:140-143`) | `emitRetainForType` **无条件**(未走 `isOwnedExpr`) | `ss_drop_Array_ref`→`ss_release`(每元素) | ⚠ borrowed 配对 / owned(`push(new X())`)over-retain 泄漏 |
| ref 数组 ARRAY_LIT 元素(`gen_calls.ss genArrayLit` 731 `ss_arraySet` / 698 `ss_arrayPush`) | **(缺)**(两路径均无) | `ss_drop_Array_ref`→`ss_release`(每元素) | ✗ 切换后 borrowed 元素 over-release(UAF) |

**两个不一致点**:
1. `emitReleaseVarList`(gen_rc.ss:108)一律 `ss_rc_release`,与按类型分派的 `emitReleaseForType`(class.ss:158)割裂。`localPtrVars` schema 的 `name:type` type 段已存(为本子阶段预留),但 reader 未启用。
2. **ref 数组元素 retain 散落 codegen 调用点且未走 `isOwnedExpr` owned/borrowed 协议** —— `.push()`(gen_builtins.ss:140-143)无条件 retain(漏判 owned);ARRAY_LIT 两路径(gen_calls.ss:698/731)完全无 retain(漏判 borrowed)。**retain 不缺位于 runtime `ss_arrayPush`** —— `.push()` 的 retain 早在 codegen 层(D168 §C.9 注释自标 / I024 §C.9-exec「须保留」);真实缺陷是该 retain 未与 `genVarDecl:653` 局部变量同走 `isOwnedExpr` 判定。`ss_arrayPush`/`ss_arraySet` 是纯数据搬运 runtime,owned/borrowed 是编译期 AST 信息,runtime 物理不可判。

## 2. 假设破裂入口

- **(a)** `emitReleaseVarList` 一律 `ss_rc_release` 隐含假设「所有 tracked 局部是旧 RC 系统」—— **假设破裂**:Phase 1 后 string、P2.2 后 Array 已是新布局(rc@0 / 无 magic guard / mimalloc),`ss_rc_release` 读 `p-4` 当 magic、失配 → 静默 no-op。
- **(b)** `f21271d` 切 `emitReleaseVarList` Array→`ss_release` 隐含假设「数组 retain/release 已对称配对」—— **假设破裂**:`isOwnedExpr` 误判致借入数组无配对 retain;`ss_rc_release` no-op 一直掩盖,`ss_release` 真实化 → over-release → UAF(d095 崩 `ss_arrayPush`,core 实证 `0xdede` 毒值)。
- **(c)** `isOwnedExpr` `CALL=1` 隐含假设「函数调用恒返回 owned」—— **假设破裂**:`genReturn`(gen_decls.ss:733)仅当 `declRet=="ptr"` 才对 borrowed 返回值 `emitRetainForType`;返回类型未声明 / `funcRetTypes` 未注册 → `declRet` 退化为 `"i32"` → 不 retain → 该函数 CALL 实为 borrowed,caller `isOwnedExpr(CALL)=1` 当 owned → over-release。
- **(d)** `isOwnedExpr` `METHOD_CALL=0` 隐含假设「方法调用恒返回 borrowed」—— **假设破裂**:user 方法返 class/Array 经 callee `genReturn` 已 +1 = owned(I023 §A);caller 再 retain → over-retain。(切 `ss_release` 后表现为泄漏非崩,但属同一协议错配。)
- **(e)** 「在 runtime `ss_arrayPush` 加 ref 元素 retain」隐含假设「runtime 能判元素 owned/borrowed」—— **假设破裂**:owned/borrowed 是**编译期** AST 节点 kind 信息(`isOwnedExpr`),`ss_arrayPush` 运行期只拿到 i64 值,物理不可判 → runtime 无条件 retain 必对 owned 元素泄漏;且与 `.push()` 既有 codegen retain(gen_builtins.ss:140-143)叠加成 double-retain;且 ARRAY_LIT 固定字面量走 `ss_arraySet` 不经 `ss_arrayPush`,改 `ss_arrayPush` 对它无效 —— P2.3b 切换后仍 over-release。**故元素 retain 决策只能在 codegen 层。**

## 3. RC 协议调用点系统审计(Execute 轮须逐点核验)

| 函数 | 文件:行 | 角色 | P2.3 处理 |
|---|---|---|---|
| `emitReleaseVarList` | gen_rc.ss:108 | 局部释放(唯一不一致点) | **改**:启用 type 段 reader,按 `isUserClass/string/isArrayType` 分派(复用 `emitReleaseForType` 逻辑) |
| `trackPtrVar` | gen_decls.ss:403/423/439/652 | 加入释放列表 | 核验:每个 track 点的 type 段写入正确(genVarDecl 已 `trackType`;destructure 三点须核 elemType) |
| `isOwnedExpr` | gen_decls.ss:60 | owned/borrowed 判定 | **改**:`METHOD_CALL` 按返回类型分派(I023 候选 A);`CALL` 与 `genReturn` declRet 修复联动 |
| `genReturn` | gen_decls.ss:733 | borrowed 返回值 retain | **改**:`declRet` 用 `inferType(valId)` 校正,消除「返回类型未声明 → 不 retain」破裂 (c) |
| `genVarDecl` 658 | gen_decls.ss:658 | string borrowed retain | **改**:`ss_rc_retain`→`emitRetainForType`(string 走 `ss_retain`),与 release 侧切换后对齐 |
| `emitRetainForType` / `emitReleaseForType` | class.ss:148/158 | 类型分派 retain/release | 不变(已正确分派 Array/string→新系统) |
| `ss_arrayPush` / `ss_arraySet` | gen_rt_array.ss:64 / 53 | runtime push/set | **不变**:纯数据搬运;元素 retain 决策上移 codegen 调用点(破裂 (e):runtime 不可判 owned/borrowed) |
| `genArrayMethod` push | gen_builtins.ss:140-143 | `.push()` 元素 retain | **改**:retain 加 `isOwnedExpr` 门 —— `if (pushNonOwning==0 && isOwnedExpr(argId)==0) emitRetainForType`(消 owned 元素 over-retain 泄漏) |
| `genArrayLit` | gen_calls.ss:698(spread)/731(固定) | ARRAY_LIT 元素 retain | **改**:两路径补 `if (ssTypeToLLVM(elemT)=="ptr" && isOwnedExpr(elemId)==0) emitRetainForType`(消 borrowed 元素切换后 over-release) |
| `ss_drop_Array_ref` / `ss_arraySlice` / `ss_arrayConcat` | gen_runtime.ss:768 / gen_rt_array.ss:256/309 | runtime 元素 RC | 已落地,核验即可(slice/concat 元素恒 borrowed,runtime 无条件 retain 正确) |

## 4. 候选方案对比

| 候选 | 层次 | 改动 | 评估 |
|---|---|---|---|
| A | 数据层 patch | 仅切 `emitReleaseVarList` 类型分派(Array/string→`ss_release`) | = `f21271d`,已实测崩 d095(借入数组 over-release,假设破裂 (b) 未消)。**淘汰** |
| B | 接口层 trap | A + 修 `isOwnedExpr`/`genReturn`(消除假设破裂 (c)(d)) | 消 CALL/METHOD_CALL 误判;但 `trackPtrVar` 各点 type 段、ref 数组元素 retain owned/borrowed 协议未含 → 切换后 ARRAY_LIT borrowed 元素 over-release。**残留缺口** |
| C | 架构层 refactor | B + §3 全调用点系统审计 + ref 数组元素 retain 纳入 `isOwnedExpr` owned/borrowed 协议(codegen 调用点 `gen_builtins`/`gen_calls`)+ `genVarDecl:658` string 对齐 + 三阶段 bootstrap 每步验证 | **唯一消除全部假设破裂 (a)-(e) 的一致修法**;工程量大、多文件、需多轮 bootstrap |
| D | 架构层 refactor | 数组局部 RC 完全纳入 PIR liveness(像 class:`pirActive` 管理,删 codegen 手工 retain/release) | 根上加根,但属 D168 **Phase 5**「PIR 全覆盖」范围;P2.3 阶段先做 C,Phase 5 再吸收 |

**决策行**:**选 C 因** 它是唯一同时消除假设破裂 (a)-(e) 的一致修法 —— A=`f21271d` 已证崩(数据层 patch 不触 owned/borrowed 协议根因);B 留 `trackPtrVar` type 段 + ref 数组元素 retain owned/borrowed 协议缺口(接口层 trap 只修判定不修全栈);D 是 Phase 5 范围(P2.3 先做 C,长期由 D 吸收,见 §长久评估)。成本(多文件 / 多 bootstrap 轮)不作排序依据 —— C 是根因解决度最高项(CLAUDE.md §Root Cause 优先)。

**§5-1 层次修正(2026-05-19)**:原 §5 第 1 项「runtime `ss_arrayPush` 加 ref 元素 retain」实测有误 —— 假设破裂 (e):(1) `.push()` 的元素 retain 早在 codegen 层 `gen_builtins.ss:140-143`(D168 §C.9 注释自标 / I024「须保留」),runtime 再加 = double-retain 泄漏;(2) ARRAY_LIT 固定字面量走 `ss_arraySet` 不经 `ss_arrayPush`,改 runtime 对它无效、切换后仍 over-release;(3) owned/borrowed 是编译期 AST 信息,runtime 物理不可判。修正:元素 retain 纳入 `isOwnedExpr` 协议、在 codegen 调用点统一(与 `genVarDecl:653` 局部变量同构),`ss_arrayPush`/`ss_arraySet` runtime 保持纯数据搬运。

**长久评估**:C 不被 D(Phase 5 PIR 全覆盖)返工 —— C 把 codegen-local RC 切到一致新系统,D 是「删 codegen 手工 retain/release、移交 PIR」,C 的一致态是 D 的前提(D 依赖 C 落地的新系统正确性)。业界对标:Perceus(Koka)/ Lean RC 均「先正确的手工 RC → 再编译器自动插入」,C→D 顺序与之一致。

## 5. 候选 C 完整改动清单(Execute 轮)

1. **codegen 元素 retain 协议 — `gen_builtins.ss` + `gen_calls.ss`**:ref 数组元素 retain 纳入 `isOwnedExpr` owned/borrowed 协议(与 `genVarDecl:653` 局部变量同构,数组持有元素一份引用)。(a) `gen_builtins.ss:140-143` `.push()`:`if (pushNonOwning==0)` 加 `&& isOwnedExpr(argId)==0`(消 `push(new X())` owned 元素 over-retain 泄漏);(b) `gen_calls.ss genArrayLit` 固定路径(731 `ss_arraySet` 前)+ spread 路径(698 `ss_arrayPush` 前):补 `if (ssTypeToLLVM(elemT)=="ptr" && isOwnedExpr(elemId)==0) emitRetainForType(val, elemT)`(消 ARRAY_LIT borrowed 元素切换后 over-release)。`ss_arrayPush`/`ss_arraySet` runtime 不动(纯数据搬运)。
2. **codegen — `gen_rc.ss` `emitReleaseVarList`**:读 entry type 段,`isUserClass||=="string"||isArrayType` → `ss_release`,else → `ss_rc_release`。
3. **协议 — `gen_decls.ss`**:(a) `genReturn` `declRet` 用 `inferType(valId)` 校正;(b) `isOwnedExpr` `METHOD_CALL` 按返回类型分派(`isUserClass||isArrayType` → 1);(c) `genVarDecl:658` string borrowed retain 改 `emitRetainForType`。
4. **审计** — §3 表逐点核验 `trackPtrVar` type 段写入、destructure 三点(403/423/439)的 elemType 正确性。
5. **string 连带** — emitReleaseVarList 切换后 string `let` 局部首次真实释放;须确认 owned/borrowed string 配对(与 (3c) 联动)。

## 6. 实施序 + 验收

- **序**:codegen 元素 retain 协议(1)先行(P2.3a,独立、安全 —— `emitReleaseVarList` 未切时数组不真实 drop,过/欠 retain 仅泄漏不崩)→ 协议(3)→ codegen 切换(2)+ string(5)同 commit(原子,中间态即崩溃态,§4-A 已证)→ 审计(4)贯穿。
- **每步验收**:三阶段 bootstrap Stage2=Stage3 `cmp` 固定点;失败 `git reset --soft HEAD^`。
- **整体 RED→GREEN**:`d095_setter_mixed` build+run exit 0;`generic_multi_constraint` 4 形态 ×≥10 build+run exit 0(I025 §步骤);`bin/ss test tests/` 0 真实 regression;`reflection_health_linter` GATE;`.bugfix` 6 gate。
- **关闭**:I024 §残留消、I025 复验 Resolved、I023 干净 baseline 成立、D168 §C Phase 2 P2.3 close。
